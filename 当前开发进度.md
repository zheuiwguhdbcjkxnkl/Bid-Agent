# bid-agent-ai M1 当前开发进度

> 最后更新：2026-08-23
> 当前分支：`worktree-m1-auth-project-slice`
> 当前工作目录：`backend/`

## 当前目标

完成 M1 第一纵向切片的真实后端：

- 登录、Session 查询、退出；
- 本人修改密码与首次改密门禁；
- 个人工作台；
- 项目创建、项目列表、项目总览。

认证方案保持 PostgreSQL 服务端 Session，不使用 JWT。

## 已完成

- [x] 任务 1：后端工程基座、`uv` 依赖、FastAPI 应用、测试 PostgreSQL。
- [x] 任务 2：统一错误体、`request_id`、配置和异常处理。
- [x] 任务 3：PostgreSQL ORM、Alembic 迁移、数据库约束和审计 append-only。
- [x] 任务 4：Argon2id 密码、Session Token 哈希、Session 时限规则。
- [x] 任务 5：Origin/Referer、CSRF、登录失败限流和 Redis 降级。
- [x] 任务 6：登录和 Session 查询。
- [x] 任务 7：退出、修改密码、首次改密门禁。
- [x] 任务 8：项目创建契约和负责人权限规则。
- [x] 任务 9：项目创建事务、强制 `Idempotency-Key`、同键重放和异体冲突的基础实现。
- [x] 任务 10：并发幂等与事务回滚。引入 `ProjectCreationHooks` 协议（after_claim/after_project/after_package/after_member/after_audit），repository 拆分为三个细粒度写入函数，完成真实多连接并发同键/异体测试与四个事务故障注入回滚测试。
- [x] 任务 11：项目列表与总览。新建 `queries.py` 只读聚合，`GET /projects` 成员隔离分页列表、`GET /projects/{id}/overview` 权限+稳定 DRAFT 结构+空动态集合。
- [x] 任务 12：个人工作台。新建 `me_router`，`GET /me/workbench` 复用列表查询按 updated_at DESC 取 5 个 recent_projects，其余空集合。
- [x] 任务 13：契约回归。OpenAPI 8 接口路由锁定、CreateProjectRequest 不含 source 字段、13 个 DomainError 错误码参数化统一契约测试。
- [x] 任务 14：最终门禁。补全 README、ruff format/check、mypy、迁移往返、完整套件 214 passed、git diff --check 通过。

**M1 后端第一纵向切片全部 14 个任务已完成。**

## 任务 9 已实现内容

- 项目、主标包、负责人项目成员、审计事件和幂等成功结果同一事务提交；
- PostgreSQL savepoint 幂等占位；
- 同作用域同键同请求重放首次响应；
- 同作用域同键不同请求返回 `IDEMPOTENCY_CONFLICT`；
- 255 字符项目名边界已处理；
- OpenAPI 已补充项目创建 `requestBody`、内部 Schema 引用和 `IdempotencyReplay` 字段命名。

## 当前阻塞

无。任务 9 质量审查已通过，两个历史阻塞问题已于 2026-08-23 修复：

1. 非法 JSON 请求（例如请求体为 `{`）现统一映射为 `422 VALIDATION_ERROR`（`parse_create_project_request` 捕获 `JSONDecodeError` 转为 `RequestValidationError`）。
2. 项目创建 OpenAPI 已补充：
   - `Idempotency-Key` header 参数（required）；
   - `X-CSRF-Token` header 参数（required）；
   - `sessionCookie` + `csrfToken` 两个 `apiKey` 安全方案及 `security` 声明；
   - `ErrorResponse` 统一错误响应 Schema 及 400/401/403/409/422/500 状态码。

## 恢复顺序

全部任务已完成，无需继续恢复。

## 验证记录

- 全量测试最终结果：`214 passed`；
- `uv run ruff check app tests migrations`：通过；
- `uv run ruff format --check app tests migrations`：通过；
- `uv run mypy app tests`：通过；
- 迁移往返（downgrade base → upgrade head → downgrade base → upgrade head）：全部成功；
- `git diff --check`：退出码 0。

## Git 与范围状态

- 当前 `backend/` 整体仍是未跟踪目录；
- 未执行 `git add`、`git commit`、`git push`；
- 未修改冻结前端；
- 未引入新依赖；
- 未超出 V1 范围。

## 重要路径

- 设计：`docs/development/2026-08-21-M1认证与项目纵向切片设计.md`
- 实施计划：`docs/superpowers/plans/2026-08-21-m1-auth-project-vertical-slice.md`
- 开发历史：`项目开发对话历史.md`
- 后端：`backend/`
- 任务 9 幂等实现：`backend/app/projects/idempotency.py`
- 任务 9 创建服务：`backend/app/projects/service.py`
- 任务 9 创建接口：`backend/app/projects/api.py`
- OpenAPI 应用工厂：`backend/app/main.py`
