# 项目开发对话历史

> 创建日期：2026-08-18  
> 最近更新：2026-08-21  
> 保存位置：仓库根目录  
> 记录范围：项目需求澄清、架构决策、开发准备和开发约束讨论，不包含系统指令、工具调用和内部执行日志。  
> 维护规则：每完成一个讨论点，立即将新增对话追加到本文件，保留问题、回答、确认结果和关键边界，不覆盖历史记录。

---

## 一、项目通俗定位

### 用户

我们的项目具体是干嘛的，通俗的给我讲一下。

### 助手

通俗地说，我们做的是一个：

> **帮助投标团队“读招标文件、找企业材料、写投标文件、检查风险”的 AI 工作台。**

它不是替公司决定要不要投标，也不是按一下按钮就自动生成最终标书，而是把标书编制过程中大量重复、容易遗漏的工作交给多个 AI Agent 辅助完成。

假设公司准备参加一个“政务信息化系统建设项目”。以前，标书编写人需要自己完成：

1. 阅读几百页采购文件；
2. 找出资格条件、实质性要求和评分标准；
3. 查询公司有哪些证书、人员和历史案例；
4. 建立标书目录和响应清单；
5. 编写技术、商务响应初稿；
6. 检查有没有漏响应、材料过期或前后矛盾；
7. 根据审核意见反复修改；
8. 在提交前确认所有风险都处理完了。

系统具体完成以下工作：

1. 帮人读取采购文件、招标文件和更正公告，提取资格条件、实质性要求、评分项和时间节点，并保留原文、页码和版本；
2. 从企业知识库查找营业执照、资质证书、人员、历史案例、合同、验收材料和历史标书模板；
3. 根据确认的要求、评分项和企业材料生成响应矩阵、标书目录、章节任务和带证据引用的初稿；
4. 由资格与合规、响应规划、评分点覆盖、一致性检查和风险复核等 Agent 分工检查；
5. 管理采购文件更新、标书修改、审核、风险处理、内部提交基线和全过程留痕。

Agent 不能擅自决定是否参与投标、资格是否最终满足、是否接受风险或是否正式提交。这些决定必须由人完成。

一句话总结：

> 这是一个面向投标企业的 AI 标书编制与审核工作台：它帮助团队读文件、找材料、写初稿、查遗漏、控版本和留记录，让人负责最终的业务判断和提交决定。

### 用户确认

懂了。

### 助手确认的主线

> **AI 负责读文件、找证据、辅助编写和检查风险；人负责确认、修改、接受风险和最终提交。**

---

## 二、服务对象与产品定位

### 讨论点 1：系统服务哪一方

#### 助手提问

V1 是否确认只服务投标企业，不服务招标方、采购人或评审专家？

系统只负责：

- 解析采购文件；
- 辅助编制投标文件；
- 匹配企业证明材料；
- 检查资格、评分覆盖和风险；
- 完成投标企业内部审核与提交留痕。

不负责采购文件编制、评标、定标或审核其他供应商的投标文件。

#### 用户确认

确认。

### 讨论点 2：智能编制还是自动生成

#### 助手提问

产品是否强调“智能编制”，而不是“自动生成”？

推荐统一为：

> **投标文件智能编制与合规审核多智能体平台**

系统可以生成目录、响应矩阵和章节初稿，但最终投标文件仍需人工确认、修改和审核。因此：

- 可以说“生成标书初稿”；
- 可以说“辅助编制投标文件”；
- 不说“全自动生成完整标书”；
- 不说“一键生成并提交标书”。

#### 用户确认

确认采用“智能编制 + 人工确认”的定位。

---

## 三、V1 工作台与项目入口

### 用户提出的 V1 定义

本项目 V1 第一版建设一个**单个投标项目投标任务工作台 Dashboard**，支持标书编写人输入项目代码后，系统自动生成项目投标标书初稿，并在内部完成查看、确认、审核、提交和版本留痕。

### 助手校准

建议将其准确表述为：

> 本项目 V1 建设一个面向单个投标项目的投标任务工作台 Dashboard。标书编写人创建项目并输入项目代码，上传或确认有效采购文件后，系统辅助生成响应矩阵、章节任务和投标文件初稿，并支持内部查看、修改、确认、审核、提交基线和版本留痕。

“自动生成”只能表示生成可编辑、待审核的初稿，不能表示自动形成最终投标文件。

### 项目编号的讨论

#### 助手给出的选择

1. 企业内部项目编号：用于创建和识别项目，采购文件由用户上传；
2. 采购项目编号：系统根据编号自动从采购平台获取公告和采购文件；
3. 两者都支持：内部编号管理项目，采购项目编号关联外部公告。

#### 用户选择

选择第 2 种：V1 输入项目编号之后，等待投标文件生成。

### 助手进一步明确流程

```text
输入采购项目编号
→ 定位采购平台和项目
→ 获取公告、采购文件及附件
→ 确认文件完整性和最新版本
→ 解析资格条件、实质性要求和评分项
→ 匹配企业资质、人员和历史案例
→ 生成响应矩阵和标书初稿
→ 人工查看、修改、审核
→ 形成内部提交基线和版本留痕
```

不能直接从“项目编号”跳到“完整标书”，因为系统还需要采购文件、企业资料、报价输入和人工确认，最终生成的是待审核初稿。

### 内部审核和确认

#### 用户提问

系统内部不能进行审核和确认么？

#### 助手回答

可以，内部审核和确认就是系统的核心能力之一：

```text
输入采购项目编号
→ 系统获取并解析采购文件
→ 自动提取要求、评分项和时间节点
→ 匹配企业证据
→ 生成响应矩阵和标书初稿
→ 标书编写人查看、修改、确认
→ 项目负责人审核
→ 商务/法务合规审核
→ 处理风险和退回意见
→ 内部提交确认
→ 生成版本基线和审计记录
```

系统内部支持：

- 查看采购文件、提取结果、证据引用和标书章节；
- 确认文件版本、资格要求、评分项、证据材料和章节初稿；
- 自动检查与人工审核，发现问题后退回修改；
- 形成内部提交基线，但不等同于向外部交易平台正式递交；
- 记录确认、修改、审核、退回和豁免过程。

系统或 Agent 可以自动检查并提出结论；是否满足要求、是否接受风险和是否允许内部提交，必须由有权限的人员确认。

---

## 四、数据和工具分散问题

### 用户指出的业务对应关系

前两个业务环节是：

1. 查询招标公告、招标文件和历史标书；
2. 从招投标平台、采购公告平台、企业知识库等平台获取项目市场、商务和报价数据。

对应痛点：

> **数据和工具分散**

- 招标文件、公告、项目市场、报价测算、历史标书和合规审核分散在不同系统中；
- 标书编写人需要在招投标平台、采购公告平台、企业知识库、历史标书库、Excel 和内部标书库之间频繁切换。

### 助手形成的模块定义

这两个环节统一为 V1 的第一个核心模块：

> **投标项目情报与资料汇聚工作台**

第一个环节：

```text
输入项目编号
→ 定位招标平台和采购公告
→ 获取招标公告、采购文件、附件、澄清/更正文件
→ 识别项目、标包、截止时间、预算和采购要求
→ 建立当前项目的文件版本链
```

第二个环节：

```text
当前项目
→ 关联历史标书和同类项目
→ 查询企业知识库中的资质、人员、案例和证明材料
→ 获取商务指标、历史报价和报价模板
→ 形成企业证据、报价数据和参考资料集合
```

数据需要分成：

- 项目事实：公告、采购文件、截止时间、预算、采购包信息；
- 企业证据：资质、人员、案例、合同、验收材料；
- 历史参考：历史标书、同类项目、历史中标方案；
- 报价数据：历史报价、报价模板、报价假设；
- Agent 结论：响应建议、证据匹配、风险判断和初稿内容。

### 项目定位键

#### 助手建议

项目不能只使用裸项目编号，应使用：

```text
来源平台 + 外部项目编号
```

例如：中国政府采购网 + 项目编号、地方公共资源交易平台 + 项目编号、企业内部招投标平台 + 项目编号。

#### 用户确认

可以。

### 平台范围讨论

考虑的平台类型包括：

- 国家级官方平台：中国政府采购网、全国公共资源交易平台、中国招标投标公共服务平台；
- 地方政府平台：各省市政府采购网、公共资源交易中心、电子交易平台；
- 央企与行业平台：电力、能源、通信、铁路、军队等采购平台；
- 商业标讯聚合平台：用于项目发现、同类项目和市场情报，不作为采购文件事实源；
- 企业内部平台：企业知识库、历史标书库、资质证书库、人员库、案例库、报价模板、项目管理和审核系统。

V1 推荐组合：

```text
主要公告源：中国政府采购网
地方交易源：一个省级或市级公共资源交易平台
补充公告源：全国公共资源交易平台 + 中国招标投标公共服务平台
内部数据源：企业知识库 + 历史标书库 + 企业证据库 + Excel 报价模板
市场情报源：一个商业标讯聚合平台，仅用于同类项目搜索
```

只有官方公告、采购文件、正式更正文件和用户确认的内部资料可以成为事实源；商业平台搜索结果、历史项目和 Agent 推断只能作为参考。

### 北京首个客户与全国复用

#### 用户说明

项目甲方是北京的一家公司，当前项目给该甲方建设，但未来可能服务其他地方的公司。

#### 助手澄清

需要区分两种关系：

- 系统建设关系：北京公司是系统建设项目的甲方和首个客户；
- 招投标关系：北京公司参加具体项目时是投标人/供应商，采购人或招标人不能称为本系统甲方。

项目定位为：

> 首先为北京某投标企业建设，但产品能力面向全国其他投标企业复用，不能把业务规则和数据源写死为北京地区。

#### 已确认部署模式

> **首个北京客户私有化部署，产品能力可复制到其他企业。**

具体边界：

- V1 不建设完整多租户 SaaS；
- 当前只管理北京甲方的用户、项目和企业资料；
- 数据模型保留 `organization_id`；
- 平台连接器、规则集和模板按组织配置；
- 服务其他企业时建立新的组织或独立部署环境；
- 不同公司的资质、历史标书、报价和人员数据严格隔离。

### 采购文件获取模式

#### 已确认方案

```text
输入来源平台 + 项目编号
→ 自动获取公开公告、公开附件和更正信息
→ 检查采购文件是否完整
├─ 文件可公开下载：系统自动导入
└─ 需要登录、报名或 CA：提示用户授权导入或上传
→ 用户确认当前有效文件版本
→ 系统开始解析和生成标书初稿
```

确认结果：

> **公开信息自动获取，受限文件由甲方授权接入或人工上传兜底，文件确认后自动生成初稿。**

### V1 项目类型

#### 已确认范围

> **政府采购信息化服务项目 + 单个投标项目 + 单个采购包。**

例如软件开发、系统集成、运维服务和数字化平台建设项目。

后续再扩展多采购包、货物采购、工程建设招标、央企和企业采购、联合体投标。

---

## 五、已完成讨论点 1：Excel 商务指标与报价计算过程

### 用户提出的业务环节

> 使用 Excel 模板或内部模型整理商务指标。

对应痛点：

> **数据口径和计算过程难统一**

- 商务指标可能来自不同数据源或不同 Excel 模板；
- 不同标书编写人、不同团队对部分指标、报价参数和采购包/项目类别模板的使用口径可能不完全一致；
- 计算过程、数据来源和公式版本不易沉淀和追溯。

### 助手对商务数据的分类

1. 招标文件事实：项目预算、最高限价、采购包预算、服务期限、人员数量要求、工作量、税率口径、分项报价表和折扣规则；
2. 企业成本数据：人员、软件、硬件、实施、运维、差旅、外采、分包、管理费用、税费和风险准备金；
3. 报价参数和假设：工期、人员投入、利润率、折扣系数、风险系数、价格预留和历史项目参数；
4. 计算结果：分项成本、含税与不含税价格、分项报价、投标总价、单价或费率、与预算及最高限价的差额；
5. 最终确认报价：由商务人员调整、项目负责人确认，并形成正式报价版本。

### 已确认方案

V1 不替代 Excel，而是把 Excel 纳入统一工作流：

```text
系统识别采购文件中的报价要求
→ 推荐对应的受控 Excel 模板
→ 自动填入预算、最高限价、服务期限等项目事实
→ 商务人员补充成本和报价假设
→ Excel 或内部模型完成计算
→ 系统读取计算结果
→ 校验公式、合计、税率、大小写金额和限价
→ 商务人员确认报价版本
→ 标书初稿引用已确认报价
```

系统负责模板统一、数据来源记录、参数版本、公式版本、结果导入、异常校验、最终确认、跨章节一致性和修改留痕；Excel 继续负责复杂灵活的计算。

### 完成结论

- V1 不替代 Excel 报价模型；
- Excel 或内部模型继续负责商务指标和报价计算；
- 系统负责统一模板、自动填入项目事实、记录数据来源；
- 系统管理报价参数、计算公式和结果版本；
- 系统校验合计、税率、限价、大小写金额和跨章节一致性；
- 最终报价必须由商务人员和项目负责人确认；
- 标书初稿只能引用已经确认的报价版本；
- Agent 不擅自确认或修改最终报价。

**讨论点完成。**

## 六、已完成讨论点 2：筛选同类项目和历史中标方案

### 用户提出的业务环节

> 筛选同类项目和历史中标方案并进行横向对比。

### 历史项目的数据边界

助手指出“历史中标方案”存在歧义，需要区分：

1. 甲方自己的历史中标标书：作为核心数据，包含技术方案、商务响应、报价版本和证明材料；
2. 甲方自己的历史未中标标书：可以用于对比，但不能让 Agent 擅自推断未中标原因；
3. 其他供应商的中标信息：主要使用公开中标公告中的供应商、中标金额、日期和项目基本信息；只有来源合法、权限明确的公开方案或授权资料才能纳入。

### 用户确认的数据范围

> 以甲方内部保存的历史中标标书及其结构化方案为核心，同时使用公开中标公告补充外部同类项目的中标单位和中标金额等信息。

竞争对手完整投标文件不能被默认获取，也不能使用来源不明或受保密限制的材料。

### “同类项目”的定义

系统根据以下维度匹配历史项目：

| 维度 | 示例 |
|---|---|
| 采购制度 | 政府采购 |
| 项目类别 | 软件开发、系统集成、运维服务 |
| 业务领域 | 政务服务、数据治理、城市管理 |
| 项目范围 | 开发、实施、培训、运维 |
| 预算区间 | 与当前项目预算相近 |
| 服务周期 | 一年运维、三年服务等 |
| 采购方式 | 公开招标、竞争性磋商等 |
| 资格要求 | CMMI、ISO、类似业绩、人员证书 |
| 评分结构 | 技术、商务、价格评分占比 |
| 地区和客户类型 | 北京、政府部门、事业单位等 |

系统返回候选项目时需要解释：

- 为什么相似；
- 哪些条件不同；
- 哪些章节可以参考；
- 哪些企业证据可能复用；
- 哪些历史报价只能作为参考。

### 用户确认的筛选方式

> **系统自动推荐同类项目，标书编写人确认最终对比项目池。**

不能让 Agent 自动选定一份历史标书并整体照搬。

### 横向对比的基础输出

助手最初建议输出四类结果：

1. 项目相似度列表：相似度、相似原因、主要差异和数据来源；
2. 横向对比矩阵：预算、中标金额、采购方式、服务周期、资格要求、评分结构、服务范围、人员和案例要求、历史章节和证明材料；
3. 可复用材料建议：历史章节、技术模块、资质、人员、案例及必须重新编写的内容；
4. 差异与风险提示：新增要求、未覆盖评分项、过期材料和禁止直接沿用的价格、名称、日期及参数。

系统不直接输出“最佳中标方案”，也不将历史中标等同于方案必然正确。

### 用户追问

四项是否够用？横向对比之后如何放入项目池，人工怎么确认，确认之后如何由系统生成初稿？

### 助手补充的执行结果

四项展示结果不足以直接驱动生成，还需要：

5. 人工确认后的参考项目池；
6. 参考项目与当前章节、要求和评分项的复用映射。

因此完整结果是：

```text
相似项目列表
+ 横向对比矩阵
+ 可复用材料建议
+ 差异风险提示
+ 已确认参考项目池
+ 章节/要求/证据复用映射
```

### 项目池状态

| 状态 | 含义 |
|---|---|
| `CANDIDATE` | 系统推荐，尚未处理 |
| `SELECTED` | 标书编写人已选择，尚未最终确认 |
| `REJECTED` | 人工判断不适合参考 |
| `CONFIRMED` | 已确认可以用于当前项目 |
| `INVALIDATED` | 当前采购文件变化后需要重新确认 |

每条候选关系需要记录：

- 当前投标项目；
- 历史项目；
- 相似度；
- 相似原因；
- 主要差异；
- 数据来源；
- 选择人；
- 选择理由；
- 确认时间；
- 可用于哪些目的。

### 人工确认的三步流程

#### 第一步：筛选候选项目

系统展示项目名称、项目类型、采购人类型、预算和中标金额、采购方式、资格条件、评分结构、服务范围、相似度、相似原因和关键差异。

标书编写人可以：

- 加入待选池；
- 排除项目；
- 修改筛选条件；
- 手动搜索并加入其他历史项目。

#### 第二步：横向比较

```text
当前项目
vs 历史项目 A
vs 历史项目 B
vs 历史项目 C
```

比较资格要求、评分项、技术范围、人员要求、案例要求、交付周期、历史报价、历史技术方案、证明材料和中标/未中标结果。

#### 第三步：确认参考用途

用户需要为每个历史项目选择参考用途：

| 用途代码 | 含义 |
|---|---|
| `STRUCTURE_REFERENCE` | 标书目录和章节结构 |
| `TECHNICAL_REFERENCE` | 技术架构、实施和运维方案 |
| `QUALIFICATION_REFERENCE` | 资格响应和证明材料组织方式 |
| `SCORING_REFERENCE` | 评分项响应方式 |
| `CASE_REFERENCE` | 企业案例和验收材料 |
| `PRICING_REFERENCE` | 历史价格和成本参考 |
| `RISK_REFERENCE` | 历史审核问题和风险处理方式 |

人工确认后冻结“当前项目参考池版本”。增删参考项目时生成新版本，不能静默覆盖。

### 确认后如何生成初稿

系统先组装编制上下文包：

```text
当前采购文件中的确认要求
+ 当前评分项
+ 当前项目章节任务
+ 已确认企业资质和证据
+ 已确认参考项目池
+ 每个参考项目的使用目的
+ 已确认报价版本
+ 当前项目风险和差异
= 当前项目编制上下文包
```

系统按章节分别生成，而不是把多个历史标书全文塞给大模型。

初稿来源优先级为：

```text
当前采购文件
> 当前项目人工确认结果
> 当前企业真实证据
> 已确认历史参考项目
> 通用模板
```

历史标书只能提供结构、写法和可验证的企业通用内容，不能覆盖当前采购文件要求，也不能直接复用历史客户名称、项目名称、日期、报价、旧人员、过期证书和不适用参数。

### 用户追问确认方式

用户询问：标书编写人如何确认，如何说明标书用途，是系统给选项还是编写人手打；如果手打，格式如何处理？

### 已确认的交互方式

> **系统推荐选项 + 编写人勾选确认 + 必要时补充结构化说明。**

不能完全依赖手打，也不能只提供无法扩展的固定选项。

#### 加入参考池

```text
○ 加入参考池
○ 暂不确定
○ 排除该项目
```

排除时选择原因：项目类型不匹配、技术路线不适用、年代较早、报价不可参考、材料过期、数据不完整、涉及敏感信息或其他原因。

#### 选择参考用途

系统根据横向对比自动推荐，编写人修改：

```text
☑ 标书目录与章节结构
☑ 技术解决方案
☑ 实施与项目管理方案
□ 运维服务方案
☑ 人员组织方案
☑ 类似项目案例
□ 资格与商务响应
□ 评分项响应方式
□ 报价与成本参考
☑ 历史风险与审核意见
```

用途来自统一字典，不能由每个编写人随意命名。

#### 选择具体使用位置

用户需要将历史内容绑定到当前项目章节、要求或评分项，例如：

```text
历史项目 A / 技术架构章节
→ 当前项目 5.1 总体技术架构
→ 当前项目 5.2 系统功能设计
→ REQ-023 微服务架构要求
→ SCORE-012 技术架构合理性评分项
```

#### 选择参考方式

- 只参考目录结构；
- 参考写作框架；
- 参考技术内容；
- 复用企业通用能力描述；
- 复用经过确认的企业案例；
- 仅作为差异比较。

“仅作为差异比较”的内容不进入初稿生成上下文。

#### 系统自动生成确认理由

系统根据相似度和差异分析自动生成推荐理由，编写人可以确认、修改、补充或删除不正确内容，不必从空白文本开始。

#### 自定义用途

固定用途不够时可以选择“其他用途”，但必须使用结构化表单：

| 字段 | 要求 |
|---|---|
| 自定义用途名称 | 必填，简短名称 |
| 用途说明 | 必填 |
| 当前目标章节 | 必选 |
| 关联要求或评分项 | 原则上至少一个 |
| 参考方式 | 必选 |
| 禁止复用内容 | 可选 |
| 补充备注 | 可选 |

推荐交互比例：

```text
80% 系统生成和固定选项
+ 20% 编写人补充说明
```

### 完成结论

- 系统自动推荐相似历史项目，人工确认参考项目池；
- 核心使用甲方自己的历史中标标书，外部公开中标信息只作补充；
- 输出相似项目列表、对比矩阵、复用建议和差异风险；
- 历史项目按章节、要求和用途授权，不能整份直接用于生成；
- 参考用途主要由系统提供标准选项；
- 系统自动推荐用途、目标章节和确认理由；
- 编写人负责勾选、修正和最终确认；
- 特殊用途通过结构化表单补充，避免自由文本格式混乱；
- 确认后冻结参考项目池版本，并生成章节级参考绑定关系；
- 系统结合当前采购文件、企业证据和已确认参考内容生成初稿。

**讨论点完成。**

---

## 七、已完成讨论点 3：调整报价假设、报价目标和报价策略

### 用户提出的业务环节

> 调整报价假设并更新报价目标和响应等级。

### 术语澄清

助手指出“响应等级”可能表示报价策略、要求响应状态、项目优先级或风险等级，不能共用一个字段。

用户确认此处的“响应等级”指：

> **报价策略等级。**

后续统一使用“报价策略等级”，避免与采购要求的响应状态混淆。

报价策略等级定义为：

| 策略 | 含义 |
|---|---|
| `CONSERVATIVE` 保守 | 优先保证利润和风险覆盖，价格竞争力相对较弱 |
| `BASELINE` 基准 | 使用企业标准成本和目标利润率 |
| `COMPETITIVE` 竞争 | 在可接受范围内降低利润或费用，提高价格竞争力 |
| `AGGRESSIVE` 激进 | 接近内部最低可接受价格，需要更高级别审批 |

### 报价假设

商务人员可以调整：

- 人员数量和投入周期；
- 人员、软件、硬件和外采成本；
- 实施、培训和运维成本；
- 税率；
- 管理费用；
- 利润率；
- 风险准备金；
- 折扣系数；
- 同类项目价格参考。

### 报价目标

报价目标定义为内部目标区间或目标值，不直接等同于最终报价。例如：

```text
最高限价：500 万元
内部目标区间：450～470 万元
当前测算报价：463 万元
最终确认报价：待确认
```

系统需要保存目标报价区间、当前候选报价、最低可接受价格、最高限价和最终确认报价。最低可接受价格属于敏感数据，需要严格权限控制，不能进入普通标书生成上下文。

### 基础流程

```text
读取项目预算和最高限价
→ 读取已确认的 Excel 商务模型
→ 商务人员调整成本、利润率和风险等假设
→ Excel 生成多个报价测算方案
→ 系统导入并比较报价方案
→ 商务人员设置目标报价区间
→ 选择报价策略等级
→ 项目负责人审核
→ 冻结已确认报价方案
→ 标书初稿引用该报价版本
```

### 报价对象

#### 报价假设版本

记录人员投入、项目周期、各类成本、税率、管理费用、利润率、风险准备金、折扣参数、Excel 模板版本、公式版本、修改人和修改理由。

#### 报价方案

同一个假设版本可以生成保守、基准、竞争和激进等候选方案。每个方案保存：

- 测算总成本；
- 含税和不含税报价；
- 利润额和利润率；
- 与预算及最高限价的差额；
- 与历史同类项目的对比；
- 主要风险提示。

### 用户提出的大模型权限

用户明确：

> **大模型可以给出一个推荐选择，最终选择权在用户手上。**

### 已确认的人机分工

```text
Excel / 内部模型负责计算
规则引擎负责硬性校验
大模型负责比较、解释和推荐
商务人员负责最终选择
项目负责人负责确认
系统负责版本和审计留痕
```

### 大模型推荐流程

```text
Excel / 内部模型生成候选报价方案
→ 规则引擎排除无效方案
→ 大模型比较剩余候选方案
→ 推荐一个方案和报价策略等级
→ 说明推荐理由、风险和替代方案
→ 商务人员选择、修改或重新测算
→ 项目负责人确认
→ 冻结最终报价版本
```

大模型可以综合：

- 项目预算和最高限价；
- 价格评分规则；
- 企业成本和目标利润；
- 当前报价假设；
- 历史同类项目报价；
- 公开中标金额；
- 项目重要程度；
- 技术和商务竞争优势；
- 当前证据完整度；
- 项目交付风险；
- 各候选方案的利润和风险差异。

### 大模型推荐输出

推荐结果需要结构化保存：

```json
{
  recommended_scenario_id: PRICE-SCENARIO-C,
  recommended_strategy: COMPETITIVE,
  recommended_quote: 4630000,
  reasoning_summary: [
    报价未超过最高限价,
    利润率仍处于企业授权范围,
    与已确认同类项目的价格区间接近,
    当前技术和案例评分项覆盖较完整
  ],
  key_risks: [
    项目运维周期较长，人员成本存在上涨风险,
    数据迁移工作量尚未完全确认
  ],
  alternative_scenario_id: PRICE-SCENARIO-B,
  confidence: 0.78,
  requires_human_confirmation: true
}
```

### 大模型限制

1. 只能在 Excel 或内部模型已经生成、且通过规则校验的候选方案中推荐，不能凭空创建报价；
2. 超过最高限价、低于企业授权底价、合计错误、税率错误、大小写金额不一致、缺少必填报价项或格式不符的方案，不能进入推荐范围；
3. 推荐必须说明使用的数据、历史参考、报价模型版本、支持因素、风险和数据缺口；
4. 商务人员可以接受推荐、选择其他方案、修改报价假设、要求重新分析，并填写不同选择理由；
5. 激进策略必须展示利润和交付风险，填写理由，并增加商务负责人或更高级别审批。

### 完成结论

- Excel 或内部模型负责生成多个候选报价方案；
- 规则引擎负责限价、成本、税率、合计和格式等硬性校验；
- 大模型在有效候选方案中比较、解释并推荐；
- 大模型不能凭空生成报价，也不能绕过企业授权边界；
- 推荐必须展示数据依据、历史参考、风险和替代方案；
- 商务人员拥有最终选择权，可以接受或拒绝推荐；
- 项目负责人负责最终确认；
- 激进策略需要更高审批和理由留痕；
- 确认后的报价方案版本才允许被标书初稿引用；
- 采购文件、报价假设或 Excel 模型变化后，相关报价版本需要重新确认。

**讨论点完成。**

---

## 八、后续追加规则

从本文件创建后开始：

1. 每完成一个讨论点，立即追加该讨论点的用户问题、助手分析、确认过程和完成结论；
2. 不删除或覆盖已经保存的历史内容；
3. 如果后续修改既有决策，追加“决策变更记录”，说明旧结论、新结论、变更原因和日期；
4. 用户只说“确认”或“可以”时，也要保留其对应的确认对象；
5. 后续继续讨论时，以本文件和 `specs/13-项目决策与工作记录.md` 共同作为恢复上下文的入口。

---

## 九、已完成讨论点 4：查询最新公告、采购包事件和市场补充材料

### 用户提出的业务环节

> 查询最新公告、标包/项目类别事件和市场补充材料。

### 助手对业务目标的校准

这个环节不是普通的新闻搜索，而是：

> **在标书编制期间持续监控项目变化，判断新信息是否会让采购文件、报价、响应章节或审核结论失效。**

原表述中的“标包/项目类别事件”和“市场补充材料”需要拆分为不同信息类型，避免正式项目事实和参考信息混在一起。

### 信息分类

#### 1. 项目官方更新

包括：

- 更正公告；
- 澄清公告；
- 补充文件；
- 答疑文件；
- 采购文件新版本；
- 投标截止时间变更；
- 开标时间或地点变更；
- 预算、最高限价变化；
- 资格条件变化；
- 评分标准变化；
- 采购需求变化；
- 项目暂停、终止或重新采购；
- 现场踏勘和答疑会通知。

这类信息属于项目事实，需要建立版本关系并触发影响分析。

#### 2. 采购包变更事件

“标包事件”统一称为“采购包变更事件”，包括：

- 新增或取消采购包；
- 采购包拆分或合并；
- 采购包预算变化；
- 服务范围变化；
- 资格要求变化；
- 兼投兼中规则变化；
- 采购包编号或名称调整；
- 某个采购包暂停或终止。

当前 V1 虽然只处理单个采购包，但仍需要监控该采购包是否发生变化。

#### 3. 外部参考信息

包括：

- 同类项目和最新中标信息；
- 行业政策、技术标准和价格变化；
- 企业内部补充的产品、人员和项目资料；
- 产品价格或人工成本变化；
- 主要厂商产品版本变化；
- 公开行业案例；
- 项目所在地相关政策。

这类信息属于参考资料，不能直接改变项目事实，也不能与采购人正式发布的更正或澄清文件混在一起。

### 用户确认的信息范围

用户确认“市场补充材料”包括：

1. 同类项目和最新中标信息；
2. 行业政策、技术标准和价格变化；
3. 企业内部补充的产品、人员和项目资料。

三类材料分别管理，不能混为一种资料。

### 基础业务流程

```text
项目创建并确认采购文件
→ 系统持续监控来源平台
→ 发现新公告或补充材料
→ 判断是否属于当前项目
→ 区分正式项目更新、采购包事件或外部参考
→ 保存来源、发布时间和内容快照
→ 分析受影响的要求、报价、章节和审核结论
→ 通知标书编写人
→ 人工确认是否采用
→ 触发局部更新或重新生成
```

### 信息发现方式

#### 助手建议

采用“自动监控 + 人工刷新”双模式：

```text
项目创建后
→ 系统按计划定时检查来源平台
→ 用户也可以点击“立即刷新”
→ 发现新信息后保存页面或文件快照
→ 去重并判断是否与当前项目相关
→ 分类为正式更新、采购包事件或参考资料
→ 进入待确认队列
```

监控频率根据截止时间动态调整：

- 距离投标截止时间较远时低频检查；
- 距离截止时间较近时提高检查频率；
- 截止前重点监控更正、答疑和截止时间变化；
- 内部提交后继续监控撤回、延期或再次更正信息；
- 具体频率根据平台访问限制和甲方配置确定，不能对所有平台无差别高频抓取。

#### 用户确认

> **后台定时监控 + 用户手动刷新 + 临近截止时间动态提高检查频率。**

### 新公告的关联确认

系统发现新公告后不能立即让现有标书内容正式失效，因为可能出现：

- 项目编号相似但并非同一项目；
- 只影响其他采购包；
- 重复发布；
- 非正式转载；
- 中标结果或其他无关公告；
- 标题变化但内容没有实质变化。

系统根据以下字段判断关联关系：

- 来源平台；
- 项目编号；
- 采购包编号；
- 采购人；
- 采购代理机构；
- 项目名称；
- 公告类型；
- 发布时间；
- 原公告链接；
- 文件摘要或哈希。

发现内容后生成待确认事件，例如：

```text
事件类型：采购文件更正
关联项目：当前投标项目
关联采购包：采购包 01
匹配置信度：高
发现时间：系统记录
当前状态：待人工确认
```

人工可以选择：

```text
○ 确认为当前项目正式更新
○ 与当前项目相关，但仅作参考
○ 只影响其他采购包
○ 重复公告
○ 与当前项目无关
○ 暂时无法判断
```

### 已确认的两阶段更新机制

```text
发现新公告
→ PENDING_CONFIRMATION
→ 显示高优先级提醒
→ 暂停生成新的内部提交基线
→ 将相关内容标记为可能已过期
→ 人工确认
├─ 正式更新：建立版本关系并触发影响分析
├─ 仅供参考：进入参考资料库
├─ 其他采购包：不影响当前采购包
└─ 无关或重复：关闭事件
```

用户确认：

> **系统发现高风险更新后先阻止最终内部提交，人工确认关联关系后再正式触发版本失效。**

### 不同事件的影响范围

| 事件 | 可能受影响的内容 |
|---|---|
| 采购需求变化 | 要求清单、响应矩阵、技术章节、风险结论 |
| 资格条件变化 | 资格审查、企业证据、商务响应 |
| 评分标准变化 | 评分项、章节任务、证据映射、覆盖检查 |
| 预算或最高限价变化 | Excel 报价模型、候选报价、报价推荐 |
| 截止时间变化 | 项目计划、证书有效性、保证金和内部审核时间 |
| 服务范围变化 | 技术方案、人员投入、项目周期和报价假设 |
| 采购包变化 | 当前采购包、文件范围、报价表和响应章节 |
| 企业资料更新 | 证据映射、案例章节、人员和资质引用 |
| 行业或市场资料变化 | 只生成参考建议，不自动让标书失效 |

### 影响分析与重新生成流程

```text
人工确认正式更新
→ 系统比较新旧文件
→ 定位新增、删除和修改内容
→ 找出受影响的要求、评分项和时间节点
→ 找出依赖这些对象的章节、证据、报价和审核结论
→ 生成影响清单
→ 用户确认更新范围
→ 系统局部重新抽取或生成
→ 产生新的章节版本
→ 展示新旧内容差异
→ 人工确认新版本
→ 重新运行相关审核任务
```

### 人工内容保护

系统不能静默覆盖已经由标书编写人修改的内容。重新生成时应同时保留：

```text
原人工版本
+ 新采购要求
+ 系统建议修改版本
```

系统展示：

- 新增内容；
- 删除内容；
- 替换内容；
- 修改原因；
- 对应的更正或澄清文件；
- 被保留的人工内容；
- 存在冲突的内容。

用户可以接受、部分接受、保留原文、手动修改或退回重新生成。

### 报价相关更新

预算、最高限价或服务范围变化后：

- 原报价方案标记为 `STALE`；
- 重新运行 Excel 或内部报价模型；
- 原大模型报价推荐失效；
- 重新生成候选方案和推荐；
- 商务人员重新选择；
- 项目负责人重新确认。

### 完成结论

- 系统持续监控项目正式更新、采购包事件和三类参考资料；
- 采用后台定时监控、用户手动刷新和临近截止时间动态提高频率；
- 正式项目更新、采购包事件和外部参考资料分别管理；
- 新公告先进入待确认状态，不直接改变项目事实；
- 高风险更新未确认前暂停生成新的内部提交基线；
- 人工确认后建立文件版本关系并执行对象级影响分析；
- 系统定位受影响的要求、评分项、证据、报价、章节和审核结论；
- 系统只做局部重新抽取和生成，不静默覆盖人工内容；
- 新旧版本通过差异方式展示，由用户确认新版本；
- 市场和行业参考信息只产生建议，不自动让标书结论失效。

**讨论点完成。**

---

## 十、已完成讨论点 5：参考历史标书和团队模板撰写标书正文

### 用户提出的业务环节

> 参考历史标书和团队模板撰写标书正文。

### 生成输入

该环节综合使用前面已经确认的数据：

```text
当前采购文件要求
+ 评分项与响应矩阵
+ 已确认企业证据
+ 已确认历史参考项目
+ 已确认报价版本
+ 团队标书模板
→ 生成投标文件正文初稿
```

### 三类输入的优先级

#### 1. 当前采购文件：最高优先级

决定：

- 必须写哪些章节；
- 每个章节响应什么要求；
- 哪些内容属于实质性要求；
- 哪些评分项必须覆盖；
- 是否有固定格式；
- 是否有字数、页数、表格或附件要求。

#### 2. 团队模板：决定结构和格式

模板可以定义标书目录、章节顺序、标题层级、固定表格、常用说明、图表样式、页眉页脚、风险提示、免责声明和商务响应格式。

团队模板需要版本化，例如：

```text
政府采购信息化服务模板 V2.1
适用范围：软件开发、系统集成、运维服务
更新时间：2026-08-10
```

#### 3. 历史标书：提供参考和复用候选

历史标书可以参考技术方案写作框架、实施计划、项目管理方法、运维方案、人员组织方式、类似案例写法和历史审核问题。

不能直接复制历史客户名称、历史项目名称、历史日期、历史报价、历史人员、旧产品参数、已过期资质或不适用于当前采购文件的承诺。

### 章节级生成

不一次性生成完整标书，而是按章节处理：

```text
确认目录
→ 拆分章节任务
→ 绑定采购要求和评分项
→ 绑定企业证据
→ 绑定历史参考内容
→ 生成章节初稿
→ 自动检查引用和覆盖情况
→ 标书编写人修改确认
→ 进入下一章节
```

每个章节保存当前章节版本、来源采购文件、关联要求、关联评分项、企业证据、历史标书、模板版本、Agent 运行记录和人工修改记录。

### 来源标记

系统生成正文时必须标记内容来源：

```text
来源类型：
- 当前采购文件
- 企业证据
- 历史参考标书
- 团队模板
- Agent 生成内容
- 待人工补充
```

示例：

```text
本项目采用分阶段实施方式，分为需求分析、系统开发、
联调测试、试运行和验收五个阶段。

来源：
- 当前采购文件：REQ-023
- 历史参考：HIST-2024-018 / 实施方案章节
- 当前企业证据：企业实施方法论 DOC-031
- 状态：待项目经理确认
```

### 初稿后的自动检查

系统自动检查：

- 是否覆盖绑定的要求；
- 是否覆盖评分项；
- 是否引用有效企业证据；
- 是否出现历史客户名称；
- 是否出现旧日期和旧报价；
- 是否存在未替换占位符；
- 是否出现当前项目不存在的产品或服务；
- 是否与其他章节的人员、日期、参数和范围冲突；
- 是否缺少风险提示或说明文件。

正文生成结果不是自动完成的最终标书，而是：

> **带来源、带风险标记、带待确认项的章节初稿。**

### 用户确认的模板方案

用户确认团队模板采用系统内置的版本化模板库：

```text
系统内置模板库
→ 按采购类型、项目类别和文件类型选择模板
→ 标书编写人基于模板创建当前项目版本
→ 模板更新生成新版本，不覆盖历史标书
```

V1 不依赖每次上传格式不一致的 Word 模板作为唯一模板来源。

### 完成结论

- 当前采购文件决定正文必须写什么；
- 团队模板决定章节结构和格式；
- 历史标书提供参考和复用候选，不能整份直接复制；
- 正文按章节生成，不一次性生成完整标书；
- 每个章节绑定要求、评分项、证据、历史参考和模板版本；
- 初稿必须展示来源、风险和待确认项；
- 系统自动检查要求覆盖、证据引用、历史信息污染和跨章节一致性；
- 团队模板建设为系统内置、可配置、可版本化的模板库；
- 模板更新生成新版本，不覆盖历史标书。

**讨论点完成。**

---

## 十一、已完成讨论点 6：补充图表、投标风险提示、数据来源和说明文件

### 用户提出的业务环节

> 补充图表、投标风险提示、数据来源和说明文件。

### 助手对产物的拆分

该环节包含四类产物：

```text
标书正文
→ 补充图表
→ 补充风险提示
→ 补充数据来源
→ 补充说明及附件
```

### 1. 图表

系统可以根据已确认数据生成：

- 项目总体架构图；
- 系统技术架构图；
- 功能模块图；
- 项目实施流程图；
- 项目组织结构图；
- 进度计划表或甘特图；
- 人员投入表；
- 服务响应流程图；
- 运维保障流程图；
- 风险管理矩阵；
- 同类项目对比表；
- 评分项响应表；
- 商务和报价汇总表。

每个图表必须绑定数据来源、数据版本、生成时间、关联章节、生成工具或模板和人工确认状态。

Agent 可以生成图表草稿，但涉及人员数量、项目周期、报价和技术参数的图表必须由人工确认。

### 2. 投标风险提示

系统可以识别：

- 资格证书临近到期；
- 类似项目案例证明材料不足；
- 评分项没有对应章节；
- 人员数量或证书不满足要求；
- 报价接近最高限价或企业最低授权价格；
- 采购文件出现新版本；
- 技术参数前后不一致；
- 历史客户名称没有替换；
- 存在无法验证的企业能力表述；
- 要求提交承诺函但当前缺失；
- 章节存在待人工补充内容。

风险提示主要用于内部审核，不能不加区分地全部写入对外投标文件。

### 3. 数据来源

关键数据需要保存来源：

```text
采购文件数据
→ 文件版本、页码、章节和原文

企业能力数据
→ 企业资料、资质证书、人员档案、合同和验收材料

历史参考数据
→ 历史项目、历史标书和公开中标公告

报价数据
→ Excel 模板、公式版本、参数版本和确认结果

行业与市场数据
→ 政策、标准、公开报告和价格资料
```

系统内部保存完整来源；是否在对外投标文件中展示来源，由采购文件要求和团队模板规则决定。

### 4. 说明文件分类

用户确认说明文件包含三类，并分别管理。

#### `BID_ATTACHMENT`：正式投标附件

例如：

- 商务偏离表；
- 技术偏离表；
- 中小企业声明函；
- 无重大违法记录声明；
- 资格承诺函；
- 售后服务承诺；
- 保密承诺；
- 分包或不分包说明；
- 联合体相关说明；
- 采购文件规定的其他声明、函件和附表。

#### `EXTERNAL_EXPLANATION`：可选择对外提交的补充说明

例如：

- 报价口径说明；
- 人员投入说明；
- 工期和服务周期说明；
- 技术参数说明；
- 方案差异说明；
- 材料缺失或后补说明；
- 数据来源说明；
- 特殊实施条件说明。

是否对外提交必须由人工确认。

#### `INTERNAL_NOTE`：仅内部审核说明

例如：

- 风险接受理由；
- 报价策略选择理由；
- 证据不足处理说明；
- 历史方案复用说明；
- 规则豁免理由；
- 未采纳 Agent 建议的原因。

此类内容不能自动进入对外投标文件。

### 材料加入规则

用户确认采用“系统识别 + 人工确认”：

```text
解析采购文件
→ 提取附件、声明和图表要求
→ 建立材料交付清单
→ 系统生成图表及说明文件草稿
→ 标书编写人检查内容和数据
→ 项目负责人确认对外范围
→ 写入标书正文或附件目录
→ 审核完整性和引用关系
```

具体规则：

- 采购文件明确要求的附件自动进入“必须提交清单”；
- 必须材料不能直接删除，只能标记不适用并填写理由；
- 评分项需要但采购文件未明确要求的图表由系统推荐、标书编写人选择；
- 团队模板中的标准图表默认生成，用户可以删除或替换；
- 内部风险和审核说明默认不进入对外标书；
- 对外补充说明必须由项目负责人确认。

### 输出格式

用户确认 V1 输出：

#### 在线可编辑标书

- 在工作台中按章节查看和修改；
- 查看来源、风险和待确认项；
- 支持版本对比。

#### DOCX 导出

- 应用团队模板；
- 插入已确认图表；
- 自动生成目录、页眉页脚和附件目录；
- 允许标书编写人继续精细排版。

#### PDF 内部审核预览

- 用于内部审核；
- 检查目录、分页、图表和附件完整性；
- 不代表已正式递交。

V1 暂不负责生成交易平台加密投标文件、CA 签章、电子签名、自动上传交易平台或取得外部递交回执。

### 用户追问风险是否输出

用户询问：风险提示是否不输出？

助手澄清：风险必须输出，但区分内部风险提示和对外风险内容。

### 内部风险输出

系统必须通过以下方式输出完整风险：

```text
项目风险看板
+ 章节内风险标记
+ 内部审核风险报告
```

例如：

```text
⚠ 高风险：本章节引用的 ISO 证书将在投标截止日前到期
来源：企业证据 CERT-018
影响：资格条件 REQ-012
处理状态：待人工确认
```

风险状态包括：

| 状态 | 含义 |
|---|---|
| `OPEN` | 新发现，尚未处理 |
| `IN_PROGRESS` | 正在补充或修改 |
| `RESOLVED` | 已解决 |
| `WAIVED` | 经授权接受风险 |
| `STALE` | 文件或内容变化后需要重新检查 |
| `CLOSED` | 已复核并关闭 |

高风险问题未解决时，系统不能生成有效内部提交基线。

### 对外风险内容

对外标书可以包含正常的项目风险管理内容，例如：

- 项目实施风险及应对措施；
- 进度风险；
- 数据迁移风险；
- 系统安全风险；
- 运维保障风险；
- 服务连续性风险；
- 应急处置方案。

这些是技术方案的一部分，可以进入标书正文。

以下内部风险不能自动进入对外标书：

- 企业资格可能不满足；
- 证明材料缺失；
- 当前报价接近企业底价；
- 利润率过低；
- 评分项尚未覆盖；
- Agent 置信度不足；
- 内部人员对报价或方案存在分歧。

### 双轨导出机制

#### 内部版本

包含完整风险、来源、批注、审核意见、报价策略、证据缺口和豁免理由。

PDF 内部审核版应增加“仅供内部审核，包含风险提示，不得对外提交”等水印或标识。

#### 对外版本

只包含正式投标内容、采购文件要求的附件和经确认的风险应对方案。

生成对外版本时自动排除：

- 内部批注；
- 内部风险标记；
- 审核意见；
- 报价策略说明；
- 证据缺口；
- Agent 置信度；
- 内部豁免理由。

### 用户最终确认

用户确认采用：

> **内部完整风险输出 + 对外受控风险内容的双轨机制。**

### 完成结论

- 图表根据已确认数据生成，并绑定数据来源和版本；
- 正式附件、可对外说明和内部说明分别管理；
- 采购文件要求的材料自动进入必须提交清单；
- 可选图表和补充说明由系统推荐、人工确认；
- 在线编辑、DOCX 导出和 PDF 内部审核预览纳入 V1；
- 外部加密、CA 签章和自动递交不纳入 V1；
- 系统必须输出内部风险看板、章节标记和审核风险报告；
- 阻断风险未处理时不能生成有效内部提交基线；
- 对外标书只包含正式风险应对内容，不暴露企业内部弱点；
- 内部版和对外版采用严格分离的双轨输出。

**讨论点完成。**

---

## 十二、已完成讨论点 7：提交负责人复核和合规审核

### 用户提出的业务环节

> 提交负责人复核和合规审核。

### 审核目标

该环节不是简单点击审批，而是：

> **不同角色按照各自职责检查标书，并把问题结构化退回、修改、复核和关闭。**

### 审核角色

用户确认 V1 至少设置两个独立审核角色，不能由项目负责人同时代替合规审核。

#### 1. 项目负责人

负责检查：

- 项目理解是否准确；
- 技术方案是否可实施；
- 服务范围是否完整；
- 人员和资源投入是否合理；
- 历史项目和案例是否适用；
- 报价方案是否完成商务确认；
- 评分项是否充分响应；
- 是否存在过度承诺；
- 是否同意进入合规审核；
- 是否需要退回标书编写人修改。

项目负责人对项目整体质量负责，但不代替法务或合规人员作专业合规判断。

#### 2. 合规/法务审核人

负责检查：

- 资格条件是否满足；
- 实质性要求是否响应；
- 是否存在投标无效或否决风险；
- 商务和技术偏离是否正确；
- 企业资质、人员和案例材料是否真实、有效；
- 数据来源和证明材料是否可追溯；
- 承诺函、声明函和附件是否齐全；
- 投标表述是否存在法律或合规风险；
- 是否泄露企业内部敏感信息；
- 内部风险是否解决或获得授权豁免；
- 对外版本是否排除内部批注和审核信息。

### 待审核版本包

提交审核时，系统冻结完整的待审核版本包：

```text
标书正文版本
+ 图表版本
+ 正式附件清单
+ 企业证据清单
+ 报价确认版本
+ 要求与评分项覆盖矩阵
+ 内部风险清单
+ 待确认事项
+ 当前采购文件版本
```

审核人员看到完整项目依据，而不只是一个 DOCX 文件。

### 提交前自动预检

标书编写人点击“提交审核”前，系统检查：

- 是否还有未生成章节；
- 是否存在未替换占位符；
- 是否缺少必须附件；
- 是否存在未确认企业证据；
- 是否存在未确认报价；
- 是否存在阻断风险；
- 是否有未确认的采购文件更新；
- 是否存在跨章节冲突；
- 当前标书是否基于最新采购文件；
- 是否还有待人工补充内容。

存在阻断问题时不能提交审核；普通风险可以提交，但必须在审核包中展示。

### 结构化审核问题

审核意见必须绑定具体业务对象，不能只保存一段总意见。例如：

```text
问题类型：评分项未充分响应
关联章节：5.3 数据迁移方案
关联评分项：SCORE-018
严重等级：高
处理要求：补充迁移工具、验证方案和回退机制
处理人：技术方案编写人
截止时间：系统记录
```

### 用户对工作量的疑问

用户询问：设置项目负责人和合规/法务两个独立审核角色，是否会增加较大的用户工作量？

### 已确认的审核减负机制

如果要求审核人逐章从头检查，工作量会很大。因此用户确认采用：

> **异常驱动审核 + 角色专属视图 + 批量处理 + 增量复核。**

#### 项目负责人审核视图

只重点展示：

```text
项目摘要
+ 关键方案
+ 报价结论
+ 评分覆盖
+ 重大变化
+ 高风险
+ 待负责人决策事项
```

项目负责人不需要逐句检查所有证据和合规条款。

#### 合规审核视图

重点展示：

- 资格条件；
- 实质性要求；
- 商务和技术偏离；
- 企业证据；
- 声明函和承诺函；
- 必须附件；
- 数据来源；
- 对外敏感信息；
- 阻断风险和豁免事项。

合规人员不负责判断技术方案是否足够专业。

#### 异常驱动颜色分级

```text
绿色：规则检查通过，来源完整
黄色：存在疑问，需要抽查
红色：高风险或缺少材料，必须处理
灰色：本次未发生变化，无需重复审核
```

审核人可以批量通过绿色项目，集中检查黄色项目，逐条处理红色项目，跳过未变化的灰色内容。

#### 增量复核

第一次提交进行相对全面审核，后续修改只重新打开：

- 被修改的章节；
- 被替换的企业证据；
- 发生变化的报价；
- 新增或变化的采购要求；
- 受影响的评分项；
- 与修改内容相关的风险。

```text
章节修改
→ 系统计算影响范围
→ 原审核结论部分失效
→ 只创建受影响的复核任务
```

#### 减少填写工作量

审核意见采用：

```text
标准问题类型
+ 严重等级
+ 处理要求模板
+ 可选补充说明
```

系统推荐问题类型和处理要求，审核人确认或修改，不从空白文本开始手打。

同时支持批量通过、批量分配处理人、常用审核意见、一键定位原文、一键查看证据、新旧版本差异、自动生成审核摘要和修改后自动回到原审核人。

### 审核顺序

用户确认采用两级正式审核：

```text
标书编写人提交
→ 系统自动预检
→ 项目负责人复核
→ 合规/法务审核
→ 问题处理与增量复核
→ 内部提交确认
```

项目负责人先确认项目方案、人员资源、报价和整体内容基本稳定，避免技术方案和报价大改后让合规人员重复审核。

合规人员基于相对稳定的版本检查资格、实质性要求、偏离表、企业证据、声明函、附件、敏感信息和阻断风险。

### 修改后的定向复核规则

- 普通文字调整：只由项目负责人复核；
- 企业证据、资格、承诺或附件变化：重新进入合规审核；
- 报价变化：重新进入商务确认，并由项目负责人复核；
- 采购文件变化：根据影响范围重新打开对应审核；
- 大范围方案变化：项目负责人和合规人员都需要增量复核。

### 完成结论

- 项目负责人和合规/法务审核人是两个独立角色；
- 系统提交的是完整待审核版本包，不只是 Word 文件；
- 提交前自动检查阻断问题和资料完整性；
- 审核问题必须绑定章节、要求、评分项、证据或附件；
- 采用异常驱动、角色专属视图、批量处理和增量复核降低工作量；
- 项目负责人先复核，合规/法务后审核；
- 修改后根据影响范围进行定向复核，不要求所有角色从头重审；
- 阻断问题未解决时不能进入有效内部提交。

**讨论点完成。**

---

## 十三、已完成讨论点 8：根据审核意见反复修改

### 用户提出的业务环节

> 根据审核意见反复修改。

### 修改闭环目标

该环节不是审核人在 Word 中写一句批注、标书编写人自行寻找位置修改，而是：

> **每条审核意见形成可跟踪的问题任务，系统定位受影响内容，Agent 生成修改建议，编写人确认修改，审核人复核关闭。**

### 审核问题任务

每条审核意见形成结构化问题，例如：

```text
问题编号：ISSUE-028
问题类型：评分项响应不足
严重等级：高
提出人：项目负责人
关联章节：5.3 数据迁移方案
关联评分项：SCORE-018
问题说明：缺少迁移验证和失败回退机制
处理要求：补充验证方法、回退流程和责任人
处理人：技术方案编写人
截止时间：系统记录
```

问题不能只存在于 Word 批注、聊天群或线下记录中。

### 问题状态

| 状态 | 含义 |
|---|---|
| `OPEN` | 新提出，尚未开始 |
| `ASSIGNED` | 已分配责任人 |
| `IN_PROGRESS` | 正在处理 |
| `PENDING_REVIEW` | 已修改，等待原审核人复核 |
| `RESOLVED` | 审核人确认已解决 |
| `REOPENED` | 修改不充分，重新打开 |
| `WAIVED` | 经授权接受风险 |
| `CLOSED` | 最终关闭并纳入提交版本 |

### 修改方式

#### 1. 人工修改

适用于专业判断、项目特殊方案、人员和资源安排、报价说明及需要企业内部决策的内容。

#### 2. Agent 辅助修改

系统根据以下上下文生成修改建议：

```text
当前章节
+ 审核意见
+ 当前采购要求
+ 关联评分项
+ 已确认企业证据
+ 已确认历史参考
→ 建议修改版本
```

标书编写人可以全部接受、部分接受、拒绝建议、手动修改后接受或要求重新生成。

#### 3. 重新生成章节

适用于采购要求或方案发生较大变化的情况。系统保留原人工版本，生成新的候选版本，并通过差异对比让用户选择，不能直接覆盖。

### 用户确认的 Agent 修改权限

用户确认：

> **Agent 只生成待确认的修改建议和候选版本，标书编写人确认后才能写入正式章节。**

Agent 可以理解审核意见、定位章节和证据、生成修改建议、检查影响范围、生成候选内容和处理说明。

Agent 不能静默覆盖人工内容、自动关闭审核问题、擅自接受风险、修改最终报价、替换企业证据或绕过审核人复核。

### 修改差异

系统必须展示原内容和修改后内容，例如：

```diff
- 原内容：项目上线前完成数据迁移。
+ 修改建议：项目上线前依次完成迁移演练、数据校验、
+ 正式迁移和失败回退验证，并由数据迁移负责人确认结果。
```

同时说明：

- 修改对应哪条审核意见；
- 使用了哪些采购要求；
- 使用了哪些企业证据；
- 是否影响其他章节；
- 是否需要重新审核；
- 是否产生新的风险。

### 修改影响范围

一次修改可能影响多个位置。例如修改项目经理姓名后，需要检查项目组织结构图、人员简历、人员投入表、实施方案、服务承诺、商务响应和附件证明材料。

```text
处理一个审核问题
→ 修改目标章节或数据对象
→ 系统分析影响范围
→ 提示需要同步更新的位置
→ 编写人确认批量修改
→ 生成新版本
→ 只重新打开受影响的审核任务
```

### 多轮修改收敛

系统记录每轮审核，例如：

```text
第 1 轮：项目负责人提出 12 个问题
→ 解决 10 个，重新打开 2 个

第 2 轮：合规审核提出 6 个问题
→ 解决 5 个，豁免 1 个

第 3 轮：文件更正导致 3 个问题重新打开
→ 全部解决
```

工作台展示当前未解决问题、阻断问题、每轮新增问题、重新打开问题、处理时间、修改次数、责任人和是否达到提交条件。

### 问题关闭权限

用户确认采用“谁提出、谁复核”的原则：

```text
审核人提出问题
→ 标书编写人或责任人修改
→ 提交处理说明和新版本
→ 原审核人查看差异
→ 原审核人确认解决或重新打开
```

具体规则：

- 项目负责人提出的问题，由项目负责人确认解决；
- 合规/法务提出的问题，由原合规审核人确认解决；
- 商务报价问题，由商务审核人确认；
- 系统自动检查问题，在规则重新运行通过后标记检查通过并保留记录；
- 阻断风险不能由标书编写人自行关闭；
- 风险豁免需要有权限的负责人填写理由并确认；
- 原审核人无法处理时，可以转交同角色授权人员，但必须记录转交原因。

审核人复核时只需查看原问题、原内容、修改后内容、新旧差异、处理说明、新增或替换证据、系统检查结果及影响范围。

审核人可以选择：

```text
○ 确认解决
○ 修改不充分，重新打开
○ 转交其他审核人
○ 申请风险豁免
```

### 完成结论

- 每条审核意见形成结构化、可分配、可跟踪的问题任务；
- 支持人工修改、Agent 辅助修改和章节重新生成；
- Agent 只生成候选修改，不能自动写入正式版本；
- 修改必须展示新旧差异、来源、影响范围和新增风险；
- 修改一个数据对象后，系统检查所有依赖位置；
- 每轮审核和修改均保存版本及问题统计；
- 标书编写人不能自行关闭审核问题；
- 问题原则上由原审核角色复核关闭；
- 修改不充分时可以重新打开，风险接受必须经过授权豁免；
- 后续只重新审核受影响内容，不要求所有角色从头复核。

**讨论点完成。**

---

## 十四、已完成讨论点 9：内部提交与受控交接

### 用户提出的业务环节

> 内部提交或对客户进行受控分发。

### 术语澄清

助手指出，在投标场景中“对客户进行受控分发”不够准确，可能错误指向采购人、采购代理机构、甲方内部人员、合作伙伴或未来其他系统客户。

用户最终确认该环节是：

> **内部受控交接给投标递交责任人。**

它不是由系统直接向采购人递交，也不是对外客户资料分发。

### 内部提交条件

只有满足以下条件，系统才能生成内部提交版本：

- 当前采购文件版本已经确认；
- 没有未确认的更正、澄清或补充文件；
- 必须章节已经完成；
- 必须附件已经齐全；
- 企业证据已经确认；
- 报价版本已经确认；
- 项目负责人审核通过；
- 合规/法务审核通过；
- 阻断风险已经解决或获得授权豁免；
- 对外版本已经排除内部批注、风险和敏感信息；
- DOCX 和 PDF 预览检查通过。

### 内部提交基线

项目负责人点击“内部提交”后，系统冻结不可静默修改的基线：

```text
基线编号：BASELINE-2026-018-V1
采购文件版本：DOC-V4
标书正文版本：BID-DOC-V12
报价版本：PRICE-V6
附件清单版本：ATTACHMENT-V5
企业证据版本：EVIDENCE-SNAPSHOT-V3
风险清单版本：RISK-V8
项目负责人：系统记录
合规审核人：系统记录
提交时间：系统记录
```

冻结后如需修改，不能覆盖原基线，而应重新打开项目并生成新的基线版本。

### 交接文件包

系统生成：

```text
投标文件正文 DOCX
+ 内部审核预览 PDF
+ 正式附件
+ 报价文件
+ 交接清单
+ 文件版本清单
+ 文件摘要或哈希
```

其中：

- 对外文件不包含内部风险和审核批注；
- 内部交接清单可以包含待递交注意事项；
- 文件清单记录文件名、版本、大小和摘要，避免递交错误版本。

### 受控交接

投标递交责任人通过系统接收文件，交接过程记录：

- 接收人；
- 交接目的；
- 交接版本；
- 下载权限；
- 下载次数；
- 链接有效期；
- 是否允许再次分享；
- 是否需要水印；
- 下载时间；
- 撤回状态。

示例：

```text
接收人：投标专员
用途：北京市某项目电子投标文件制作
允许下载：2 次
有效期：24 小时
水印：姓名 + 工号 + 下载时间
禁止转发：是
```

### 旧版本控制

如果采购文件再次更正，或者标书项目被重新打开：

```text
原内部提交基线失效
→ 原下载链接自动撤回
→ 旧文件标记“禁止递交”
→ 通知投标递交责任人
→ 重新审核并生成新基线
```

已经下载到用户电脑的旧文件无法由系统自动删除，因此：

- 文件名包含版本号；
- PDF 和交接清单带明显版本标识；
- 旧版本标记“已失效，禁止递交”；
- 递交责任人必须确认使用最新有效基线。

### 递交责任人确认

接收人需要确认：

```text
○ 已收到文件包
○ 文件数量与清单一致
○ 当前版本是最新有效版本
○ 已了解递交截止时间
○ 已了解 CA、签章和加密要求
○ 已开始外部递交
```

确认后，内部受控交接完成。

### 外部递交结果登记

用户确认外部递交仍由人工完成，但投标递交责任人需要回到系统中登记结果：

```text
外部递交由人工完成
→ 回到系统登记递交平台、时间和操作人
→ 上传递交回执或截图
→ 标记递交成功、失败或待确认
```

系统不自动操作外部交易平台，但通过人工登记形成项目闭环。

递交记录至少包括：

- 递交平台；
- 递交项目和采购包；
- 使用的内部提交基线；
- 递交人；
- 递交时间；
- 递交状态；
- 回执、截图或证明文件；
- 失败原因或待确认说明；
- 后续更正或重新递交记录。

### 完成结论

- 最后环节统一为内部提交和受控交接，不使用“对客户分发”的模糊表述；
- 所有必审任务、附件、报价和阻断风险满足条件后才能提交；
- 内部提交冻结采购文件、正文、报价、附件、证据和风险版本；
- 修改不能覆盖已提交基线，只能生成新版本；
- 系统生成带版本清单和摘要的交接文件包；
- 文件通过限时、限次、水印和禁止转发等方式受控交接；
- 基线失效后撤回下载链接并通知递交责任人；
- 递交责任人必须确认收到文件并使用最新有效版本；
- 外部平台递交仍由人工完成；
- 人工递交后回到系统登记结果并上传回执，形成完整闭环。

**讨论点完成。**

---

## 十五、已完成讨论点 10：业务痛点覆盖分析与新增痛点

### 用户提出的分析任务

用户要求将已澄清的端到端业务流程与以下六个原始业务痛点逐项对比，检查哪些问题尚未解决：

1. 数据和工具分散；
2. 数据口径和计算过程难统一；
3. 同类项目对比和报价测算重复劳动较多；
4. 标书正文格式和图表标准不统一；
5. 合规问题暴露偏后；
6. 标书提交和后续维护缺少统一闭环。

### 总体判断

六个原始痛点都有对应解决方案，没有完全遗漏的大项，但存在两类问题：

1. 部分痛点还残留投研项目术语或模糊表述；
2. 部分痛点已经完成产品方案设计，但实现规则仍需在技术方案阶段继续细化。

覆盖情况：

| 痛点 | 当前覆盖情况 | 仍需补充 |
|---|---|---|
| 数据和工具分散 | 部分解决 | 平台连接器、数据冲突和授权方式 |
| 数据口径和计算难统一 | 部分解决 | 商务指标字典、Excel 模板治理 |
| 同类项目和报价重复劳动 | 基本解决 | 相似度规则和推荐效果评估 |
| 正文格式和图表不统一 | 基本解决 | DOCX 精细排版和图表编辑 |
| 合规问题暴露偏后 | 基本解决 | 合规规则库和豁免权限 |
| 提交和后续维护缺少闭环 | 基本解决 | 归档策略和外部递交边界 |

### 痛点 1：数据和工具分散

已经设计统一的“投标项目情报与资料汇聚工作台”：

```text
来源平台 + 外部项目编号
→ 获取公告、采购文件和更正信息
→ 关联企业知识库
→ 关联历史标书库
→ 关联企业证据库
→ 关联 Excel 报价模板
→ 汇聚到当前投标项目
```

已确认公开信息自动获取，登录、报名和 CA 受限文件由甲方授权接入或人工上传；项目事实、企业证据、历史参考、报价数据和 Agent 结论分别管理。

仍需在技术方案中明确：

- V1 实现哪些平台连接器；
- API、网页采集、授权账号和人工导入的边界；
- 同一项目跨平台去重与合并；
- 数据冲突时的事实源优先级；
- 下载失败后的重试和人工补充；
- 平台账号、Cookie、CA 和下载文件安全管理；
- 外部数据过期判断。

原痛点中的“公告”重复，“项目市场”含义模糊。建议改为：

> 招标公告、采购文件、澄清更正、同类项目、中标结果、企业资料、历史标书、报价模型和审核记录分散在不同系统中。

### 痛点 2：数据口径和计算过程难统一

已确认 V1 不替代 Excel；Excel 或内部模型继续负责计算，系统统一管理模板、项目事实、报价假设、公式版本、计算结果、异常校验和人工确认。

仍需明确：

- 商务指标清单；
- 指标标准名称、单位、公式和数据来源；
- 指标口径维护责任人；
- Excel 模板审核发布流程；
- 可修改单元格和锁定公式；
- 不同 Excel 格式读取方式；
- 线下修改后的版本识别；
- 最低可接受价格等敏感数据权限；
- 模板或公式变化后的历史报价重算范围。

需要建立：

> **商务指标字典 + Excel 模板注册中心 + 公式版本管理。**

### 痛点 3：同类项目对比和报价测算重复劳动

已经形成：

```text
自动推荐同类项目
→ 横向对比
→ 人工确认项目池
→ 按章节和用途绑定历史内容
→ 冻结参考池版本
→ 生成章节初稿
```

报价方面，Excel 生成候选方案，规则引擎排除无效方案，大模型比较、解释并推荐，商务人员最终选择，项目负责人确认。

仍需明确：

- 同类项目匹配维度权重；
- 预算、类别和评分结构的优先级；
- 相似项目推荐效果评估；
- 历史项目标签维护；
- 历史数据缺失处理；
- 跨年份价格调整；
- 大模型报价建议离线评测；
- 用户接受或拒绝推荐后的反馈闭环。

### 痛点 4：标书正文格式和图表标准不统一

已确认建设版本化模板库，当前采购文件决定写什么，团队模板决定结构和格式，历史标书提供参考；正文按章节生成，图表绑定数据来源和版本，支持在线编辑、DOCX 导出和 PDF 内部审核预览。

仍需明确：

- DOCX 模板占位符和组件；
- Word 线下修改是否回传；
- 在线版本和线下版本冲突处理；
- 图表是图片还是可编辑对象；
- 图表编号、表格编号和交叉引用；
- 目录、页码、页眉页脚一致性；
- 超大 DOCX 导出稳定性；
- 附件插入、分页和打印检查。

需要单独设计文档生成和排版引擎。

### 痛点 5：合规问题暴露偏后

当前方案已经把合规检查前移到采购文件解析、要求识别、证据检查、章节生成、提交预检、项目负责人复核和合规审核全过程。

原文中的“投资表述不规范”是投研项目残留，必须删除；“响应等级依据不充分”已经拆分为采购要求响应状态、报价策略等级和风险等级。

建议改为：

> 常见问题包括数据来源不清、投标承诺或合规表述不规范、资格与实质性要求遗漏、评分项响应不足、风险提示不足、报价策略依据不完整，以及声明函和附件缺失等。

仍需明确：

- 合规规则库维护责任人；
- 规则按采购制度和项目类型分类；
- 法规、政策和甲方制度更新机制；
- 可豁免风险范围；
- 风险审批权限矩阵；
- 合规记录保存期限；
- 敏感信息范围；
- 合规抽查比例配置。

### 痛点 6：标书提交和后续维护缺少统一闭环

已经形成：

```text
项目负责人审核
→ 合规/法务审核
→ 阻断风险清零或豁免
→ 冻结内部提交基线
→ 生成交接文件包
→ 受控交接给投标递交责任人
→ 人工完成外部递交
→ 回到系统登记结果
→ 上传回执
```

原文中的“客户分发和阅读记录”不适合投标场景，建议改为：

> 内部提交、受控交接、下载、撤回、外部递交和递交回执需要统一管理。

原文中的“标包/项目类别政策发生变化”建议改为：

> 当采购文件、采购包、预算、最高限价、截止时间、采购政策、技术标准或报价假设发生变化时，系统需要判断影响范围并提醒重新审核。

仍需明确：

- 项目归档和数据保留期限；
- 递交成功后的项目状态；
- 递交失败或重新递交流程；
- 已下载旧文件的风险控制；
- 撤回和重新提交权限；
- 外部回执真实性校验；
- 中标、未中标和终止项目归档；
- 项目结果是否进入历史项目库。

### 建议新增的三个痛点

助手建议增加：

7. 多人协作、任务分配和进度跟踪缺少统一管理；
8. 企业资料的有效性、完整性和使用权限难以统一管理；
9. AI 生成内容缺少事实依据、来源定位和可信审核机制。

用户要求进一步解释这三个痛点，并最终确认可以纳入。

### 新增痛点 7：多人协作、任务分配和进度跟踪缺少统一管理

一份投标文件通常涉及标书编写人、技术方案人员、商务报价人员、资料管理员、项目负责人、合规/法务人员和投标递交责任人。

实际问题包括：

- 章节责任人不清；
- 正文完成但证明材料未准备；
- 报价修改后未同步正文；
- 项目负责人审核旧版本；
- 合规问题散落在聊天工具中；
- 临近截止时间才发现承诺函未完成；
- 多人同时修改 Word，产生多个“最终版”；
- 审核人无法快速知道本次修改范围。

原有“数据和工具分散”主要描述系统分散，“合规问题暴露偏后”主要描述审核问题，均未完整覆盖多人任务、责任、进度和版本协同。

业务影响：

- 项目进度不透明；
- 责任人不清；
- 修改遗漏；
- 重复审核；
- 多版本冲突；
- 截止时间风险；
- 问题难以追责和复盘。

系统通过投标任务 Dashboard 统一展示：

```text
项目截止时间
+ 项目状态
+ 章节任务
+ 材料任务
+ 报价任务
+ 审核任务
+ 风险任务
+ 责任人
+ 完成进度
+ 超期提醒
```

每个任务记录负责人、截止时间、前置任务、状态、文件版本、待处理问题和修改审核历史。

该痛点直接支撑 Dashboard、任务分配、截止提醒、状态机和增量复核。

### 新增痛点 8：企业资料的有效性、完整性和使用权限难以统一管理

企业知识库中“存在一份材料”不等于它可以用于当前投标项目。系统还必须判断：

- 材料是否属于当前投标主体；
- 是否已经过期；
- 投标截止日是否仍有效；
- 是否有完整扫描件；
- 是否允许当前项目使用；
- 是否涉及个人或客户敏感信息；
- 是否需要合同、验收材料共同证明；
- 是否已经被新版本替代。

典型问题包括：

- 同一证书存在多个版本；
- 旧证书失效但仍可检索；
- 已离职人员仍出现在历史标书；
- 案例有合同但缺少验收材料；
- 案例名称相似但业务范围不匹配；
- 敏感材料被无权限人员使用；
- 历史标书包含其他客户敏感信息；
- Agent 找到材料但无法证明其真实有效。

普通知识库只解决“能不能搜索到资料”，投标系统还要解决“是否真实、有效、完整、有权限，并适用于当前项目和截止时间”。

企业材料需要结构化保存：

```text
材料所属组织
+ 材料类型
+ 有效期
+ 当前状态
+ 适用范围
+ 保密等级
+ 来源文件
+ 证明关系
+ 人工确认状态
```

系统处理流程：

```text
企业资料上传
→ 结构化提取
→ 有效期和主体检查
→ 保密等级和权限控制
→ 人工核验
→ 建立企业证据对象
→ 与要求、评分项和章节建立映射
→ 投标截止日再次校验
```

只有经过确认的证据才能进入正式标书。

该痛点直接支撑企业证据库、有效期检查、权限控制和证据映射。

### 新增痛点 9：AI 生成内容缺少事实依据、来源定位和可信审核机制

大模型生成得像真的，不代表内容真实。可能出现：

- 编造企业能力；
- 引用错误或不完整材料；
- 漏掉实质性要求或评分项；
- 带入历史客户名称、报价、日期、人员和参数；
- 不同章节的周期、人员和报价矛盾；
- 关键表述缺少来源，审核人无法验证。

如果系统只能生成一份看起来完整的 Word，却无法说明内容依据、采购文件版本、企业证据、评分项和确认人，它只能是写作 Demo，无法进入真实投标流程。

业务影响：

- 虚构企业能力；
- 作出无法履行的承诺；
- 使用错误或过期材料；
- 遗漏高风险要求；
- 审核人员无法验证；
- 输出无法追责；
- 用户不敢实际使用生成结果。

系统采用证据约束生成：

```text
当前采购要求
+ 已确认评分项
+ 已确认企业证据
+ 已确认历史参考
+ 已确认报价版本
+ 团队模板
→ 生成带来源的章节初稿
```

每段关键内容记录来源采购文件、要求、评分项、企业证据、历史参考、模型与提示词版本、置信度和人工确认状态。

资格与合规 Agent、评分点覆盖 Agent、一致性 Agent 和风险复核 Agent 分工检查；证据不足时必须输出“证据不足，需要人工补充或确认”，不能让模型自行补齐企业事实。

该痛点直接支撑带来源生成、无证据拒答、多 Agent 审核和人工确认。

### 用户确认

用户确认可以新增以上三个痛点。

### 完成结论

- 原有六个痛点均有解决方案，但部分实现规则仍需在技术方案阶段明确；
- 修正“投资表述”“响应等级”“客户分发”“项目市场”等投研残留或模糊术语；
- 新增多人协作和进度管理痛点，用于支撑 Dashboard 和任务状态机；
- 新增企业资料质量和权限痛点，用于支撑企业证据库；
- 新增 AI 可信度痛点，用于支撑证据约束、多 Agent 审核和人工确认；
- 最终项目背景建议使用九个业务痛点。

**讨论点完成。**

---

## 十六、核心价值未落实点讨论 1：数据与工具接入深度

### 讨论背景

用户明确要求不修改甲方已经确定的三条项目核心价值，而是针对尚未落实的方面继续讨论：

```text
让系统承担重复、标准化、可规则化的工作；
让标书编写人聚焦标包/项目类别理解、项目判断、报价假设和投标方案确认；
让负责人和商务/法务审核人员更高效地复核标书质量和提交风险。
```

经分析，当前仍需继续落实以下四个方面：

1. 外部平台和内部系统接入到什么程度；
2. Excel 商务指标和报价测算如何形成系统闭环；
3. 如何避免大量人工确认反而增加标书编写人工作量；
4. 如何使用指标证明三条核心价值已经实现。

本次先讨论第一个方面：数据与工具接入深度。

### 方案 A：所有平台全部自动接入

标书编写人输入项目编号后，系统自动登录各个平台、下载文件、获取历史数据和企业资料。

优点是用户操作最少、自动化程度最高。问题是不同平台的接口、登录、验证码和授权方式不同；部分平台依赖 CA、专用客户端或加密环境；还可能受到反爬、接口变更和访问频率限制。因此 V1 建设成本高，稳定性难以保证，不适合作为统一接入要求。

### 方案 B：分级接入，自动获取与人工兜底结合

- 公开且允许访问的平台：系统自动查询公告并下载公开文件；
- 提供正式接口的平台：通过甲方授权 API 接入；
- 需要账号登录的平台：具备授权和技术条件时使用连接器接入，无法连接时提示用户上传；
- 需要 CA、专用客户端或加密环境的平台：系统不自动操作，由用户下载后上传；
- 企业知识库和历史标书库：通过 API、数据库同步或受控文件目录接入；
- Excel 报价模板：由系统管理模板和版本，Excel 或内部模型继续负责实际计算。

用户操作流程：

```text
输入项目来源和项目编号
→ 系统调用对应数据连接器查询项目
→ 汇总公告、采购文件和补充文件
→ 记录数据来源、获取时间和文件版本
→ 用户确认当前有效采购文件
→ 获取失败时提示用户补充链接或上传文件
→ 确认后进入文件解析和初稿生成
```

该方案的重点不是承诺所有平台都能自动下载，而是建立统一的数据连接器、来源记录和人工兜底机制。单个平台无法访问时，不会阻断整个投标项目流程。

### 方案 C：V1 全部依靠人工上传

优点是实现成本最低，不受外部平台接口和登录方式影响。问题是用户仍需在多个平台之间查找和下载资料，系统只承担后续解析工作，无法充分解决数据与工具分散问题，也没有充分落实“让系统承担重复、标准化、可规则化工作”的核心价值。

### 推荐方案

推荐方案 B：分级接入，自动获取与人工兜底结合。该方案能够在自动化价值、建设成本、平台限制和流程稳定性之间取得平衡，并支持未来逐步增加新的平台连接器。

### 用户确认

用户选择并确认方案 B。

### 完成结论

- V1 采用分级数据接入模式；
- 公开或具备正式接口的数据尽可能自动获取；
- 登录、授权和内部系统通过经过授权的连接器接入；
- CA、专用客户端和加密文件保留人工下载上传方式；
- 任一自动接入失败时必须提供链接补充或文件上传兜底；
- 所有获取文件记录来源、时间、版本和确认状态；
- 只有用户确认的有效采购文件才能进入正式解析和初稿生成流程。

**讨论点完成。**

---

## 十七、核心价值未落实点讨论 2：Excel 商务指标与报价测算闭环

### 讨论背景

在确认数据与工具采用分级接入方案后，继续讨论 Excel 商务指标和报价测算如何形成系统闭环，以落实“让系统承担重复、标准化、可规则化工作”的核心价值。

### 方案 A：只上传最终报价

商务人员在系统外完成 Excel 测算，只把最终报价填入系统。

该方案实现简单，但模板版本、公式、报价假设和计算过程仍然不可追溯，无法解决数据口径和计算过程难统一的问题。

### 方案 B：系统治理 Excel，Excel 负责计算

系统不重新实现 Excel 的全部计算公式，而是管理整个计算过程：

```text
管理员发布受控 Excel 模板
→ 系统记录模板版本、适用项目和公式校验值
→ 商务人员填写成本、税率、利润率、风险系数等假设
→ Excel 或内部模型执行计算
→ 系统读取约定单元格中的计算结果
→ 校验预算、最高限价、合计、税率和异常值
→ 形成多个有效报价候选方案
→ 大模型分析各方案并给出推荐与风险说明
→ 商务人员最终选择
→ 项目负责人确认
→ 冻结正式报价版本
→ 标书初稿只能引用该确认版本
```

系统管理内容包括：

- Excel 模板名称、版本、适用范围和审批状态；
- 输入字段与 Excel 单元格映射；
- 输出字段与 Excel 单元格映射；
- 公式区域和文件校验值，防止公式被私自修改；
- 报价假设、执行人、执行时间和计算结果；
- 预算、最高限价、税率、合计及跨章节一致性校验；
- 大模型推荐理由、用户最终选择及选择原因；
- 报价变更后的影响章节和重新确认状态。

如果甲方已有内部报价系统，系统可以通过 API 获取计算结果；如果暂时没有接口，则采用“受控模板下载—本地计算—结果上传”的方式兜底。

### 方案 C：将 Excel 全部重写成系统报价引擎

该方案自动化程度最高，但需要重写大量行业和项目类型公式，公式兼容、宏、插件和历史模板迁移成本很高，不适合 V1。

### 推荐方案

推荐方案 B：系统负责模板治理、数据口径、过程留痕、结果校验和推荐解释，Excel 或甲方内部模型继续负责具体计算。

### 用户确认

用户选择并确认方案 B。

### 完成结论

- V1 不废弃甲方现有 Excel 模板和内部报价模型；
- 系统建立受控 Excel 模板库并进行版本管理；
- 系统管理输入口径、报价假设、公式校验和输出字段映射；
- Excel 或内部模型继续负责实际计算；
- 系统读取计算结果并执行预算、最高限价、合计、税率和一致性校验；
- 规则引擎排除不满足约束的报价方案；
- 大模型只能在有效候选方案中提供推荐、理由和风险说明；
- 商务人员拥有最终选择权，项目负责人负责确认；
- 只有冻结并确认的报价版本才能进入标书初稿和内部交接基线；
- 报价假设、模板或结果变化后，相关章节和审核结论进入待复核状态。

**讨论点完成。**

---

## 十八、核心价值未落实点讨论 3：降低人工确认工作量

### 讨论背景

系统需要保留人工确认机制，但如果采购要求、企业证据、历史参考、章节内容和风险全部要求逐条确认，可能把原有编写工作变成大量点击确认工作，反而增加用户负担。因此需要明确自动处理与人工确认的边界。

### 方案 A：所有结果逐项人工确认

采购要求、企业证据、历史参考、每个章节和每条风险均要求用户逐项点击确认。

该方案风险控制严格，但用户操作量很大，系统可能从“减少工作”变成“制造确认工作”。

### 方案 B：风险分级、例外驱动确认

系统自动完成低风险事项，只把异常、高风险和关键决策交给用户。

| 级别 | 处理方式 | 示例 |
|---|---|---|
| 低风险 | 自动处理，可批量查看或抽查 | 格式调整、目录生成、普通字段引用 |
| 中风险 | 支持批量确认 | 一般证据匹配、历史项目推荐、普通章节初稿 |
| 高风险 | 必须逐项人工确认 | 资格条件、实质性要求、重要评分项、关键承诺 |
| 阻断风险 | 必须解决或授权豁免 | 资格不满足、报价超限、缺少强制附件 |

用户主要通过“待确认事项中心”处理例外：

```text
系统自动运行
→ 低风险事项自动处理
→ 中风险事项归入批量确认
→ 高风险事项进入个人待办
→ 阻断风险置顶并暂停交接基线
→ 用户只查看问题、差异和系统建议
```

必须保留人工确认的关键关口：

1. 当前有效采购文件版本；
2. 关键资格与企业证据缺口；
3. 历史参考项目池及使用范围；
4. 最终报价方案；
5. 高风险承诺和风险豁免；
6. 内部审核通过及交接基线。

文件、证据、报价或章节发生变化后，系统只重新打开受影响的待确认项和审核任务，不要求整份标书从头确认。

### 方案 C：系统全部自动处理，只在最后确认标书

该方案用户操作最少，但错误容易积累到最后，不适合资格、报价和履约承诺风险较高的投标业务。

### 推荐方案

推荐方案 B：默认自动运行、只处理例外；低风险自动化，中风险批量确认，高风险逐项确认，阻断风险必须解决或由授权人员豁免。

### 用户确认

用户选择并确认方案 B。

### 完成结论

- V1 采用风险分级和例外驱动的人工确认模式；
- 低风险事项默认自动处理并保留查看与抽查入口；
- 中风险事项支持列表化和批量确认；
- 高风险事项必须由对应责任人逐项确认；
- 阻断风险必须解决或由授权人员填写理由并豁免；
- 系统统一提供待确认事项中心，按角色、风险、截止时间和项目状态排序；
- 人工主要确认关键业务关口，不承担机械式逐项点击；
- 修改后只重新确认受影响内容，避免全量重复审核；
- 所有自动处理、批量确认、人工确认和风险豁免均保留版本与审计记录。

**讨论点完成。**

---

## 十九、核心价值未落实点讨论 4：项目价值指标与验收体系

### 讨论背景

为了证明三条核心价值确实落实，项目不能只描述“提高效率、降低风险”，还需要建立可采集、可对比和可验收的指标体系。

### 方案 A：只进行定性描述

项目验收时只说明系统提高了效率、降低了风险。

该方案容易写入方案，但无法证明系统真的减少了工作量，也无法判断 V1 是否达到预期。

### 方案 B：建立基线并进行量化对比

先选择一批具有代表性的历史投标项目，统计原流程数据；系统上线试点后，再使用相同口径进行对比。

#### 核心价值一：系统承担重复工作

建议采集：

- 已配置平台的项目自动查询成功率；
- 公告和采购文件自动获取率；
- 采购要求、评分项和附件要求自动抽取覆盖率；
- 企业证据自动匹配率；
- 初稿自动生成覆盖率；
- 文件来源、版本和关键内容可追溯率；
- 自动处理事项占全部处理事项的比例。

#### 核心价值二：标书编写人聚焦业务判断

建议采集：

- 从创建项目到生成第一版初稿的时间；
- 人工查找、下载和整理资料的时间；
- 人工复制粘贴和重复录入次数；
- 用户需要处理的确认事项数量；
- 普通章节初稿直接接受或轻微修改的比例；
- 标书编写人用于方案判断、报价策略和关键内容确认的时间占比。

#### 核心价值三：提高负责人和商务/法务审核效率

建议采集：

- 项目负责人和合规人员的平均审核时间；
- 平均审核退回轮次；
- 系统预检发现问题的比例；
- 正式审核阶段才发现的阻断问题数量；
- 修改后需要重新审核的章节比例；
- 审核问题按期关闭率；
- 内部交接前未解决阻断风险数量。

#### 质量底线

无论效率提升多少，以下质量要求不能降低：

- 关键资格、实质性要求和评分项必须具有来源；
- AI 不得编造企业资质、人员、业绩和报价；
- 未解决的阻断风险不能生成有效交接基线；
- 新采购文件版本必须使旧分析结果失效或进入待复核状态；
- 最终报价和内部交接必须由授权人员确认。

### 方案 C：使用中标率作为主要指标

中标率可以作为长期业务指标，但不适合作为 V1 的主要验收标准。中标结果还受到竞争对手、预算、评分方法、关系资源和报价策略等大量外部因素影响，不能直接归因于系统。

### 推荐验收方式

采用方案 B：

```text
选择历史项目建立人工流程基线
→ 使用代表性项目进行系统试点
→ 系统自动采集任务、耗时、修改和审核数据
→ 对比效率、质量和风险指标
→ 判断三条核心价值是否落实
```

初期建议选取 5～10 个历史项目建立基线，再选取 5～10 个同类型项目进行试点。具体提升比例在取得甲方真实基线数据后确定，不在缺少数据时凭空承诺。

### 用户确认

用户选择并确认方案 B。

### 完成结论

- V1 建立覆盖自动化、编写效率、审核效率和质量风险的指标体系；
- 指标数据尽量由系统任务日志、审计事件和版本记录自动采集；
- 使用历史项目建立原流程基线，并使用同类型试点项目进行对比；
- 中标率不作为 V1 核心验收指标；
- 效率提升不能以降低事实准确性、来源追溯和风险控制为代价；
- 具体提升目标应在获得甲方真实基线后共同确定；
- 项目验收需要同时证明三条核心价值，而不只证明系统能够生成 Word 文件。

**讨论点完成。**

### 本轮核心价值落实讨论总结

针对三条既定核心价值尚未完全落实的方面，已确认：

1. 数据接入采用分级连接器与人工上传兜底方案；
2. Excel 与报价采用系统治理、原模型计算、规则校验、模型推荐和人工确认方案；
3. 人工确认采用风险分级、批量处理和例外驱动方案；
4. 项目价值采用历史基线、试点对比和质量底线相结合的验收方案。

以上四项共同补齐核心价值从业务描述到产品机制和验收指标的落地路径。

---

## 二十、文档回写决策：PRD 必须同步最终澄清结论

### 用户问题

用户询问 PRD 文档是否需要回写。

### 分析结论

PRD 必须回写，而且是最需要更新的正式文档之一。

`项目开发对话历史.md` 用于保存完整讨论过程和决策依据，但不能直接替代产品设计、开发与验收使用的正式 PRD。当前 PRD 中仍存在与后续确认结论冲突的旧范围，例如：

- 将 V1 过度收窄为文件解析、证据检索、响应编制和审核；
- 排除公告与采购文件自动获取能力；
- 排除 Excel 报价治理、报价候选方案和模型推荐能力；
- 未完整覆盖同类项目对比、公告监控、双轨风险、增量复核和受控内部交接；
- 未纳入新增的三个业务痛点及核心价值量化验收体系。

PRD 需要结构化回写以下内容：

1. 最终确认的九个业务痛点；
2. V1 完整业务范围与明确非目标；
3. 分级平台接入和人工上传兜底；
4. Excel 模板治理与报价方案闭环；
5. 同类项目对比和历史参考池；
6. 公告、采购文件和补充文件监控；
7. 章节化初稿、图表、来源和双轨风险输出；
8. 项目负责人及商务/法务审核流程；
9. 结构化修改、增量复核和内部交接；
10. 风险分级、例外驱动人工确认；
11. 核心价值指标和 V1 验收标准；
12. CA 签章、外部自动递交等明确非目标。

PRD 不能只在末尾补充说明，需要清理原有冲突内容并重新形成统一的 V1 需求基线。

### 用户确认

用户确认 PRD 需要回写。

### 完成结论

- 对话历史继续作为完整决策记录；
- `specs/11-产品需求文档（PRD）.md` 作为产品范围、功能和验收的正式基线；
- 回写时保留已经确认且仍然有效的内容；
- 删除或修正与后续决策冲突的旧范围；
- PRD 更新后必须检查业务流程、角色、数据对象、业务规则和验收标准之间的一致性。

**讨论点完成。**

---

## 二十一、正式需求文档同步执行记录

在 PRD v2.0 回写完成后，继续同步以下正式文档：

1. `07-客户画像与场景设定.md` 升级为 v6.0：纳入九个业务痛点、分级数据接入、历史参考池、Excel 报价闭环、结构化审核和递交责任人场景；
2. `09-业务流程与项目状态机.md` 升级为 v2.0：增加报价方案状态、递交记录状态、例外驱动确认、受控交接和人工外部递交记录；
3. `10-核心业务对象与数据字典.md` 升级为 v2.0：增加连接器、来源记录、参考项目池、报价模板与方案、图表、说明材料、审核问题、交接包和递交记录对象；
4. `12-项目难点与解决思路.md` 升级为 v3.0：增加异构平台连接、历史项目安全复用、Excel 报价治理、风险分级、增量复核和受控交接难点。

核验结果：

- 客户画像中的业务痛点数量为九个；
- PRD 中新增的十二个核心对象均已在数据字典中定义；
- 状态机已覆盖报价确认和人工递交记录；
- 技术难点已覆盖新增业务范围；
- “V1 不做公告接入、报价推荐、图表”等旧冲突表述已清除；
- 五份核心文档 Markdown 结构和代码围栏检查通过。

本次同步只修改需求与设计文档，不代表相关软件功能已经编码完成或上线。

---

## 二十二、PRD 基线治理与后续文档约束

### 用户要求

用户明确后续还会继续编写技术方案、版本规划、开发计划、评估体系、生产级增强方案和数据库设计，要求补充 PRD，使这些下游文档统一参照 PRD。

### 已确认原则

- PRD 是 V1 产品范围、角色、功能需求、业务规则、验收场景和发布门槛的正式主基线；
- 后续文档必须引用对应的 `FR`、`PRD-BR` 或 `AC` 编号；
- 第六章功能需求默认属于 V1 产品范围，版本规划可以拆分里程碑，但不能静默删除 V1 验收范围；
- 下游文档可以细化实现方式，但不得扩大、缩小或改变 PRD 中的业务范围和人工决策边界；
- 如果技术限制导致需求需要变化，必须先修改 PRD，再同步其他文档。

### PRD 新增内容

`specs/11-产品需求文档（PRD）.md` 升级为 v2.1，并新增：

1. 文档状态和 V1 主基线声明；
2. 技术方案、版本规划、开发计划、评估体系、生产级增强方案和数据库设计的下游约束；
3. 各类文档的职责边界；
4. PRD、状态机、数据字典、技术方案和数据库设计之间的冲突处理优先级；
5. 九个业务痛点到核心价值、功能需求、业务规则、验收场景和支撑文档的需求追踪矩阵；
6. 从需求变化提出到 PRD 更新、配套文档同步和重新验收的正式变更流程。

### 使用规则

- 技术方案回答“如何实现”，不能重新决定“做不做”；
- 版本规划回答“何时交付”，不能未经 PRD 变更就将 V1 必做能力移出；
- 开发计划中的任务必须引用需求编号；
- 评估体系补充样本、指标、公式和阈值，但不能降低质量底线；
- 生产级增强方案补充高可用、安全、监控、灾备、性能和成本治理，不改变核心业务语义；
- 数据库设计负责物理表、字段、索引、约束和迁移，但必须遵守领域数据字典。

本次修改只建立文档治理和需求追踪规则，不代表下游文档已经完成。

---

## 二十三、评估体系讨论 1：四层评估与质量门禁

### 用户提出的问题

用户提出当前仍不确定如何制定项目评估计划：

1. 正常情况下是否应该每完成一个开发步骤就进行一次评估；
2. 每一步的评估标准应该如何设置；
3. 项目完成后应该使用哪些指标判断系统是否达到可用和可交付水平；
4. 如果没有明确指标，就无法判断最终产出是否足够好。

### 讨论结论

评估不能只安排在项目完成后，也不适合把所有开发动作都机械地单独评分。V1 采用“分层持续评估”的方式：模块完成后验证模块能力，业务链路打通后验证跨模块协作，完整流程可运行后验证端到端结果，进入试点后再验证真实业务价值。

用户确认采用：

> **方案 B：四层评估 + 零容忍门禁 + 基线对比。**

评估层级如下：

```text
L1 单模块评估
  → L2 业务链路评估
  → L3 单项目端到端评估
  → L4 试点业务价值评估
```

### 四层评估定义

1. **L1 单模块评估**
   - 每个核心模块开发完成后立即评估其独立能力；
   - 例如数据接入、文档解析、需求提取、证据检索、报价校验、章节生成、风险审核和版本失效处理；
   - 目标是尽早发现模块自身问题，避免错误进入后续链路。

2. **L2 业务链路评估**
   - 多个模块形成一条可用业务链路后进行联合评估；
   - 例如“采购文件解析 → 资格要求提取 → 企业证据检索 → 响应内容生成”；
   - 重点检查数据是否正确传递、引用是否可追溯、异常是否被拦截、人工确认是否生效。

3. **L3 单项目端到端评估**
   - 使用完整投标项目样本，从输入项目编号或上传采购文件开始，验证直到内部受控交接和递交记录的完整流程；
   - 重点检查完整性、一致性、可审核性、版本正确性、风险闭环和人工决策边界；
   - V1 是否具备发布条件，主要由本层评估和零容忍门禁共同决定。

4. **L4 试点业务价值评估**
   - 在真实或高度仿真的试点项目中，与现有人工流程和历史基线进行对比；
   - 重点衡量时间节省、重复劳动减少、审核效率、返工情况、风险前置发现和用户采纳情况；
   - 中标率受项目竞争、价格、资质和客户关系等大量外部因素影响，不作为 V1 核心验收指标。

### 零容忍质量门禁

以下问题不能只通过平均分抵消；只要出现，就应阻止对应版本进入有效交接或发布状态：

1. AI 编造企业资质、人员、案例、能力、报价或其他关键事实；
2. 存在未关闭的阻断级风险，系统仍允许生成有效交接基线；
3. 采购文件或补充文件已有新版本，系统仍使用已失效旧版本生成或审核；
4. 关键资格条件、实质性响应结论或否决项判断缺少可追溯来源；
5. 无效报价方案被推荐、确认或写入投标文件；
6. 用户已经确认或修改的内容被系统静默覆盖；
7. 已撤回或已失效的交接包仍可继续作为有效版本使用；
8. 关键审批、风险豁免、版本确认或交接动作缺少审计记录。

### 指标分类

评估指标分为三类，不使用一个笼统的“准确率”概括整个系统：

1. **模块质量指标**
   - 文档和需求抽取：Precision、Recall、F1；高风险要求优先保证 Recall；
   - 检索与引用：Recall@K、MRR、引用正确率、证据与结论一致率；
   - 无证据拒答：正确拒答率、错误编造率；
   - 风险检查：风险召回率、误报率、阻断项漏报数；
   - 一致性检查：冲突识别准确率、跨章节一致率；
   - 内容生成：需求覆盖率、引用完整率、事实支撑率、模板符合率；
   - 工作流执行：成功率、重试率、人工接管率和异常恢复率。

2. **业务改进指标**
   - 首版初稿生成耗时；
   - 人工搜索和资料整理耗时；
   - 重复录入次数；
   - 需要人工确认的事项数量；
   - 负责人和商务/法务审核耗时；
   - 审核退回轮次；
   - 后期才发现的问题数量；
   - 初稿可接受率；
   - 修改后的增量复核比例；
   - 用户对系统建议的采纳、修改和拒绝比例。

3. **发布判断指标**
   - 零容忍门禁是否全部通过；
   - 核心模块是否达到最低发布阈值；
   - 端到端验收场景是否全部通过；
   - 与现有人工基线相比是否产生可验证的效率或质量改善；
   - 失败场景是否能够正确阻断、降级或转人工处理。

### 阈值制定原则

当前阶段不直接拍脑袋规定“准确率必须达到 95%”等统一数字。每项指标的正式阈值必须基于以下三个依据确定：

1. **业务风险**：资格条件、否决项、报价和关键事实等高风险能力采用更严格门槛；
2. **人工流程基线**：先测量当前人工流程耗时、错误、返工和审核情况，再确定改善目标；
3. **金标准评测集**：由业务人员对代表性项目样本进行标注，以真实样本测试模块和端到端效果。

后续评估体系文档需要为每个模块建立统一评估卡，至少包含：评估对象、测试数据集、金标准、指标、硬门禁、发布阈值、观察指标、样本量和失败处理规则。

### 用户确认

用户确认采用“方案 B：四层评估 + 零容忍门禁 + 基线对比”作为 V1 评估体系的总体框架。

### 后续待讨论内容

1. 统一的模块评估卡格式；
2. 各核心模块分别使用哪些指标；
3. 初始发布阈值如何通过基线和样本校准；
4. 单项目端到端评估总表；
5. 试点阶段业务价值评估方案。

本次只确认评估体系总体框架，尚未形成完整正式评估文档，也尚未确定各项数值阈值。

**讨论点完成。**

---

## 二十四、评估体系讨论 2：统一模块评估卡

### 讨论背景

在确认“四层评估 + 零容忍门禁 + 基线对比”总体框架后，需要进一步统一每个模块的评估记录方式，避免不同模块各自选择指标、口径不一致，也避免开发完成后才临时思考如何验收。

### 建议方案

每个需要进入 V1 业务链路的核心模块，都必须建立一张标准化模块评估卡。评估卡既是开发过程中的质量检查表，也是模块能否进入下一阶段联调和发布的判断依据。

统一评估卡包含以下九个字段：

1. **评估对象**
   - 明确被评估的模块、能力边界、输入和输出；
   - 不使用“AI 效果”等过于宽泛的描述；
   - 例如“从采购文件中识别资格条件、实质性要求和否决项”。

2. **测试数据集**
   - 说明测试样本来源、项目类型、文件格式、难度和版本；
   - 区分开发集、调优集、验收集和回归集；
   - 验收集不得被用于提示词、规则或模型调优。

3. **金标准**
   - 由具备业务能力的人员标注正确答案或可接受结果；
   - 记录标注人、复核人、标注规则和争议处理结果；
   - 对主观生成任务，可使用“必须满足项 + 禁止项 + 分级评分标准”，不强求唯一文本答案。

4. **质量指标**
   - 设置能够直接反映模块能力的核心指标；
   - 例如 Recall、Precision、F1、Recall@K、引用正确率、事实支撑率、模板符合率；
   - 每个指标必须写清计算公式、统计单位和适用范围。

5. **硬门禁**
   - 定义一旦出现就不能进入下一阶段的严重错误；
   - 硬门禁不参与平均分抵消；
   - 例如编造资质、漏掉否决项、使用失效文件、写入无效报价或缺少关键审计记录。

6. **发布阈值**
   - 定义模块进入业务链路、试点或正式发布必须达到的最低标准；
   - 阈值依据业务风险、人工基线和金标准评测结果制定；
   - 未达到阈值时不能用“整体平均表现较好”替代整改。

7. **观察指标**
   - 记录暂不阻断发布、但需要持续监控的指标；
   - 例如平均响应时间、Token 成本、重试率、人工接管率、用户修改率；
   - 观察指标连续恶化时，可以升级为发布阈值或硬门禁。

8. **样本量**
   - 明确本轮测试的项目数、文件数、页面数、要求项数或问答数；
   - 样本必须覆盖正常、边界、异常和高风险场景；
   - 小样本结果只能用于发现问题，不能直接证明模块已经稳定。

9. **失败处理规则**
   - 明确指标未达标或触发硬门禁后的处置方式；
   - 包括阻断发布、回退版本、转人工、补充规则、重新训练或重新评测；
   - 修复后必须使用固定回归集重新验证，防止旧问题再次出现。

### 标准模板

```text
模块名称：
关联需求：FR- / PRD-BR- / AC-
评估层级：L1 / L2 / L3 / L4

1. 评估对象：
2. 测试数据集：
3. 金标准：
4. 质量指标：
5. 硬门禁：
6. 发布阈值：
7. 观察指标：
8. 样本量：
9. 失败处理规则：

本轮结果：通过 / 有条件通过 / 不通过
遗留问题：
评估人：
复核人：
评估日期：
模块版本：
模型、提示词、规则和数据版本：
```

### 使用规则

1. 模块开发前先建立评估卡草案，避免完成开发后才寻找指标；
2. 模块开发完成后执行 L1 评估，达到阈值后才能进入业务链路联调；
3. 评估卡必须关联 PRD 中的 `FR`、`PRD-BR` 或 `AC` 编号；
4. 模型、提示词、规则、数据集或解析器发生实质变化后，应重新执行受影响的评估；
5. “有条件通过”只能用于不存在硬门禁问题、且遗留问题有明确责任人和期限的情况；
6. 触发任何零容忍门禁时，评估结果只能为“不通过”；
7. 所有评估结果、失败样本和修复结果必须保留版本记录，以支持回归测试和审计追溯。

### 用户确认

用户确认采用统一模块评估卡，并同意使用上述九个核心字段作为后续各模块评估设计的标准结构。

### 完成结论

- 后续评估体系文档中的所有核心模块统一使用同一张评估卡结构；
- 不同模块可以选择不同质量指标和阈值，但字段、版本记录和判定规则保持一致；
- 下一步按 V1 业务流程顺序，逐个确定核心模块的具体评估指标和门禁。

**讨论点完成。**

---

## 二十五、评估体系讨论 3：数据接入模块评估

### 评估对象

评估“项目创建与分级数据接入”能力，关联 PRD `FR-101`、`FR-201` 和 `AC-01`，覆盖：

1. 用户选择来源平台并输入外部项目编号；
2. 系统识别对应项目并创建内部 `project_id`；
3. 自动获取允许访问的公告、采购文件和补充文件；
4. 对 API、企业知识库、历史标书库和受控目录进行授权接入；
5. 自动接入失败时创建重试任务，并提供链接补充或人工上传兜底；
6. 为所有自动和人工导入内容记录来源、时间、文件哈希、连接器版本和操作者；
7. 识别当前生效文件版本，防止后续模块继续使用失效文件。

本模块不评估采购文件正文解析质量。PDF、Word、Excel、扫描件中的章节、表格和文本识别效果由下一张“文档解析与 OCR 模块评估卡”负责。

### 测试数据集

测试集应覆盖 V1 目标场景和主要失败方式：

1. 北京地区及其他地区的政府采购信息服务类项目；
2. 可直接公开访问的平台项目；
3. 需要授权 API 或企业内部数据源的项目；
4. 存在登录、验证码、CA、专用客户端或加密限制的项目；
5. 同一项目存在公告、采购文件、附件、澄清、更正、补遗和答疑的情况；
6. 项目名称相似、编号格式不同、跨平台重复发布等容易误匹配的情况；
7. 网络超时、接口限流、链接失效、文件损坏、文件重复和连接器部分失败情况；
8. 自动获取失败后由用户上传文件并确认有效版本的情况。

测试数据划分为开发集、验收集和固定回归集。验收集不得用于连接器规则和项目匹配逻辑调优。

### 金标准

每个测试项目由业务人员预先确认：

1. 正确的来源平台和外部项目编号；
2. 对应的真实项目及采购包；
3. 截至评估时间应当获取的公告和文件清单；
4. 每份文件的来源链接、发布时间、文件类型、文件哈希和版本关系；
5. 当前生效版本及被覆盖、撤回或失效的旧版本；
6. 哪些内容允许自动获取，哪些必须授权接入或人工上传；
7. 连接器失败时系统应采取的重试、降级或人工兜底动作。

### 核心质量指标

1. **项目匹配正确率**：输入来源平台和项目编号后，系统关联到正确项目及采购包的比例；
2. **应接入文件完整率**：金标准中应获取文件被系统成功发现或形成明确人工待办的比例；
3. **最新有效版本识别率**：系统正确识别当前生效文件及覆盖关系的比例；
4. **文件完整性通过率**：下载或上传文件能够通过大小、格式、哈希和可读取性检查的比例；
5. **来源可追溯率**：成功导入内容具备完整来源、获取时间、哈希、连接器版本和操作者记录的比例；
6. **重复文件识别率**：相同内容或重复发布文件被正确合并或标记的比例；
7. **自动接入成功率**：在法律、权限和技术条件允许的情况下，连接器自动完成接入的比例；
8. **人工兜底闭环率**：自动接入失败后，系统成功生成明确待办，用户能够补充链接或上传文件并完成确认的比例；
9. **错误项目接入率**：系统将其他项目或其他采购包文件接入当前项目的比例；
10. **接入时效**：从创建项目到完成公开文件发现、下载和状态反馈所需时间。

其中项目匹配、应接入文件完整性、有效版本和来源追溯属于发布判断指标；自动接入成功率和接入时效主要作为持续优化指标，不能为了提高自动化率绕过平台权限或安全限制。

### 硬门禁

出现以下任一情况，本模块评估结果直接判定为“不通过”：

1. 接入了错误项目或错误采购包的文件，并进入后续正式处理；
2. 漏掉已经生效的澄清、更正、补遗或答疑文件，且系统没有形成缺失告警或人工待办；
3. 已存在新版本或明确覆盖关系，系统仍将旧版本标记为当前有效版本；
4. 损坏、截断、空白或无法读取的文件被标记为接入成功并进入后续处理；
5. 文件缺少关键来源、获取时间、哈希或版本记录，无法审计追溯；
6. 自动接入失败后没有重试状态、失败原因或人工上传兜底入口；
7. 未经授权绕过账号、验证码、CA、访问控制或平台限制获取数据；
8. 单一连接器失败导致已确认的项目事实或文件记录被删除；
9. 用户已确认的新文件版本被系统静默替换或回退。

### 发布阈值原则

当前先确定阈值原则，不拍脑袋填写统一百分比：

1. 错误项目接入和硬门禁事件必须为零；
2. 生效补充文件、版本关系和来源追溯采用高风险标准；
3. 数值阈值由代表性项目金标准测试结果、人工流程基线和连接器能力共同校准；
4. 不同接入类型分别统计，不能用公开平台的高成功率掩盖内部数据源或人工兜底链路的问题；
5. 自动接入率不是越高越好，无法合法或稳定自动获取时，正确降级到人工流程同样视为合格结果。

### 观察指标

- 各连接器成功率、失败率和平均重试次数；
- 平均接入耗时和超时比例；
- 人工上传比例及完成耗时；
- 重复下载和重复存储比例；
- 每个项目需要人工处理的连接器异常数；
- 平台规则变化导致的连接器失效率；
- 单项目接入成本、调用次数和存储量。

### 样本量原则

样本统计单位至少同时包含项目数、采购包数、文件数和文件版本数。首轮小样本用于发现问题；形成发布结论前，样本必须覆盖主要平台类型、受限接入、跨地区项目、补充文件、多版本、相似项目、网络异常和人工上传兜底场景。

### 失败处理规则

1. 项目匹配不确定时暂停自动绑定，要求用户确认候选项目；
2. 文件清单不完整或版本关系不确定时，将项目标记为待确认，不允许形成新的有效交接基线；
3. 文件损坏时重新下载；仍失败则生成用户补充文件待办；
4. 连接器失败时记录失败原因并独立重试，不回滚其他已成功连接器结果；
5. 触发权限或安全限制时停止自动化，转入授权接入或人工上传；
6. 修复连接器或匹配规则后，必须使用固定回归项目重新评估；
7. 已进入后续流程的错误文件需要触发影响分析，使相关要求、章节、风险、审核任务和交接基线失效或待复核。

### 用户确认

用户确认数据接入模块按照项目匹配、文件完整性、最新版本、文件有效性、来源追溯和人工兜底进行评估，并确认上述硬门禁方向。

### 完成结论

- 数据接入模块的重点不是单纯追求自动抓取率，而是保证“项目正确、文件完整、版本有效、来源可查、失败可兜底”；
- 受限平台能够安全、明确地转为授权接入或人工上传，同样属于正确业务结果；
- 下一步讨论文档解析与 OCR 模块的评估指标和质量门禁。

**讨论点完成。**

---

## 二十六、评估体系讨论 4：文档解析与 OCR 模块评估

### 评估对象

评估采购文件和相关附件的文档解析能力，关联 PRD `FR-201` 和 `AC-02`，覆盖：

1. 解析原生 PDF、扫描 PDF、Word、Excel 和常见图片扫描件；
2. 识别文档页数、正文、标题层级、段落、列表、页眉页脚和阅读顺序；
3. 还原普通表格、复杂表格、合并单元格、跨页表格和 Excel 工作表结构；
4. 对扫描件执行 OCR，保留原始页面和识别文本之间的定位关系；
5. 输出章节、表格、页码、原文片段、坐标位置和解析质量；
6. 对低质量、乱码、漏页、顺序异常和结构还原失败进行告警；
7. 为后续要求提取、证据引用和人工复核提供可追溯的解析结果。

本模块只判断“文件是否被完整、正确地转化为可处理内容”，不负责判断某段文字是不是资格条件、评分项或风险要求，该部分由下一张“要求与评分项提取模块评估卡”负责。

### 测试数据集

测试集应覆盖以下文件和页面类型：

1. 文本型 PDF、扫描型 PDF、混合型 PDF；
2. 正常 Word 文件以及包含文本框、页眉页脚、分栏、批注和嵌入对象的 Word 文件；
3. 包含多个工作表、公式、合并单元格、隐藏行列和打印区域的 Excel 文件；
4. 清晰扫描件、倾斜页面、低分辨率、印章遮挡、背景水印、手写标记和复杂版式；
5. 普通表格、无边框表格、跨页表格、嵌套表格和单元格内多段文字；
6. 中文、英文、数字、金额、百分比、日期、项目编号、符号和上下标混合内容；
7. 页码缺失、页面重复、页面倒序、文件截断、密码保护、字体异常和损坏文件；
8. 资格条件、否决项、评分表、报价表、时间表和附件清单等高风险页面。

测试数据划分为开发集、验收集和固定回归集。高风险页面需要单独建立回归子集，不能只根据整本文档平均结果判断质量。

### 金标准

每份测试文件由标注人员和复核人员共同确认：

1. 原始文件总页数、有效页数、页面顺序和页面类型；
2. 页面上的正确文本、段落顺序和阅读顺序；
3. 标题文本、标题级别和章节边界；
4. 表格行列、单元格内容、合并关系、表头和跨页延续关系；
5. 关键原文片段对应的文件版本、页码和页面坐标；
6. 图片、印章、图表和无法可靠 OCR 的区域；
7. 应当触发低质量告警、人工复核或重新上传的页面；
8. 金额、日期、编号、百分比、资格条件和评分值等关键字符的正确结果。

对 OCR 文本可以使用字符级金标准；对版面结构和表格使用结构化标注；对难以唯一还原的复杂版式，应明确允许结果和不可接受结果。

### 核心质量指标

1. **页数完整率**：解析输出中正确保留的有效页面数占源文件有效页面数的比例；
2. **页面顺序正确率**：页面和页面内阅读顺序与原文件一致的比例；
3. **正文识别准确率**：文本型文件抽取正确率以及扫描件 OCR 的字符准确率，可结合字符错误率 `CER` 统计；
4. **关键字符准确率**：金额、日期、编号、百分比、单位和否定词等关键字符被正确识别的比例；
5. **标题层级准确率**：标题内容、级别和章节边界被正确识别的比例；
6. **表格结构还原率**：表格行列、表头、合并单元格和跨页关系被正确还原的比例；
7. **表格单元格准确率**：单元格内容及其行列归属正确的比例；
8. **原文定位正确率**：解析片段能够准确返回对应文件版本、页码和页面位置的比例；
9. **高风险页面解析通过率**：资格、否决项、评分、报价、时间和附件页面达到可供后续处理质量的比例；
10. **低质量告警召回率**：实际存在漏页、乱码、低置信度、结构错乱或不可可靠解析的页面被系统发现并告警的比例；
11. **误告警率**：质量合格页面被错误标记为不可用或必须人工处理的比例；
12. **格式覆盖率**：V1 承诺支持的文件格式能够被正确识别、解析或明确降级的比例。

整本文档平均准确率只能作为综合观察指标。资格条件、否决项、评分表和报价表等高风险内容必须单独统计，不能被大量普通正文稀释。

### 硬门禁

出现以下任一情况，本模块评估结果直接判定为“不通过”：

1. 文件存在漏页、页面重复或页面顺序错误，系统未告警并继续进入正式处理；
2. 表格行列或单元格归属错误，导致金额、分值、资格条件、时间或响应要求含义改变；
3. 原文片段返回了错误文件版本、错误页码或错误页面位置；
4. 低质量、乱码、空白或不可可靠解析的页面被标记为高质量并作为可靠事实继续处理；
5. 金额、日期、编号、百分比、否定词或单位识别错误，导致业务含义发生变化且未触发告警；
6. 资格条件、否决项、评分表、报价表或附件清单所在页面解析失败，系统没有阻断或人工复核任务；
7. 损坏、加密或不支持的文件被静默跳过，没有明确失败状态和人工处理入口；
8. 解析结果无法追溯到原文件、文件版本和具体页码；
9. 用户重新上传或确认新文件后，系统仍将旧解析结果作为当前有效结果使用。

### 发布阈值原则

当前先确定阈值制定原则，具体数值通过金标准评测集校准：

1. 漏页未告警、关键表格含义改变和错误原文定位等硬门禁事件必须为零；
2. 高风险页面单独设置更严格阈值，并优先保证完整性和告警召回；
3. 文本型 PDF、扫描 PDF、Word 和 Excel 分别统计，不能用简单文件的高准确率掩盖复杂格式问题；
4. OCR 指标不仅统计全文字符，还需单独统计金额、日期、编号、百分比、否定词和单位；
5. 无法可靠解析时，正确告警并转人工比输出看似完整但错误的结果更重要；
6. 发布阈值由人工处理质量、样本难度和后续业务风险共同确定，不使用一个统一准确率覆盖所有解析任务。

### 观察指标

- 单页和单文件平均解析耗时；
- OCR 页面占比及平均 OCR 成本；
- 各文件格式成功率和失败原因分布；
- 每份文件需要人工复核的页面数；
- 表格、图片、印章和复杂版式的降级比例；
- 解析重试率和重新上传率；
- 解析器、OCR 模型和版面分析版本变化带来的质量波动；
- 单项目解析资源消耗和存储量。

### 样本量原则

样本统计单位至少包含文件数、页面数、OCR 页面数、表格数、单元格数和高风险页面数。发布评估必须覆盖所有 V1 支持格式，以及清晰、一般、困难和不可解析四类质量等级。不可解析样本用于验证系统是否能够正确拒绝、告警和转人工，不能从评估集中删除。

### 失败处理规则

1. 页数、页面顺序或文件完整性检查失败时，停止正式解析并尝试重新下载或要求重新上传；
2. 单页 OCR 或版面质量不足时，标记具体页面和区域，创建人工复核任务；
3. 高风险页面未达到质量要求时，阻止其进入自动要求提取和标书生成；
4. 表格结构不确定时，保留页面截图和原表，禁止仅依据错误结构化结果计算金额或分值；
5. 原文定位异常时，相关引用结果全部标记为无效并重新建立定位；
6. 解析器、OCR 模型或规则升级后，使用固定回归集重新评估所有支持格式；
7. 已被下游使用的解析结果发生修正时，触发影响分析，使相关要求、证据、章节、风险和审核任务进入待复核状态。

### 用户确认

用户确认文档解析与 OCR 模块按照页数完整性、正文和关键字符识别、表格结构、章节层级、原文定位、扫描件质量和低质量告警进行评估，并确认上述硬门禁方向。

### 完成结论

- 文档解析质量不能只看全文平均准确率，必须对资格、否决项、评分、报价和时间等高风险页面单独评估；
- 系统无法可靠解析时必须明确告警并转人工，不能用低质量结果冒充可靠事实；
- 下一步讨论要求、评分项和响应矩阵提取模块的评估指标与质量门禁。

**讨论点完成。**

---

## 二十七、评估体系讨论 5：要求、评分项与响应矩阵提取模块评估

### 评估对象

评估从已确认采购文件中提取并组织投标要求的能力，关联 PRD `FR-301` 和 `AC-02`，覆盖：

1. 提取资格条件、实质性要求、否决项、技术要求和商务要求；
2. 提取评分项、分值、评分档位、评分条件、证明材料和计算规则；
3. 提取截止时间、答疑时间、保证金时间、服务周期等时间节点；
4. 提取附件、格式、签章、装订、份数、命名和递交方式要求；
5. 为每项结果记录类型、标准化描述、原文、页码、文件版本、后果、置信度和确认状态；
6. 识别重复、包含、引用、覆盖和冲突关系，避免同一要求被遗漏或重复执行；
7. 生成“要求—评分项—章节—证据—责任人”的响应矩阵；
8. 支持按阻断、待确认、未映射、部分覆盖和已覆盖状态筛选。

本模块负责“采购文件要求是什么以及需要在哪里响应”，不负责判断企业是否具备相应证据，也不负责生成最终章节正文。

### 测试数据集

测试集应覆盖：

1. V1 目标领域的政府采购信息服务类项目；
2. 资格审查表、符合性审查表、评分表、采购需求、合同条款和投标人须知；
3. 正文、表格、脚注、附件、补充文件和跨章节引用中的要求；
4. 使用“必须、应当、不得、否则无效”等明确约束的内容；
5. 没有特殊符号、只通过上下文表达的隐含高风险要求；
6. `★`、黑体、加粗、下划线等版式信号存在或缺失的情况；
7. 一个段落包含多个独立要求、一个要求分散在多个段落或文件中的情况；
8. 评分项包含多档得分、公式计算、封顶分、客观分和主观分的情况；
9. 新增澄清、更正或补遗后，原要求被修改、覆盖、删除或新增的情况；
10. 相似要求、重复要求、互相矛盾要求和引用错误等异常情况。

测试数据划分为开发集、验收集和固定回归集，并建立资格条件、实质性要求、否决项和评分项四个高风险子集。

### 金标准

每个测试项目由标书编写人员初标，项目负责人或商务/法务人员复核，形成：

1. 完整的原子要求清单；
2. 每项要求的业务类型和风险等级；
3. 原文、页码、文件版本和跨文件引用关系；
4. 违反要求可能产生的后果；
5. 评分项名称、分值、档位、条件、证明材料和计算方式；
6. 时间节点、附件、格式和递交要求；
7. 应合并、应拆分、重复、冲突和已被新文件覆盖的要求；
8. 要求对应的目标章节、证据类型和责任角色；
9. 对存在业务歧义的内容，记录允许解释、必须人工确认的原因和最终复核结论。

金标准以“原子要求”为基本单位：一个要求应具有独立的执行动作、责任主体或违反后果。不能为了提高数量而过度拆分，也不能把多个独立条件错误合并。

### 核心质量指标

1. **要求总体召回率**：金标准中的要求被系统正确识别的比例；
2. **资格条件召回率**：资格审查相关条件被完整识别的比例；
3. **实质性要求与否决项召回率**：可能导致无效投标或否决的要求被识别的比例；
4. **评分项拆解完整率**：评分名称、分值、档位、条件、证明材料和计算规则被完整提取的比例；
5. **时间节点识别率**：关键日期、时间、周期和先后关系被正确提取的比例；
6. **附件与格式要求识别率**：附件、签章、装订、份数、命名和递交格式要求被正确识别的比例；
7. **要求分类准确率**：资格、实质性、技术、商务、评分、时间和格式等类型分类正确的比例；
8. **原文定位正确率**：要求能够定位到正确文件版本、页码和原文片段的比例；
9. **标准化保真率**：标准化描述没有遗漏限定条件、改变否定关系或扩大、缩小原意的比例；
10. **后果判断准确率**：系统对未满足要求可能后果的标记与金标准一致的比例；
11. **原子化准确率**：要求拆分和合并结果符合独立执行与独立后果原则的比例；
12. **重复要求识别准确率**：重复或等价要求被正确关联且未误合并的比例；
13. **冲突识别召回率**：采购文件内部及补充文件之间的矛盾、覆盖和变更被发现的比例；
14. **响应矩阵覆盖率**：已确认要求成功关联目标章节、证据类型和责任人的比例；
15. **响应矩阵映射正确率**：要求与评分项、章节、证据和责任人的关联关系正确的比例；
16. **虚假要求率**：系统提取了采购文件中不存在或无法被原文支持的要求比例；
17. **高风险低置信度告警召回率**：资格、否决项和评分项存在不确定性时，系统正确转入人工确认的比例。

总体 F1 可以用于综合观察，但资格条件、实质性要求、否决项和评分项优先看召回率，不能用普通技术条款的高准确率掩盖高风险漏项。

### 硬门禁

出现以下任一情况，本模块评估结果直接判定为“不通过”：

1. 漏掉资格条件、实质性要求或否决项，且系统没有产生低置信度告警或人工确认任务；
2. 漏掉评分项、评分档位、封顶条件或关键证明材料，导致响应策略或得分判断发生变化；
3. 系统凭空生成采购文件不存在的资格、评分、商务或技术要求；
4. 要求关联了错误文件版本、错误页码或不支持该结论的原文；
5. 错误合并多个独立要求，导致其中部分条件、责任人或违反后果消失；
6. 错误拆分或改写导致否定关系、适用范围、时间条件、数量或金额含义改变；
7. 将 `★`、黑体或表格位置作为唯一依据，错误判定要求属于否决项或非高风险项；
8. 高风险内容置信度不足或存在歧义，系统未要求人工确认就进入已确认状态；
9. 新澄清、更正或补遗已经改变原要求，响应矩阵仍使用失效要求；
10. 已确认的人工编辑、分类或风险结论被系统静默覆盖；
11. 要求未映射章节、证据或责任人，却被系统标记为“已覆盖”；
12. 关键时间节点或递交格式要求提取错误，系统未告警并继续生成投标文件。

### 发布阈值原则

当前先确认阈值制定方式，具体数值后续通过金标准评测集校准：

1. 硬门禁事件必须为零；
2. 资格条件、实质性要求和否决项以召回优先，宁可增加合理待确认项，也不能静默漏项；
3. 评分项既要求高召回，也要求分值、档位、证明材料和计算规则的结构完整性；
4. 虚假要求率需要单独设限，不能通过无限扩大召回来制造大量不存在的要求；
5. 不同要求类型分别统计，不用一个总体准确率替代分类评估；
6. 高风险结果必须来源可追溯，并经过置信度或规则触发的人工确认机制；
7. 响应矩阵只有在要求、章节、证据和责任关系真实存在时才能标记为已覆盖。

### 观察指标

- 单项目提取要求数量及与人工基线的差异；
- 每百页需要人工确认的要求数；
- 高、中、低风险要求的人工修改率；
- 系统建议分类的接受、修改和拒绝比例；
- 重复要求合并比例和误合并比例；
- 补充文件引发的新增、变更、失效要求数量；
- 响应矩阵未映射项、部分覆盖项和阻断项数量；
- 单文件和单项目要求提取耗时与成本。

### 样本量原则

样本统计单位至少包含项目数、文件数、页面数、原子要求数、资格条件数、否决项数、评分项数、时间节点数和矩阵映射关系数。高风险类别即使在真实项目中数量较少，也必须通过专项样本和历史项目补足，不能因为样本稀少而不评估。

### 失败处理规则

1. 高风险要求漏提或含义改变时，阻断模块进入下一阶段并修复提取规则、提示词或模型；
2. 原文定位不正确时，相关要求和响应矩阵关系全部标记为无效并重新生成；
3. 置信度不足或业务歧义无法自动消除时，创建人工确认任务，不强行给出确定结论；
4. 评分项拆解不完整时，禁止系统据此生成完整得分策略或宣称已经覆盖；
5. 新文件变更要求后，触发影响分析，使旧要求、章节、证据、风险、审核任务和交接基线失效或待复核；
6. 修复后使用固定回归集重新评估，并重点复测所有资格、否决项和评分项样本；
7. 评估失败样本进入长期回归库，记录失败类型、修复版本和复核结论。

### 用户确认

用户确认要求、评分项与响应矩阵提取模块按照高风险要求召回、评分项完整性、分类、原文定位、原子化、冲突识别和矩阵覆盖进行评估，并确认上述硬门禁方向。

### 完成结论

- 本模块首先保证“不漏掉会导致无效投标、资格不符或失分的关键要求”，同时控制凭空生成要求；
- 要求必须保留原文、页码、版本、后果和确认状态，响应矩阵不得把未真实映射的要求标记为已覆盖；
- 下一步讨论企业证据检索、引用与无证据拒答模块的评估指标和质量门禁。

**讨论点完成。**

---

## 二十八、评估体系讨论 6：企业证据检索、引用与无证据拒答模块评估

### 评估对象

评估企业证据库对投标要求的检索、校验、引用和拒答能力，关联 PRD `FR-401`、`AC-03` 及安全权限要求，覆盖：

1. 管理企业资质、人员、案例、合同、验收材料、授权文件、财务资料和证明附件；
2. 根据资格条件、评分项、技术要求和商务要求检索候选企业证据；
3. 检查证据主体、有效期、适用范围、材料完整性、保密等级和访问权限；
4. 返回原文片段、页码、文件版本、有效期和匹配理由；
5. 对证据进行排序，并解释为什么推荐该证据；
6. 在没有有效证据或证据不足时明确拒答并创建人工补充任务；
7. 只有经过人工确认且权限允许的证据才能进入正式标书；
8. 记录要求、证据、引用位置、确认人和证据版本之间的可追溯关系。

本模块负责判断“现有企业材料能否支持该要求或表述”，不负责决定企业是否投标，也不允许根据常识、相似案例或模型记忆补全企业真实能力。

### 测试数据集

测试集应覆盖以下证据和检索场景：

1. 营业执照、行业资质、认证证书、人员证书、社保材料和劳动关系证明；
2. 合同、验收报告、中标通知书、案例说明和客户证明；
3. 授权文件、财务资料、纳税或社保证明、信用材料和承诺材料；
4. 当前有效、即将到期、已经过期、有效期不明确和需要人工核验的证据；
5. 总公司、分公司、子公司、关联公司和联合体成员等主体容易混淆的情况；
6. 同名人员、同名项目、相似案例和多个版本材料；
7. 适用地域、行业、金额、项目类型、服务周期或用途受限的证据；
8. 公开、内部、机密和严格受限等不同保密等级资料；
9. 完全有证据、部分有证据、证据互相冲突、证据缺失和只有无效证据的情况；
10. 用户有权限、无权限、权限过期和跨项目越权访问情况；
11. 已确认、待确认、已撤回和被新版本替代的证据；
12. 问题可以由单份证据支持，以及必须由多份材料组合支持的情况。

测试数据划分为开发集、验收集和固定回归集，并建立“无证据”“无权限”“已过期”“主体不符”和“部分支持”专项子集。

### 金标准

每条测试要求由资料管理员提供候选材料，标书编写人员判断业务适用性，必要时由项目负责人或商务/法务人员复核，形成：

1. 可以支持该要求的有效证据集合；
2. 最优证据及合理的候选排序；
3. 每份证据的主体、有效期、适用范围、完整性和保密等级；
4. 证据能够支持的具体结论和不能支持的边界；
5. 正确的原文片段、页码、文件版本和引用位置；
6. 是否允许当前角色和当前项目访问、引用或导出；
7. 是否需要多份证据组合才能形成完整支持；
8. 无有效证据时应输出的拒答结论和人工补充任务；
9. 需要人工确认的原因及最终确认结果。

金标准必须区分“检索到了相关材料”和“材料足以支持结论”。语义相似但主体、有效期、适用范围或证明内容不满足要求的材料，不算有效证据。

### 核心质量指标

1. **证据召回率**：金标准中的有效证据被系统检索到的比例；
2. **Recall@K**：前 K 个候选结果中包含至少一项有效证据的比例；
3. **Precision@K**：前 K 个候选结果中真正适用证据所占比例；
4. **排序效果**：使用 `MRR` 或首个有效证据排名衡量有效证据是否优先出现；
5. **主体匹配准确率**：系统正确判断证据所属企业、人员或项目主体的比例；
6. **有效期判断准确率**：系统正确识别有效、过期、即将到期和待核验证据的比例；
7. **适用范围判断准确率**：系统正确判断地域、行业、项目类型、金额、用途和其他限制的比例；
8. **材料完整性判断准确率**：系统正确识别缺页、缺章、缺签字、缺附件或组合材料不完整的比例；
9. **权限校验正确率**：系统正确允许或拒绝当前角色、项目和用途访问证据的比例；
10. **引用正确率**：引用指向正确证据版本、页码和原文片段的比例；
11. **证据—结论一致率**：被引用证据能够直接支持生成结论且没有扩大含义的比例；
12. **组合证据完整率**：需要多份材料共同支持时，系统推荐的证据组合完整的比例；
13. **无证据正确拒答率**：不存在有效证据时，系统正确输出“证据不足”的比例；
14. **错误肯定率**：不存在充分有效证据时，系统仍输出企业具备相应能力或条件的比例；
15. **错误拒答率**：实际存在有效且有权限证据时，系统错误输出证据不足的比例；
16. **人工补充任务创建率**：无证据、证据无效或材料不完整时，系统正确创建补充任务的比例；
17. **已确认使用合规率**：进入正式标书的证据均已人工确认、版本有效且权限允许的比例。

检索效果不能只看向量相似度或 `Recall@K`。主体、有效期、适用范围、权限和证据能否真正支持结论，必须作为独立业务指标评估。

### 硬门禁

出现以下任一情况，本模块评估结果直接判定为“不通过”：

1. 系统编造企业资质、人员经历、项目案例、合同、验收结果、财务数据或其他企业事实；
2. 使用已经过期、被撤回、主体不符或适用范围不符的证据支持正式结论；
3. 无有效证据或只有部分证据时，系统仍输出无条件肯定结论；
4. 引用指向错误文件、错误版本、错误页码，或引用内容不能支持生成结论；
5. 将关联公司、其他项目、其他人员或相似案例的材料错误当作当前主体证据；
6. 未经授权向用户、日志、评测集、导出文件或其他项目泄露机密证据；
7. 未经人工确认、权限不允许或状态为待确认的证据直接写入正式标书；
8. 证据已经失效或更新，系统仍静默沿用旧证据和旧引用；
9. 无证据时没有明确拒答，也没有创建人工补充任务；
10. 用户已确认或撤回的证据选择被系统静默覆盖；
11. 证据仅能支持部分条件，系统却扩大为完全满足全部要求；
12. 权限校验失败时系统仍返回原文、下载地址或敏感摘要。

### 发布阈值原则

当前先确认阈值制定原则，具体数值后续通过金标准评测集校准：

1. 编造企业事实、越权泄露和无证据肯定结论等硬门禁事件必须为零；
2. 资格条件和评分证明等高风险检索优先保证有效证据召回，但不能牺牲主体、有效期和权限正确性；
3. `Recall@K`、`Precision@K` 和 `MRR` 只评价候选检索，不能代替证据适用性和引用正确性评估；
4. “正确拒答”是合格能力，不得为了降低拒答率而生成没有证据的内容；
5. 不同证据类型、保密等级和有效状态分别统计，不能用公开、简单材料的高表现掩盖高风险证据问题；
6. 进入正式标书的证据必须满足“已确认、有效、主体正确、适用、权限允许、来源可追溯”；
7. 对证据只能部分支持的情况，应输出支持边界和缺口，不允许将部分支持计为完全通过。

### 观察指标

- 用户对前 K 个推荐证据的采纳率；
- 用户调整证据排序、替换证据和拒绝推荐的比例；
- 平均检索耗时、向量检索成本和重排成本；
- 单项要求返回的候选证据数量；
- 无证据、部分证据和无权限结果的比例；
- 人工补充任务数量及平均完成时间；
- 即将到期证据数量和提前预警时间；
- 证据版本更新导致的引用失效数量；
- 不同角色和保密等级下的权限拒绝次数。

### 样本量原则

样本统计单位至少包含要求数、查询数、候选证据数、有效证据数、引用数、拒答样本数、权限场景数和证据类型数。发布评估必须覆盖有证据、无证据、部分支持、过期、主体不符、无权限和多证据组合场景，并保证资格和评分证明材料具有足够专项样本。

### 失败处理规则

1. 检索结果不确定时只返回候选证据和匹配理由，不自动确认；
2. 主体、有效期、适用范围、完整性或权限任一项不满足时，将证据标记为不可用于正式标书；
3. 无有效证据时明确输出“证据不足”，创建资料补充任务并指定所需材料类型；
4. 只有部分证据时展示已支持部分、缺失部分和可能风险，不输出完全满足结论；
5. 发现错误引用或证据失效时，立即使相关章节、要求覆盖状态、风险、审核任务和交接基线失效或待复核；
6. 发生越权访问或泄露时停止相关访问链路，记录安全事件并执行权限和日志审查；
7. 检索模型、嵌入模型、重排模型、切分规则或证据数据更新后，使用固定回归集重新评估；
8. 所有错误肯定、错误拒答、越权和引用错误样本进入长期回归库。

### 用户确认

用户确认企业证据模块按照证据召回、排序、主体、有效期、适用范围、权限、引用、证据与结论一致性和无证据拒答进行评估，并确认上述硬门禁方向。

### 完成结论

- 企业证据检索的目标不是找出最相似的材料，而是找出当前主体、当前项目和当前用途下真正有效且可授权使用的材料；
- 系统必须具备“没有证据就拒答、证据不足就说明缺口”的能力，不能使用模型知识补全企业事实；
- 下一步讨论同类项目与历史参考池推荐模块的评估指标和质量门禁。

**讨论点完成。**

---

## 二十九、评估体系讨论 7：同类项目与历史参考池推荐模块评估

### 评估对象

评估同类项目发现、横向对比、参考池确认和章节用途绑定能力，关联 PRD `FR-402`、`PRD-BR-06` 和 `AC-03`，覆盖：

1. 以甲方自己的历史中标标书为核心推荐同类项目；
2. 将自有未中标项目用于事实对比，但不推断未中标原因；
3. 将公开中标结果和合法获取的同类项目信息作为补充；
4. 按采购内容、预算规模、行业、技术路线、评分结构、资格条件、服务周期和地域计算相似性；
5. 输出项目概况、评分结构、方案特征、报价信息、可复用内容和风险差异；
6. 由用户确认参考项目池，并按技术、结构、资格、评分、案例、报价和风险等用途授权；
7. 对参考项目池增删和用途变化进行版本管理；
8. 章节生成只使用已经确认且用途匹配的参考绑定关系。

本模块提供“可参考哪些历史项目以及可以参考什么”，不负责直接复制历史标书，也不负责判断当前项目一定应采用历史方案或历史报价。

### 测试数据集

测试集应覆盖以下历史项目和推荐场景：

1. 甲方自有历史中标项目和历史未中标项目；
2. 只有公开中标公告、没有完整标书正文的外部项目；
3. 采购内容高度相似但预算、地域、服务周期或评分结构不同的项目；
4. 项目名称相似但实际采购内容不同的干扰项目；
5. 同一行业但技术路线、交付模式或资格条件明显不同的项目；
6. 采购内容不同但章节结构、案例表达或风险类型具有参考价值的项目；
7. 报价接近但服务范围不同，或者服务范围相同但报价口径不同的项目；
8. 历史资料完整、部分缺失、版本冲突、权限受限和已撤回的项目；
9. 有多个合理参考项目，以及没有足够相似项目的情况；
10. 参考池新增、移除、用途调整和历史项目资料更新后的版本变化场景。

测试数据划分为开发集、验收集和固定回归集，并建立“相似名称不同内容”“同内容不同范围”“无合适参考”“无权限资料”和“历史未中标项目”专项子集。

### 金标准

每个当前投标项目由有经验的标书编写人员筛选候选历史项目，项目负责人复核，形成：

1. 可作为核心参考、辅助参考和不应推荐的项目集合；
2. 每个参考项目的推荐理由和相似维度；
3. 当前项目与历史项目在采购内容、预算、评分、资格、周期、地域和技术路线上的相同点与差异；
4. 可以复用、只能借鉴结构、需要改写和禁止复用的内容；
5. 历史报价的口径、范围和可比较边界；
6. 技术、结构、资格、评分、案例、报价和风险等允许使用的章节用途；
7. 资料来源、所有权、保密等级、访问权限和允许使用范围；
8. 历史项目资料的有效版本和已失效版本；
9. 没有足够参考项目时应输出的不足说明；
10. 对未中标项目只记录可验证事实，不记录未经证实的失败原因。

金标准不要求只有唯一正确参考池。多个项目都可以合理参考时，应标记允许集合、推荐等级和用途边界，而不是强制模型复现某一个人的唯一选择。

### 核心质量指标

1. **相关项目召回率**：金标准中可作为核心或辅助参考的项目被系统发现的比例；
2. **Precision@K**：前 K 个推荐项目中真正具有参考价值的项目比例；
3. **Recall@K**：前 K 个推荐项目覆盖金标准参考项目的比例；
4. **推荐排序效果**：使用 `MRR`、`NDCG@K` 或首个核心参考项目排名衡量高价值项目是否优先出现；
5. **相似理由正确率**：系统给出的采购内容、预算、行业、技术、评分、资格、周期和地域等相似理由真实成立的比例；
6. **项目关键信息准确率**：项目名称、采购内容、预算、中标金额、供应商、日期、服务范围和评分结构等信息正确的比例；
7. **差异识别完整率**：会影响方案复用、资格判断、报价比较或风险判断的重要差异被识别的比例；
8. **风险差异识别率**：当前项目新增、加强或不同于历史项目的风险被正确提示的比例；
9. **可复用内容判断准确率**：系统正确区分可复用、需改写、仅可参考结构和禁止复用内容的比例；
10. **报价可比性判断准确率**：系统正确判断历史报价口径、范围和比较边界的比例；
11. **章节用途绑定正确率**：参考项目及其内容被绑定到正确用途和目标章节的比例；
12. **参考池版本正确率**：章节生成使用当前已确认参考池版本的比例；
13. **确认状态合规率**：进入章节生成的参考内容均经过用户确认的比例；
14. **权限合规率**：推荐、查看、引用和导出的历史资料符合权限与保密要求的比例；
15. **来源可追溯率**：推荐结论能够追溯到历史项目、文件版本、章节或公开来源的比例；
16. **无合适参考正确拒绝率**：不存在足够相似项目时，系统正确说明参考不足而非强行推荐的比例；
17. **用户采纳率**：用户对推荐项目、用途和可复用内容的接受比例；
18. **用户修正率**：用户调整参考项目、推荐理由、用途或复用边界的比例。

推荐指标不能只看向量相似度。一个项目即使文本相似，如果采购范围、资格条件、评分结构或报价口径不同，也可能不适合作为直接参考。

### 硬门禁

出现以下任一情况，本模块评估结果直接判定为“不通过”：

1. 系统擅自推断自有未中标项目的未中标原因，并将推断作为事实或决策依据；
2. 推荐、展示或使用无权访问的第三方标书、客户机密或其他组织内部资料；
3. 将历史项目要求、评分标准、资格条件或合同条款当作当前项目有效要求；
4. 混淆不同项目、采购包、供应商、案例、预算、中标金额或报价版本；
5. 未识别采购范围、服务周期、税费、人员投入或交付内容差异，直接比较历史报价；
6. 未经用户确认就把历史项目内容写入正式初稿；
7. 参考项目只被授权用于结构或风险分析，系统却将其技术内容、案例或报价用于其他章节；
8. 使用已撤回、已失效或被新版本替代的历史资料；
9. 复制历史内容时保留了旧项目名称、客户名称、金额、日期、人员或其他不属于当前项目的事实；
10. 公开中标公告只包含有限信息，系统却编造完整技术方案、评分细节或未公开报价构成；
11. 参考池已经增删或用途已经变化，章节生成仍使用旧参考池版本；
12. 用户移除、限制或修改的参考绑定被系统静默恢复或覆盖。

### 发布阈值原则

当前先确认阈值制定原则，具体数值后续通过金标准评测集校准：

1. 越权使用资料、推断未中标原因、混淆项目事实和未经确认写入初稿等硬门禁事件必须为零；
2. 推荐结果同时评价召回、精度、排序和理由正确性，不能只追求返回更多项目；
3. 核心参考项目、辅助参考项目和不应推荐项目分别统计；
4. 相似性必须展示维度和差异，无法解释的相似分数不能直接驱动章节复用；
5. 历史报价必须先判断范围和口径可比性，不能仅按金额接近判断；
6. 没有合适参考项目时正确拒绝属于合格结果，不得强行填满参考池；
7. 只有已确认、权限允许、版本有效且用途匹配的参考绑定才能进入章节生成。

### 观察指标

- 每个当前项目推荐的候选项目数量；
- 用户保留、移除和新增参考项目的比例；
- 用户对推荐理由和差异说明的修改率；
- 不同推荐维度对最终采纳的贡献；
- 参考池从创建到确认的平均耗时；
- 每个章节实际使用的参考项目数量；
- 可直接复用、需改写、仅参考结构和禁止复用内容的分布；
- 历史资料权限拒绝次数；
- 参考池版本更新导致的章节增量复核数量；
- 推荐与横向对比的平均耗时和计算成本。

### 样本量原则

样本统计单位至少包含当前项目数、历史候选项目数、推荐关系数、相似维度判断数、差异项数、参考池版本数和章节用途绑定数。评估集必须同时包含高相似、部分相似、误导性相似和无合适参考四类场景，并覆盖自有中标、自有未中标、公开中标信息和权限受限资料。

### 失败处理规则

1. 相似性不足或理由不确定时，将项目作为低置信度候选，不自动加入已确认参考池；
2. 无合适参考项目时明确提示参考不足，由用户决定是否人工补充；
3. 资料来源、权限或版本不明确时禁止展示敏感正文和进入章节生成；
4. 历史报价不可直接比较时，展示不可比原因，不生成价格趋势或建议结论；
5. 用户确认参考池和章节用途前，系统只能提供预览和横向对比；
6. 参考池或用途发生变化后，触发受影响章节、引用、风险和审核任务的增量复核；
7. 发现历史事实被错误带入当前项目时，使相关章节和交接基线失效并重新生成；
8. 推荐模型、特征、权重、历史资料或权限规则变化后，使用固定回归集重新评估；
9. 所有误推荐、越权、错误复用和项目事实混淆样本进入长期回归库。

### 用户确认

用户确认同类项目与历史参考池模块按照相关项目召回、推荐排序、相似理由、项目事实、差异识别、复用边界、章节用途、权限和版本进行评估，并确认上述硬门禁方向。

### 完成结论

- 历史项目只能作为经用户确认、有明确用途边界的参考资料，不能替代当前采购文件和当前项目判断；
- 系统既要推荐相似项目，也要清楚说明“哪里相似、哪里不同、什么可以用、什么不能用”；
- 下一步讨论商务指标、报价校验与报价方案推荐模块的评估指标和质量门禁。

**讨论点完成。**

---

## 三十、评估体系讨论 8：商务指标、报价校验与报价方案推荐模块评估

### 评估对象

评估报价模板治理、报价假设留痕、候选方案校验、推荐解释和人工确认闭环，关联 PRD `FR-501`、`PRD-BR-03`、`PRD-BR-05` 和 `AC-04`，覆盖：

1. 管理受控 Excel 报价模板及内部报价模型版本；
2. 记录模板适用范围、审批状态、输入/输出映射、公式校验值和文件哈希；
3. 记录成本、税率、利润率、风险系数、优惠假设和其他报价假设版本；
4. 由 Excel、甲方内部模型或授权报价系统执行实际计算；
5. 读取候选报价结果并校验预算、最高限价、税率、合计、大小写金额和跨章节一致性；
6. 由规则引擎排除无效报价方案；
7. 大模型只比较有效候选方案，说明差异、推荐理由和风险；
8. 商务人员最终选择报价方案，项目负责人确认，激进策略增加授权审批；
9. 只有冻结的确认报价版本可以写入正文、报价表和交接基线；
10. 模板、假设、候选结果、人工选择和最终确认均保留版本与审计记录。

本模块不允许大模型代替 Excel 或内部模型成为正式报价计算器。大模型可以解释和比较规则校验通过的候选结果，但最终报价选择权始终属于业务人员。

### 测试数据集

测试集应覆盖以下模板、报价和异常场景：

1. 不同项目类型、预算规模和报价口径对应的受控 Excel 模板；
2. 当前有效、待审批、已停用和被新版本替代的模板；
3. 单价、总价、分项报价、人员成本、服务周期、税前税后和含税不含税等不同计算方式；
4. 成本、税率、利润率、风险系数、优惠比例和其他假设的多种组合；
5. 正常方案、接近最高限价方案、超过最高限价方案、低于成本或违反内部规则方案；
6. 合计错误、舍入误差、税率错误、大小写金额不一致、单位错误和跨工作表引用错误；
7. Excel 公式被修改、单元格映射变化、隐藏公式、外部链接失效和宏不可执行情况；
8. 多个有效候选方案在价格、利润、风险和竞争策略上存在不同权衡的情况；
9. 商务人员修改假设、重新计算、选择方案、撤回选择和项目负责人确认的流程；
10. 激进报价需要额外授权，以及授权被拒绝或过期的情况；
11. 报价冻结后正文、报价表、图表和交接基线引用同一版本的情况；
12. 报价版本更新后旧章节、旧审核结论和旧交接包需要失效的情况。

测试数据划分为开发集、验收集和固定回归集，并建立“超过限价”“税率错误”“合计不一致”“失效模板”“未确认报价”和“跨章节不一致”专项子集。

### 金标准

每个测试报价项目由商务/报价人员使用已批准模板或内部模型完成计算，项目负责人复核，形成：

1. 正确的模板版本、适用范围、审批状态、哈希和公式校验值；
2. 正确的输入字段、来源、单位、数据类型和单元格映射；
3. 每组报价假设及其版本；
4. Excel 或内部模型产生的正确候选报价结果；
5. 预算、最高限价、税率、利润、合计、大小写金额和内部规则校验结论；
6. 有效方案、无效方案及无效原因；
7. 各有效方案之间可客观说明的价格、利润、风险和策略差异；
8. 商务人员最终选择、项目负责人确认和额外授权结果；
9. 可以进入正文和交接基线的冻结报价版本；
10. 报价更新后应失效或待复核的章节、表格、风险、审核任务和交接包。

金标准中的正式金额必须来自 Excel、内部模型或授权报价系统的实际计算结果，不能由大模型重新计算后作为标准答案。

### 核心质量指标

1. **模板版本正确率**：系统为项目选择或允许使用正确、有效且已批准模板的比例；
2. **模板完整性校验率**：模板哈希、公式校验值、工作表和关键单元格结构被正确检查的比例；
3. **输入映射完整率**：应填入模板或模型的商务字段全部正确映射的比例；
4. **输入值准确率**：写入模板的数值、文本、单位和数据类型与确认来源一致的比例；
5. **数据来源可追溯率**：报价输入能够追溯到项目文件、企业数据、人工录入或确认记录的比例；
6. **假设版本完整率**：成本、税率、利润率、风险系数、优惠和其他假设均保留版本的比例；
7. **计算结果读取准确率**：系统从 Excel 或内部模型读取的候选结果与原计算结果一致的比例；
8. **预算与最高限价校验率**：超过预算或最高限价的方案被正确识别的比例；
9. **税率校验准确率**：税率、含税不含税口径和税额关系被正确校验的比例；
10. **合计校验准确率**：分项、汇总、舍入和总价关系错误被正确识别的比例；
11. **大小写金额一致率**：数字金额与大写金额一致且转换正确的比例；
12. **跨章节报价一致率**：正文、报价表、图表、摘要和交接包引用同一确认报价的比例；
13. **无效方案拦截率**：违反采购限制或内部硬规则的报价方案被规则引擎排除的比例；
14. **有效方案误杀率**：实际有效方案被规则错误排除的比例；
15. **推荐候选合规率**：大模型推荐范围全部来自规则校验通过的有效方案比例；
16. **推荐理由正确率**：推荐解释中的金额、利润、风险和差异与候选数据一致的比例；
17. **风险说明完整率**：推荐方案的重要成本、利润、履约和竞争风险被说明的比例；
18. **人工确认合规率**：最终报价均由商务人员选择并由项目负责人确认的比例；
19. **激进策略授权完整率**：需要额外授权的报价均完成有效审批的比例；
20. **冻结版本引用正确率**：进入正式标书和交接基线的报价均来自当前冻结确认版本的比例；
21. **报价变更影响识别率**：报价更新后相关章节、表格、风险、审核任务和交接基线被正确标记失效或待复核的比例。

报价评估必须区分“计算是否正确”“规则是否有效”“推荐解释是否可靠”和“人工确认是否完整”。不能仅用最终金额正确来替代整个报价闭环评估。

### 硬门禁

出现以下任一情况，本模块评估结果直接判定为“不通过”：

1. 大模型脱离 Excel、内部模型或授权报价系统，自行计算或修改正式最终报价；
2. 违反预算、最高限价、税率、最低利润或其他硬规则的无效方案进入推荐或确认候选；
3. 最终报价超过最高限价，系统未阻断；
4. 分项、合计、税额、大小写金额或跨章节金额不一致，系统未告警并允许形成有效交接基线；
5. 未经商务人员选择或项目负责人确认的报价写入正式标书；
6. 使用待审批、已停用、哈希异常或被新版本替代的报价模板；
7. 使用未确认、已撤回或旧版报价方案生成正文、报价表或交接包；
8. 大模型推荐了被规则引擎判定无效的方案；
9. 推荐解释编造成本、利润、竞争情况或其他候选数据中不存在的事实；
10. 用户最终选择、报价假设或项目负责人确认结果被系统静默覆盖；
11. 激进报价缺少规定的额外授权，系统仍将其冻结为有效最终报价；
12. 报价输入、假设、模板、计算结果或审批缺少版本和来源记录，无法审计追溯；
13. 报价版本变化后，系统仍允许旧报价关联的章节或交接包保持有效；
14. 系统为提高自动化率绕过受控 Excel 模板、内部报价模型或现有审批流程。

### 发布阈值原则

当前先确认阈值制定原则，具体数值后续通过金标准评测集和真实 Excel 模板校准：

1. 超过最高限价、无效方案进入推荐、未确认报价写入标书和大模型擅自计算等硬门禁事件必须为零；
2. 金额、税率、合计、大小写和冻结版本等确定性规则应尽量采用程序化精确校验，不依赖大模型概率判断；
3. 不同模板、项目类型和报价口径分别统计，不能用简单模板表现掩盖复杂模板问题；
4. 大模型只评估推荐解释和风险说明，不以其重新计算结果验证 Excel；
5. 有效方案推荐质量与无效方案拦截能力分别统计；
6. 人工确认和额外授权属于发布前置条件，不是可由平均指标抵消的体验项；
7. 自动处理失败但能够保持原计算结果、明确告警并转人工复核，可以作为正确降级结果。

### 观察指标

- 每个项目使用的模板版本和模板切换次数；
- 自动填入字段比例和人工修正字段比例；
- 报价假设修改次数和方案重算次数；
- 规则排除方案数量及原因分布；
- 有效候选方案数量；
- 大模型推荐被接受、修改和拒绝的比例；
- 商务人员从初次计算到最终选择的耗时；
- 项目负责人确认和额外授权耗时；
- 报价变化引发的章节增量复核数量；
- 单项目报价校验和推荐成本。

### 样本量原则

样本统计单位至少包含模板数、模板版本数、输入字段数、报价假设版本数、候选方案数、规则校验项数、确认报价版本数和跨章节引用数。发布评估必须覆盖所有 V1 支持的主要报价模板，以及正常、边界、无效、激进、版本变化和授权异常场景。

### 失败处理规则

1. 模板版本、审批状态、哈希或公式校验异常时，停止自动填入和结果读取，要求管理员确认模板；
2. 输入来源或字段映射不明确时，创建商务人员确认任务，不自动猜测数值和口径；
3. 预算、最高限价、税率、合计或内部硬规则失败时，将方案标记为无效并禁止推荐；
4. Excel 或内部模型执行失败时保留现有事实和假设，提示用户在受控环境完成计算后重新导入；
5. 有效方案之间无法形成可靠推荐时，展示客观差异和风险，由商务人员自行选择；
6. 报价未完成选择、确认或必要授权时，章节只能显示占位状态，不写入正式金额；
7. 报价版本更新后，触发相关章节、报价表、图表、风险、审核任务和交接基线的增量复核；
8. 模板、映射、规则或报价读取逻辑修改后，使用固定回归集和真实模板重新评估；
9. 所有限价漏拦截、金额不一致、无效方案推荐和版本错误样本进入长期回归库。

### 用户确认

用户确认商务指标与报价方案模块按照模板、映射、来源、公式校验、限价、税率、合计、无效方案拦截、推荐解释、人工确认和跨章节一致性进行评估，并确认上述硬门禁方向。

### 完成结论

- 正式报价必须由 Excel、内部模型或授权报价系统计算，Agent 只负责治理、校验、比较和解释；
- 规则先排除无效方案，大模型只在有效方案中给出建议，商务人员和项目负责人保留最终决定权；
- 下一步讨论章节生成、图表、风险提示、数据来源和说明材料模块的评估指标与质量门禁。

**讨论点完成。**

---

## 三十一、评估体系讨论 9：章节生成、图表、风险提示、数据来源和说明材料模块评估

### 评估对象

评估从已确认要求、企业证据、历史参考、报价版本和团队模板生成投标文件内容的能力，关联 PRD `FR-601`、`FR-702`、`PRD-BR-03`、`PRD-BR-09` 和 `AC-05`，覆盖：

1. 当前采购文件决定写作内容和响应边界；
2. 团队模板决定章节结构、格式和表达规范；
3. 历史标书只在已确认参考池和授权用途范围内提供参考；
4. 按章节生成正文，不一次性生成不可控的大文档；
5. 每个章节绑定要求、评分项、企业证据、参考项目、报价版本和模板版本；
6. 生成带来源的正文、图表、数据说明、风险提示和说明材料；
7. 图表绑定数据来源、生成时间和版本；
8. 保持跨章节人员、金额、日期、参数、案例和承诺一致；
9. 区分 `BID_ATTACHMENT`、`EXTERNAL_EXPLANATION` 和 `INTERNAL_NOTE`；
10. 内部版本展示完整风险、证据缺口、报价策略和豁免，外部版本只包含审核批准的正式响应内容；
11. 输出在线可编辑文档、DOCX 和内部审核 PDF；
12. 局部重生成展示差异并由用户确认，不静默覆盖人工编辑。

本模块评估风险提示是否被正确展示、分级和隔离。风险发现算法本身的召回率和误报率，将在后续“Agent 预检与风险审核模块评估卡”中单独评估。

### 测试数据集

测试集应覆盖以下生成与编辑场景：

1. 资格、技术、商务、服务、实施、运维、培训、案例、报价和风险等不同章节；
2. 有完整证据、部分证据、无证据、待确认和证据冲突的要求；
3. 有明确评分项、评分档位和证明材料要求的章节；
4. 使用不同团队模板、模板版本和项目类型的情况；
5. 使用已确认参考池，以及参考用途仅限结构、技术、案例、报价或风险的情况；
6. 使用冻结确认报价、未确认报价和旧报价版本的情况；
7. 表格、柱状图、折线图、饼图、流程图、架构图和项目进度图等图表；
8. 人员、案例、金额、日期、服务周期和技术参数在多个章节重复出现的情况；
9. `BID_ATTACHMENT`、`EXTERNAL_EXPLANATION` 和 `INTERNAL_NOTE` 三类材料；
10. 内部风险版本和外部正式版本的内容隔离场景；
11. 用户编辑章节后执行局部重生成、证据更新、报价更新和模板更新的情况；
12. 在线文档、DOCX 和 PDF 导出中的目录、页码、表格、图片、字体和分页场景；
13. 长章节、复杂表格、来源较多、引用冲突和上下文接近模型限制的困难场景。

测试数据划分为开发集、验收集和固定回归集，并建立“无证据拒绝生成”“历史事实污染”“报价版本错误”“内外风险泄露”“人工编辑覆盖”和“跨章节冲突”专项子集。

### 金标准

每个测试章节由标书编写人员形成可接受初稿或评分清单，项目负责人和相应业务人员复核，形成：

1. 章节必须覆盖的要求、评分项和响应点；
2. 可以使用的企业证据、历史参考、报价版本和模板版本；
3. 每项关键事实允许使用的来源和引用位置；
4. 禁止生成、需要拒答、需要占位或必须人工确认的内容；
5. 章节结构、标题、表格、图表、格式和篇幅要求；
6. 图表的正确数据、计算口径、来源、时间和版本；
7. 必须提供的附件、外部说明和内部说明材料；
8. 内部版本应展示的风险、缺口和豁免，以及外部版本允许保留的正式内容；
9. 跨章节人员、金额、日期、案例、参数和承诺的一致结果；
10. 人工编辑内容、允许局部重生成范围和预期差异；
11. DOCX、PDF 等导出结果必须满足的版式和内容要求。

生成任务不要求与金标准逐字一致。评估应采用“必须满足项、禁止项、事实和来源校验、结构格式要求、业务人员分级评分”组合，而不是只比较文本相似度。

### 核心质量指标

1. **要求覆盖率**：章节应响应的已确认要求被正文实际覆盖的比例；
2. **评分项覆盖率**：评分项、得分条件和证明材料在目标章节中得到响应的比例；
3. **响应矩阵一致率**：章节内容与要求—评分项—证据—责任人映射保持一致的比例；
4. **事实支撑率**：章节中的企业事实、项目事实、报价和承诺具有有效来源支持的比例；
5. **无来源关键结论率**：资格、能力、案例、报价、参数和风险等关键结论缺少来源的比例；
6. **引用完整率**：需要引用的关键内容均绑定来源文件、版本、页码、证据或报价版本的比例；
7. **引用正确率**：引用内容能够支持正文结论且定位正确的比例；
8. **历史参考使用合规率**：历史内容来自已确认参考池、版本有效且用途匹配的比例；
9. **确认报价引用率**：正文和报价表引用当前冻结确认报价版本的比例；
10. **模板结构符合率**：章节标题、顺序、层级和必填内容符合团队模板的比例；
11. **格式符合率**：字体、编号、表格、页眉页脚、目录、分页和图表样式符合要求的比例；
12. **图表数据一致率**：图表数据与来源数据、正文描述和表格数据一致的比例；
13. **图表来源完整率**：图表具备数据来源、生成时间、口径和版本信息的比例；
14. **风险提示完整率**：已识别且适用于该章节的风险、证据缺口和待确认项在内部版本中完整展示的比例；
15. **内外版本隔离正确率**：内部风险和敏感策略未泄露到外部版本，批准的正式内容未被错误删除的比例；
16. **说明材料齐备率**：应生成或提示准备的附件、外部说明和内部说明材料齐全的比例；
17. **材料类型分类准确率**：说明材料被正确归类为 `BID_ATTACHMENT`、`EXTERNAL_EXPLANATION` 或 `INTERNAL_NOTE` 的比例；
18. **跨章节一致率**：人员、金额、日期、参数、案例、服务周期和承诺在不同章节一致的比例；
19. **人工编辑保留率**：用户已确认或手工修改内容在后续生成中未被静默覆盖的比例；
20. **局部重生成范围准确率**：系统只更新受影响内容且正确展示差异的比例；
21. **导出内容一致率**：在线文档、DOCX 和 PDF 之间的正文、表格、图表和附件清单一致的比例；
22. **导出版式通过率**：DOCX 和 PDF 的目录、页码、分页、字体、表格和图片满足使用要求的比例；
23. **初稿可接受率**：业务人员认为章节无需重写、只需局部修改即可进入审核的比例；
24. **用户修改率**：生成内容被用户修改的句段、表格、图表和结构比例；
25. **错误事实率**：生成内容中与确认数据或来源冲突的事实比例。

文本流畅度和语言风格可以评价，但不能高于要求覆盖、事实支撑、引用、报价、风险和版本正确性。语言写得好不能抵消关键业务错误。

### 硬门禁

出现以下任一情况，本模块评估结果直接判定为“不通过”：

1. 编造企业资质、人员、案例、能力、项目事实、报价、合作关系或履约承诺；
2. 遗漏资格条件、实质性要求、否决项或关键评分响应，系统却将章节标记为已完成；
3. 使用未确认、已撤回或旧版报价，或者正文与报价表金额不一致；
4. 引用错误证据、错误文件版本、错误页码，或引用内容不能支持正文结论；
5. 将历史项目名称、客户、人员、案例、金额、日期、参数或承诺错误带入当前项目；
6. 使用未确认参考池、无权限历史资料或超出授权用途的历史内容；
7. 图表数据与原始数据、正文或表格不一致，可能改变业务判断；
8. 图表缺少来源、口径或版本，却被作为正式结论使用；
9. 内部版本漏掉已识别的阻断风险、关键证据缺口或未确认事项；
10. 内部风险、报价策略、豁免理由、敏感证据或内部备注泄露到外部版本；
11. 缺少采购文件明确要求的附件、说明文件、签章材料或格式内容，系统未告警；
12. `INTERNAL_NOTE` 被错误加入投标文件，或 `BID_ATTACHMENT` 被错误隐藏为内部材料；
13. 人员、金额、日期、参数或承诺在跨章节出现实质性冲突，系统未告警；
14. 人工编辑或已确认内容被模型静默覆盖；
15. 局部重生成未展示差异，或修改了未授权的其他章节；
16. 无有效证据时系统没有拒绝生成或标记占位，而是输出肯定事实；
17. 在线文档、DOCX 或 PDF 导出丢失正文、表格、图表、页码或附件清单；
18. 已失效章节或旧版本内容仍进入有效内部交接基线。

### 发布阈值原则

当前先确认阈值制定原则，具体数值后续通过金标准评测集和人工流程基线校准：

1. 编造事实、关键要求遗漏、错误报价、错误引用、风险泄露和人工编辑覆盖等硬门禁事件必须为零；
2. 资格、否决项、评分、报价和附件等高风险内容单独评估，不能被普通正文平均分稀释；
3. 事实支撑、引用和版本正确性优先于语言流畅度与文本相似度；
4. 不同章节类型、模板和导出格式分别统计；
5. 没有足够证据时正确拒绝、占位或转人工，优于生成看似完整但无法证明的内容；
6. 内部版本和外部版本分别验收，并执行敏感内容泄露专项测试；
7. 初稿可接受率和用户修改率用于衡量实用性，但不能抵消硬门禁问题；
8. 局部重生成必须同时通过内容质量、影响范围和人工编辑保护评估。

### 观察指标

- 各章节初稿生成耗时和成本；
- 单章节来源、证据和参考项目数量；
- 用户接受、局部修改、重写和拒绝的比例；
- 用户修改集中在哪些章节、句段和问题类型；
- 图表自动生成数量、采用率和修改率；
- 说明材料自动准备、人工补充和缺失比例；
- 内部风险提示数量及用户处理状态；
- 跨章节冲突数量和修复耗时；
- 局部重生成次数、影响范围和人工确认耗时；
- 在线文档、DOCX 和 PDF 导出失败率；
- 单项目全文生成和导出的资源消耗。

### 样本量原则

样本统计单位至少包含项目数、章节数、要求覆盖关系数、评分项数、事实陈述数、引用数、图表数、说明材料数、风险项数、跨章节字段数和导出文件数。发布评估必须覆盖主要章节类型、所有说明材料类型、内部外部双版本、报价引用、历史参考、人工编辑和局部重生成场景。

### 失败处理规则

1. 关键要求、评分项、证据或报价缺失时，将章节保持在待补充或待确认状态，不标记完成；
2. 无证据或来源不足时生成明确占位、风险和资料补充任务，不输出肯定事实；
3. 发现错误引用、历史事实污染或报价版本错误时，使相关章节、图表、风险、审核任务和交接基线失效；
4. 图表数据或口径不确定时保留来源表格并转人工确认，不生成误导性图表；
5. 必需说明材料缺失时创建责任人和截止时间明确的任务；
6. 内外版本隔离失败时阻止外部版本导出和交接；
7. 局部重生成前锁定人工编辑，生成差异预览，用户确认后才写入；
8. 模板、提示词、模型、证据、参考池、报价或生成规则变化后，使用固定回归集重新评估受影响章节；
9. 所有事实编造、遗漏、引用错误、风险泄露、格式丢失和人工编辑覆盖样本进入长期回归库。

### 用户确认

用户确认章节生成模块按照要求和评分项覆盖、事实支撑、引用、模板格式、图表、风险提示、说明材料、跨章节一致、人工编辑保护和导出质量进行评估，并确认上述硬门禁方向。

### 完成结论

- 标书初稿的首要标准是“要求覆盖、事实有据、版本正确、风险清楚”，而不是单纯语言流畅；
- 内部风险和外部正式内容必须双轨隔离，人工编辑和确认结果不得被模型静默覆盖；
- 下一步讨论 Agent 预检、负责人及商务/法务审核、结构化返工和风险闭环模块的评估指标与质量门禁。

**讨论点完成。**

---

## 三十二、评估体系讨论 10：Agent 预检、人工审核、结构化返工与风险闭环模块评估

### 评估对象

评估系统预检、人工审核、问题返工、增量复核和风险关闭的完整闭环，关联 PRD `FR-701`、`FR-702`、`PRD-BR-07`、`PRD-BR-08`、`PRD-BR-09`、`PRD-BR-10`、`PRD-BR-11` 和 `AC-05`，覆盖：

1. 系统预检资格与合规、评分点覆盖、一致性、附件完整性、报价引用和风险；
2. Supervisor Agent 编排预检任务、重试、暂停和人工接管，但不直接改变项目主状态；
3. 按系统预检、项目负责人、商务审核、合规/法务审核的顺序执行必要审核；
4. 根据问题类型和角色职责，只向相关审核人员展示需要处理的异常；
5. 将每条审核意见转化为结构化问题任务；
6. 记录问题提出人、责任人、风险等级、证据、修改要求和截止时间；
7. Agent 生成修改建议，标书编写人确认后才写入正式章节；
8. 修改后由原审核角色查看差异并复核关闭；
9. 只重新运行和复核受影响的章节、证据、报价和任务；
10. 风险按 `LOW`、`MEDIUM`、`HIGH` 和 `BLOCKING` 分级；
11. 低风险自动处理并可抽查，中风险支持批量确认，高风险逐项确认，阻断风险必须解决或授权豁免；
12. 置信度不足、证据缺失或规则冲突转为 `WAITING_HUMAN`；
13. 风险豁免记录授权人、理由、适用范围和版本；
14. 未解决的阻断风险禁止生成有效 `HANDOVER_BASELINE`；
15. 所有确认、退回、修改、豁免和关闭动作生成审计记录。

本模块评估“系统是否及时发现问题并推动正确的人完成闭环”，不要求 Agent 替代项目负责人、商务人员或法务人员作出最终审核决定。

### 测试数据集

测试集应在完整或局部标书中植入已知问题，并覆盖：

1. 资格条件不满足、证明材料缺失、证据过期和主体不符；
2. 实质性要求遗漏、否决项未响应和评分点覆盖不足；
3. 人员、金额、日期、参数、案例和承诺跨章节冲突；
4. 附件缺失、格式错误、签章遗漏、文件命名或份数不符合要求；
5. 报价版本错误、超过限价、大小写金额不一致和正文报价引用错误；
6. 无来源结论、错误引用、历史事实污染和企业能力编造；
7. `LOW`、`MEDIUM`、`HIGH`、`BLOCKING` 四类风险；
8. 置信度不足、规则互相冲突和需要人工判断的灰度问题；
9. 项目负责人、商务人员、法务人员和标书编写人的不同责任场景；
10. 单个问题影响一个章节、多个章节、证据、报价或附件的情况；
11. 用户接受、修改、拒绝 Agent 修改建议，以及自行修改内容的情况；
12. 审核问题被退回、重新打开、修改、复核、关闭和授权豁免的完整状态流转；
13. 审核人缺席、超时、任务失败、Agent 重试和人工接管场景；
14. 低中风险批量处理、高风险逐项确认和阻断风险强制处理场景；
15. 无关章节不应重复审核的增量复核场景。

测试数据划分为开发集、验收集和固定回归集，并建立资格、否决项、报价、附件、跨章节冲突、编造事实、错误豁免和阻断风险专项子集。

### 金标准

每个测试项目由项目负责人、商务审核人员和合规/法务人员按职责共同标注，形成：

1. 应被系统预检发现的问题清单；
2. 每个问题的风险类型、严重程度和业务后果；
3. 支持问题结论的采购文件、企业证据、报价版本或章节差异；
4. 正确的责任人、审核角色和处理截止时间；
5. 问题应当阻断、待确认、批量处理、逐项确认或允许自动处理的方式；
6. 可接受的修改建议、不可接受建议和必须由人工决定的部分；
7. 修改后应重新运行和复核的对象范围；
8. 正确的问题状态变化和有权执行每个动作的角色；
9. 风险是否允许豁免、豁免授权人、理由、范围和版本；
10. 问题关闭所需证据和复核条件；
11. 审核结束后是否允许生成有效交接基线。

对存在主观判断的风险，应由至少两名相应角色人员复核并记录争议结论。金标准重点确定业务底线和允许处理范围，不要求所有审核人员使用完全相同的措辞。

### 核心质量指标

1. **预检问题召回率**：金标准中应由系统发现的问题被正确识别的比例；
2. **阻断问题召回率**：`BLOCKING` 问题被系统正确发现的比例；
3. **高风险问题召回率**：`HIGH` 和 `BLOCKING` 问题被正确发现的比例；
4. **误报率**：没有真实问题的内容被错误标记为风险或审核问题的比例；
5. **风险分级准确率**：问题被正确分类为 `LOW`、`MEDIUM`、`HIGH` 或 `BLOCKING` 的比例；
6. **问题类型分类准确率**：资格、评分、证据、一致性、附件、报价、合规和其他类型分类正确的比例；
7. **问题证据正确率**：审核问题绑定的原文、证据、报价或差异能够支持问题结论的比例；
8. **审核角色路由准确率**：问题被分配给正确责任人和审核角色的比例；
9. **结构化问题完整率**：问题具备提出人、责任人、风险等级、证据、修改要求、截止时间和状态的比例；
10. **修改建议正确率**：Agent 建议能够修复问题且不会引入新问题的比例；
11. **修改建议可采纳率**：标书编写人直接接受或局部修改后接受建议的比例；
12. **人工确认合规率**：Agent 建议写入正式章节前均经过标书编写人确认的比例；
13. **受影响范围识别率**：问题修改后应重跑和复核的章节、证据、报价、附件和任务被完整识别的比例；
14. **增量复核准确率**：受影响对象被重新复核、未受影响对象未被无谓重开的比例；
15. **遗漏复核率**：实际受影响对象没有进入重新审核的比例；
16. **无效重复审核率**：未受影响内容被不必要重复审核的比例；
17. **问题关闭合规率**：问题由原审核角色或授权复核角色在满足条件后关闭的比例；
18. **编写人自关拦截率**：系统阻止标书编写人关闭自己审核问题的比例；
19. **风险豁免合规率**：豁免具备有效授权人、理由、范围、版本和审计记录的比例；
20. **阻断交接拦截率**：存在未解决或未授权豁免阻断风险时，系统成功阻止生成有效交接基线的比例；
21. **`WAITING_HUMAN` 转入准确率**：置信度不足、证据缺失或规则冲突正确转人工的比例；
22. **审计记录完整率**：确认、退回、修改、豁免、转交和关闭动作均形成不可修改审计事件的比例；
23. **审核问题按期关闭率**：问题在规定截止时间内完成处理和复核的比例；
24. **平均审核耗时**：从审核任务创建到最终关闭的时间；
25. **审核退回轮次**：同一章节或问题反复退回修改的次数；
26. **预检前置发现率**：最终人工审核发现的问题中，已经被系统预检提前发现的比例；
27. **末期阻断问题数**：临近交接时才首次发现的阻断问题数量；
28. **Agent 编排成功率**：预检任务正确执行、重试、暂停和转人工的比例。

风险召回率和误报率必须同时观察。高风险和阻断问题优先保证召回，但不能用大量无依据告警让审核人员失去对系统的信任。

### 硬门禁

出现以下任一情况，本模块评估结果直接判定为“不通过”：

1. 漏掉资格、否决项、报价、合规或其他阻断问题，系统也没有转人工确认；
2. 存在未解决且未授权豁免的 `BLOCKING` 风险，系统仍允许生成有效 `HANDOVER_BASELINE`；
3. Agent 未经标书编写人确认，直接修改正式章节或人工确认内容；
4. 标书编写人能够关闭自己的审核问题；
5. 修改后没有由原审核角色或授权复核角色检查，就将问题标记为关闭；
6. 未经授权人员执行风险接受、审核通过或风险豁免；
7. 风险豁免缺少授权人、理由、适用范围、版本或审计记录；
8. Supervisor Agent 直接改变项目主状态、审核结论或交接状态；
9. 返工时静默覆盖无关章节、人工编辑、报价选择、证据确认或审核结论；
10. 修改影响多个章节、证据、报价或附件，系统只复核局部内容并遗漏其他受影响对象；
11. 用户已拒绝的修改建议被系统自动恢复或再次写入；
12. 置信度不足、证据缺失或规则冲突时，系统输出确定性审核通过结论而未转 `WAITING_HUMAN`；
13. 阻断问题被错误降级为低中风险并自动或批量通过；
14. 审核意见、修改、确认、退回、豁免或关闭动作缺少不可修改的审计记录；
15. 旧文件、旧报价或旧章节版本上的审核结论被直接沿用到新版本，未重新执行影响分析；
16. 无权限角色查看、修改或批准不属于其职责和权限范围的敏感审核内容。

### 发布阈值原则

当前先确认阈值制定原则，具体数值后续通过植入缺陷评测集和人工审核基线校准：

1. 漏掉阻断问题、未经授权豁免、编写人自关、Agent 擅自修改和阻断风险仍可交接等硬门禁事件必须为零；
2. `BLOCKING`、`HIGH`、`MEDIUM` 和 `LOW` 风险分别统计，不能用低风险高准确率掩盖阻断问题漏报；
3. 高风险召回优先，但误报率和审核人员实际采纳情况必须同时监控；
4. 结构化问题完整性、角色路由、关闭权限和审计记录主要通过确定性规则校验；
5. Agent 修改建议质量不能只看语言，应验证是否真正修复原问题以及是否引入新冲突；
6. 增量复核既不能漏掉受影响内容，也不能退化成每次全量重审；
7. 人工审核结论和风险接受属于授权人员职责，不得由模型评分替代；
8. 正确转人工、暂停或要求补充证据属于合格结果，不应被统计为单纯自动化失败。

### 观察指标

- 各类预检问题数量和风险等级分布；
- 人工审核新增但预检未发现的问题数量；
- 审核人员对 Agent 问题和修改建议的接受、修改和拒绝比例；
- 低中风险批量处理比例和抽查不合格率；
- 高风险逐项确认耗时；
- 阻断问题平均解决时长和豁免比例；
- 每个角色的待办数量、处理时长和逾期率；
- 审核退回轮次和重复问题比例；
- 增量复核对象占全部内容的比例；
- Agent 重试、暂停和人工接管次数；
- 末期新发现问题和末期阻断问题数量；
- 单项目预检和审核资源成本。

### 样本量原则

样本统计单位至少包含项目数、章节数、植入问题数、审核问题数、风险等级数、修改建议数、状态转换数、豁免数、审计事件数和增量复核关系数。发布评估必须覆盖全部审核角色、四个风险等级、正常关闭、退回重开、授权豁免、超时、Agent 失败和人工接管场景。

### 失败处理规则

1. 发现阻断问题漏报时，停止发布并修复规则、提示词、模型或证据链；
2. 风险等级不确定时提升到人工确认，不自动降低风险；
3. 修改建议不可靠时只展示问题、证据和影响范围，由编写人自行修改；
4. 受影响范围不确定时扩大待复核范围，由相应审核角色确认后再关闭；
5. 审核角色、权限或关闭条件不满足时，拒绝状态转换并记录失败审计事件；
6. 风险豁免信息不完整或授权无效时，保持风险未解决状态；
7. Agent 任务失败时独立重试，超过限制后转人工，不删除已确认事实和人工结论；
8. 文件、证据、报价或章节版本变化后，重新执行影响分析并重开相关审核问题；
9. 规则、模型、提示词、风险分级或工作流状态机变化后，使用固定回归集重新评估；
10. 所有漏报、误报、错误分级、越权关闭、无效豁免和增量复核遗漏样本进入长期回归库。

### 用户确认

用户确认审核与风险闭环模块按照问题召回、误报、风险分级、角色路由、结构化问题、修改建议、影响范围、增量复核、关闭权限、风险豁免和审核效率进行评估，并确认上述硬门禁方向。

### 完成结论

- Agent 负责提前发现问题、提供证据和修改建议，但审核通过、风险接受和问题关闭仍由授权人员完成；
- 评估既要保证阻断问题不漏报，也要控制误报和无效重复审核，真正降低负责人和商务/法务人员工作量；
- 下一步讨论版本失效、状态转换、内部交接和人工递交记录模块的评估指标与质量门禁。

**讨论点完成。**

---

## 三十三、评估体系讨论 11：版本失效、状态转换、内部交接与人工递交记录模块评估

### 评估对象

评估关键内容变化后的版本失效、合法状态转换、基线冻结、受控交接和人工递交留痕能力，关联 PRD `FR-801`、`PRD-BR-02`、`PRD-BR-07`、`PRD-BR-08`、`PRD-BR-10`、`PRD-BR-12` 和 `AC-06`，覆盖：

1. 项目负责人生成 `REVIEW_BASELINE` 和 `HANDOVER_BASELINE`；
2. 基线冻结采购文件、正文、报价、附件、证据、风险和审批结果；
3. 基线生成文件清单、版本号、哈希和“内部审核版/禁止递交”等标识；
4. 受控交接记录接收人、有效期、下载次数、水印和禁止转发策略；
5. 投标递交责任人确认收到交接包并确认使用最新版本；
6. 采购文件、证据、报价、章节、附件、风险或审批发生关键变化后，旧基线自动转为 `INVALIDATED`；
7. 旧交接链接撤销，旧文件标记“禁止递交”；
8. 递交记录按照 `PENDING_RECEIPT`、`RECEIVED`、`SUBMITTING`、`SUBMITTED`、`RECEIPT_UPLOADED` 或 `FAILED` 流转；
9. 外部递交由投标递交责任人在采购或交易平台人工完成；
10. 系统只记录递交平台、时间、状态、操作人、失败原因和回执，不替代外部交易平台；
11. 重新打开、撤回、过期、交接、接收和递交均保留历史事件，不覆盖原记录；
12. 所有基线、交接、状态转换和递交动作生成不可修改的审计事件。

本模块的目标是保证递交责任人始终拿到“正确、最新、已审核、可追溯”的内部交接包。V1 不自动登录外部平台、不自动使用 CA 签章，也不宣称系统已经代替人工完成外部递交。

### 测试数据集

测试集应覆盖以下版本、状态和交接场景：

1. 正常生成 `DRAFT_BASELINE`、`REVIEW_BASELINE`、`HANDOVER_BASELINE` 的流程；
2. 采购文件、澄清更正、证据、报价、章节、附件、风险或审批任一对象发生变化；
3. 变更只影响局部内容，以及变更影响整个交接基线的情况；
4. 存在未解决阻断风险、未完成审核、未确认报价或缺失附件的情况；
5. 项目负责人、普通编写人、审核人和递交责任人的不同权限场景；
6. 交接包正常下载、下载次数达到限制、链接过期、主动撤回和旧链接撤销；
7. 接收人正确、接收人错误、接收人未确认和多人重复接收场景；
8. 文件内容与清单一致、文件被替换、哈希不一致、文件缺失和文件损坏场景；
9. 交接后发现内部错误并重新打开到 `REWORKING` 的情况；
10. 项目主动撤回、截止时间到达、交接包过期和交接重新生成场景；
11. 递交责任人从 `PENDING_RECEIPT` 到 `RECEIVED`、`SUBMITTING`、`SUBMITTED` 和 `RECEIPT_UPLOADED` 的正常流程；
12. 外部平台递交失败、超时、退回、重新递交和回执缺失情况；
13. 用户错误填写递交成功、重复记录递交结果或上传错误回执的情况；
14. 系统任务失败、状态并发冲突、重复点击和网络重试场景；
15. 旧交接包、旧链接、旧回执和旧状态记录的历史查询场景。

测试数据划分为开发集、验收集和固定回归集，并建立“旧基线未失效”“链接撤销失败”“阻断风险仍可交接”“错误接收人”“哈希不一致”和“虚假递交成功”专项子集。

### 金标准

每个测试场景依据 PRD、状态机和权限规则预先定义：

1. 允许的起始状态、目标状态、操作角色和前置条件；
2. 应冻结到基线中的文件、版本、报价、证据、风险和审批结果；
3. 正确的文件清单、版本号、哈希、标识和水印信息；
4. 关键变化发生后应失效或待复核的对象范围；
5. 旧基线、旧链接和旧文件应采取的失效、撤销和禁止递交状态；
6. 正确的交接接收人、有效期、下载限制和权限策略；
7. 递交责任人需要核对和确认的最新版本信息；
8. 外部人工递交的平台、时间、状态、操作人、失败原因和回执要求；
9. 正确的递交记录状态流转；
10. 每次状态变化、交接、接收、撤回和递交应生成的审计事件；
11. 错误操作应被拒绝时的状态、提示和审计结果。

金标准以确定性业务规则和状态转换表为主。系统不能通过大模型判断替代权限、哈希、状态机和基线完整性校验。

### 核心质量指标

1. **变更影响识别率**：关键内容变化后，所有受影响基线、链接、章节、审核和交接对象被正确识别的比例；
2. **旧版本失效正确率**：依赖旧采购文件、证据、报价、章节或审批的基线被正确标记为 `INVALIDATED` 的比例；
3. **错误失效率**：未受影响的有效基线或对象被错误失效的比例；
4. **状态转换正确率**：状态按照允许的起点、目标、角色和前置条件转换的比例；
5. **非法状态转换拦截率**：不满足权限或前置条件的状态变化被系统拒绝的比例；
6. **基线内容完整率**：采购文件、正文、报价、附件、证据、风险和审批结果全部进入基线的比例；
7. **基线版本正确率**：基线中所有对象均使用当前已确认有效版本的比例；
8. **文件清单完整率**：交接清单与实际交接文件一一对应的比例；
9. **文件哈希一致率**：交接文件内容与基线记录哈希一致的比例；
10. **审核与豁免完整率**：生成交接基线前必要审核均通过，风险均已解决或有效豁免的比例；
11. **阻断交接拦截率**：存在未解决阻断风险时成功阻止生成有效交接基线的比例；
12. **交接权限正确率**：只有授权项目负责人能够生成和确认内部交接的比例；
13. **接收人匹配率**：交接包实际接收人与指定投标递交责任人一致的比例；
14. **最新版本确认率**：递交责任人在接收时完成最新基线版本、清单和哈希核对的比例；
15. **链接撤销成功率**：基线失效、撤回或过期后旧下载链接立即不可继续使用的比例；
16. **下载策略执行率**：有效期、下载次数、水印和禁止转发策略被正确执行的比例；
17. **禁止递交标识完整率**：旧版、审核版和失效文件被正确标记为禁止递交的比例；
18. **递交状态记录准确率**：记录状态与递交责任人的真实人工操作一致的比例；
19. **递交记录完整率**：平台、时间、状态、操作人、失败原因和回执等必填信息完整的比例；
20. **回执关联正确率**：上传回执或截图关联到正确项目、采购包、交接基线和递交记录的比例；
21. **外部递交边界合规率**：系统没有冒充外部平台或自动声明递交成功的比例；
22. **审计事件完整率**：基线生成、交接、接收、失效、撤回、重新打开和递交动作均有不可修改审计记录的比例；
23. **并发幂等正确率**：重复点击、网络重试或并发操作不会生成矛盾状态和重复有效基线的比例；
24. **交接后变更告警率**：交接完成后发生关键变化时，系统正确通知项目负责人和递交责任人的比例；
25. **交接闭环耗时**：从项目负责人生成基线到递交责任人确认最新版本所需时间。

版本和状态评估必须以对象版本、哈希、权限和审计事件为依据，不能只检查界面上显示的状态文字是否正确。

### 硬门禁

出现以下任一情况，本模块评估结果直接判定为“不通过”：

1. 采购文件、证据、报价、章节、附件、风险或审批发生关键变化后，旧 `HANDOVER_BASELINE` 仍保持有效；
2. 旧交接包、旧链接或旧文件在基线失效、撤回或过期后仍可被当作有效版本下载或使用；
3. 存在未解决且未授权豁免的 `BLOCKING` 风险，系统仍允许生成有效交接基线；
4. 缺少必要审核、最终报价确认、附件、证据或授权豁免，系统仍允许完成内部交接；
5. 非授权人员生成、确认、撤回或重新打开交接基线；
6. 投标递交责任人接收到错误项目、错误采购包、错误版本或不完整文件清单；
7. 文件内容与基线哈希不一致，系统未阻断或告警；
8. 递交责任人未确认最新版本，系统却将交接状态标记为已接收或可递交；
9. 撤销、过期或超过下载次数的链接仍可访问交接文件；
10. 系统虚构、自动推断或错误标记外部递交成功；
11. 外部递交实际由人工执行，但递交记录缺少平台、时间、操作人、状态或回执要求；
12. 回执关联到错误项目、采购包、基线或递交记录；
13. 交接后发生关键变化，系统没有使旧基线失效，也没有通知递交责任人；
14. 已撤回或失效的文件没有“禁止递交”等明显标识；
15. 基线、交接、接收、失效、撤回、重新打开或递交动作缺少不可修改的审计记录；
16. 重试或并发操作生成两个同时有效但内容不同的 `HANDOVER_BASELINE`；
17. 系统自动登录外部平台、使用 CA 或代替递交责任人执行 V1 范围外的外部递交；
18. 历史状态、旧基线或旧递交记录被删除或覆盖，无法追溯完整过程。

### 发布阈值原则

当前先确认阈值制定原则，具体数值后续通过状态机测试和端到端场景校准：

1. 旧基线未失效、阻断风险仍可交接、错误版本交接、链接撤销失败和虚假递交成功等硬门禁事件必须为零；
2. 状态转换、权限、哈希、前置条件和审计事件使用确定性规则进行全量测试；
3. 每一种合法状态转换、非法状态转换和关键失败路径都必须有自动化测试；
4. 变更影响识别既要避免漏失效，也要控制无关对象被错误失效；
5. 交接包内容完整性和最新版本正确性优先于交接速度；
6. 外部人工递交记录必须与真实操作一致，系统不能把“内部已交接”当作“外部已递交”；
7. 正确拒绝非法操作、撤销旧链接或要求重新确认属于合格结果，不应视为流程失败。

### 观察指标

- 每个项目生成的审核基线和交接基线数量；
- 基线失效和重新生成次数；
- 引发基线失效的变更类型分布；
- 从基线生成到递交责任人接收的平均耗时；
- 交接包下载次数、过期次数和撤销次数；
- 递交责任人发现版本或文件问题的次数；
- 外部递交成功、失败、重试和回执补充数量；
- 交接后关键变更和重新交接次数；
- 状态转换失败和权限拒绝次数；
- 单项目基线、交接文件和审计事件存储量；
- 项目撤回、过期和重新打开数量。

### 样本量原则

样本统计单位至少包含项目数、基线数、文件数、哈希校验数、状态转换数、权限操作数、交接包数、下载链接数、递交记录数、回执数和审计事件数。发布评估必须覆盖所有基线状态、递交记录状态、合法与非法转换、版本变更、撤回、过期、重新打开、并发和失败重试场景。

### 失败处理规则

1. 基线内容、版本、清单或哈希异常时，禁止交接并重新生成基线；
2. 关键内容变化后立即将旧基线标记为 `INVALIDATED`，撤销链接并通知相关人员；
3. 审核、报价、风险或附件前置条件不满足时，拒绝生成 `HANDOVER_BASELINE`；
4. 接收人或权限不正确时，拒绝下载和确认接收，并记录安全审计事件；
5. 递交责任人未核对最新版本时保持 `PENDING_RECEIPT`，不得自动进入 `RECEIVED`；
6. 外部递交失败时记录失败原因、保持真实状态并创建处理任务，不伪造成功；
7. 回执缺失或关联不确定时保持待补充状态，由递交责任人确认；
8. 并发或重复操作时使用幂等控制，只保留一个合法状态结果并记录重复请求；
9. 状态机、权限、基线结构或链接策略变化后，重新执行全部状态转换和安全回归测试；
10. 所有旧基线未失效、错误版本交接、撤销失败、虚假递交和审计缺失样本进入长期回归库。

### 用户确认

用户确认版本与交接模块按照变更影响、旧版本失效、状态转换、基线完整性、文件哈希、审核与豁免、接收人确认、链接撤销、递交记录和审计追溯进行评估，并确认上述硬门禁方向。

### 完成结论

- 内部交接不是简单下载文件，而是冻结一个通过审核、版本一致、风险闭环且可审计的交接基线；
- 外部递交仍由投标递交责任人完成，系统只负责受控交接、版本保护和真实记录递交结果；
- 至此，V1 各核心模块的 L1 评估方向已经逐项确认，下一步应讨论 L2 业务链路评估。

**讨论点完成。**

---

## 三十四、评估体系讨论 12：L2 业务链路划分与统一评估原则

### 讨论背景

L1 单模块评估可以判断每个模块独立运行是否合格，但不能证明模块连接后仍然能够正确传递数据、保留版本、执行人工确认和处理异常。因此，V1 需要在 L1 之后增加 L2 业务链路评估。

L2 不再只看单个算法指标，而是检查一项业务从输入到产生可供下一阶段使用的结果时，整条链路是否正确、完整、可追溯、可恢复。

### 已确认的五条 L2 业务链路

```text
链路 1：项目编号
  → 文件接入
  → 有效版本确认
  → 文档解析

链路 2：要求提取
  → 企业证据
  → 历史参考
  → 响应矩阵

链路 3：报价测算
  → 规则校验
  → 人工确认
  → 章节引用

链路 4：章节生成
  → Agent 预检
  → 人工审核
  → 结构化返工

链路 5：内容变更
  → 影响分析
  → 增量复核
  → 交接基线
  → 递交记录
```

### 链路 1：项目与采购文件准备链路

**起点**：用户选择来源平台并输入外部项目编号，或者创建项目后补充链接和文件。

**核心过程**：

1. 匹配正确项目和采购包；
2. 自动或人工接入公告、采购文件、附件和补充文件；
3. 记录来源、时间、哈希、连接器和操作者；
4. 用户确认当前生效文件及覆盖关系；
5. 解析正文、章节、表格、页码和原文位置；
6. 对失败、受限或低质量内容创建重试或人工处理任务。

**终点产物**：一组经过确认、版本有效、来源可追溯且达到解析质量要求的采购文件，可以安全进入要求提取阶段。

### 链路 2：要求与响应依据准备链路

**起点**：已经确认并完成解析的采购文件。

**核心过程**：

1. 提取资格、实质性要求、评分项、时间、附件和格式要求；
2. 为要求保留原文、页码、版本、后果和确认状态；
3. 检索有效企业证据，无证据时拒答并创建待办；
4. 推荐同类历史项目并输出差异和复用边界；
5. 用户确认企业证据、参考项目池和章节用途；
6. 建立要求—评分项—章节—证据—责任人的响应矩阵。

**终点产物**：每条已确认要求都有明确的来源、响应位置、证据状态、参考边界和责任人；未满足项、待确认项和阻断项清晰可见。

### 链路 3：报价确认与章节引用链路

**起点**：已确认的项目预算、最高限价、商务数据和受控报价模板。

**核心过程**：

1. 商务人员选择有效模板并填写报价假设；
2. Excel、内部模型或授权报价系统执行实际计算；
3. 系统读取候选结果并校验限价、税率、合计、大小写和一致性；
4. 规则排除无效方案；
5. 大模型比较有效方案并说明理由和风险；
6. 商务人员选择方案，项目负责人确认，必要时执行额外授权；
7. 冻结确认报价版本并写入正文、报价表和相关图表。

**终点产物**：一个规则有效、来源可查、人工确认且跨章节一致的冻结报价版本。

### 链路 4：初稿生成与审核返工链路

**起点**：已确认的要求、证据、参考池、报价版本和团队模板。

**核心过程**：

1. 按章节生成带来源的正文、图表、说明材料和内部风险；
2. Agent 执行资格、评分、证据、一致性、附件、报价和风险预检；
3. 项目负责人、商务人员和合规/法务人员按职责审核；
4. 审核意见形成结构化问题任务；
5. Agent 提供修改建议，标书编写人确认后写入；
6. 原审核角色查看差异并复核关闭；
7. 只重新运行和审核受影响内容。

**终点产物**：要求覆盖、事实有据、风险闭环、必要审核完成，并具备形成交接基线条件的标书版本。

### 链路 5：变更、基线与递交留痕链路

**起点**：采购文件、证据、报价、章节、附件、风险、审批或已形成基线发生变化，或者项目进入内部交接阶段。

**核心过程**：

1. 识别变化对象和受影响范围；
2. 将旧要求、章节、审核结论或基线标记失效或待复核；
3. 重新运行受影响任务并由对应角色增量复核；
4. 满足审核、报价、附件和风险条件后生成 `HANDOVER_BASELINE`；
5. 冻结文件清单、版本和哈希，并受控交接给投标递交责任人；
6. 递交责任人确认收到和使用最新版本；
7. 外部递交由人工完成，系统记录真实状态和回执；
8. 后续变化使旧基线、旧链接和旧文件失效。

**终点产物**：一个最新、有效、已确认、可审计的内部交接包，以及与真实人工外部递交一致的状态和回执记录。

### L2 统一评估维度

每条链路除继承相关 L1 模块门禁外，还统一评估以下维度：

1. **链路完成率**：合法输入能够走到预期终点产物的比例；
2. **跨模块数据正确率**：上游输出进入下游后，关键字段、版本、来源和状态保持正确的比例；
3. **链路完整率**：没有跳过必要确认、规则校验、人工审核或版本冻结步骤；
4. **来源追溯率**：终点产物能够反向定位到全部关键上游对象和版本；
5. **状态转换正确率**：链路中任务、风险、审核、报价和基线状态按照规则转换；
6. **人工确认合规率**：应由用户决定的事项都由正确角色确认；
7. **异常拦截率**：错误、缺失、无权限、低质量或高风险输入不会静默进入下一阶段；
8. **降级与人工接管成功率**：自动链路失败时能够保留已有结果并转入可执行的人工流程；
9. **版本传播正确率**：上游版本变化能够正确使下游结果失效或待复核；
10. **幂等与重试正确率**：重试和重复操作不会产生重复、冲突或两个同时有效的结果；
11. **审计完整率**：确认、失败、重试、修改、审核、豁免、交接和递交动作均可追溯；
12. **链路耗时**：从合法输入到终点产物的时间，以及其中人工等待和系统执行时间；
13. **人工处理量**：每条链路需要用户确认、修改、补充和重新审核的事项数量；
14. **终点产物可用率**：终点结果无需回退重做即可进入下一业务阶段的比例。

### L2 硬门禁共性原则

出现以下任一情况，对应链路直接判定为“不通过”：

1. 任一相关 L1 零容忍门禁在链路运行中被触发；
2. 上游对象已经失效，下游仍继续使用旧结果；
3. 跨模块传递时丢失来源、版本、人工确认或权限信息；
4. 需要人工决策的步骤被 Agent 或系统自动越权完成；
5. 链路失败后删除或覆盖已经确认的事实和人工结论；
6. 错误或不完整的终点产物被标记为可进入下一业务阶段；
7. 重试、并发或重复提交产生两个互相冲突的有效结果；
8. 关键链路动作缺少审计记录，无法还原发生过程。

### L2 评估执行方式

1. 每条链路使用完整业务样本运行，不通过手工拼接中间结果绕过上游；
2. 同时测试正常路径、边界路径、失败路径、转人工路径和版本变化路径；
3. L1 已通过是进入 L2 的前置条件，但 L1 通过不代表 L2 自动通过；
4. 链路评估保留全部输入、输出、版本、状态、日志、人工操作和审计事件；
5. 每条链路分别形成评估卡，记录样本、指标、门禁、结果和失败样本；
6. 五条链路全部通过后，才能进入 L3 单项目端到端评估。

### 用户确认

用户确认将 V1 的 L2 业务链路划分为上述五条，并同意使用跨模块数据、版本传播、人工确认、异常处理、审计和终点产物可用性作为统一评估维度。

### 完成结论

- L1 回答“单个模块能不能工作”，L2 回答“多个模块连接后能不能完成真实业务任务”；
- 五条链路覆盖从项目创建、要求与依据准备、报价确认、章节审核到交接递交留痕的完整业务协作；
- 下一步从链路 1 开始，逐条确定具体测试场景、指标和质量门禁。

**讨论点完成。**

---

## 三十五、评估体系讨论 13：L2 链路 1——项目与采购文件准备链路评估

### 链路范围

```text
选择来源平台并输入项目编号
  → 匹配项目与采购包
  → 自动或人工接入公告和文件
  → 记录来源、哈希与连接器信息
  → 确认当前有效文件及覆盖关系
  → 解析正文、章节、表格和页码
  → 输出可供要求提取使用的有效解析结果
```

本链路关联 PRD `FR-101`、`FR-201`、`AC-01` 和 `AC-02`。链路通过的前提不是“连接器返回了文件”，而是最终形成了正确项目、正确采购包、正确文件版本、完整来源记录和达到质量要求的解析结果。

### 场景 1：公开平台自动获取成功

**输入条件**：

- 用户选择支持的平台并输入正确外部项目编号；
- 项目公告和采购文件允许公开访问；
- 网络和连接器工作正常。

**预期过程**：

1. 系统创建唯一内部 `project_id`；
2. 正确匹配项目和采购包；
3. 自动获取公告、采购文件和附件；
4. 记录来源链接、发布时间、获取时间、哈希和连接器版本；
5. 用户确认当前有效文件；
6. 系统完成解析并输出页码、章节、表格、原文位置和质量状态。

**通过标准**：链路无需人工补充文件即可到达可供要求提取使用的解析结果，且项目、文件、版本和来源全部正确。

### 场景 2：受限文件失败后转人工上传

**输入条件**：

- 公告可公开访问；
- 部分采购文件需要登录、验证码、CA、专用客户端或加密环境；
- 系统无法合法、稳定地自动获取受限文件。

**预期过程**：

1. 系统保留已经成功获取的公告和文件；
2. 对失败文件记录连接器、失败原因、重试状态和所需文件类型；
3. 创建链接补充或人工上传待办；
4. 用户上传文件后，系统记录操作者、时间、哈希和来源说明；
5. 用户确认上传文件与当前项目、采购包和版本的关系；
6. 链路继续完成解析，不要求重新执行已经成功的接入步骤。

**通过标准**：自动获取失败被正确降级为可执行的人工任务，用户补充后能够继续链路，且不会绕过访问控制或丢失已成功结果。

### 场景 3：多连接器部分成功、部分失败与独立重试

**输入条件**：

- 同一项目需要从公告平台、采购平台或内部资料源获取不同内容；
- 一个连接器成功，一个连接器超时或返回错误。

**预期过程**：

1. 成功连接器的结果立即保存并可追溯；
2. 失败连接器形成独立失败状态和重试任务；
3. 项目创建不因单一连接器失败而整体回滚；
4. 重试不产生重复文件、重复项目或冲突版本；
5. 失败超过限制后转人工处理；
6. 所有必需文件齐备并确认后才进入正式解析完成状态。

**通过标准**：部分失败不会删除或覆盖成功结果，重试具有幂等性，缺失必需文件时链路不会错误宣称完成。

### 场景 4：新增澄清文件后的版本与解析失效

**输入条件**：

- 项目已完成文件确认和解析；
- 官方新增澄清、更正、补遗或答疑文件；
- 新文件修改、覆盖或补充原采购文件内容。

**预期过程**：

1. 系统发现或由用户上传新文件；
2. 记录新文件来源、时间、哈希和版本；
3. 新文件进入待确认状态，暂时阻止形成新的有效交接基线；
4. 用户确认文件覆盖和生效关系；
5. 依赖旧版本的解析结果被标记为失效或待复核；
6. 系统只重新解析受影响文件和页面；
7. 新解析结果保留与原文件版本的映射；
8. 变更继续向要求、章节、风险、审核任务和基线传播。

**通过标准**：新文件不会与旧文件混为同一版本，旧解析结果不会被静默沿用，受影响范围得到正确更新。

### 场景 5：相似项目或错误项目编号的拦截

**输入条件**：

- 平台存在名称相似、编号接近、重复公告或多个采购包；
- 用户输入编号不完整、格式异常或与来源平台不匹配。

**预期过程**：

1. 系统不直接绑定低置信度候选；
2. 展示候选项目的编号、名称、采购人、地区、公告时间和采购包；
3. 要求用户确认正确项目或修正输入；
4. 无法确认时保持待处理状态，不下载并解析可能属于其他项目的文件；
5. 用户确认后记录候选选择和操作者；
6. 后续文件全部绑定已确认的内部项目和采购包。

**通过标准**：错误或不确定输入被及时拦截，其他项目文件不会进入当前项目的正式解析链路。

### 测试数据与金标准

每个链路样本需要预先准备：

1. 正确来源平台、外部项目编号、项目名称和采购包；
2. 截至评估时应获取的公告、采购文件、附件和补充文件清单；
3. 每份文件的来源、发布时间、哈希、版本和覆盖关系；
4. 允许自动获取、必须授权接入和必须人工上传的文件分类；
5. 正确的页面数、文件格式、解析结果和低质量页面；
6. 连接器成功、超时、失败、重试和转人工的预期状态；
7. 文件更新后应失效或待复核的解析结果和下游对象；
8. 相似项目候选和正确人工确认结果。

评估时必须从真实链路起点运行，不允许预先把正确文件直接放入解析模块来绕过项目匹配、数据接入和版本确认。

### 核心链路指标

1. **链路完成率**：合法输入能够形成有效解析结果的项目比例；
2. **项目—采购包匹配正确率**：内部项目绑定正确外部项目和采购包的比例；
3. **项目—文件关联正确率**：所有接入文件均属于当前项目和采购包的比例；
4. **应接入文件链路完整率**：应获取文件成功接入，或因受限形成明确人工待办的比例；
5. **有效版本传递正确率**：进入解析模块的文件均为用户确认的当前有效版本比例；
6. **来源信息保留率**：从接入到解析结果始终保留来源、时间、哈希、连接器和操作者的比例；
7. **人工兜底闭环率**：受限或失败文件从待办创建到用户补充、确认和解析完成的比例；
8. **部分失败隔离率**：单一连接器失败时其他成功结果不受影响的比例；
9. **重试幂等正确率**：重复获取或重试没有产生重复项目、重复文件或冲突版本的比例；
10. **解析前置条件合规率**：只有项目、采购包和有效文件确认完成后才进入正式解析的比例；
11. **终点解析结果可用率**：链路终点结果达到页数、文本、表格、定位和质量要求的比例；
12. **更新发现率**：生效澄清、更正、补遗和答疑被链路发现或形成待办的比例；
13. **旧解析失效正确率**：文件版本变化后依赖旧版本的解析结果被正确失效或待复核的比例；
14. **变更传播正确率**：新文件确认后相关下游对象收到正确失效或待复核状态的比例；
15. **误匹配拦截率**：错误编号、相似项目和采购包不确定场景被正确拦截的比例；
16. **链路审计完整率**：输入、匹配、下载、失败、重试、上传、确认、解析和失效动作均可追溯的比例；
17. **系统执行耗时**：项目创建到完成自动接入和解析的系统时间；
18. **人工等待耗时**：等待用户上传、确认版本或处理异常的时间；
19. **人工处理事项数**：每个项目需要用户补充、选择和确认的事项数量。

### 链路硬门禁

出现以下任一情况，本链路直接判定为“不通过”：

1. 错误项目或错误采购包的文件进入正式解析；
2. 未经用户确认的文件版本进入正式解析和后续要求提取；
3. 已生效的澄清、更正、补遗或答疑被漏掉，且系统没有告警或人工待办；
4. 新文件确认后旧解析结果仍保持有效并继续被下游使用；
5. 文件来源、版本或哈希在跨模块传递中丢失；
6. 连接器失败导致已成功获取或已人工确认的数据被删除、覆盖或回滚；
7. 受限文件获取失败后没有明确失败原因、重试状态和人工上传入口；
8. 系统绕过账号、验证码、CA、权限或平台限制自动获取受限文件；
9. 损坏、空白、截断或不可可靠解析的文件被标记为链路成功；
10. 相似项目匹配不确定时，系统未要求人工确认就自动绑定；
11. 重试或重复操作产生两个互相冲突的有效文件版本；
12. 链路缺少关键审计记录，无法证明使用了哪个项目、文件和解析版本。

### 发布阈值原则

1. 链路硬门禁事件必须为零；
2. 项目匹配、采购包匹配、有效版本和来源追溯采用高风险标准；
3. 自动接入成功和人工兜底成功分别统计，不能把正确降级算作单纯失败；
4. 各连接器、文件格式、自动路径和人工路径分别评估；
5. 新文件更新场景必须专项测试，不能只评估首次接入；
6. 系统执行耗时和人工等待耗时分开统计，便于识别真正瓶颈；
7. 链路终点只有在解析质量达到下游使用要求时才算完成。

### 失败处理规则

1. 项目或采购包匹配不确定时暂停链路并要求用户确认；
2. 必需文件缺失时保持待补充状态，不允许进入链路完成状态；
3. 连接器失败时独立重试，超过限制后转人工，不回滚成功结果；
4. 用户上传文件后执行项目归属、格式、哈希和版本确认；
5. 解析质量不足时定位具体文件和页面并创建重新上传或人工复核任务；
6. 新文件确认后立即使旧解析结果和受影响下游结果失效或待复核；
7. 发现误匹配时隔离错误文件，重新执行项目绑定，并使所有受污染下游结果失效；
8. 连接器、匹配规则、版本逻辑或解析器变化后，重新运行固定链路回归集。

### 用户确认

用户确认 L2 链路 1 按照公开自动接入、受限文件人工兜底、多连接器部分失败、新文件版本更新和相似项目误匹配五类场景进行评估，并确认上述指标和硬门禁方向。

### 完成结论

- 链路 1 的成功标准不是“下载到了文件”，而是形成正确项目、完整文件、有效版本、可靠解析和完整来源链；
- 自动接入失败后能够安全转人工并继续流程属于正确结果，错误项目或旧文件进入下游属于阻断问题；
- 下一步讨论 L2 链路 2：要求提取、企业证据、历史参考和响应矩阵。

**讨论点完成。**

---

## 三十六、评估体系讨论 14：L2 链路 2——要求、证据、历史参考与响应矩阵链路评估

### 链路范围

```text
已确认并完成解析的采购文件
  → 提取资格、实质性、评分、时间、附件和格式要求
  → 检索企业证据并校验主体、有效期、范围和权限
  → 推荐同类历史项目并说明差异与复用边界
  → 用户确认企业证据、参考池和章节用途
  → 建立要求—评分项—章节—证据—责任人响应矩阵
  → 输出已覆盖、部分覆盖、未映射、待确认和阻断状态
```

本链路关联 PRD `FR-301`、`FR-401`、`FR-402` 和 `AC-03`。链路成功不等于“每条要求都找到了材料”，而是每条要求都有真实、可追溯的支撑状态：有证据就正确绑定，无证据就明确拒答，有历史参考就限定用途，没有参考就说明不足。

### 场景 1：要求具有完整有效证据并正确建立响应关系

**输入条件**：

- 采购文件已经确认并完成高质量解析；
- 资格条件、评分项、技术或商务要求能够明确提取；
- 企业证据库中存在主体正确、有效期满足、适用范围匹配且权限允许的证据；
- 团队已经定义目标章节和责任角色。

**预期过程**：

1. 系统提取原子要求并保留原文、页码、版本、类型和后果；
2. 检索候选证据并校验主体、有效期、适用范围、完整性和权限；
3. 返回证据原文、页码、版本和匹配理由；
4. 用户确认使用的企业证据；
5. 系统将要求关联到评分项、目标章节、确认企业证据和责任人；
6. 响应矩阵状态根据真实覆盖情况标记为已覆盖或待编制；
7. 所有关系保留来源、版本、确认人和确认时间。

**通过标准**：要求、证据、章节和责任人之间的关系正确，证据确实能够支持要求，且未经确认的候选证据不会进入正式生成。

### 场景 2：证据缺失或只能部分支持

**输入条件**：

- 采购文件存在明确资格、评分或技术要求；
- 企业证据库中没有有效证据，或者现有材料只能支持其中部分条件。

**预期过程**：

1. 系统仍然保留并展示完整要求；
2. 检索结果区分无证据、证据过期、主体不符、权限不足、材料不完整和部分支持；
3. 系统不输出企业完全具备该能力的肯定结论；
4. 对无证据情况输出“证据不足”；
5. 对部分支持情况展示已支持部分、缺失部分和风险；
6. 创建资料补充任务，说明所需材料类型、责任人和截止时间；
7. 响应矩阵保持未覆盖、部分覆盖、待确认或阻断状态；
8. 后续补充证据并确认后，矩阵状态才允许更新。

**通过标准**：链路能够正确表达证据缺口并转人工补充，不会为了完成矩阵而编造证据或错误标记已覆盖。

### 场景 3：推荐多个历史项目并确认参考池与章节用途

**输入条件**：

- 历史项目库中存在多个具有不同相似维度的候选项目；
- 有的适合参考技术方案，有的适合参考结构、案例、评分响应或风险；
- 用户具有访问相应历史资料的权限。

**预期过程**：

1. 系统按采购内容、预算、行业、技术路线、评分结构、资格、周期和地域推荐候选；
2. 每个候选项目展示相似点、差异、资料来源和权限；
3. 系统说明可复用、需改写、仅参考结构和禁止复用内容；
4. 用户选择参考项目并分别确认技术、结构、资格、评分、案例、报价或风险用途；
5. 系统生成参考池版本和章节用途绑定；
6. 响应矩阵只记录已确认且与当前要求相关的参考关系；
7. 后续章节生成只能使用对应用途允许的历史内容。

**通过标准**：系统既能推荐相关项目，也能保留差异和使用边界；用户确认前，历史内容不会进入正式章节生成。

### 场景 4：没有合适历史项目

**输入条件**：

- 历史项目与当前采购内容、范围、资格、评分或技术路线差异较大；
- 公开中标信息只能提供有限项目事实；
- 没有足够材料支持可靠复用建议。

**预期过程**：

1. 系统说明没有足够相似或可授权使用的历史项目；
2. 不为了填满参考池而推荐误导性项目；
3. 不从有限公开公告编造完整技术方案、评分细节或报价构成；
4. 用户可以选择人工补充参考项目，或者确认本项目无历史参考；
5. 响应矩阵保留“无历史参考”状态，但不影响使用当前采购文件和企业证据继续编制；
6. 后续章节生成不虚构历史依据。

**通过标准**：正确说明参考不足，既不阻塞可继续的业务，也不使用不可靠项目冒充有效参考。

### 场景 5：要求、证据或参考池版本变化后的矩阵失效

**输入条件**：

- 已建立要求、证据、参考池和章节响应关系；
- 新澄清文件修改了要求，企业证据到期或被替换，或者用户调整参考池和章节用途。

**预期过程**：

1. 系统识别发生变化的要求、证据或参考绑定；
2. 依赖旧版本的响应矩阵关系进入失效或待复核状态；
3. 已覆盖状态不能静默保留；
4. 新要求重新执行证据检索和参考匹配；
5. 证据失效后相关章节和风险转入待复核；
6. 参考池用途变化后，超出新授权范围的章节引用失效；
7. 只重新处理受影响关系，不删除其他已确认结果；
8. 用户重新确认后形成新的矩阵和参考池版本。

**通过标准**：上游版本变化能够准确传播到矩阵和下游章节，旧覆盖结论和旧参考授权不会被继续使用。

### 测试数据与金标准

每个链路样本需要准备：

1. 已确认采购文件及完整原子要求清单；
2. 要求类型、风险等级、后果、原文、页码和版本；
3. 有效证据、无效证据、部分支持证据和无证据情况；
4. 证据主体、有效期、适用范围、完整性、保密等级和权限；
5. 核心参考项目、辅助参考项目、不应推荐项目和无参考情况；
6. 历史项目的相似维度、关键差异、可复用边界和允许用途；
7. 正确的目标章节、责任人和矩阵状态；
8. 用户应确认、拒绝或补充的证据和参考选择；
9. 版本变化后应失效或待复核的矩阵关系和章节；
10. 每个链路步骤的输入、输出、确认人和审计事件。

评估必须从已解析采购文件开始运行完整链路，不允许直接向响应矩阵写入人工准备好的最终关系来绕过要求提取、证据检索、参考推荐和用户确认。

### 核心链路指标

1. **链路完成率**：要求能够形成明确响应矩阵状态的比例；
2. **要求传递完整率**：提取出的已确认要求全部进入证据、参考和矩阵处理的比例；
3. **高风险要求支撑状态完整率**：资格、实质性要求、否决项和评分项均具有有效证据、部分支持、无证据或待确认状态的比例；
4. **证据绑定正确率**：要求绑定的确认企业证据主体、有效期、范围、权限和证明内容正确的比例；
5. **证据支撑完整率**：标记为已满足的要求确实具有足够有效证据支持的比例；
6. **无证据拒答正确率**：无有效证据时系统正确输出证据不足的比例；
7. **部分支持识别率**：证据只能支持部分条件时，系统正确展示支持边界和缺口的比例；
8. **资料补充任务完整率**：证据缺失时正确创建材料类型、责任人和截止时间明确的任务比例；
9. **历史参考推荐有效率**：推荐候选中具有真实参考价值的项目比例；
10. **历史参考差异完整率**：影响复用的采购范围、资格、评分、报价和技术差异被正确展示的比例；
11. **章节用途绑定正确率**：参考内容只绑定到用户确认的章节和用途比例；
12. **无参考正确说明率**：没有合适历史项目时系统正确说明不足的比例；
13. **响应矩阵覆盖状态正确率**：已覆盖、部分覆盖、未映射、待确认和阻断状态与真实情况一致的比例；
14. **响应矩阵映射正确率**：要求、评分项、章节、证据、参考和责任人关系正确的比例；
15. **未满足项可见率**：所有证据缺口、未映射、待确认和阻断要求在矩阵中清晰可见的比例；
16. **人工确认合规率**：进入正式生成的证据和历史参考均经过正确角色确认的比例；
17. **来源与版本保留率**：要求、证据、参考和矩阵关系能够追溯到对应来源和版本的比例；
18. **版本传播正确率**：要求、证据或参考池变化后受影响矩阵关系被正确失效或待复核的比例；
19. **错误失效率**：未受影响矩阵关系被错误重开或删除的比例；
20. **终点产物可用率**：矩阵无需回退重做即可安全进入章节生成的比例；
21. **链路审计完整率**：提取、检索、推荐、确认、绑定、失效和重新确认动作均有记录的比例；
22. **人工处理事项数**：每个项目需要确认证据、参考池、用途和缺口的事项数量；
23. **链路耗时**：从已解析文件到形成可用响应矩阵所需系统时间和人工等待时间。

### 链路硬门禁

出现以下任一情况，本链路直接判定为“不通过”：

1. 没有有效证据或只有部分证据的要求被标记为完全满足或已覆盖；
2. 使用主体不符、已过期、适用范围不符、材料不完整或权限不允许的证据；
3. 无证据时系统输出企业具备相应资质、人员、案例或能力的肯定结论；
4. 企业证据引用不能支持要求，却仍被当作有效支撑；
5. 将历史项目中的要求、评分、资格、报价或合同条款当作当前项目有效要求；
6. 使用无权访问的第三方标书、客户机密或其他组织内部资料；
7. 未经用户确认的企业证据、参考项目或章节用途进入正式章节生成；
8. 参考项目只允许用于结构或风险分析，系统却将其事实、案例、报价或技术内容用于其他用途；
9. 没有合适参考项目时，系统强行推荐误导性项目或编造历史方案；
10. 要求、证据或参考池版本变化后，旧响应矩阵仍标记为已覆盖；
11. 响应矩阵标记已覆盖，但缺少目标章节、有效证据或责任人；
12. 跨模块传递时丢失要求原文、证据来源、参考版本、确认状态或权限信息；
13. 用户拒绝、移除或限制的证据和参考绑定被系统静默恢复；
14. 链路失败后删除其他已确认要求、证据或参考池结果；
15. 缺少关键确认和审计记录，无法证明矩阵关系如何形成。

### 发布阈值原则

1. 链路硬门禁事件必须为零；
2. 资格条件、实质性要求、否决项和评分项单独统计，优先保证支撑状态完整性；
3. “无证据”“部分支持”和“无历史参考”都是合法终点状态，不能为了提高完成率错误标记已覆盖；
4. 证据检索指标与证据业务有效性分别统计；
5. 历史参考的推荐准确性与用途授权合规性分别统计；
6. 响应矩阵只有在来源、版本、确认和权限完整时才算可用；
7. 上游版本变化场景必须专项测试，确保旧关系不会静默沿用；
8. 系统时间和人工确认等待时间分开统计。

### 失败处理规则

1. 要求提取或分类不确定时转人工确认，不进入已确认矩阵；
2. 证据无效、无权限或不足时保持未覆盖或部分覆盖，并创建补充任务；
3. 参考项目相似性或使用边界不明确时只展示候选，不自动加入确认参考池；
4. 没有参考项目时允许用户确认“无历史参考”并继续使用当前要求和企业证据；
5. 矩阵关系缺少章节、证据、责任人或来源时禁止标记已覆盖；
6. 要求、证据或参考版本变化后重新执行受影响检索、绑定和确认；
7. 发现错误证据或历史事实污染时，使相关矩阵、章节、风险、审核任务和基线失效；
8. 检索、推荐、权限、绑定或版本传播逻辑变化后，重新运行固定链路回归集。

### 用户确认

用户确认 L2 链路 2 按照完整有效证据、证据缺失或部分支持、多历史项目确认、无合适历史参考和版本变化五类场景进行评估，并确认上述指标和硬门禁方向。

### 完成结论

- 链路 2 的目标不是把所有要求都显示成已覆盖，而是让每条要求的证据、参考、责任和缺口状态真实可见；
- 没有证据时拒答、证据不足时展示缺口、没有历史参考时明确说明，都属于正确业务结果；
- 下一步讨论 L2 链路 3：报价测算、规则校验、人工确认与章节引用。

**讨论点完成。**

---

## 三十七、评估体系讨论 15：L2 链路 3——报价测算、规则校验、人工确认与章节引用链路评估

### 链路范围

```text
已确认项目预算、最高限价和商务数据
  → 选择受控 Excel 模板或内部报价模型
  → 商务人员填写报价假设并执行实际计算
  → 系统读取候选结果并执行确定性规则校验
  → 排除无效方案
  → 大模型比较有效方案并说明理由与风险
  → 商务人员选择方案
  → 项目负责人确认并完成必要授权
  → 冻结确认报价版本
  → 正文、报价表、图表和交接基线引用同一版本
```

本链路关联 PRD `FR-501`、`PRD-BR-05` 和 `AC-04`。链路成功的核心不是“系统给出了一个推荐价格”，而是报价由受控模型真实计算、无效方案被规则排除、最终选择由授权人员确认，并且冻结版本在所有下游内容中保持一致。

### 场景 1：多个有效报价方案完成比较、选择和确认

**输入条件**：

- 项目预算、最高限价和商务口径已经确认；
- 管理员已经发布适用且审批有效的报价模板；
- 商务人员准备多组成本、税率、利润率、风险系数和优惠假设；
- Excel 或内部模型计算出多个规则有效的候选方案。

**预期过程**：

1. 系统记录模板版本、哈希、输入输出映射和公式校验值；
2. 每组报价假设生成独立版本；
3. 系统准确读取候选金额、成本、利润和其他输出；
4. 所有候选通过预算、限价、税率、合计和内部硬规则校验；
5. 大模型只比较有效候选，说明差异、推荐理由和风险；
6. 商务人员接受、修改选择或自行选择方案；
7. 项目负责人确认最终方案；
8. 系统冻结确认报价版本；
9. 正文、报价表和图表使用同一冻结版本。

**通过标准**：候选数据与 Excel 或内部模型一致，大模型没有重新计算正式价格，人工选择顺序完整，下游引用版本一致。

### 场景 2：超过最高限价或违反规则的方案被拦截

**输入条件**：

- 候选方案超过采购最高限价、预算或内部最低利润要求；
- 或存在税率、合计、大小写金额、单位和口径错误。

**预期过程**：

1. 系统读取真实计算结果；
2. 确定性规则识别具体无效原因；
3. 方案状态转为 `INVALID`；
4. 无效方案不进入大模型推荐候选；
5. 用户可以查看失败规则和原始计算结果；
6. 商务人员修改假设并由 Excel 或内部模型重新计算；
7. 新结果形成新版本，旧无效方案保留历史但不能写入章节。

**通过标准**：所有违反硬约束的方案均被阻断，用户能够理解无效原因，修正过程不会覆盖历史假设和结果。

### 场景 3：激进报价需要额外授权

**输入条件**：

- 方案未违反采购最高限价等外部硬规则；
- 但利润率、风险系数、优惠幅度或履约风险触发内部激进策略规则；
- 组织要求特定授权人员审批。

**预期过程**：

1. 系统将方案标记为需要额外授权；
2. 展示触发规则、关键假设、风险和影响；
3. 大模型可以解释风险，但不能自动批准；
4. 商务人员仍可选择该方案；
5. 项目负责人确认后，系统创建额外授权任务；
6. 授权人员批准或拒绝并记录理由、范围和版本；
7. 只有有效授权完成后，方案才能冻结为最终报价；
8. 授权被拒绝或过期时，方案保持不可进入正式章节状态。

**通过标准**：激进方案不会绕过组织授权；授权决定与具体报价版本绑定，报价变化后旧授权不能自动沿用。

### 场景 4：商务已选择但项目负责人未确认

**输入条件**：

- 商务人员已经从有效候选中选择一个方案；
- 项目负责人尚未确认，或者确认流程被退回、撤销或超时。

**预期过程**：

1. 方案状态进入 `SELECTED`，但不进入 `CONFIRMED`；
2. 系统可以在内部页面展示候选金额和待确认状态；
3. 正文、报价表、图表和交接基线不得写入该方案作为正式报价；
4. 需要报价的章节显示待确认占位或阻断状态；
5. 项目负责人确认后才形成冻结报价版本；
6. 用户撤销选择时，系统保留历史事件并返回候选状态。

**通过标准**：系统严格区分“商务已选择”和“负责人已确认”，未完成确认的报价不会通过跨模块链路进入正式内容。

### 场景 5：报价变化后旧章节和交接基线失效

**输入条件**：

- 已有确认报价被正文、报价表、图表、审核结论或交接基线引用；
- 后续预算、最高限价、模板、假设或候选选择发生变化；
- 新报价版本完成重新计算和确认。

**预期过程**：

1. 系统识别报价输入、模板或确认版本变化；
2. 旧报价状态转为 `STALE` 或保留为历史确认版本但不再用于当前基线；
3. 依赖旧报价的正文、报价表、图表、风险和审核结论进入失效或待复核；
4. 旧 `HANDOVER_BASELINE` 转为 `INVALIDATED`；
5. 系统只更新受影响章节和表格；
6. 对新报价重新执行跨章节一致性检查；
7. 相关审核角色完成增量复核后，才能形成新交接基线；
8. 旧链接和旧文件标记为禁止递交。

**通过标准**：报价版本变化能够完整传播到所有依赖对象，旧金额不会与新金额混用，旧交接包不会保持有效。

### 测试数据与金标准

每个链路样本需要准备：

1. 已确认预算、最高限价、税率和商务数据来源；
2. 正确的报价模板版本、哈希、映射和公式校验值；
3. 多组报价假设及 Excel 或内部模型真实计算结果；
4. 有效方案、无效方案和具体规则失败原因；
5. 各有效方案可客观比较的金额、利润、风险和策略差异；
6. 商务人员选择、项目负责人确认和额外授权的预期结果；
7. 正确的报价方案状态转换；
8. 正文、报价表、图表和交接基线应引用的冻结版本；
9. 报价变化后应失效或待复核的下游对象；
10. 每次计算、校验、推荐、选择、确认、授权、冻结和失效的审计事件。

评估必须使用真实 Excel 模板、内部模型或授权报价系统输出，不允许用人工预先填写最终金额来绕过模板、假设、计算读取和规则校验。

### 核心链路指标

1. **链路完成率**：合法报价输入能够形成冻结确认版本并正确进入章节的比例；
2. **模板与项目匹配正确率**：使用模板适用范围与当前项目一致的比例；
3. **输入传递正确率**：项目商务数据和人工假设正确写入报价模型的比例；
4. **计算结果传递正确率**：系统读取结果与 Excel、内部模型或授权系统一致的比例；
5. **规则校验完整率**：预算、限价、税率、合计、大小写和内部硬规则全部执行的比例；
6. **无效方案拦截率**：违反规则的候选被阻止进入推荐和确认的比例；
7. **有效方案保留率**：规则有效方案没有被错误排除的比例；
8. **推荐候选合规率**：大模型比较的所有方案均为规则有效候选的比例；
9. **推荐解释一致率**：推荐理由、金额、利润和风险与候选数据一致的比例；
10. **人工确认顺序合规率**：商务选择、项目负责人确认和必要额外授权按正确顺序完成的比例；
11. **激进策略授权完整率**：所有触发额外授权的方案均完成有效审批的比例；
12. **冻结版本传递正确率**：下游只接收到当前冻结确认报价版本的比例；
13. **未确认报价拦截率**：`DRAFT`、`CANDIDATE`、`SELECTED`、`INVALID` 或 `STALE` 方案被阻止写入正式内容的比例；
14. **跨章节报价一致率**：正文、报价表、图表和摘要中的报价数据一致的比例；
15. **来源与版本追溯率**：章节报价能够反向定位模板、假设、计算结果、选择、确认和授权的比例；
16. **报价变更传播正确率**：报价变化后所有依赖对象正确失效或待复核的比例；
17. **旧基线失效率**：报价更新后旧交接基线正确转为 `INVALIDATED` 的比例；
18. **增量更新准确率**：仅受报价影响的章节、表格、图表和审核任务被重开的比例；
19. **链路审计完整率**：计算、校验、推荐、选择、确认、授权、冻结和失效动作均有记录的比例；
20. **人工处理事项数**：每个项目需要商务选择、负责人确认和额外授权的事项数量；
21. **链路耗时**：从报价假设准备到冻结确认版本进入章节的系统时间和人工等待时间。

### 链路硬门禁

出现以下任一情况，本链路直接判定为“不通过”：

1. 大模型代替 Excel、内部模型或授权系统计算、修改或决定正式最终报价；
2. 超过最高限价、税率错误、合计错误或违反内部硬规则的方案进入大模型推荐；
3. 无效报价方案被商务人员选择、项目负责人确认或冻结；
4. 未经商务人员选择或项目负责人确认的报价写入正文、报价表、图表或交接基线；
5. 激进报价缺少有效额外授权却被标记为最终确认；
6. 非授权人员选择、确认、授权或冻结最终报价；
7. 正文、报价表、图表或摘要引用不同报价版本或金额；
8. 章节引用待审批、已停用、哈希异常或失效模板产生的结果；
9. 报价更新后旧章节、旧审核结论或旧交接基线仍保持有效；
10. 大模型推荐解释编造成本、利润、竞争情况或候选数据中不存在的事实；
11. 用户报价假设、最终选择、确认或授权结果被系统静默覆盖；
12. 跨模块传递时丢失模板、假设、计算结果、选择、确认或授权版本；
13. 报价链路失败后删除已有人工确认和历史报价记录；
14. 旧报价文件和旧交接包没有明显禁止递交标识；
15. 缺少关键审计事件，无法证明最终报价如何形成。

### 发布阈值原则

1. 链路硬门禁事件必须为零；
2. 金额、限价、税率、合计、状态和权限使用确定性规则进行验证；
3. 无效方案拦截和有效方案保留分别统计；
4. 推荐解释质量不能替代报价计算正确性和人工确认完整性；
5. 商务选择、负责人确认和额外授权是独立前置条件，不能被合并成一个笼统“已确认”状态；
6. 各模板和报价口径分别评估；
7. 报价变更路径必须专项测试，确保旧金额不会残留在任何下游对象；
8. 系统时间和人工审批等待时间分开统计。

### 失败处理规则

1. 模板、映射、公式或输入来源异常时停止链路并转管理员或商务人员处理；
2. 无效方案明确展示失败规则并禁止推荐；
3. 有效方案无法可靠推荐时仅展示客观差异，由商务人员选择；
4. 商务已选择但负责人未确认时保持 `SELECTED`，所有正式章节显示待确认状态；
5. 激进策略授权失败、拒绝或过期时禁止冻结该方案；
6. 报价更新后立即使旧引用、审核结论和交接基线失效；
7. 发现跨章节金额不一致时阻止生成有效交接基线并重新同步冻结版本；
8. 模板、规则、映射、报价状态机或确认流程变化后，重新运行固定链路回归集。

### 用户确认

用户确认 L2 链路 3 按照多有效方案比较、无效方案拦截、激进报价授权、未确认报价禁止写入和报价变化传播五类场景进行评估，并确认上述指标和硬门禁方向。

### 完成结论

- 链路 3 的核心是“受控模型计算、规则先行过滤、人工最终确认、冻结版本统一引用”；
- 大模型可以解释有效报价方案，但不能计算最终价格、绕过规则或代替授权人员作出决定；
- 下一步讨论 L2 链路 4：章节生成、Agent 预检、人工审核与结构化返工。

**讨论点完成。**

---

## 三十八、评估体系讨论 16：L2 链路 4——章节生成、Agent 预检、人工审核与结构化返工链路评估

### 链路范围

```text
已确认要求、证据、参考池、报价和团队模板
  → 按章节生成带来源初稿、图表、说明材料和内部风险
  → Agent 执行资格、评分、一致性、附件、报价和风险预检
  → 项目负责人、商务人员和合规/法务人员按职责审核
  → 审核意见形成结构化问题任务
  → Agent 生成修改建议
  → 标书编写人接受、修改或拒绝建议
  → 只重生成受影响内容
  → 原审核角色查看差异并复核关闭
  → 形成满足交接前置条件的审核版本
```

本链路关联 PRD `FR-601`、`FR-701`、`FR-702` 和 `AC-05`。链路通过的标准不是“生成了一份文档”，而是要求和来源正确传入章节、预检能提前发现问题、审核任务分配给正确角色、返工由编写人确认，并由原审核角色完成闭环。

### 场景 1：初稿生成后通过预检并按角色完成审核

**输入条件**：

- 要求、评分项、证据、历史参考、报价和模板已经确认；
- 不存在已知阻断风险；
- 各章节责任人和审核角色已配置。

**预期过程**：

1. 系统按章节生成正文、图表、说明材料和内部风险清单；
2. 每个章节保留要求、评分项、证据、参考、报价和模板版本；
3. Agent 执行资格、评分覆盖、一致性、附件、报价引用和风险预检；
4. 预检通过项和观察项保留证据及结果；
5. 项目负责人审核整体方案和响应完整性；
6. 商务人员只审核报价、商务承诺和相关内容；
7. 合规/法务人员只审核资格、实质性要求、承诺和法律风险；
8. 无关角色不重复审核全部内容；
9. 必要审核完成后，章节进入可形成审核基线的状态。

**通过标准**：初稿来源完整、预检结果可追溯、审核顺序和职责正确，所有必要审核完成且没有跳过人工确认。

### 场景 2：植入阻断问题并生成结构化任务

**输入条件**：

- 初稿中植入资格材料缺失、否决项遗漏、错误报价、无来源结论、附件缺失或跨章节冲突；
- 问题已由业务人员标注为 `BLOCKING` 或 `HIGH`。

**预期过程**：

1. Agent 在人工审核前发现问题；
2. 问题绑定正确采购文件、证据、报价、章节或差异；
3. 风险等级和问题类型正确；
4. 系统生成结构化问题任务；
5. 任务包含提出人、责任人、风险等级、证据、修改要求、截止时间和影响范围；
6. 问题被路由给正确编写人和审核角色；
7. 阻断问题保持未解决状态并阻止后续交接；
8. 人工审核能够查看问题证据和 Agent 判断依据。

**通过标准**：阻断问题不漏报、不错误降级，结构化任务信息完整，正确责任人能够直接开始处理。

### 场景 3：Agent 修改建议由编写人接受、修改或拒绝

**输入条件**：

- 审核问题已经创建；
- Agent 能够基于原文、证据和模板生成修改建议；
- 标书编写人对建议具有确认权限。

**预期过程**：

1. Agent 展示原内容、问题、证据、修改建议和预期影响；
2. 建议不直接写入正式章节；
3. 编写人可以接受、编辑后接受或拒绝建议；
4. 接受后只更新授权范围内的内容；
5. 拒绝后保留原内容和拒绝记录，不自动恢复建议；
6. 修改形成新章节版本并保留差异；
7. 问题状态进入待复核，而不是由编写人直接关闭；
8. 修改不得引入新的事实、报价、引用或跨章节冲突。

**通过标准**：Agent 始终停留在建议角色，正式修改由编写人确认，接受、修改和拒绝三种路径均正确留痕。

### 场景 4：局部修改后只重新运行和复核受影响内容

**输入条件**：

- 某个章节中的案例、证据、人员、参数或报价引用发生局部修改；
- 其他章节和人工编辑内容不受影响。

**预期过程**：

1. 系统识别修改对象和依赖关系；
2. 只重新运行相关章节生成、引用检查、一致性检查和风险预检；
3. 只重开受影响审核任务；
4. 未受影响章节、人工编辑、审核结论和确认状态保持不变；
5. 如果共享字段影响多个章节，所有受影响章节均进入待复核；
6. 审核人员能够查看新旧差异和影响范围；
7. 增量复核完成后形成新审核版本；
8. 系统记录未重跑内容及其未受影响依据。

**通过标准**：增量复核不漏掉真实依赖，也不退化为无差别全量重审，不覆盖无关人工内容。

### 场景 5：风险解决或授权豁免后由原审核角色关闭

**输入条件**：

- 存在 `HIGH` 或 `BLOCKING` 风险；
- 编写人已修改内容，或者授权人员决定接受风险并申请豁免；
- 原审核角色可执行复核。

**预期过程**：

1. 修改后问题进入待复核状态；
2. 原审核角色查看修改差异、证据和影响范围；
3. 问题已修复时，由原审核角色关闭；
4. 编写人不能关闭自己的问题；
5. 风险需要豁免时，由授权人员记录理由、范围、版本和有效期；
6. 无效、缺失或越权豁免不能关闭阻断风险；
7. 文件、报价或章节版本再次变化时，已关闭问题按影响关系重新打开；
8. 只有阻断风险全部解决或有效豁免后，审核版本才具备进入交接链路的条件。

**通过标准**：问题关闭和风险接受由正确角色执行，所有决定与具体版本绑定，阻断风险不会因错误状态转换被绕过。

### 测试数据与金标准

每个链路样本需要准备：

1. 已确认要求、证据、参考池、报价和模板版本；
2. 应生成章节、图表、说明材料和内部风险；
3. 人工准备的可接受初稿检查表；
4. 植入的资格、评分、引用、一致性、附件、报价和风险问题；
5. 每个问题的风险等级、证据、责任人和审核角色；
6. 可接受、需修改和不可接受的 Agent 修改建议；
7. 正确的影响范围和应重跑、应复核对象；
8. 正确的问题状态变化、关闭条件和豁免条件；
9. 各角色允许执行的确认、修改、审核和关闭动作；
10. 全链路预期版本、差异和审计事件。

评估必须从生成章节开始运行完整链路，不允许预先把问题任务和最终审核状态直接写入系统来绕过生成、预检、人工确认和增量复核。

### 核心链路指标

1. **链路完成率**：合法输入能够形成满足交接前置条件的审核版本比例；
2. **要求到章节传递完整率**：响应矩阵中的要求和评分项正确进入目标章节的比例；
3. **来源到章节传递正确率**：证据、参考、报价和模板版本在生成与审核过程中保持正确的比例；
4. **初稿结构与格式通过率**：章节、图表和说明材料符合模板要求的比例；
5. **预检问题召回率**：植入问题被 Agent 预检发现的比例；
6. **阻断问题召回率**：`BLOCKING` 问题被正确发现并阻断的比例；
7. **预检误报率**：正确内容被错误标记为问题的比例；
8. **问题证据正确率**：问题绑定证据能够支持问题结论的比例；
9. **审核角色路由准确率**：问题被分配给正确责任人和审核角色的比例；
10. **结构化问题完整率**：问题字段、证据、影响范围和截止时间完整的比例；
11. **修改建议正确率**：建议能够修复问题且不引入新问题的比例；
12. **修改建议可采纳率**：编写人接受或编辑后接受建议的比例；
13. **人工确认合规率**：正式章节修改均经过编写人确认的比例；
14. **拒绝建议保留率**：用户拒绝的建议未被自动恢复或写入的比例；
15. **受影响范围识别率**：实际受影响章节、证据、报价、附件和任务被完整识别的比例；
16. **增量复核准确率**：应重跑内容全部重跑、无关内容保持不变的比例；
17. **遗漏复核率**：受影响内容没有被重新检查的比例；
18. **无效重复审核率**：未受影响内容被不必要重开的比例；
19. **人工编辑保留率**：无关人工编辑和确认内容未被覆盖的比例；
20. **问题关闭合规率**：问题由原审核角色或授权复核角色在满足条件后关闭的比例；
21. **风险豁免合规率**：豁免授权、理由、范围和版本完整正确的比例；
22. **阻断交接拦截率**：未解决或无效豁免的阻断风险被阻止进入交接链路的比例；
23. **版本变化重开正确率**：相关版本变化后已关闭问题被正确重新打开的比例；
24. **链路审计完整率**：生成、预检、分派、修改、确认、复核、关闭和豁免均有记录的比例；
25. **审核闭环耗时**：从初稿生成到必要问题全部关闭的系统时间和人工处理时间；
26. **审核退回轮次**：章节和问题反复退回修改的次数；
27. **人工处理事项数**：各角色需要处理的预检、确认、修改和审核任务数量；
28. **终点审核版本可用率**：审核版本无需回退重做即可进入交接链路的比例。

### 链路硬门禁

出现以下任一情况，本链路直接判定为“不通过”：

1. 资格、否决项、报价、附件或其他 `BLOCKING` 问题未被发现且未转人工；
2. Agent 未经标书编写人确认直接修改正式章节；
3. 编写人能够关闭自己的审核问题；
4. 非原审核角色或无权限人员关闭高风险、阻断问题或批准豁免；
5. 未解决且未有效豁免的阻断风险被标记为审核通过或可进入交接；
6. Agent 修改建议引入新的企业事实、错误报价、错误引用或跨章节冲突，却未被发现；
7. 用户已经拒绝的建议被系统自动写入或恢复；
8. 局部返工静默覆盖无关章节、人工编辑、报价选择、证据确认或审核结论；
9. 修改影响多个对象，系统遗漏必要的重跑或复核；
10. 文件、证据、报价或章节版本变化后，旧审核结论继续有效；
11. 内部风险、豁免理由、敏感证据或内部备注泄露到外部版本；
12. 审核角色路由错误导致必要负责人、商务或法务审核被跳过；
13. Supervisor Agent 直接改变项目主状态、审核通过状态或风险接受结果；
14. 置信度不足、证据缺失或规则冲突时，系统未转人工就输出确定性通过结论；
15. 缺少生成、修改、审核、关闭或豁免的关键审计记录。

### 发布阈值原则

1. 链路硬门禁事件必须为零；
2. 阻断和高风险问题单独统计，不能被低风险高准确率稀释；
3. 预检召回与误报、修改建议采纳与正确性同时评估；
4. 角色权限、关闭条件、风险豁免和审计记录使用确定性规则全量测试；
5. 增量复核同时控制漏复核和无效全量重审；
6. 人工确认、审核通过和风险接受不能由 Agent 自动化率替代；
7. 正确转人工、拒绝不可靠建议或扩大复核范围属于合格降级；
8. 系统执行时间和各角色人工处理时间分开统计。

### 失败处理规则

1. 阻断问题漏报时停止发布，修复预检规则、模型或证据链并重新回归；
2. 修改建议不可靠时仅展示问题和证据，由编写人自行修改；
3. 影响范围不确定时扩大待复核范围，不允许直接沿用旧审核结论；
4. 角色、权限、关闭条件或豁免信息不完整时拒绝状态转换；
5. Agent 任务失败时独立重试，超过限制后转人工并保留已有结果；
6. 人工编辑被错误覆盖时恢复人工版本，使受影响生成和审核结果失效；
7. 文件、证据、报价或章节版本变化后重新运行影响分析并重开相关问题；
8. 生成、预检、提示词、审核规则、角色权限或状态机变化后，重新运行固定链路回归集。

### 用户确认

用户确认 L2 链路 4 按照正常生成与审核、植入阻断问题、修改建议人工确认、局部增量复核和风险解决或授权豁免五类场景进行评估，并确认上述指标和硬门禁方向。

### 完成结论

- 链路 4 的核心是“系统提前发现问题并辅助修改，授权人员完成判断、确认和关闭”；
- 增量返工必须既不遗漏受影响内容，也不覆盖无关人工工作，才能真正减少审核人员工作量；
- 下一步讨论 L2 链路 5：内容变更、影响分析、增量复核、交接基线与递交记录。

**讨论点完成。**

---

## 三十九、评估体系讨论 17：L2 链路 5——内容变更、影响分析、交接基线与递交记录链路评估

### 链路范围

```text
采购文件、证据、报价、章节、附件、风险或审批发生变化
  → 识别受影响对象
  → 旧结果失效或待复核
  → 重新运行受影响任务
  → 对应角色完成增量复核
  → 检查审核、报价、附件和风险前置条件
  → 生成并冻结 HANDOVER_BASELINE
  → 生成文件清单、版本和哈希
  → 受控交接给投标递交责任人
  → 递交责任人确认最新版本
  → 人工完成外部递交
  → 记录真实状态、失败原因和回执
```

本链路关联 PRD `FR-801`、`PRD-BR-02`、`PRD-BR-07`、`PRD-BR-08`、`PRD-BR-10`、`PRD-BR-12` 和 `AC-06`。链路通过的标准不是“生成了一个压缩包”，而是变更能够正确传播、旧版本真正失效、必要复核全部完成、递交责任人拿到最新文件，并且外部人工递交结果被真实记录。

### 场景 1：审核完成后正常生成并受控交接最新基线

**输入条件**：

- 当前采购文件版本已经确认；
- 正文、报价、附件和证据均为当前有效版本；
- 所有必要审核已经通过；
- 所有 `BLOCKING` 风险已经解决或获得有效授权豁免；
- 项目负责人和投标递交责任人已经明确。

**预期过程**：

1. 系统检查采购文件、章节、报价、附件、证据、风险和审批前置条件；
2. 项目负责人生成 `HANDOVER_BASELINE`；
3. 系统冻结所有对象版本；
4. 生成交接文件清单、基线版本号、文件哈希和内部标识；
5. 设置接收人、有效期、下载次数、水印和禁止转发策略；
6. 生成受控下载链接；
7. 交接状态进入 `PENDING_RECEIPT`；
8. 所有操作形成审计事件。

**通过标准**：交接包内容、清单、版本和哈希一致，只有授权项目负责人能够生成，且不存在未满足前置条件。

### 场景 2：交接后发生关键变更并使旧基线失效

**输入条件**：

- 已生成并交接有效 `HANDOVER_BASELINE`；
- 后续新增澄清文件、证据更新、报价调整、章节修改、附件替换、风险变化或审批结论变化。

**预期过程**：

1. 系统识别变化对象和依赖关系；
2. 受影响要求、章节、图表、风险、审核任务和基线进入失效或待复核；
3. 旧 `HANDOVER_BASELINE` 转为 `INVALIDATED`；
4. 旧下载链接立即撤销；
5. 旧文件增加“禁止递交”标识；
6. 项目负责人和投标递交责任人收到变更通知；
7. 系统只重新运行受影响任务；
8. 对应审核角色完成增量复核；
9. 满足条件后生成新的交接基线、清单和链接。

**通过标准**：旧交接包不能继续使用，变更影响不遗漏，未受影响的人工确认和内容不会被错误删除。

### 场景 3：递交责任人接收并核对最新版本

**输入条件**：

- 当前存在有效 `HANDOVER_BASELINE`；
- 指定投标递交责任人具有接收权限；
- 交接链接仍在有效期和下载次数限制内。

**预期过程**：

1. 递交责任人打开受控链接；
2. 系统验证身份、角色、项目和接收权限；
3. 展示项目、采购包、基线版本、生成时间、文件清单和哈希；
4. 递交责任人下载并核对文件；
5. 递交责任人明确确认收到并使用最新版本；
6. 状态从 `PENDING_RECEIPT` 转为 `RECEIVED`；
7. 系统记录接收人、时间、版本、下载次数和确认结果；
8. 如果版本已失效或链接过期，系统拒绝接收并提示获取新基线。

**通过标准**：只有指定责任人能够接收，接收确认与具体基线绑定，失效版本不能被确认成最新版本。

### 场景 4：人工外部递交成功并上传正确回执

**输入条件**：

- 递交责任人已经确认收到最新交接基线；
- 外部采购或交易平台可正常使用；
- 外部递交由递交责任人人工执行。

**预期过程**：

1. 递交责任人将状态标记为 `SUBMITTING`；
2. 在外部平台人工完成文件上传、签章和递交；
3. 回到系统记录递交平台、时间、状态和操作人；
4. 状态转为 `SUBMITTED`；
5. 上传平台回执、截图或确认文件；
6. 系统校验回执文件可读取并关联正确项目、采购包和交接基线；
7. 状态转为 `RECEIPT_UPLOADED`；
8. 所有记录形成审计事件，但系统不宣称自己完成了外部递交动作。

**通过标准**：系统记录与真实人工操作一致，回执关联正确，内部交接和外部递交在状态和审计上明确区分。

### 场景 5：外部递交失败、重新递交、项目撤回或截止时间到达

**输入条件**：

- 外部平台报错、网络中断、文件被退回或回执未生成；
- 或项目负责人决定撤回；
- 或截止时间到达，项目尚未形成有效递交记录。

**预期过程**：

1. 递交责任人记录失败平台、时间、原因和当前文件版本；
2. 状态转为 `FAILED`，并创建处理任务；
3. 系统不得自动标记为成功；
4. 允许在问题解决后基于同一有效基线重新递交，或在内容变化后生成新基线再递交；
5. 重试保留历史失败记录，不覆盖原事件；
6. 项目撤回时撤销交接链接并保留撤回原因；
7. 截止时间到达时，系统按照真实状态标记过期或提示处理；
8. 已失效或撤回项目不能继续生成有效递交记录；
9. 错误回执可以撤回并重新上传，但保留历史记录。

**通过标准**：失败、重试、撤回和过期状态真实可靠，不伪造成功，不删除历史，后续动作受当前基线和权限约束。

### 测试数据与金标准

每个链路样本需要准备：

1. 当前有效采购文件、正文、报价、附件、证据、风险和审批版本；
2. 正确的交接前置条件和缺失条件；
3. 正确的 `HANDOVER_BASELINE` 内容、文件清单、版本号和哈希；
4. 指定接收人、有效期、下载次数、水印和权限策略；
5. 关键变化后应失效或待复核的对象；
6. 正确的旧链接撤销、旧文件标识和通知结果；
7. 递交记录的合法状态转换；
8. 外部递交成功、失败、重试、撤回和过期的真实结果；
9. 正确回执、错误回执和缺失回执；
10. 每次生成、失效、接收、递交、失败、撤回和重试的审计事件。

评估必须从内容变化或交接前状态运行完整链路，不允许直接写入最终 `SUBMITTED` 或 `RECEIPT_UPLOADED` 状态绕过基线生成、接收确认和人工递交记录。

### 核心链路指标

1. **链路完成率**：满足条件的项目能够形成有效交接基线并完成真实递交记录的比例；
2. **变更影响识别率**：关键变化后所有受影响对象被正确识别的比例；
3. **旧结果失效正确率**：依赖旧版本的章节、审核结论和基线被正确失效的比例；
4. **错误失效率**：未受影响内容被错误失效或重开的比例；
5. **增量复核完整率**：受影响对象全部由正确角色重新复核的比例；
6. **交接前置条件校验率**：审核、报价、附件、证据和风险条件全部正确校验的比例；
7. **交接基线完整率**：基线冻结全部必要对象和版本的比例；
8. **文件清单完整率**：清单与实际交接文件一致的比例；
9. **文件哈希一致率**：交接文件与基线哈希一致的比例；
10. **最新版本传递正确率**：递交责任人接收到当前唯一有效基线的比例；
11. **链接撤销成功率**：基线失效、撤回或过期后旧链接不可继续使用的比例；
12. **下载策略执行率**：身份、有效期、次数、水印和禁止转发策略正确执行的比例；
13. **接收人确认合规率**：指定责任人核对版本、清单和哈希后确认接收的比例；
14. **交接后变更通知率**：基线失效后项目负责人和递交责任人收到通知的比例；
15. **递交状态准确率**：系统状态与真实人工外部递交动作一致的比例；
16. **递交记录完整率**：平台、时间、状态、操作人、失败原因和回执信息完整的比例；
17. **回执关联正确率**：回执关联正确项目、采购包、基线和递交记录的比例；
18. **失败记录保留率**：失败和重试历史没有被覆盖或删除的比例；
19. **外部递交边界合规率**：系统没有冒充外部平台自动完成递交的比例；
20. **状态转换正确率**：接收、递交、失败、重试、撤回和过期按照状态机执行的比例；
21. **链路审计完整率**：变更、失效、复核、基线、交接、接收、递交和回执动作均可追溯的比例；
22. **并发与幂等正确率**：重复点击和网络重试不产生冲突基线或重复成功记录的比例；
23. **交接闭环耗时**：从满足交接条件到递交责任人确认接收的时间；
24. **递交记录耗时**：从开始外部人工递交到完成状态和回执记录的时间；
25. **人工处理事项数**：项目负责人和递交责任人需要确认、补充和处理的事项数量。

### 链路硬门禁

出现以下任一情况，本链路直接判定为“不通过”：

1. 内容关键变化后旧交接基线、旧文件或旧链接仍可作为有效版本使用；
2. 必要增量复核未完成，系统仍生成新的有效 `HANDOVER_BASELINE`；
3. 存在未解决或无效豁免的阻断风险，系统仍允许交接；
4. 缺少最终报价确认、必要附件、证据或审核结论，系统仍完成交接；
5. 递交责任人拿到错误项目、错误采购包、错误版本或不完整文件；
6. 文件内容与基线哈希不一致，系统未阻断；
7. 基线失效、撤回、过期或超过下载次数后，链接仍可下载；
8. 非指定接收人或无权限人员能够访问交接包；
9. 递交责任人未核对最新版本，系统却标记为 `RECEIVED`；
10. 系统虚构、推断或自动标记外部递交成功；
11. 递交失败却被记录为 `SUBMITTED` 或 `RECEIPT_UPLOADED`；
12. 回执关联错误项目、采购包、基线或递交记录；
13. 项目撤回或过期后仍可生成新的有效递交成功记录；
14. 重试覆盖历史失败记录或产生两个互相冲突的成功记录；
15. 交接、接收、递交、失败、撤回或回执动作缺少不可修改审计记录；
16. 系统自动登录外部平台、使用 CA 或替代投标递交责任人执行 V1 范围外操作；
17. 关键变化后没有通知已经接收旧版本的递交责任人；
18. 历史基线和递交记录被删除，无法还原完整过程。

### 发布阈值原则

1. 链路硬门禁事件必须为零；
2. 版本、哈希、权限、状态、链接和审计采用确定性规则全量测试；
3. 变更影响识别同时控制漏失效和错误扩大失效；
4. 基线完整性和最新版本正确性优先于交接速度；
5. 内部已交接、责任人已接收和外部已递交必须作为三个不同业务状态评估；
6. 外部递交成功、失败、重试、撤回和过期场景分别测试；
7. 正确拒绝旧版本、撤销链接和保持失败状态属于合格结果；
8. 系统执行时间和人工外部平台操作时间分开统计。

### 失败处理规则

1. 变更影响不确定时扩大待复核范围，立即使旧基线失效；
2. 前置条件不满足时拒绝生成交接基线并展示缺失项；
3. 清单、版本或哈希异常时重新生成交接包；
4. 链接撤销失败时停止交接服务并按安全事件处理；
5. 接收人或权限不正确时拒绝访问并记录审计事件；
6. 外部递交失败时记录真实原因和状态，创建处理任务，不伪造成功；
7. 回执错误或缺失时保持待补充状态，由递交责任人修正；
8. 项目撤回或过期时撤销链接、阻止新交接并保留历史；
9. 版本、影响分析、状态机、权限或链接策略变化后，重新运行固定链路回归集。

### 用户确认

用户确认 L2 链路 5 按照正常交接、交接后关键变更、递交责任人接收核对、人工递交成功以及递交失败、重试、撤回和过期五类场景进行评估，并确认上述指标和硬门禁方向。

### 完成结论

- 链路 5 的核心是“变更能失效旧版本、复核能形成新基线、递交责任人能拿到最新文件、系统能真实记录外部结果”；
- 内部交接、接收确认和外部递交必须严格区分，任何旧版本可用或虚假成功记录都属于阻断问题；
- 至此，五条 L2 业务链路评估方向已经全部确认，下一步进入 L3 单项目端到端评估。

**讨论点完成。**

---

## 四十、评估体系讨论 18：L1、L2 内容与 PRD 的职责边界

### 用户提出的问题

用户询问：截至目前已经讨论的 L1 单模块评估和 L2 业务链路评估内容，是否需要写入 PRD。

### 讨论结论

L1、L2 内容需要回写 PRD，但只能回写“评估基线和发布约束”，不能把全部评估卡、测试场景和指标细节直接复制到 PRD。

原因如下：

1. PRD 负责定义产品范围、角色、功能、业务规则、验收条件、质量底线和发布门槛；
2. 正式评估体系文档负责定义样本集、金标准、指标公式、数值阈值、样本量、评测流程、失败处理和报告模板；
3. 如果把所有 L1、L2 详细内容写入 PRD，PRD 会退化为评测执行手册；
4. 后续样本、指标和阈值调整会导致 PRD 频繁变更，不利于产品需求基线稳定；
5. 但如果 PRD 完全不记录 L1、L2，又无法约束后续评估文档和发布判断，因此需要保留摘要与强制门槛。

### PRD 应回写的内容

1. `L1` 单模块、`L2` 业务链路、`L3` 单项目端到端和 `L4` 试点业务价值四层评估框架；
2. 九类 L1 核心模块名称；
3. 五条 L2 业务链路名称；
4. L1、L2、L3 之间的进入顺序和发布关系；
5. 零容忍质量门禁；
6. L1、L2 必须通过后才能进入 L3 的约束；
7. L3 通过后才能进入 V1 试点或正式发布判断的约束；
8. 正式评估体系文档不得降低 PRD 质量底线的治理规则。

### 不应写入 PRD 的内容

以下内容保留在后续正式评估体系文档：

1. 每个 L1 模块的完整九字段评估卡；
2. 每条 L2 链路的详细测试场景；
3. Precision、Recall、F1、Recall@K、MRR 等指标公式；
4. 金标准制作与复核流程；
5. 具体样本数量和数据集划分；
6. 具体数值阈值和校准方法；
7. 失败样本库、回归集和重评规则；
8. 评测执行记录和评测报告模板。

### 用户确认

用户确认采用以下文档分工：

```text
PRD：评估目标、层级、范围、质量底线、发布门槛
评估体系文档：L1/L2/L3/L4 的详细评估卡、样本、指标、阈值和执行流程
```

### PRD 回写结果

`specs/11-产品需求文档（PRD）.md` 由 `v2.1` 升级为 `v2.2`，完成以下更新：

1. `FR-901` 增加四层评估框架；
2. `FR-901` 增加九类 L1 核心模块和五条 L2 业务链路摘要；
3. 明确 L1、L2 通过后才能进入 L3，L3 通过后才能进入 V1 试点或发布判断；
4. 增加“正常、边界、失败、转人工和版本变化路径必须评估”的要求；
5. 新增 `10.3 四层评估基线`；
6. 新增 `10.4 L1 与 L2 范围摘要`；
7. 第十二章发布门槛增加 L1、L2、L3 的强制进入条件；
8. 补充无效报价、未经确认的证据或参考内容、审计记录缺失等阻断级缺陷；
9. 明确详细样本、金标、公式、阈值和失败处理由正式评估体系文档维护。

本次 PRD 更新只建立评估范围和发布约束，不代表 L1、L2 评测已经执行，也不代表相关软件功能已经实现。

### 完成结论

- PRD 保持产品需求基线的稳定和可读性；
- 正式评估体系文档承担详细可执行评测设计；
- 后续 L3、L4 讨论完成后，继续按照相同职责边界回写 PRD 摘要，并将完整内容写入独立评估体系文档。

**讨论点完成。**

---

## 四十一、评估体系讨论 19：L3 单项目端到端评估判定方式

### L3 评估对象

L3 以一个完整投标项目为评估单位，从选择来源平台、输入项目编号或补充采购文件开始，持续运行到内部受控交接和人工递交记录，不允许通过手工拼接中间结果绕过真实业务流程。

L3 使用以下五类完整项目样本：

1. **标准项目**：资料完整，能够正常完成接入、生成、审核和交接；
2. **受限接入项目**：部分文件需要登录、CA、专用环境或人工上传兜底；
3. **过程变更项目**：编制过程中新增澄清、更正、补遗或其他关键数据变化；
4. **业务缺口项目**：存在证据不足、无效报价、附件缺失或阻断风险；
5. **异常递交项目**：交接后发生修改、外部递交失败、项目撤回或截止时间到达。

### 判定原则

L3 不使用一个可以相互抵消风险的单一总分，而采用“三道门槛 + 综合记录”：

```text
第一道：零容忍质量门禁
  → 第二道：AC-01～AC-06 业务闭环验收
  → 第三道：核心质量阈值
  → 记录效率、成本和人工工作量
```

### 第一道：零容忍质量门禁

任一完整项目出现以下问题，L3 直接判定为不通过：

1. 编造企业资质、人员、案例、能力、证据、报价或当前项目事实；
2. 漏掉资格条件、否决项或其他阻断风险，且未转人工确认；
3. 无效、未确认或旧报价进入正式标书；
4. 已失效采购文件、证据、章节、审核结论或交接基线继续被使用；
5. Agent 越权完成最终报价、风险接受、审核通过、问题关闭或内部交接决定；
6. 人工修改或确认结果被系统静默覆盖；
7. 无有效证据时输出肯定企业事实；
8. 未解决或无效豁免的阻断风险仍允许生成有效交接基线；
9. 递交责任人获得错误项目、错误采购包、错误版本或不完整交接文件；
10. 系统虚构或自动推断外部递交成功；
11. 关键来源、版本、确认、审核、豁免、交接或递交记录不可追溯。

零容忍问题不能通过其他指标高分抵消，也不能通过“有条件通过”进入试点。

### 第二道：业务闭环验收

每个 L3 项目必须完整通过 PRD 中的 `AC-01` 至 `AC-06`：

1. `AC-01`：项目编号与分级数据接入；
2. `AC-02`：文件解析、变更与影响分析；
3. `AC-03`：企业证据、历史参考和拒答；
4. `AC-04`：报价方案闭环；
5. `AC-05`：章节生成、审核和增量返工；
6. `AC-06`：内部交接与人工递交记录。

`AC-07` 评估历史基线与试点业务价值，属于 L4 试点评估，不作为单个 L3 项目流程验收场景。

任一核心验收场景失败，该项目不能判定为 L3 通过。

### 第三道：核心质量阈值

在零容忍门禁和业务闭环均通过后，继续检查：

1. 要求、评分项、时间节点、附件和格式要求覆盖情况；
2. 企业事实、项目事实、报价和风险结论的证据支撑与引用情况；
3. 响应矩阵、章节、附件和责任人之间的完整映射；
4. 报价、人员、日期、参数、案例和承诺的跨章节一致性；
5. Agent 预检问题召回、风险分级、角色路由和审核闭环；
6. 人工编辑保护和局部重生成差异确认；
7. 上游变化向要求、证据、报价、章节、风险、审核和基线的版本传播；
8. 交接包文件清单、版本和哈希一致性；
9. 异常情况下的阻断、拒答、降级、重试和人工接管能力；
10. 全过程来源、版本、确认、审核、交接和递交审计完整性。

具体数值阈值由正式评估体系文档根据金标准评测集、业务风险和人工基线制定，不在缺少样本时拍脑袋确定。

### 综合记录指标

以下指标在 L3 中必须采集，但在尚未形成真实人工基线前，不直接作为任意设定的硬门槛：

- 从项目创建到首版初稿的时间；
- 从项目创建到审核版本完成的时间；
- 从满足条件到交接基线生成和责任人接收的时间；
- 系统自动处理时间与人工等待时间；
- 人工查询、整理、上传、确认和修改事项数量；
- 各角色审核时间和审核退回轮次；
- 用户对证据、参考、报价和修改建议的接受、修改和拒绝比例；
- 全量审核与增量复核内容比例；
- Agent、模型、解析、检索和导出的资源成本；
- 异常、重试、人工接管和失败恢复次数。

### L3 最终结论

1. **通过**
   - 零容忍问题为零；
   - `AC-01` 至 `AC-06` 全部通过；
   - 核心质量指标达到正式评估体系文档规定的阈值；
   - 可以进入 L4 试点业务价值评估。

2. **有条件通过**
   - 没有零容忍问题；
   - 核心业务闭环可以运行；
   - 仅存在非阻断观察指标、体验指标或待校准指标未达到预期；
   - 必须记录问题、责任人、整改期限和复测计划；
   - 只能继续内部验证，不能直接进入正式试点或发布。

3. **不通过**
   - 触发任一零容忍门禁；
   - 任一核心验收场景失败；
   - 核心质量指标未达到发布阈值；
   - 失败后需要整改并重新执行受影响的 L1、L2 和 L3 评估。

### 用户确认

用户确认 L3 使用五类完整项目样本，并采用“零容忍门禁 → `AC-01` 至 `AC-06` 业务闭环 → 核心质量阈值”的三道门槛进行判定。

用户同时确认：

- `AC-07` 归入 L4 试点业务价值评估；
- L3 不使用单一平均总分抵消严重业务风险；
- “有条件通过”只能继续内部验证，不能直接进入正式试点或发布；
- 效率、成本和人工工作量必须采集，但应在建立真实人工基线后再确定正式改善阈值。

### 完成结论

- L3 回答的是“一个完整投标项目能否从输入走到可交接、可追溯的正确结果”；
- 只有零容忍问题为零、核心验收场景全部通过且质量阈值达标，才能进入 L4；
- 下一步需要确定 L3 评测项目的样本数量、选择规则和金标准制作方式。

**讨论点完成。**

---

## 四十二、评估体系讨论 20：甲方无法提供历史项目时的评测数据方案

### 现实条件

用户确认甲方无法提供脱敏后的完整历史投标项目资料，因此不能依赖甲方历史项目包建设开发集、正式验收集和人工流程基线。

### 可选方案比较

1. **只使用公开采购文件**：可以评估接入、解析和要求提取，但无法完整评估企业证据、报价、审核、交接和递交，不足以完成 L3；
2. **混合评测数据**：使用真实公开采购文件、虚构测试企业、受控报价模板、模拟历史项目、模拟审核交接和人工植入异常，可以完整运行 L3；
3. **等待真实试点再评估**：真实性高，但开发阶段缺少完整验收依据，不能单独采用。

### 用户确认方案

用户确认采用：

> **方案 B：公开采购文件 + 可控模拟企业数据 + 受控报价数据 + 模拟历史项目与工作流 + 人工构造异常 + 后续前瞻性试点数据。**

### 数据组成

1. **真实公开采购文件**
   - 选取公开且允许使用的政府采购信息服务类项目；
   - 包含公告、采购文件、附件、评分办法、资格和实质性要求、澄清更正及公开中标结果；
   - 用于评估项目匹配、文件接入、解析、要求提取和补充文件变化。

2. **虚构测试企业数据**
   - 建立资料完整、部分缺失、存在过期或范围问题等不同测试企业画像；
   - 准备测试资质、测试人员、测试案例、测试合同、验收记录、权限和版本状态；
   - 所有资料明确标记为测试数据，不使用真实公司、人员和印章，不进入生产知识库。

3. **受控报价数据**
   - 准备测试 Excel 模板、确定公式和标准计算结果；
   - 覆盖正常报价、超过限价、税率错误、合计错误、大小写不一致、激进策略和版本变化；
   - 正式金额金标准来自受控 Excel 或内部测试模型，不由大模型计算。

4. **模拟历史项目和参考池**
   - 创建明确标注为测试用途的中标、未中标、高相似、误导性相似和不可比报价项目；
   - 定义可参考内容、禁止复用内容和章节用途；
   - 用于评估推荐、差异说明、参考池确认和用途限制。

5. **模拟审核、交接和递交记录**
   - 项目团队分别扮演标书编写人、项目负责人、商务人员、合规/法务人员和递交责任人；
   - 覆盖审核通过、退回、阻断风险、授权豁免、修改建议接受或拒绝、交接接收、递交成功、失败、重试和撤回。

6. **人工构造异常**
   - 在评测副本中构造漏页、过期证据、主体不符、无效报价、金额冲突、旧基线未失效、错误回执等场景；
   - 构造数据只用于验证发现、阻断和恢复能力，不作为真实企业事实。

### 金标准制作方式

- 采购文件侧由人工标注资格、否决项、评分项、时间、附件、原文、页码和版本关系；
- 测试企业、报价、权限和工作流侧使用预设确定性事实作为金标准；
- 每项结果记录标注人、复核人、规则版本和争议处理；
- 正式验收样本不得用于提示词、规则和模型调优。

### 能够证明与不能证明的内容

该方案可以验证系统功能、安全边界、拒答、规则拦截、版本传播、审核权限、交接和审计闭环。

该方案不能证明：

1. 已经适配甲方全部真实企业资料；
2. 已经实现甲方真实编写或审核效率提升；
3. 已经提高真实中标率；
4. 已经达到甲方生产环境的实际业务效果。

上述真实业务价值必须在 L4 采用前瞻性试点数据验证。

### L4 前瞻性数据采集

1. 选择真实试点项目；
2. 人工团队继续按原流程完成正式工作；
3. 系统以旁路方式运行，不影响正式投标；
4. 同步记录人工耗时、修改、审核、问题和系统结果；
5. 首批项目形成真实人工基线；
6. 后续项目再比较系统辅助后的效率和质量变化。

### 完成结论

- L3 使用混合评测数据验证系统是否正确、安全、可追溯；
- L4 使用前瞻性真实试点数据验证是否产生实际业务价值；
- 评测报告必须明确区分模拟评测结果和真实试点结果，不得将模拟数据包装成甲方实际收益。

**讨论点完成。**

---

## 四十三、评估体系讨论 21：V1 各阶段需要通过的评估层级

### 用户提出的问题

用户询问：V1 阶段是否需要 L1、L2、L3 和 L4 全部通过。

### 讨论结论

V1 最终业务验收需要 L1 至 L4 全部通过，但四层评估不是在同一个时间点完成，而是按照开发、联调、试点和最终验收阶段逐层进入。

```text
L1 通过
  → 允许进入模块联调
L1 + L2 通过
  → 达到 V1 技术完成，可进入完整项目测试
L1 + L2 + L3 通过
  → 达到 V1 试点就绪，可进入真实项目旁路试点
L1 + L2 + L3 + L4 通过
  → V1 最终业务验收通过，可考虑正式生产使用或扩大推广
```

### 状态 1：V1 技术完成

必须满足：

1. 九类核心 L1 模块通过；
2. 五条 L2 业务链路通过；
3. 零容忍门禁未被触发；
4. 正常、边界、失败、转人工和版本变化路径完成评估；
5. 关键输出来源、版本、确认和审计可追溯。

该状态表示模块和业务链路能够工作，但还不能进入甲方真实项目试点。

### 状态 2：V1 试点就绪

在 V1 技术完成基础上，必须满足：

1. 完成 L3 单项目端到端评估；
2. L3 零容忍问题为零；
3. `AC-01` 至 `AC-06` 全部通过；
4. 核心质量指标达到正式评估体系文档规定的阈值；
5. 异常阻断、降级、人工接管和版本传播路径正确。

达到该状态后，可以进入真实项目旁路试点。旁路试点期间，人工团队继续按原流程完成正式投标，系统不得代替最终报价、风险接受、审核通过、内部交接和外部递交决定。

L3 “有条件通过”只能继续内部验证，不能直接进入正式试点或生产发布。

### 状态 3：V1 最终业务验收

在 V1 试点就绪基础上，必须完成 L4：

1. 使用真实项目旁路运行数据；
2. 与历史人工基线或前瞻性建立的人工基线进行比较；
3. 评估自动化、编写效率、审核效率和质量风险；
4. 达到双方在获得真实数据后确认的业务价值门槛；
5. L4 期间不出现新的零容忍缺陷；
6. 评测结果、样本量和未测指标真实披露。

只有 L4 通过，才能声称 V1 已经验证实际业务价值，并进入正式生产使用或扩大推广范围的决策。

### L4 未通过或数据不足的处理

1. 出现安全、合规或零容忍问题：最终业务验收不通过，退回受影响 L1、L2 或 L3 整改；
2. 系统功能正确但效率改善不足：保留技术成果，但不能宣称已经实现预期业务价值；
3. 真实项目数量不足：标记为“L4 待继续评测”，不能虚构通过；
4. 部分指标达标：可以限定范围继续试点，但不直接全面推广；
5. 未测指标必须明确标记，不能用模拟数据替代真实业务收益。

### 用户确认

用户确认采用以下最终规则：

> **V1 进入试点要求 L1 至 L3 通过；V1 最终业务验收和正式推广要求 L1 至 L4 全部通过。**

### PRD 回写结果

`specs/11-产品需求文档（PRD）.md` 由 `v2.2` 升级为 `v2.3`，完成以下更新：

1. `FR-901` 明确 L3 通过后达到“V1 试点就绪”，L4 通过后完成最终业务验收；
2. `10.1 基线方法` 改为优先使用授权历史数据，当前无历史项目时采用混合 L3 数据和前瞻性 L4 基线；
3. `10.3` 明确 L4 支持 V1 最终业务验收；
4. `AC-07` 增加无历史数据时通过首批旁路试点建立基线的流程；
5. 第十二章拆分为 `V1 技术完成`、`V1 试点就绪`、`V1 最终业务验收` 和 `评测报告约束`；
6. 明确 L3 有条件通过不能进入正式试点；
7. 明确 L4 样本不足时保持待评测，不能判定最终验收通过；
8. 明确模拟评测、旁路试点和生产结果必须区分。

本次修改建立 V1 分阶段评估与验收门槛，不代表评测已经执行或 V1 已经通过任何评估层级。

### 完成结论

- L1、L2 验证技术模块和业务链路；
- L3 验证完整项目是否安全、正确、可审核、可交接；
- L4 验证系统是否在真实业务中产生实际价值；
- V1 可以在 L3 通过后进入旁路试点，但只有 L4 通过后才能完成最终业务验收。

**讨论点完成。**

---

## 四十四、评估体系讨论 22：L3 样本规模、数据隔离与金标准制作

### 已确认总体方案

L3 采用：

> **5 个开发冒烟项目 + 15 个隐藏正式验收项目 + 持续扩充的固定回归集。**

该数量用于覆盖主要业务路径和风险类型，不代表仅凭 15 个模拟或混合项目即可证明生产环境的统计效果。真实业务价值仍由 L4 试点验证。

### 开发冒烟集

共 5 个完整项目，五类项目各 1 个：

1. 标准项目；
2. 受限接入项目；
3. 过程变更项目；
4. 业务缺口项目；
5. 异常递交项目。

开发冒烟集用于：

- 跑通完整端到端流程；
- 调整提示词、规则、检索和数据结构；
- 发现明显流程和状态问题；
- 建立首批完整项目回归测试。

因为冒烟集参与过开发和调优，不能作为正式 L3 验收通过的证据。

### 隐藏正式验收集

共 15 个完整项目，每类 3 个：

| 项目类型 | 数量 |
|---|---:|
| 标准项目 | 3 |
| 受限接入项目 | 3 |
| 过程变更项目 | 3 |
| 业务缺口项目 | 3 |
| 异常递交项目 | 3 |
| 合计 | 15 |

正式验收集需要满足：

1. 开发期间不得用于提示词、规则、检索和模型调优；
2. 开发人员在正式评估前不得看到完整金标准；
3. 与开发集使用不同采购项目；
4. 同一采购文件不能仅更换测试企业后同时进入开发集和验收集；
5. 验收失败后可以查看失败样本，但修复后必须重新运行完整固定验收集，并保留新增隐藏样本或由评测负责人监督复测；
6. 验收结果记录模型、提示词、规则、连接器、解析器、检索、模板和数据版本。

### 固定回归集

固定回归集初始包含 5 个开发冒烟项目，并持续加入真实发现或人工构造的严重失败场景，例如：

- 资格条件或否决项漏提；
- 无证据时输出肯定结论；
- 使用过期、主体不符或无权限证据；
- 超过最高限价或无效报价进入正文；
- 未确认报价进入正式章节；
- 人工修改被静默覆盖；
- 新文件发布后旧结果仍有效；
- 阻断风险仍允许交接；
- 旧交接链接未撤销；
- 虚构外部递交成功；
- 错误回执关联。

回归集不设置固定上限。每发现一个具有代表性的严重失败样本，就记录失败原因、修复版本和预期结果，并加入后续回归。

### 正式项目选择规则

15 个正式验收项目必须组合覆盖：

1. 北京及其他地区项目；
2. 不同公开采购公告来源；
3. PDF、Word、Excel 和扫描件；
4. 普通文本、复杂表格、跨页表格和低质量页面；
5. 有无澄清、更正、补遗和答疑；
6. 不同预算规模、最高限价和报价口径；
7. 不同服务周期、评分结构和技术路线；
8. 简单和复杂资格条件；
9. 完整证据、部分证据、无证据和无权限证据；
10. 正常报价、无效报价和激进报价；
11. 正常审核、阻断问题、授权豁免和增量返工；
12. 正常交接、交接后变更、递交失败、重试和撤回。

不能只选择文件清晰、要求简单、证据完整和流程正常的项目。

### 单项目评测包

每个 L3 项目形成独立、版本化的评测包：

```text
01 公开采购文件及版本关系
02 人工标注的要求和评分项
03 测试企业资料和权限
04 企业证据及有效性标准
05 模拟历史项目和参考用途
06 受控 Excel 报价模板
07 报价假设、候选方案和正确结果
08 章节必须满足项和禁止项
09 植入的风险与异常
10 正确审核角色和处理方式
11 正确状态转换和失效范围
12 交接包、递交状态和回执标准
13 预期审计事件
```

评测包需要记录项目版本、制作人、复核人、适用评估层级、允许用途和变更历史。

### 金标准制作方式

#### 采购文件部分

- 第一名标注人员提取资格条件、否决项、评分项、时间节点、附件和格式要求；
- 第二名复核人员逐项检查原文、页码、分类、后果和文件版本；
- 有争议内容记录争议原因、参与人员和最终结论；
- 正式金标准至少满足“一人标注、一人复核”，不能由同一人无复核完成。

#### 企业证据部分

使用可控测试企业的确定性事实：

- 哪些证据有效、过期或被撤回；
- 哪些主体、地域、行业和用途适用；
- 哪些材料只能部分支持；
- 哪些角色和项目没有访问权限；
- 正确引用对应的文件、版本、页码和原文。

#### 报价部分

金标准来自受控 Excel 或内部测试模型的真实计算结果：

- 输入值和来源；
- 公式、税率和口径；
- 正确候选结果；
- 无效方案及失败规则；
- 允许推荐的有效方案；
- 商务选择、负责人确认和额外授权版本。

不得使用大模型生成的金额作为报价金标准。

#### 审核、状态与交接部分

依据 PRD 和状态机预设：

- 问题风险等级和证据；
- 正确责任人和审核角色；
- 是否允许豁免及授权条件；
- 应重新运行和复核的对象；
- 合法与非法状态转换；
- 应失效的章节、审核结论、基线和链接；
- 正确交接、递交和回执状态；
- 应生成的审计事件。

### 责任分工

| 金标准内容 | 制作人 | 复核人 |
|---|---|---|
| 要求和评分项 | 招投标业务标注人员 | 有经验的标书负责人 |
| 企业证据 | 评测数据管理员 | 业务或合规人员 |
| 报价 | 商务/报价人员 | 项目负责人 |
| 风险与审核 | 项目负责人 | 商务或法务人员 |
| 状态与交接 | 产品或测试人员 | 业务负责人 |
| 最终项目结论 | 评测负责人 | 产品负责人和业务代表 |

如果当前团队没有具备真实标书经验的人员，至少需要引入一名招投标领域顾问负责金标准复核。否则结果只能称为技术自测，不能称为正式业务验收。

### 用户确认

用户确认采用上述 L3 样本规模、数据隔离、项目选择、评测包结构、金标准制作和责任分工方案。

### 完成结论

- 开发集用于调优，隐藏验收集用于形成正式 L3 结论，回归集用于防止严重问题复发；
- 正式验收样本和金标准必须与开发调优隔离；
- L3 下一步需要建立项目级评估总表，统一汇总零容忍门禁、`AC-01` 至 `AC-06`、核心质量指标和观察指标。

**讨论点完成。**

---

## 四十五、评估体系讨论 23：L3 单项目评估总表

### 总体原则

每个 L3 正式验收项目使用同一张项目级评估总表。总表不计算一个可以相互抵消风险的综合平均分，而采用以下判定顺序：

```text
零容忍门禁全部未触发
  → AC-01 至 AC-06 全部通过
  → 各核心质量域分别达到阈值
  → 记录效率、成本和人工工作量
  → 形成通过 / 有条件通过 / 不通过结论
```

### 第一部分：项目与评测版本

记录以下信息：

- L3 项目编号和项目类型；
- 公开采购项目来源和外部项目编号；
- 采购包、地区、采购方式和预算规模；
- 采购文件、澄清更正和附件版本；
- 测试企业数据版本；
- 企业证据集和权限版本；
- 历史参考池版本；
- Excel 报价模板、假设和候选方案版本；
- 团队模板和章节结构版本；
- 模型、提示词、规则、连接器、解析器、OCR、嵌入、重排和工作流版本；
- 评测数据集版本、金标准版本和回归集版本；
- 评测开始时间、结束时间和运行环境；
- 评测负责人、业务复核人和专项复核人员。

任何核心版本缺失时，不允许形成正式 L3 结论。

### 第二部分：零容忍门禁

每项填写：

```text
未触发 / 已触发 / 无法评测
```

至少检查：

1. 是否编造企业资质、人员、案例、能力、证据、报价或项目事实；
2. 是否漏掉资格、否决项或阻断问题；
3. 是否使用无效、未确认或旧报价；
4. 是否使用已失效采购文件、证据、章节、审核结论或基线；
5. Agent 是否越权执行人工决策；
6. 人工编辑是否被静默覆盖；
7. 无有效证据时是否输出肯定企业事实；
8. 未解决阻断风险是否仍允许交接；
9. 递交责任人是否获得错误或不完整交接文件；
10. 系统是否虚构外部递交成功；
11. 关键来源、版本、确认、审核、豁免、交接或递交记录是否缺失；
12. 是否发生越权访问、敏感资料泄露或权限绕过。

任一门禁填写“已触发”，项目直接判定为不通过。“无法评测”不能视为“未触发”，必须补充样本或说明未完成原因。

### 第三部分：AC-01 至 AC-06 验收结果

每个验收场景记录：

- 通过 / 不通过 / 未执行；
- 使用的测试步骤和数据；
- 实际结果；
- 预期结果；
- 失败步骤；
- 失败证据、日志和截图；
- 责任模块和责任人；
- 是否需要重跑 L1 或 L2；
- 修复版本和复测结果。

六个场景分别为：

1. `AC-01` 项目编号与分级数据接入；
2. `AC-02` 文件解析、变更与影响分析；
3. `AC-03` 企业证据、历史参考和拒答；
4. `AC-04` 报价方案闭环；
5. `AC-05` 章节生成、审核和增量返工；
6. `AC-06` 内部交接与人工递交记录。

任一场景“不通过”或“未执行”，项目不能判定为通过。

### 第四部分：核心质量域

每个质量域独立记录核心指标、阈值、实际值、结果和失败样本，不允许通过其他质量域高分抵消。

#### 质量域 A：项目文件与解析

- 项目和采购包匹配；
- 应接入文件完整性；
- 有效版本识别；
- 页数、正文、表格和原文定位；
- 低质量告警和人工兜底；
- 文件更新后的解析失效。

#### 质量域 B：要求与响应矩阵

- 资格、实质性要求和否决项召回；
- 评分项、时间、附件和格式要求完整性；
- 原文定位和标准化保真；
- 要求原子化、重复和冲突识别；
- 要求—评分—章节—证据—责任人映射；
- 未覆盖、部分覆盖、待确认和阻断状态准确性。

#### 质量域 C：企业证据与历史参考

- 证据召回、主体、有效期、适用范围和权限；
- 引用与结论一致性；
- 无证据拒答和资料补充任务；
- 历史项目推荐、差异说明和复用边界；
- 参考池和章节用途确认；
- 证据与参考版本变化传播。

#### 质量域 D：报价与跨章节一致性

- 模板和输入映射；
- Excel 或内部模型结果读取；
- 预算、最高限价、税率、合计和大小写金额校验；
- 无效方案排除；
- 推荐解释与候选数据一致性；
- 商务选择、负责人确认和额外授权；
- 冻结报价版本和跨章节一致性。

#### 质量域 E：章节、图表和说明材料

- 要求和评分项覆盖；
- 事实支撑、引用和模板符合；
- 图表数据、来源、时间和版本；
- `BID_ATTACHMENT`、`EXTERNAL_EXPLANATION` 和 `INTERNAL_NOTE` 分类；
- 内部风险和外部正式内容隔离；
- 人工编辑保护和导出质量。

#### 质量域 F：风险、审核与返工

- 预检问题和阻断问题召回；
- 风险分级和问题证据；
- 审核角色路由；
- 结构化问题完整性；
- 修改建议正确性和人工确认；
- 增量复核；
- 问题关闭权限和风险豁免；
- 审核审计和闭环耗时。

#### 质量域 G：版本、交接与递交记录

- 变更影响分析和旧结果失效；
- 合法状态转换和非法转换拦截；
- 交接前置条件；
- 基线、清单、版本和哈希；
- 受控链接、接收人和最新版本确认；
- 人工外部递交状态和回执；
- 历史记录、失败、重试、撤回和审计。

每个质量域填写：

```text
核心指标：
发布阈值：
实际结果：
是否达到阈值：是 / 否 / 未评测
失败样本：
影响范围：
修复与复测要求：
```

任一核心质量域未达到发布阈值，项目不能判定为通过。

### 第五部分：效率、成本和人工工作量

记录但不在缺少真实基线时任意设置否决阈值：

- 项目创建到首稿生成时间；
- 首稿到审核版本完成时间；
- 审核版本到交接责任人接收时间；
- 系统执行时间和人工等待时间；
- 人工查询、整理、上传和重复录入次数；
- 用户确认、修改、拒绝和补充事项数量；
- 各审核角色处理时长；
- 审核退回轮次和增量复核比例；
- Agent 重试、失败和人工接管次数；
- 模型 Token、OCR、检索、重排、存储和导出成本；
- 单项目失败恢复和重新评测成本。

这些数据用于 L4 建立真实业务价值基线，也用于发现系统瓶颈。

### 第六部分：最终结论

#### 通过

- 零容忍门禁全部未触发；
- `AC-01` 至 `AC-06` 全部通过；
- 七个核心质量域全部达到阈值；
- 所有必测内容均已执行；
- 评测证据、版本和签署完整。

#### 有条件通过

- 零容忍门禁全部未触发；
- 核心业务闭环能够运行；
- 仅存在非阻断观察指标、体验指标或待校准指标问题；
- 记录遗留问题、责任人、整改期限和复测计划；
- 只能继续内部验证，不进入正式试点。

#### 不通过

- 任一零容忍门禁被触发；
- 任一 `AC-01` 至 `AC-06` 不通过或未执行；
- 任一核心质量域未达到发布阈值；
- 关键版本、数据、证据或评测记录缺失；
- 需要整改并重新执行受影响的 L1、L2 和 L3 评估。

### 结论签署

每个项目结论至少需要：

1. 评测负责人确认数据、版本和执行过程；
2. 业务复核人确认要求、证据、风险和项目结论；
3. 商务人员确认报价专项结果；
4. 合规/法务人员确认资格、承诺和法律风险专项结果；
5. 产品负责人确认遗留问题和进入下一阶段的决定。

签署人不能签署自己无权确认的专项内容。任何结论变更都必须产生新版本和审计记录，不能直接覆盖原结论。

### 用户确认

用户确认 L3 单项目评估总表采用“项目与版本、零容忍门禁、`AC-01` 至 `AC-06`、七个核心质量域、效率成本观察、最终结论与签署”六部分结构。

用户同时确认 L3 不计算笼统综合总分，采用门禁、验收场景和各质量域分别通过的方式判定。

### 完成结论

- 每个 L3 项目都使用统一总表，确保不同项目结果可以比较和追溯；
- 严重风险不能通过其他指标高分抵消；
- 下一步需要讨论各核心质量域的初始发布阈值如何制定和校准。

**讨论点完成。**

---

## 四十六、评估体系讨论 24：发布阈值制定、冻结与重评规则

### 讨论背景

在完成 L3 单项目评估总表设计后，需要进一步明确七个核心质量域的发布阈值如何制定。阈值不能凭经验统一设成一个百分比，也不能在看到正式验收结果后为了让项目通过而临时降低。

本次讨论的目标是建立一套可解释、可版本化、可冻结、可重评的阈值管理机制。

### 一、指标分为四类

#### 1. 确定性门禁

适用于可以通过规则或系统状态明确判断对错的事项，例如：

- 权限校验；
- 状态转换；
- 文件哈希与版本绑定；
- 已失效版本隔离；
- 无效报价方案排除；
- 审核、确认、交接和递交记录完整性。

这类指标不使用模糊的平均分，要求：

```text
100% 正确
或
严重错误数 = 0
```

任一确定性门禁失败，相关项目不能判定为通过。

#### 2. 高风险质量指标

适用于一旦漏报、误判或错误肯定，就可能导致废标、错误承诺、错误报价或重大合规风险的事项，例如：

- 资格条件；
- 实质性要求和否决项；
- 阻断级风险；
- 关键报价约束；
- 关键企业事实和证据引用；
- 无证据时的拒答与待确认处理。

这类指标需要同时满足两层要求：

1. 在 15 个隐藏正式验收项目中，不得出现关键漏报、错误肯定或其他零容忍错误；
2. 对 Recall、引用正确率、风险召回率等质量指标设置明确的最低阈值。

零容忍错误不能被普通质量指标的高分抵消。

#### 3. 普通质量指标

适用于不会直接造成重大投标风险，但影响标书质量、完整性和使用体验的事项，例如：

- 普通技术要求提取；
- 历史项目推荐；
- 章节结构；
- 图表和说明材料质量；
- 语言表达、格式和模板符合度。

这类指标的初始阈值根据以下依据共同确定：

- 5 个开发冒烟项目的实际指标分布；
- 失败样本及其业务影响；
- 人工编制结果或领域专家复核结果；
- 业务人员能够接受的最低质量水平；
- 当前技术条件下可稳定重复达到的水平。

不应脱离样本和业务风险，直接统一设为 `95%` 或其他单一比例。

#### 4. 观察指标

适用于当前需要持续采集，但在缺少甲方真实历史基线时不宜直接设为发布硬门槛的事项，例如：

- Token 使用量；
- 单项目模型、OCR、检索和存储成本；
- 系统运行耗时；
- Agent 重试率；
- 人工确认、修改和补充事项数量；
- 人工接管次数。

观察指标用于后续发现瓶颈、优化系统，并为 L4 真实试点建立业务基线。在没有真实基线前，不任意设定否决阈值，也不包装为甲方已经实现的效率收益。

### 二、初始阈值制定流程

初始阈值按照以下顺序制定：

```text
运行 5 个开发冒烟项目
→ 收集各指标分布和失败样本
→ 由业务人员明确不可接受风险
→ 制定七个质量域的初始阈值
→ 冻结 evaluation_policy_v1
→ 运行 15 个隐藏正式验收项目
```

其中：

- 开发冒烟项目用于观察系统当前能力、校准指标公式和发现失败模式；
- 业务人员负责确认哪些错误不可接受，以及哪些指标必须作为门禁；
- 技术、产品和业务共同制定普通质量指标的最低发布要求；
- 正式验收开始前，必须冻结指标定义、计算公式、适用范围、样本范围和发布阈值。

### 三、阈值冻结与版本管理

阈值策略作为独立评估策略版本管理，例如：

```text
evaluation_policy_v1
```

每个策略版本至少记录：

- 指标名称和业务含义；
- 指标计算公式；
- 数据范围和排除规则；
- 指标所属类别；
- 发布阈值或门禁条件；
- 适用模块、链路、项目类型和质量域；
- 制定依据和审批人；
- 生效时间和失效时间；
- 关联的评测数据集版本。

正式验收过程中，不允许因为结果不理想而直接修改当前策略中的阈值。

### 四、阈值修改与重新评测

如果发现指标公式不合理、业务风险判断变化或阈值确实需要调整，应当：

```text
提出阈值变更原因
→ 评估变更影响范围
→ 生成新的 evaluation_policy 版本
→ 重新冻结评测数据和策略
→ 对受影响范围执行完整评测
```

不得覆盖原策略版本，也不得只重跑失败样本后直接宣告通过。

如果阈值变化影响 L1、L2 或 L3 的判断，应重新执行受影响层级；如果无法证明影响范围，应重新执行完整的 L1、L2 和 L3 评测。

### 五、用户确认

用户确认发布阈值按照“确定性门禁、高风险质量指标、普通质量指标、观察指标”四类管理。

用户确认先运行 5 个开发冒烟项目收集分布和失败样本，再由业务人员确定不可接受风险并制定初始阈值；在运行 15 个隐藏正式验收项目前冻结 `evaluation_policy_v1`。

用户确认正式验收过程中不得为了通过而临时降低阈值。任何阈值、指标公式或适用范围修改都必须生成新版本，并按影响范围重新执行评测。

### 完成结论

- 发布阈值不采用脱离业务风险和样本分布的统一百分比；
- 确定性门禁和高风险错误采用严格控制，严重风险不能被平均分抵消；
- 普通质量阈值通过开发样本、专家复核和业务可接受度共同校准；
- 观察指标在缺少真实基线时只采集，不任意设为否决门槛；
- 阈值策略必须先冻结、再正式验收，修改后必须形成新版本并重新评测。

**讨论点完成。**

---

## 四十七、评估体系讨论 25：L3 初始阈值采用风险分级方式

### 讨论问题

在明确发布阈值需要先校准、再冻结后，继续讨论七个 L3 核心质量域的初始阈值应采用哪种设置方式。

### 候选方案

#### 方案 A：统一阈值

所有质量域和指标统一使用同一个比例阈值。

优点是规则简单、容易理解；缺点是忽略不同错误的业务风险差异，可能让严重风险被普通指标平均，也可能对低风险体验指标设置不合理的过高要求。

#### 方案 B：风险分级阈值

根据指标性质分别设置要求：

```text
确定性门禁：100% 正确或严重错误数为 0
高风险质量指标：设置严格底线，并禁止关键漏报和错误肯定
普通质量指标：先设置待校准范围，再根据开发冒烟项目冻结具体值
观察指标：持续采集，暂不作为缺少真实基线时的否决门槛
```

该方案兼顾投标安全、技术可行性和后续校准能力，能够避免使用单一分数掩盖严重问题。

#### 方案 C：完全数据驱动

不预先设置任何阈值，等待 5 个开发冒烟项目全部运行后，再根据结果分布制定所有阈值。

优点是充分参考实际数据；缺点是冒烟评测前缺少明确的最低安全底线，也可能把系统当前能达到的水平错误地当成业务可以接受的水平。

### 推荐方案

推荐采用方案 B：风险分级阈值。

具体执行方式为：

1. 先固定确定性门禁和高风险指标的最低安全要求；
2. 普通质量指标先标记为“待冒烟评测校准”，不凭空统一填写 `95%`；
3. 运行 5 个开发冒烟项目后，结合指标分布、失败样本、人工质量和业务可接受度确定普通指标阈值；
4. 将最终阈值冻结到 `evaluation_policy_v1`；
5. 使用冻结后的策略运行 15 个隐藏正式验收项目。

### 用户确认

用户确认采用方案 B，即七个 L3 核心质量域使用风险分级阈值。

用户确认确定性门禁和高风险指标先设置明确底线，普通质量指标在开发冒烟评测后校准，观察指标在缺少真实业务基线时主要用于记录和后续优化。

### 完成结论

- L3 不采用所有指标统一一个百分比的方式；
- 业务风险等级决定指标的阈值类型和严格程度；
- 系统当前能力不能直接替代业务可接受标准；
- 初始模板先明确门禁和高风险底线，普通指标保留校准位；
- 下一步逐个制定质量域 A 至 G 的初始阈值模板。

**讨论点完成。**

---

## 四十八、评估体系讨论 26：质量域 A 项目文件与解析初始阈值模板

### 讨论对象

本次为 L3 七个核心质量域中的质量域 A 制定初始阈值模板。

质量域 A 覆盖项目文件接入、采购包与版本绑定、文件有效性、文档解析、OCR、表格还原、原文定位和异常文件处理。

### 一、确定性门禁

以下事项必须 `100%` 正确：

- 项目、采购包、文件和版本绑定正确；
- 文件来源、获取时间、文件哈希和有效状态完整；
- 原招标文件、更正公告、补充文件和最新版本之间的覆盖关系正确；
- 旧版本失效后，不再进入后续提取、生成、审核和交接流程；
- 加密、损坏、缺页或不支持的文件必须明确报错或阻断，不能静默跳过；
- 解析结果能够定位到原文件、页码、段落或表格位置。

任一确定性门禁失败，质量域 A 不通过，项目不得继续进入依赖该文件的自动生成流程。

### 二、高风险质量指标

在隐藏正式验收项目中，以下高风险错误数量必须为 `0`：

- 关键招标文件、补充文件或更正公告漏接入；
- 项目名称、项目编号、采购人、预算、最高限价、投标截止时间等关键字段解析错误；
- 资格条件、否决项、评分办法、报价表所在页面漏解析；
- OCR 低置信度或无法确认的内容被系统直接作为确定事实使用；
- 补充文件与原文件存在冲突，但系统没有识别、提示或进入人工确认；
- 解析内容与原文定位不一致，导致后续无法核验来源。

对于高风险字段和关键页面，不允许使用普通正文的平均准确率抵消关键错误。

### 三、普通质量指标

以下指标先纳入质量域 A 评估卡，但具体发布阈值标记为“待 5 个开发冒烟项目校准”：

- 普通正文文字识别准确率；
- 表格结构还原率；
- 标题和章节层级识别准确率；
- 图表、附件和跨页表格完整率；
- 版面阅读顺序正确率；
- 单文件解析耗时；
- 人工修正数量和修正耗时。

冒烟评测后，应根据可编辑 PDF、扫描 PDF、Word、Excel、图片附件等不同文件类型分别观察指标分布，不能把所有文件类型简单合并成一个平均值。

### 四、观察指标

持续记录：

- OCR 调用量和成本；
- 单页和单文件解析耗时；
- 解析重试次数；
- 人工接管次数；
- 低置信度内容数量；
- 不支持文件类型和失败原因分布。

这些指标用于发现解析瓶颈和制定后续优化计划，在没有稳定数据分布前不作为任意否决门槛。

### 五、失败处理规则

任一确定性门禁或高风险指标失败时：

```text
阻断依赖该文件的项目生成
→ 标记失败文件、失败类型和原文位置
→ 要求重新获取文件、补充文件或人工校正
→ 生成新的文件或解析结果版本
→ 重新执行质量域 A
→ 重新执行受影响的 L2 链路和 L3 质量域
```

不得在缺少关键文件、关键页面解析失败或版本关系不明确时继续生成正式标书内容。

### 用户确认

用户确认质量域 A 采用以上初始阈值模板。

用户确认项目文件、采购包、版本、哈希、有效状态和原文定位采用确定性门禁；关键文件、关键字段、关键页面和版本冲突采用高风险零错误要求；普通解析质量指标在 5 个开发冒烟项目后校准。

用户确认质量域 A 的门禁或高风险指标失败时，应阻断依赖该文件的后续生成，并在修复后重新评测受影响范围。

### 完成结论

- 质量域 A 首先保证文件完整、版本正确、关键内容不漏和结果可追溯；
- 关键字段与关键页面不使用平均准确率掩盖错误；
- 普通 OCR、版面和表格指标根据不同文件类型进行冒烟校准；
- 不确定内容必须显式标记并交由人工确认；
- 失败后需要生成新版本并重新评测受影响链路。

**讨论点完成。**

---

## 四十九、评估体系讨论 27：质量域 B 要求与响应矩阵初始阈值模板

### 讨论对象

本次为 L3 七个核心质量域中的质量域 B 制定初始阈值模板。

质量域 B 覆盖招标要求提取、要求分类、原子化、评分项识别、冲突识别，以及要求与章节、证据、责任人和响应状态之间的响应矩阵。

### 一、确定性门禁

以下事项必须 `100%` 满足：

- 每条要求具有唯一编号、要求类型、原文位置和适用采购包；
- 资格条件、否决项、实质性要求和评分项必须进入响应矩阵；
- 每条高风险要求必须关联响应状态、责任人、响应章节和证据状态；
- “未覆盖、部分覆盖、待确认、无法响应”不能被标记为“已覆盖”；
- 文件版本变化后，受影响的要求、评分项和响应关系必须自动失效或重新确认；
- 人工修改、确认和状态变化必须保留版本与审计记录。

任一确定性门禁失败，质量域 B 不通过，项目不得进入正式标书生成或审核通过状态。

### 二、高风险质量指标

在 15 个隐藏正式验收项目中，以下高风险错误数量必须为 `0`：

- 资格条件、否决项或实质性要求漏提取；
- 强制要求被错误识别为普通建议；
- 评分项、分值、评分规则或评分材料出现关键错误；
- 无法满足、部分满足或证据不足的要求被错误判定为满足；
- 招标文件中的关键冲突要求没有被识别和提示；
- 系统自行编造招标要求、评分规则或响应依据；
- 高风险要求的原文定位错误，导致人工无法核验。

因此，高风险要求召回率实际要求为 `100%`。普通要求识别结果再好，也不能抵消任何一条高风险要求漏项或错误肯定。

### 三、普通质量指标

以下指标先纳入质量域 B 评估卡，但具体发布阈值标记为“待 5 个开发冒烟项目校准”：

- 普通技术要求召回率；
- 普通商务要求召回率；
- 要求类型分类准确率；
- 复合要求原子化准确率；
- 重复要求识别与合并准确率；
- 普通冲突和歧义识别率；
- 要求与章节、证据和责任人的映射准确率；
- 响应矩阵人工修正数量和修正耗时。

冒烟评测时应分别统计资格、否决、实质性、评分、技术、商务、格式、时间和附件等不同要求类型，不能只报告所有要求合并后的平均结果。

### 四、观察指标

持续记录：

- 单项目提取的要求数量；
- 人工新增、删除、拆分、合并和改分类的数量；
- 待确认、部分覆盖和无法响应要求数量；
- 要求冲突数量及人工处理时间；
- 响应矩阵自动映射率；
- 版本变化导致的失效和重确认数量。

这些指标用于识别提取粒度、分类规则和矩阵工作量问题，在没有稳定业务分布前不直接作为否决门槛。

### 五、失败处理规则

任一确定性门禁或高风险指标失败时：

```text
阻断正式标书生成或审核通过
→ 定位遗漏、误判、冲突或错误映射的要求
→ 人工修正要求、分类和响应状态
→ 更新响应矩阵版本
→ 重新生成受影响章节
→ 重新执行响应矩阵检查
→ 对受影响章节执行增量审核
```

不得在资格条件、否决项、实质性要求或关键评分项尚未确认完整时，将项目判定为可以正式交接。

### 用户确认

用户确认质量域 B 采用以上初始阈值模板。

用户确认响应矩阵的高风险要求完整性、状态真实性、版本传播和审计采用确定性门禁；资格、否决、实质性要求、关键评分项和错误肯定采用高风险零错误要求；普通要求提取、分类、原子化和映射指标在 5 个开发冒烟项目后校准。

用户确认质量域 B 的门禁或高风险指标失败时，应阻断正式标书生成或审核通过，并在修正矩阵和受影响章节后重新评测。

### 完成结论

- 质量域 B 首先保证高风险要求一条不漏、响应状态真实、原文能够核验；
- 高风险要求召回率要求为 `100%`，不能由普通要求平均结果抵消；
- 普通要求质量按类型分别统计并通过冒烟项目校准；
- 版本变化必须传播到要求、评分项、响应矩阵和相关章节；
- 失败后需要修正矩阵、重新生成受影响内容并执行增量审核。

**讨论点完成。**

---

## 五十、评估体系讨论 28：质量域 C 企业证据与历史参考初始阈值模板

### 讨论对象

本次为 L3 七个核心质量域中的质量域 C 制定初始阈值模板。

质量域 C 覆盖企业证据检索、权限控制、主体和有效期判断、事实引用、无证据拒答、历史项目推荐、历史内容复用和差异说明。

### 一、数据边界

由于甲方暂时不能提供完整历史企业资料，L3 评测使用：

```text
可控模拟企业资料
+ 模拟历史投标项目
+ 公开采购文件
+ 人工构造的过期、缺失、冲突和越权样本
```

该数据策略可以验证证据机制、拒答机制、权限机制、版本机制和历史复用边界，但不能证明甲方真实知识库的最终检索效果，也不能直接证明生产环境中的效率收益。

### 二、确定性门禁

以下事项必须 `100%` 满足：

- 企业证据关联正确的企业主体、资料类型、有效期和适用范围；
- 每项事实引用能够定位到具体文件、页码、段落或数据记录；
- 过期、失效、撤回或不适用资料不得作为有效证据使用；
- 历史标书参考与企业事实证据必须明确区分；
- 没有证据时只能标记为“待补充”或“待确认”，不能直接生成确定性结论；
- 历史内容复用必须记录来源、修改情况、适用范围和人工确认；
- 用户只能检索和引用其权限范围内的资料。

任一确定性门禁失败，质量域 C 不通过，受影响事实和章节不得进入正式交接。

### 三、高风险质量指标

在 15 个隐藏正式验收项目中，以下高风险错误数量必须为 `0`：

- 引用错误企业主体的资质、业绩、人员或证书；
- 将过期、失效或适用范围不符的资料判断为有效；
- 将不满足项目范围要求的历史业绩作为合格业绩；
- 生成不存在的企业事实、证书编号、人员经历或项目业绩；
- 引用内容与生成结论不一致；
- 未经授权访问、引用或泄露其他项目资料；
- 将历史方案直接作为当前项目承诺；
- 无证据内容被错误标记为“已证实”。

资格、业绩、人员、证书和承诺等关键事实的引用正确率要求为 `100%`，无证据错误肯定数量要求为 `0`。

### 四、普通质量指标

以下指标先纳入质量域 C 评估卡，但具体发布阈值标记为“待 5 个开发冒烟项目校准”：

- 企业证据召回率和排序质量；
- 历史项目推荐相关性；
- 证据重复识别与合并准确率；
- 历史内容差异说明质量；
- 推荐理由与实际资料的一致性；
- 证据与响应章节的自动映射率；
- 人工补充、替换和拒绝证据的数量；
- 资料补充任务完成时间。

企业证据与历史参考应分开统计，不能用历史项目推荐结果掩盖企业事实证据漏召回或引用错误。

### 五、观察指标

持续记录：

- 证据检索耗时；
- 检索、重排和模型调用成本；
- 无证据拒答率；
- 待补充资料任务数量；
- 用户打开引用和查看原文的次数；
- 历史推荐被采纳、修改和拒绝的数量；
- 权限拦截和越权访问告警数量。

这些指标用于后续优化检索、权限和资料治理，在缺少甲方真实基线时不直接设为任意否决门槛。

### 六、失败处理规则

任一确定性门禁或高风险指标失败时：

```text
阻断受影响事实或章节
→ 标记错误、缺失、过期、越权或不适用证据
→ 创建资料补充或人工确认任务
→ 更新证据、权限和引用版本
→ 重新生成受影响内容
→ 重新执行事实、权限和引用检查
```

不得用历史标书内容填补当前项目缺少的企业事实，也不得把系统“找不到证据”改写成“企业不具备该能力”。

### 用户确认

用户确认质量域 C 采用以上初始阈值模板和数据边界。

用户确认企业证据的主体、有效期、适用范围、权限、原文引用和无证据处理采用确定性门禁；关键资质、业绩、人员、证书和承诺采用高风险零错误要求；普通检索、历史推荐和差异说明指标在 5 个开发冒烟项目后校准。

用户确认质量域 C 的门禁或高风险指标失败时，应阻断受影响事实和章节，并在补充或确认资料后重新评测。

### 完成结论

- 质量域 C 首先保证企业事实可证实、可定位、未过期且权限合法；
- 历史参考不能替代当前企业证据，也不能自动变成当前项目承诺；
- 无证据时必须拒答、待补充或待确认，不能编造；
- 公开资料、模拟资料和未来真实试点资料必须严格区分；
- 失败后需要修正证据版本、重新生成受影响内容并复核引用。

**讨论点完成。**

---

## 五十一、评估体系讨论 29：质量域 D 报价与跨章节一致性初始阈值模板

### 讨论对象

本次为 L3 七个核心质量域中的质量域 D 制定初始阈值模板。

质量域 D 覆盖报价数据输入、Excel 或内部模型结果接入、报价规则校验、方案推荐、人工确认、报价版本冻结和跨章节一致性。

### 一、基本边界

- V1 不替代 Excel 或企业内部报价模型；
- Excel 或内部模型继续承担复杂、灵活的商务计算；
- 系统负责参数、来源、公式、结果、异常和确认版本管理；
- 大模型可以提供候选报价方案、推荐选择和推荐理由；
- 最终报价由商务人员和项目负责人确认，Agent 不拥有最终决定权。

### 二、确定性门禁

以下事项必须 `100%` 满足：

- 报价输入、数据来源、Excel 模板、公式和计算结果具有明确版本；
- 预算、最高限价、税率、数量、单价、合计和大小写金额校验正确；
- 超预算、超最高限价或计算错误的方案必须自动判定为无效；
- 最终报价必须经过规定角色确认，特殊金额或特殊策略按规则增加授权；
- 标书只能引用已经冻结的确认报价版本；
- 报价表、开标一览表、商务章节和其他引用位置必须使用同一报价版本；
- 报价修改后，所有旧版本引用必须自动失效并进入重新生成或复核；
- Agent 不得擅自修改、覆盖或确认最终报价。

任一确定性门禁失败，质量域 D 不通过，项目不得进入审核通过、交接或递交记录状态。

### 三、高风险质量指标

在 15 个隐藏正式验收项目中，以下高风险错误数量必须为 `0`：

- 报价超过预算或最高限价；
- 单价、数量、税率、折扣或合计计算错误；
- 大小写金额不一致；
- 元、万元等金额单位使用错误；
- 含税、不含税或税率口径不一致；
- 标书不同章节、表格或附件引用不同报价版本；
- 使用未确认、已失效或由无权限人员确认的报价；
- 推荐理由与实际报价数据、规则或计算结果不一致；
- 系统将推荐方案直接当成最终报价；
- 报价变化后，受影响章节没有重新生成或复核。

关键报价字段、预算限价校验和跨章节最终金额一致性要求为 `100%` 正确。

### 四、普通质量指标

以下指标先纳入质量域 D 评估卡，但具体发布阈值标记为“待 5 个开发冒烟项目校准”：

- Excel 输入字段自动映射准确率；
- 报价异常识别准确率；
- 推荐方案合理性；
- 推荐解释完整性和可理解性；
- 同类项目报价参考相关性；
- 报价敏感性分析质量；
- 人工修改参数、结果和推荐选择的次数；
- 跨章节报价更新耗时。

报价计算正确性和报价推荐质量必须分开统计。系统即使推荐质量一般，只要未越权且所有规则校验正确，可以继续内部优化；但任何最终报价错误都属于阻断问题。

### 五、观察指标

持续记录：

- 报价方案数量；
- 无效方案数量和原因；
- 商务人员修改参数和结果的次数；
- 推荐方案被采纳、修改和拒绝的数量；
- 报价确认耗时；
- 报价变化触发的章节和表格数量；
- 跨章节更新和增量复核耗时。

这些指标用于后续评估报价工作量和推荐价值，在缺少真实业务基线时不直接作为否决门槛。

### 六、失败处理规则

任一确定性门禁或高风险指标失败时：

```text
阻断标书生成、审核通过或交接
→ 标记报价错误和所有受影响位置
→ 返回商务人员调整、重新计算或重新确认
→ 生成新的报价版本
→ 重新生成全部受影响章节、表格和附件
→ 执行跨章节一致性检查
→ 执行商务增量复核
```

不得只修改某一个报价表而保留其他章节中的旧金额，也不得通过人工口头确认绕过系统版本记录。

### 用户确认

用户确认质量域 D 采用以上初始阈值模板和报价责任边界。

用户确认报价版本、规则校验、无效方案排除、人工确认和跨章节一致性采用确定性门禁；预算限价、计算、税率、金额单位、大小写金额、最终版本和越权确认采用高风险零错误要求；普通映射、推荐和敏感性分析指标在 5 个开发冒烟项目后校准。

用户确认质量域 D 的门禁或高风险指标失败时，应阻断生成、审核通过或交接，并在产生新报价版本后重新生成和复核所有受影响内容。

### 完成结论

- 质量域 D 首先保证最终报价合法、计算正确、人工确认且全标书一致；
- Excel 或内部模型负责计算，系统负责接入、校验、版本、确认和引用；
- 大模型只提供推荐，不拥有最终报价决定权；
- 推荐效果与最终报价安全分开评估；
- 报价变化必须传播到所有引用位置并触发增量复核。

**讨论点完成。**

---

## 五十二、评估体系讨论 30：质量域 E 章节、图表和说明材料初始阈值模板

### 讨论对象

本次为 L3 七个核心质量域中的质量域 E 制定初始阈值模板。

质量域 E 覆盖标书章节生成、模板应用、图表、正式附件、对外说明材料、内部备注、人工编辑保护和文件导出。

### 一、确定性门禁

以下事项必须 `100%` 满足：

- 招标文件要求的必备章节、表格、附件和说明文件完整；
- 标书目录、章节结构和格式使用已经确认的模板版本；
- 每个高风险要求能够映射到具体响应章节；
- 企业事实、项目数据和报价数据具有明确来源和版本；
- 图表记录数据来源、统计时间和数据版本；
- 材料必须正确分类为 `BID_ATTACHMENT`、`EXTERNAL_EXPLANATION` 或 `INTERNAL_NOTE`；
- `INTERNAL_NOTE` 不得进入对外标书、正式交接包或客户可见文件；
- 人工锁定、确认或签署的内容不得被 Agent 静默覆盖；
- 导出文件必须与审核通过的内容版本一致，并记录文件哈希。

任一确定性门禁失败，质量域 E 不通过，项目不得进入正式交接。

### 二、高风险质量指标

在 15 个隐藏正式验收项目中，以下高风险错误数量必须为 `0`：

- 必备章节、法定声明、承诺函或关键附件缺失；
- 章节遗漏或错误响应资格条件、否决项或实质性要求；
- 企业名称、项目名称、项目编号等主体信息错误；
- 生成无证据支撑的企业能力、业绩、人员或承诺；
- 报价、工期、人员、服务范围等关键内容跨章节冲突；
- 图表关键数据与正文、来源或当前版本不一致；
- 内部风险、内部批注、审核意见或敏感信息进入对外文件；
- Agent 覆盖用户已经锁定或确认的正式内容；
- 导出错误标书版本或混入失效附件。

关键章节完整性、关键事实正确性、内外材料隔离和最终导出版本一致性要求为 `100%` 正确。

### 三、普通质量指标

以下指标先纳入质量域 E 评估卡，但具体发布阈值标记为“待 5 个开发冒烟项目校准”：

- 普通要求章节覆盖率；
- 章节结构和逻辑完整性；
- 团队模板和格式符合度；
- 语言清晰度、重复率和人工改写量；
- 图表可读性和图表引用准确率；
- 说明材料完整性；
- Word 和 PDF 导出还原度；
- 人工排版和格式修正耗时。

内容质量与格式质量需要分别统计。格式表现较好不能抵消事实错误或关键要求漏响应，内容正确也不能允许必备格式和附件缺失。

### 四、观察指标

持续记录：

- 自动生成章节比例；
- 人工新增、删除和重写内容比例；
- 被锁定章节和锁定内容数量；
- 图表自动生成和人工修改数量；
- 附件和说明材料补充数量；
- 导出失败、重试和人工调整次数；
- 单项目排版和导出耗时。

这些指标用于后续评估自动生成和格式处理价值，在缺少真实业务基线时不直接作为否决门槛。

### 五、失败处理规则

任一确定性门禁或高风险指标失败时：

```text
阻断审核通过或正式交接
→ 定位缺失章节、错误引用、冲突内容或材料分类
→ 修正数据、模板、章节、图表或附件
→ 生成新的标书版本
→ 重新执行章节覆盖和跨章节一致性检查
→ 对受影响内容执行增量审核
→ 重新导出并核对文件版本与哈希
```

不得通过手工替换最终文件而绕过系统版本、审核和哈希记录，也不得将内部风险提示直接复制到对外正式材料。

### 用户确认

用户确认质量域 E 采用以上初始阈值模板。

用户确认必备章节、模板版本、来源版本、材料分类、内部与外部内容隔离、人工编辑保护和导出版本采用确定性门禁；关键章节、主体信息、事实承诺、跨章节一致性、敏感信息隔离和最终导出采用高风险零错误要求；普通结构、语言、图表、格式和导出质量指标在 5 个开发冒烟项目后校准。

用户确认质量域 E 的门禁或高风险指标失败时，应阻断审核通过或正式交接，并在生成新标书版本后重新检查、增量审核和导出。

### 完成结论

- 质量域 E 首先保证必备内容完整、关键事实正确、材料分类清楚和导出版本一致；
- 内部风险、审核意见和敏感信息不得进入对外文件；
- 人工锁定或确认内容不得被 Agent 静默覆盖；
- 内容质量、格式质量和导出质量分别评估；
- 失败后必须生成新版本并重新审核受影响内容。

**讨论点完成。**

---

## 五十三、评估体系讨论 31：质量域 F 风险、审核与返工初始阈值模板

### 讨论对象

本次为 L3 七个核心质量域中的质量域 F 制定初始阈值模板。

质量域 F 覆盖 Agent 预检、风险提示、审核角色路由、结构化审核意见、问题关闭、风险豁免、结构化返工、变更影响分析和增量复核。

### 一、基本边界

- Agent 负责预检、风险识别、问题证据整理、修改建议和影响分析；
- 项目负责人、商务、法务、技术等人员负责各自权限范围内的审核和最终判断；
- Agent 不能自行关闭阻断问题；
- Agent 不能代替人工批准风险豁免；
- 人工审核结果必须保留版本和审计记录。

### 二、确定性门禁

以下事项必须 `100%` 满足：

- 每个审核问题记录问题类型、风险等级、证据、原文位置、影响内容、责任人和处理状态；
- 阻断问题未关闭时，不得审核通过、正式交接或记录为可递交；
- 商务、法务、技术和项目负责人问题必须路由到正确角色；
- 只有授权人员可以关闭问题或批准风险豁免；
- 审核意见必须关联具体标书版本、章节和内容位置；
- 修改后必须执行影响分析；
- 受影响内容必须重新预检和增量复核；
- 问题创建、分配、修改、关闭、豁免和重新打开均保留审计记录。

任一确定性门禁失败，质量域 F 不通过，项目不得进入正式交接。

### 三、高风险质量指标

在 15 个隐藏正式验收项目中，以下高风险错误数量必须为 `0`：

- 资格条件、否决项、报价、关键承诺等阻断风险漏报；
- 阻断风险被错误降级为普通提示；
- Agent 未经人工确认自动关闭问题；
- 无权限人员关闭问题或批准风险豁免；
- 存在未解决阻断问题的标书进入交接状态；
- 修改关键内容后绕过重新审核；
- 审核通过后内容发生变化，但原审核状态继续有效；
- 风险证据、原文位置或影响范围指向错误内容；
- 风险已经失效或条件改变，但系统没有重新评估问题状态。

阻断风险召回、问题关闭权限和审核状态有效性要求为 `100%` 正确。

### 四、普通质量指标

以下指标先纳入质量域 F 评估卡，但具体发布阈值标记为“待 5 个开发冒烟项目校准”：

- 中低风险问题召回率和准确率；
- 风险误报率；
- 修改建议正确性和可执行性；
- 审核角色路由准确率；
- 审核意见结构化完整率；
- 变更影响范围识别准确率；
- 增量复核范围准确率；
- 审核退回轮次和问题关闭耗时。

风险召回和修改建议质量必须分开统计。Agent 可以识别出正确风险但给出不合适的修改建议，此时风险记录仍然有效，但建议不得自动应用，必须由人工决定。

### 五、观察指标

持续记录：

- 各风险等级问题数量；
- Agent 发现、人工新增和人工驳回的问题数量；
- 审核角色处理时间；
- 单问题往返次数；
- 风险豁免数量和原因；
- 增量复核覆盖范围；
- 全量复核与增量复核耗时差异；
- 问题重新打开和重复出现次数。

这些指标用于评估预检价值、审核工作量和返工效率，在缺少真实业务基线时不直接作为否决门槛。

### 六、失败处理规则

任一确定性门禁或高风险指标失败时：

```text
阻断审核通过或正式交接
→ 恢复或重新打开相关问题
→ 分配给具有权限的审核角色
→ 修正内容或按流程记录风险豁免
→ 生成新的标书版本
→ 重新执行 Agent 预检和变更影响分析
→ 完成受影响范围的增量复核
```

如果无法可靠判断变更影响范围，应执行全量复核，不能为了节省时间而缩小复核范围。

### 用户确认

用户确认质量域 F 采用以上初始阈值模板和人机责任边界。

用户确认问题结构、审核角色、关闭权限、风险豁免、版本关联、影响分析和增量复核采用确定性门禁；阻断风险漏报、错误降级、自动关闭、越权关闭、绕过复核和审核状态失效采用高风险零错误要求；普通风险识别、修改建议、路由和增量范围指标在 5 个开发冒烟项目后校准。

用户确认质量域 F 的门禁或高风险指标失败时，应阻断审核通过或正式交接，并在重新打开问题、修正内容和完成相应复核后重新评测。

### 完成结论

- 质量域 F 首先保证阻断风险不漏、问题不被越权关闭、审核状态与当前版本一致；
- Agent 提供预检和建议，人工负责审核、关闭和风险接受；
- 风险识别结果与修改建议质量分别评估；
- 内容变化必须执行影响分析和受影响范围复核；
- 不能可靠确定影响范围时必须执行全量复核。

**讨论点完成。**

---

## 五十四、评估体系讨论 32：质量域 G 版本、交接与递交记录初始阈值模板

### 讨论对象

本次为 L3 七个核心质量域中的质量域 G 制定初始阈值模板。

质量域 G 覆盖版本失效、项目状态转换、交接前置条件、交接基线、受控链接、接收确认，以及人工外部递交后的状态和回执记录。

### 一、V1 边界

- 系统完成内部受控交接；
- 交接接收人是投标递交责任人；
- 外部递交仍由人工执行；
- 系统记录递交结果、递交时间、回执和异常；
- V1 不自动操作外部采购平台，不把内部交接等同于外部递交成功。

### 二、确定性门禁

以下事项必须 `100%` 满足：

- 项目只能按照规定状态机转换，非法或越权转换必须被拦截；
- 内容变化后，受影响的审核、导出和交接结果必须自动失效；
- 阻断问题全部关闭并完成规定审核后，才能建立交接基线；
- 交接包记录项目、采购包、标书版本、文件清单、文件哈希和审核状态；
- 已接受风险、注意事项和递交清单必须随交接包传递；
- 交接对象、交接时间、权限范围和接收确认完整；
- 接收人必须确认获得的是最新有效版本；
- 受控链接必须进行访问权限和有效期控制；
- 递交状态和回执只能依据人工操作记录；
- 替换、撤回、失败、重试和更正记录不能覆盖原历史。

任一确定性门禁失败，质量域 G 不通过，项目不得被记录为完成交接或递交成功。

### 三、高风险质量指标

在 15 个隐藏正式验收项目中，以下高风险错误数量必须为 `0`：

- 未审核、已失效或旧版本被交接；
- 存在未关闭阻断问题时完成交接；
- 交接给错误人员、错误项目或错误采购包；
- 文件清单、版本或哈希与实际文件不一致；
- 内容变化后，原交接状态继续有效；
- 未授权人员访问交接包；
- 系统自动对外递交投标文件；
- 没有人工记录或回执时，系统声称递交成功；
- 非法状态转换未被拦截；
- 撤回、更正、失败或重试覆盖原始记录。

合法状态转换、交接版本、交接权限、递交真实性和历史留痕要求为 `100%` 正确。

### 四、普通质量指标

以下指标先纳入质量域 G 评估卡，但具体发布阈值标记为“待 5 个开发冒烟项目校准”：

- 交接清单完整率；
- 接收人确认操作的易用性；
- 交接包生成耗时；
- 版本失效通知准确率；
- 递交记录填写完整率；
- 历史版本和审计记录查询效率；
- 交接失败后的恢复耗时。

交接效率和交接安全必须分开统计。交接速度较快不能抵消错误版本、错误接收人或缺少审核等严重问题。

### 五、观察指标

持续记录：

- 单项目交接次数；
- 接收确认耗时；
- 版本失效和重新交接次数；
- 受控链接访问、过期和权限拦截记录；
- 递交失败、重试、更正和撤回数量；
- 人工补录递交状态和回执的时间；
- 交接后发现问题并退回的数量。

这些指标用于后续评估交接效率、使用体验和外部递交记录完整性，在缺少真实业务基线时不直接作为否决门槛。

### 六、失败处理规则

任一确定性门禁或高风险指标失败时：

```text
撤销或失效原交接
→ 回退到合法项目状态
→ 通知编写人、审核人和递交责任人
→ 修正内容、版本、权限或交接对象
→ 重新完成必要审核
→ 建立新的交接基线
→ 生成新交接记录并保留原记录
```

递交记录发生错误时必须通过更正记录修复，不能直接覆盖原始记录；如果无法证明已经完成外部递交，系统状态只能保持为待确认或递交异常。

### 用户确认

用户确认质量域 G 采用以上初始阈值模板和 V1 外部递交边界。

用户确认状态转换、版本失效、交接前置条件、交接基线、文件哈希、权限、接收确认和递交记录采用确定性门禁；错误版本、未审核交接、错误接收人、越权访问、虚假递交成功和覆盖历史记录采用高风险零错误要求；普通交接体验、通知和查询指标在 5 个开发冒烟项目后校准。

用户确认质量域 G 的门禁或高风险指标失败时，应撤销或失效原交接，回退到合法状态，并在重新审核后建立新的交接基线，同时保留全部历史记录。

### 完成结论

- 质量域 G 首先保证状态合法、交接版本正确、权限受控和递交记录真实；
- V1 完成内部受控交接，不自动执行外部递交；
- 内部交接成功不能被记录为外部递交成功；
- 内容或版本变化必须使原审核和交接结果失效；
- 任何失败、更正、撤回和重试都必须保留原始历史记录。

**讨论点完成。**

---

## 五十五、评估体系讨论 33：L4 试点业务价值指标框架

### 讨论对象

在完成 L3 技术正确性和单项目闭环评估后，进入 L4 试点业务价值评估。L4 重点判断系统是否真正改善标书编写、审核和内部交接工作，而不是只判断 Agent 能否生成内容。

### 一、效率指标

记录：

- 项目创建到首稿生成时间；
- 首稿到审核版本完成时间；
- 审核版本到内部交接完成时间；
- 人工查询、整理、上传和重复录入次数；
- 审核退回轮次；
- 用户和审核角色的人工总投入时间。

### 二、质量指标

记录：

- 关键要求漏报率；
- 阻断风险漏报率；
- 报价和跨章节错误率；
- 首次审核通过率；
- 标书返工率；
- 版本、来源和审核记录完整率。

质量指标优先关注严重错误、返工和审核结果，不用单纯的文字相似度代替业务质量。

### 三、风险与控制指标

记录：

- 版本错误次数；
- 未审核交接次数；
- 越权访问次数；
- 内部信息进入外部文件的次数；
- 阻断问题未关闭而继续流转的次数；
- 审计记录完整率。

其中版本错误、越权访问、内部信息外泄和未审核交接属于零容忍类业务风险。

### 四、用户工作量与使用指标

记录：

- 人工确认次数；
- 人工修改比例；
- Agent 建议采纳、修改和拒绝比例；
- 人工接管次数；
- 编写人、负责人、商务和法务人员的使用反馈；
- 用户对结果可控性、可解释性和工作负担的评价。

用户满意度作为辅助指标，不替代客观质量和安全结果。

### 五、成本与稳定性指标

记录：

- 单项目模型、OCR、检索、重排、存储和导出成本；
- 单项目失败和重试次数；
- 系统可用性；
- 失败恢复时间；
- 单项目综合处理成本。

在 L4 初期，这些指标主要用于识别系统瓶颈、估算规模化成本和判断是否具备推广条件。

### 六、中标率的边界

V1 不把中标率作为主要验收指标。中标结果还会受到报价、竞争对手、采购人偏好、预算变化、市场环境和项目策略等因素影响，不能直接归因于标书 Agent。

中标率可以作为长期业务观察指标，但不能在小样本试点中直接作为系统通过或不通过的依据。

### 用户确认

用户确认 L4 采用“效率、质量、风险与控制、用户工作量与使用、成本与稳定性”五类业务价值指标。

用户确认 L4 应同时记录 Agent 辅助流程和人工工作量，不使用单一综合分判断价值；中标率作为长期观察指标，不作为 V1 主要验收门槛。

### 完成结论

- L4 评估系统是否改善真实业务流程，而不只是评估模型输出质量；
- 效率、质量、安全、人工工作量和成本需要同时观察；
- 严重风险不能被效率提升抵消；
- 用户满意度不能替代客观业务结果；
- 中标率不作为 V1 的主要验收门槛。

**讨论点完成。**

---

## 五十六、评估体系讨论 34：L4 双轨旁路与前瞻性配对试点

### 讨论背景

甲方目前不能提供完整、统一口径的历史项目数据，因此无法直接使用历史项目证明 Agent 带来的真实效率、质量和成本变化。L4 需要通过前瞻性真实项目建立人工基线和 Agent 辅助流程基线。

### 一、候选方案

#### 方案 A：历史前后对比

使用系统上线前的历史项目与上线后的 Agent 项目进行对比。

优点是操作简单；缺点是当前缺少完整历史数据，而且项目难度、人员经验、时间压力和招标要求可能不同，容易产生错误归因。

#### 方案 B：前瞻性配对试点

将项目类型、文件规模、要求数量和时间紧迫度接近的真实项目进行配对：

```text
人工常规流程项目
对比
Agent 辅助流程项目
```

两组使用相同口径记录效率、质量、风险、人工工作量和成本。

该方案不要求同一个项目做两遍，较适合正式业务试点，但需要做好项目匹配和差异记录。

#### 方案 C：同项目双轨旁路

同一个真实项目分别由人工常规流程和 Agent 辅助流程处理，再比较结果。

优点是项目条件最一致；缺点是工作量接近翻倍，两个流程之间可能互相影响，不适合长期或大规模使用。

该方案适合少量项目进行指标采集验证和早期正确性检查。

### 二、推荐方案

采用“少量同项目双轨旁路验证指标，再进行前瞻性配对试点”的组合方案：

1. 先选择少量真实项目执行双轨旁路，验证指标定义、采集方式和人工计时是否可执行；
2. 指标采集稳定后，正式试点采用前瞻性配对方式；
3. 按项目类型、文件规模、要求数量、参与角色和时间紧迫度进行匹配；
4. 人工组和 Agent 组使用相同的质量、风险、工作量和成本记录口径；
5. 试点开始前冻结指标定义、数据采集方法和排除规则；
6. 试点过程中不得因为结果不理想而修改计算方式；
7. L1、L2 和 L3 未通过时，不进入 L4 价值试点；
8. 出现零容忍风险时，即使效率明显提升，也不能判定试点通过。

### 三、样本建议

- 探索性双轨旁路：至少 `5` 个真实项目；
- 正式配对试点：建议 Agent 组与人工组各不少于 `10` 个真实项目；
- 样本应覆盖不同文件规模、要求数量和时间压力；
- 同一项目不能同时被重复计入多个独立样本；
- 样本不足时只能形成趋势性或探索性结论，不能宣称已经充分证明业务价值。

样本数量可根据甲方真实项目量和试点周期调整，但任何调整都必须在查看最终结果前完成并留痕。

### 四、旁路试点安全边界

- 旁路阶段的 Agent 结果默认不直接作为正式递交文件；
- 必须由真实业务人员审核后，才能决定是否采纳 Agent 结果；
- Agent 组仍保留全部人工确认、审核和交接门禁；
- 任何严重错误都需要进入 L1 至 L3 的失败样本和回归集；
- 不能将探索性试点结果包装为正式生产收益。

### 用户确认

用户确认 L4 采用“少量双轨旁路验证指标，再进行前瞻性配对试点”的组合方案。

用户确认探索性旁路建议至少使用 `5` 个真实项目，正式配对试点建议 Agent 组与人工组各不少于 `10` 个项目；样本不足时只能形成趋势性结论。

用户确认 L1 至 L3 未通过时不进入 L4，且零容忍风险不能被效率提升抵消。

### 完成结论

- L4 通过前瞻性真实项目建立人工和 Agent 辅助流程基线；
- 少量双轨旁路用于验证指标采集，正式业务结论主要来自配对试点；
- 两组必须使用一致的指标、采集方法和排除规则；
- 试点方案和指标必须在查看最终结果前冻结；
- 样本不足时不能夸大业务价值结论。

**讨论点完成。**

---

## 五十七、评估体系讨论 35：V1 交付验收与 L4 业务价值验证分离

### 用户提出的问题

用户指出：L4 很可能因为甲方真实项目数据和试点样本不足而无法通过。如果继续把 L4 通过作为 V1 软件最终验收的必要条件，即使系统已经开发完成并通过技术评估，也可能因为无法控制的数据条件而长期无法验收。

### 一、问题判断

该风险成立。

L1 至 L3 主要验证系统是否具备正确、安全、完整、可审核和可追溯的业务闭环，这些内容可以通过公开采购文件、受控模拟企业数据、隐藏验收项目和人工领域复核完成。

L4 验证真实项目中的效率、人工工作量、成本和业务价值，必须依赖甲方真实项目数量、试点周期、业务人员参与和完整数据采集。这些条件不完全由开发团队控制。

因此，不应因为 L4 样本不足而否定已经完成的软件开发和交付成果。

### 二、候选方案

#### 方案 A：L1 至 L4 全部通过后完成 V1 验收

```text
L1 + L2 + L3 + L4 全部通过
→ V1 最终验收
```

该方案将真实项目数量和试点数据作为软件交付验收的必要条件，存在项目长期无法验收的风险，不采用。

#### 方案 B：软件交付验收与业务价值验证分离

```text
L1 + L2 通过
→ V1 技术完成

L3 通过
→ V1 交付验收通过、试点就绪

L4 通过
→ V1 业务价值验证通过、允许扩大使用
```

该方案使软件交付质量和真实业务收益分别承担不同的证明责任。

#### 方案 C：使用模拟数据替代 L4

使用 L3 模拟评测数据推算真实效率和业务收益。该方案无法证明真实业务价值，容易形成虚假收益结论，不采用。

### 三、方案 B 的具体规则

1. `L1` 和 `L2` 通过，判定 V1 技术完成；
2. `L3` 通过，判定 V1 软件交付验收通过，并达到真实项目受控旁路试点条件；
3. `L4` 独立验证真实业务价值，决定是否扩大使用或规模化推广；
4. L4 样本不足时，结论为“L4 待继续评测”；
5. “L4 待继续评测”不撤销已经通过的 V1 软件交付验收；
6. L4 未通过或待评测时，不得宣称已经证明真实效率提升、成本下降或业务价值；
7. L4 发现新的零容忍缺陷时，仍需退回受影响的 L1、L2 或 L3 重新评估；
8. 只有 L4 通过，才能判定“V1 业务价值验证通过”并建议扩大使用。

### 四、术语调整

为避免“验收”一词混用，后续统一使用：

- **V1 技术完成**：L1 和 L2 通过；
- **V1 交付验收通过**：L3 通过；
- **V1 业务价值验证通过**：L4 通过；
- **L4 待继续评测**：真实样本不足或数据尚未形成稳定基线；
- **扩大使用决策**：依据 L4 结果决定是否扩大真实业务使用范围。

### 用户确认

用户选择并确认方案 B：将 V1 软件交付验收与 L4 业务价值验证分离。

用户确认 L3 通过即可完成 V1 软件交付验收并进入受控旁路试点；L4 数据不足不影响已经通过的交付验收，但不能宣称业务价值已经得到证明，也不能据此建议扩大使用。

### PRD 回写

本次同步将 PRD 从 `v2.3` 更新为 `v2.4`，修订评估分层、`AC-07` 和发布门槛：

- L3 对应“V1 交付验收通过、试点就绪”；
- L4 对应“V1 业务价值验证通过、扩大使用决策”；
- L4 待继续评测不撤销 V1 软件交付验收；
- L4 未通过或待评测时不得宣称已经实现真实业务价值。

### 完成结论

- 软件是否完成交付与真实业务价值是否得到证明分开判断；
- L3 通过是 V1 软件交付验收的完成条件；
- L4 是扩大使用前的业务价值验证，不是软件交付验收的阻塞条件；
- 真实数据不足时保持诚实的待评测状态，不虚构收益；
- PRD 已按该决策调整验收分层。

**讨论点完成。**

---

## 五十八、评估体系讨论 36：L4 三种正式结论

### 讨论背景

在将 L3 软件交付验收与 L4 业务价值验证分离后，需要明确 L4 最终可以形成哪些正式结论，以及这些结论是否影响已经通过的 L3 软件交付验收。

### 一、L4 三种正式结论

#### 1. L4 业务价值验证通过

适用条件：

- 真实试点样本和数据采集满足冻结要求；
- 未出现新的零容忍缺陷；
- 质量和安全不低于冻结的业务底线；
- 效率、审核、返工、工作量或成本等指标达到双方冻结的业务价值门槛。

该结论允许进入扩大使用范围或规模化推广决策，但不代表系统可以取消人工最终决策边界。

#### 2. L4 业务价值验证未通过

适用条件：

- 已获得足够且有效的正式试点样本；
- 评测已经完成；
- 结果未达到冻结的业务价值门槛。

该结论不撤销已经通过的 V1 软件交付验收和已验证技术能力，但不能宣称已经实现预期业务价值，也不能据此建议扩大使用。

#### 3. L4 待继续评测

适用条件：

- 真实项目样本不足；
- 数据采集不完整；
- 试点周期尚未结束；
- 尚未形成稳定、可比较的人工基线；
- 其他原因导致无法形成有效业务价值结论。

该结论不等于通过，也不等于未通过；不撤销已经通过的 V1 软件交付验收，但不能虚构提升比例或宣称业务价值已经得到证明。

### 二、与 L3 软件交付验收的关系

L4 的业务价值结论本身不撤销已经完成的 L3 软件交付验收：

```text
L3 通过
→ V1 软件交付验收通过

L4 通过 / 未通过 / 待继续评测
→ 决定业务价值结论和扩大使用决策
→ 不直接改变 L3 交付验收结论
```

但保留独立的安全例外：如果 L4 真实试点发现新的零容忍缺陷，说明此前对应的 L1、L2 或 L3 结论可能不再成立。此时不是因为“L4 未通过”而撤销交付验收，而是因为发现了新的产品缺陷，需要按影响范围重新评估相关层级，并依据复评结果处理状态。

### 用户确认

用户确认 L4 使用“业务价值验证通过、业务价值验证未通过、待继续评测”三种正式结论。

用户确认 L4 的三种业务价值结论本身均不撤销已经完成的 L3 软件交付验收；同时保留零容忍缺陷触发 L1、L2 或 L3 重新评估的安全规则。

### PRD 回写

本次同步将 PRD 从 `v2.4` 更新为 `v2.5`，在“V1 业务价值验证与扩大使用”中明确三种 L4 结论及其与 L3 软件交付验收的关系。

### 完成结论

- L4 只使用通过、未通过和待继续评测三种正式结论；
- 数据不足必须使用待继续评测，不能强行判定通过或未通过；
- L4 结论用于业务价值和扩大使用决策；
- L4 结论本身不撤销 L3 软件交付验收；
- 新发现的零容忍缺陷仍会触发受影响层级重新评估。

**讨论点完成。**

---

## 五十九、评估体系讨论 37：建立正式评估体系与测试计划文档

### 用户确认

在完成 L1、L2、L3 和 L4 的核心评估设计后，用户确认将已讨论内容整理为独立的正式评估体系与测试计划文档，供后续技术方案、开发计划、测试实现和项目验收引用。

### 文档编号

现有 `14-面试讲解话术.md` 已占用编号 `14`，因此正式评估文档使用：

```text
specs/15-评估体系与测试计划.md
```

### 正式文档内容

文档 `v1.0` 包含：

- 四层评估与发布关系；
- 开发冒烟集、隐藏正式验收集、固定回归集和 L4 真实试点集；
- 五类 L3 项目和数据隔离规则；
- 九字段统一模块评估卡；
- 通用质量、效率、成本和稳定性指标公式；
- 九个 L1 模块的测试数据、金标准、门禁、阈值和失败处理；
- 五条 L2 业务链路的正式评估要求；
- L3 样本、三道判定门、七个质量域、项目总表和签署；
- 阈值制定、冻结、版本和重评规则；
- L4 五类业务价值指标、双轨旁路、前瞻性配对试点和三种结论；
- 评测执行流程、报告产物、变更回归和 PRD 追踪矩阵。

### PRD 与项目记录回写

- PRD 从 `v2.5` 更新为 `v2.6`；
- PRD 关联文档增加 `15-评估体系与测试计划.md`；
- PRD 中“正式评估体系文档”的泛称替换为明确文件名；
- `13-项目决策与工作记录.md` 增加正式评估文档，并更新 PRD 版本记录。

### 完成结论

- L1 至 L4 的详细评估规则已有独立正式基线；
- PRD 继续负责产品范围、质量底线和发布门槛；
- `15` 文档负责样本、金标、指标、阈值、评测流程和报告；
- 后续技术方案、开发计划和测试实现应引用 `FR`、`PRD-BR`、`AC` 及 `15` 文档对应章节；
- 普通质量指标的具体数值仍需通过 5 个开发冒烟项目校准后冻结，不在缺少数据时虚构。

**讨论点完成。**

---

## 六十、文档体系复盘：哪些文档必要、哪些应当降级

### 用户提出的问题

用户询问：

> 我们的文档这么做，你认为都有必要么？

### 助手总体判断

当前已经形成的内容大部分有必要，但没有必要让全部文档长期保持同样的重量和维护优先级。

该项目涉及招标文件、企业证据、报价、审核、版本、权限和内部交接，前期通过文档澄清业务边界具有实际价值。当前主要风险已经从“需求没有说清楚”转变为“文档重复、维护成本上升和内容失去同步”。

项目已经接近“文档充分，应转入技术方案和实现”的阶段，不建议继续无限增加同类说明文档。

### 一、必须保留的开发主文档

以下文档直接决定后续产品设计、技术实现和验收，应作为主基线保留：

| 文档 | 必要性 | 核心作用 |
|---|---|---|
| `07-客户画像与场景设定.md` | 必须 | 明确系统给谁使用、解决什么业务问题和 V1 场景边界 |
| `09-业务流程与项目状态机.md` | 必须 | 定义业务流程、状态转换、异常路径和人工关口 |
| `10-核心业务对象与数据字典.md` | 必须 | 定义领域对象、关系、版本语义，并作为接口和数据库设计上游 |
| `11-产品需求文档（PRD）.md` | 必须 | 作为产品范围、功能需求、人工边界和验收门槛的唯一主基线 |
| `15-评估体系与测试计划.md` | 必须 | 定义系统完成后如何通过 L1 至 L4 判断质量和业务价值 |
| 后续技术方案 | 必须 | 定义 Agent、工具、模型、数据流、接口和部署如何实现 |

这些文档应成为开发人员真正需要持续阅读和引用的核心文档。

### 二、有价值但属于辅助文档

#### `08-招投标术语与业务知识手册.md`

- 有助于统一招投标业务语言；
- 适合作为业务词典和术语查询材料；
- 不需要像 PRD 一样频繁更新；
- 不应重复承担产品需求和技术方案职责。

#### `12-项目难点与解决思路.md`

- 有助于解释为什么采用当前方案；
- 可以作为技术方案的输入和设计取舍参考；
- 技术方案完成后可降级为设计背景材料；
- 不应继续扩写成第二份技术方案。

#### `13-项目决策与工作记录.md`

- 有必要保留关键决策、当前状态和下一步入口；
- 应保持短小，只记录最终决策摘要；
- 不应重复复制 PRD、评估文档和技术方案正文。

### 三、不属于开发主基线的文档

#### `14-面试讲解话术.md`

- 对面试和项目讲解有用；
- 不参与产品设计、技术实现或测试验收；
- 开发人员不需要将其作为实现依据。

#### `项目开发对话历史.md`

- 用于保存完整讨论、选择过程和确认依据；
- 用于重新打开 Codex 时恢复上下文和讨论顺序；
- 不应要求开发人员从数千行对话中查找正式需求；
- 正式结论仍应回写 PRD、评估文档或项目决策记录。

#### `specs/research` 目录

- 属于前期研究和方向论证材料；
- 可以保留作为参考；
- 不应直接作为实现和验收依据。

### 四、当前最大的文档风险

L1 至 L4、业务边界和产品规则可能同时出现在：

- PRD；
- 正式评估文档；
- 对话历史；
- 项目决策记录；
- 项目难点文档。

如果后续在多份文档中同时修改同一规则，很容易出现版本冲突和旧结论残留。

建议统一文档责任关系：

```text
对话历史
保存讨论证据和确认顺序
→
项目决策记录
保存最终决策摘要和当前入口
→
PRD
定义做什么、为什么做和产品质量底线
→
评估体系
定义如何测试和如何判定结果
→
技术方案
定义系统如何实现
```

下游文档应通过 `FR`、`PRD-BR`、`AC` 和评估章节编号引用上游内容，不应再次复制大段正文。

### 五、后续计划文档的必要性建议

- **技术方案**：必须独立编写，是下一阶段重点；
- **版本规划与开发计划**：建议合并成一份《V1 实施与开发计划》，避免两个计划重复维护；
- **数据库设计**：有必要，但应在技术架构、领域对象和持久化方案确定后编写；
- **生产级增强方案**：暂时不要编写，等 V1 原型跑通并发现真实生产缺口后再编写；
- **评估体系**：当前已经足够完整，下一步应通过冒烟项目填入真实阈值，而不是继续扩写理论内容。

### 六、助手建议的下一阶段

```text
停止继续堆叠同类需求文档
→ 编写技术方案
→ 拆解 V1 实施与开发计划
→ 设计数据库和接口
→ 开始原型实现
→ 使用 5 个冒烟项目校准评估阈值
```

### 本次记录边界

- 本次内容是对现有文档必要性和维护优先级的分析建议；
- 用户尚未要求删除、合并或废弃任何现有文档；
- 当前不删除任何文件，也不改变现有正式文档状态；
- 如果后续决定精简文档，应单独讨论并记录明确的保留、归档、合并和废弃清单。

### 用户归档要求

用户要求将本次对话写入 `项目开发对话历史.md`，确保重新打开 Codex 后不会打乱讨论顺序，也不会丢失上下文。

### 完成结论

- 当前核心文档总体有必要，但维护优先级应分层；
- PRD、流程状态机、领域对象、评估体系和后续技术方案是开发主基线；
- 术语、难点、项目记录、面试话术、研究材料和对话历史承担辅助或归档职责；
- 现阶段主要风险是重复维护，不是文档不足；
- 下一阶段建议停止继续扩写同类需求文档，转入技术方案和实现；
- 本次仅完成分析和归档，未删除或合并任何文档。

**讨论记录已归档。**

---

## 六十一、产品版本规划讨论：正式采用 V1、V2、V3

### 用户提出的建议

用户指出当前尚未正式确定 V2 和 V3，因此建议不再使用 `V1.1`、`V1.2`，直接将后续能力划分为 V2 和 V3。

### 现有文档核查

正式 PRD 和项目主基线此前没有定义完整的 V2、V3 产品范围。`specs/research` 中出现过“V2 图 RAG”等早期研究设想，但这些内容属于历史研究材料，且部分来自独立 RAG 方向，不是当前投标 Agent 项目的正式版本规划。

### 一、版本划分原则

- 正式产品版本只使用 `V1`、`V2`、`V3`；
- 不再使用 `V1.1`、`V1.2` 作为正式对外产品版本；
- V1 内部研发过程使用 `M1` 至 `M6` 或 Phase 标识；
- 产品版本表示可以独立说明价值和范围的正式阶段，不表示内部开发半成品；
- V2、V3 的方向可以在 PRD 中记录，但不进入当前 V1 验收范围。

### 二、V1：单项目完整闭环

V1 的目标保持为：

> 从输入项目代码到内部提交留痕，跑通单个政府采购信息化服务项目、单个采购包的完整闭环。

V1 必须保留六项核心工作台能力：

1. 任务流程可视化；
2. 核心结论集中展示；
3. 数据证据可查看；
4. 标书内容可预览；
5. 审批与合规可闭环；
6. 内部提交和版本可追溯。

V1 采用固定流程、有限数据源、简化历史参考、Excel 报价接入和人工外部递交，不建设通用工作流、全国平台全面接入、自动最优报价或生产级运营平台。

### 三、V2：多场景与团队协同

V2 重点将系统从“单项目能用”扩展为“团队日常更好用”，方向包括：

- 多种标书类型；
- 更多招投标平台和企业系统连接器；
- 公告、澄清和补充文件持续监控；
- 更完整的历史项目横向对比；
- 多模板和行业模板管理；
- 更精细的变更影响分析和增量复核；
- 更灵活的审核角色和流程协同；
- 客户版或对外版本受控生成与分发；
- 多项目任务、进度和团队协同看板。

### 四、V3：投标质量运营与平台化

V3 重点将系统从标书生产工具扩展为质量和运营平台，方向包括：

- 投标质量评估体系产品化；
- Agent、模型、提示词和检索方案回测；
- 跨项目质量趋势和风险分析；
- 历史项目复盘和知识沉淀；
- 业务效率、审核质量和成本分析；
- 多组织、权限治理和规模化部署；
- 生产级高可用、安全、监控和灾备；
- 多标包、联合体、工程类和外部平台集成评估。

### 五、V1 内部里程碑

V1 内部研发不再使用 `V1.1`、`V1.2`，建议使用：

```text
M1：项目与文件
M2：要求与证据
M3：标书生成与工作台展示
M4：审核、合规与返工
M5：内部交接与版本留痕
M6：评估、修复与交付验收
```

### 用户确认

用户确认正式采用以下版本路线：

```text
V1：单项目完整闭环
V2：多场景与团队协同
V3：投标质量运营与平台化
```

用户确认不再将 `V1.1`、`V1.2` 作为正式产品版本，V1 内部开发阶段使用里程碑或 Phase 管理。

### PRD 与项目记录回写

- PRD 从 `v2.6` 更新为 `v2.7`；
- PRD 新增“产品版本路线”，明确 V1、V2、V3 的定位和边界；
- `13-项目决策与工作记录.md` 新增正式产品版本划分；
- 早期 research 中的 V2 表述继续保留为历史研究，不作为正式版本承诺。

### 完成结论

- V1 保留单项目完整业务闭环和六项工作台能力；
- V2 承担多场景、更多连接器、客户版和团队协同增强；
- V3 承担投标质量评估、模型回测、跨项目运营和平台化；
- 内部研发里程碑与正式产品版本彻底分开；
- 当前只验收 V1，V2 和 V3 的详细需求需后续单独立项和确认。

**讨论点完成。**

---

## 六十二、产品版本规划补充：独立记录 V2、V3 目标和功能方向

### 用户提出的问题

用户指出：虽然已经确认 V1、V2、V3 的版本划分，但如果不把 V2、V3 具体应做的功能和目标记录下来，后续可能遗忘。

### 问题判断

该担忧成立。

此前 PRD 的“产品版本路线”和对话历史已经记录 V2、V3 的高层方向，但粒度仍不足以支撑后续重新立项。只保留一行版本摘要，未来仍可能遗忘：

- V2 为什么存在；
- 哪些 V1 延后能力应进入 V2；
- 客户版提交、多标书类型和团队协同属于哪个版本；
- 投标质量评估和模型回测属于哪个版本；
- V2、V3 启动前还需要确认哪些关键问题。

### 处理方案

新增独立文档：

```text
specs/16-产品版本规划.md
```

该文档是“产品版本方向基线”，不是 V2 或 V3 的正式 PRD。它负责防止版本方向遗忘，但不提前承诺尚未立项的详细范围、工期和收益。

### 一、V1 记录内容

- 从输入项目代码到内部提交留痕的单项目完整闭环；
- 六项核心工作台能力；
- 端到端业务流程；
- 固定流程、有限数据源、简化历史参考、Excel 报价接入和人工递交边界；
- `M1` 至 `M6` 内部研发里程碑；
- L1 至 L4 的完成关系。

### 二、V2 记录内容

V2 定位为“多场景与团队协同”，记录：

- 多种标书类型；
- 更多招投标平台和企业系统连接器；
- 公告、澄清、更正和补充文件持续监控；
- 更完整历史项目横向对比；
- 多模板和行业模板；
- 更精细的变更影响分析和增量复核；
- 更灵活的角色、审核和团队协同；
- 客户版或对外版本受控生成与分发；
- 多项目任务、进度和协同看板；
- V2 启动前必须确认的标书类型、平台、权限、法律边界和验收数据。

### 三、V3 记录内容

V3 定位为“投标质量运营与平台化”，记录：

- 投标质量评估产品化；
- Agent、模型、提示词、规则和检索配置回测；
- 跨项目质量趋势、风险和运营分析；
- 历史项目复盘和知识沉淀；
- 业务效率、审核质量、人工工作量和成本分析；
- 多组织、权限治理和规模化部署；
- 生产级高可用、安全、监控和灾备；
- 多采购包、联合体、工程类和外部平台集成评估；
- V3 启动前必须确认的数据、反馈、租户、安全、SLO 和合规边界。

### 四、文档边界

- V1 是当前正式实施和验收范围；
- V2、V3 只记录已经确认的方向；
- V2、V3 启动前必须分别建立独立 PRD、评估体系和实施计划；
- `16` 文档不将所有规划能力默认承诺进入对应版本；
- `research` 中的早期 V2 设想不覆盖 `16` 文档；
- 后续版本不能以提前建设平台化能力为理由拖慢 V1 完整闭环。

### 文档同步

- 新增 `16-产品版本规划.md` v1.0；
- PRD 从 `v2.7` 更新为 `v2.8`，关联文档增加 `16`；
- PRD 的版本路线明确由 `16` 维护详细方向；
- `13-项目决策与工作记录.md` 增加 `16` 文档入口和版本记录。

### 完成结论

- V2、V3 的目标和计划能力已经独立保存，不再只依赖对话记忆；
- V1、V2、V3 的能力演进关系已有明确矩阵；
- V2、V3 方向被记录，但没有被错误包装为已承诺范围；
- 下次重新打开 Codex 时，可以先读 `13`，再通过 `16` 恢复完整版本规划；
- 当前继续聚焦 V1，后续版本在正式立项时再细化 PRD 和验收范围。

**讨论点完成。**

---

## 六十三、V1 范围收敛：企业级核心做深、场景做窄

### 用户提出的问题

用户提出：当前 V1 既要保留一定深度、体现企业级项目价值，又不能过重；需要判断哪些能力可以放到 V2，同时不能把 V1 简化成只会生成一份文档的 Demo。

用户再次确认，V1 必须继续实现以下目标：

```text
从“输入项目代码”到“内部提交留痕”的完整闭环；
任务流程可视化；
核心结论集中展示；
数据证据可查看；
标书内容可预览；
审批与合规可闭环；
内部提交和版本可追溯。
```

### 方案判断

V1 不应通过删除核心链路来减负，而应采用：

> 场景做窄，核心做深。

“企业级”不等于在第一版接入所有平台、覆盖所有标书类型、支持任意工作流和建设完整运营平台。V1 的企业级深度应重点体现在：

- 来源可靠；
- 权限明确；
- 人工关口不可绕过；
- 状态和版本正确；
- 失败可恢复；
- 全过程可审计；
- 结论和标书内容可追溯到证据。

### 一、V1 保留并做深的能力

1. **项目范围**
   - 单个政府采购信息化服务项目；
   - 单个采购包；
   - 从项目代码输入到内部受控交接和人工递交记录的完整闭环。

2. **数据接入与文件治理**
   - 一个主要数据连接器或受控模拟连接器；
   - 人工上传作为正式兜底路径；
   - 支持 PDF、Word、Excel 和常见扫描件；
   - 管理文件来源、版本、哈希、覆盖关系、有效版本确认和异常恢复。

3. **要求、评分和证据**
   - 抽取资格条件、否决项、实质性要求、评分项、时间节点和格式要求；
   - 形成要求—评分项—章节—证据—责任人的响应矩阵；
   - 企业证据管理主体、有效期、适用范围、项目级权限和引用；
   - 无证据时拒答并转人工，不允许编造企业能力。

4. **历史参考**
   - 建设简化但真实的历史参考池；
   - 只推荐少量受控候选项目；
   - 输出差异和可复用边界；
   - 由用户确认参考项目及其章节用途后才能参与生成。

5. **报价闭环**
   - 接入受控 Excel 报价模板或内部计算结果；
   - 校验预算、最高限价、税率、合计、单位和大小写金额；
   - 大模型只在有效候选中解释和推荐；
   - 商务人员最终选择，项目负责人确认，并冻结正式报价版本。

6. **标书生成与工作台**
   - 保留六项核心工作台能力；
   - 使用固定标书类型和固定章节流程；
   - 按章节生成带引用的初稿；
   - 展示图表、说明材料、内部风险、来源和版本；
   - 支持章节预览、DOCX 导出和人工编辑保护。

7. **审核、返工和风险**
   - 使用固定审核路径；
   - Agent 预检后由负责人、商务和合规/法务按职责审核；
   - 审核意见结构化为问题任务；
   - 支持风险分级、基础豁免、修改建议、人工确认和原审核角色复核关闭；
   - 保留对象级或章节级影响判断，无法确定时扩大复核范围。

8. **内部交接与追溯**
   - 生成交接基线、文件清单、版本号和哈希；
   - 记录审核状态、交接人、接收人和接收确认；
   - 外部递交仍由人工执行；
   - 系统只记录简单递交结果和回执，不代替采购平台。

9. **评估范围**
   - L1、L2 和 L3 继续作为 V1 软件交付评估；
   - 不降低正确性、权限、版本、人工关口和零容忍质量底线；
   - L4 继续作为真实试点业务价值验证，数据不足时允许保持待评测。

### 二、移动到 V2 的能力

- 多种标书类型和更多业务场景；
- 更多采购平台和企业系统连接器；
- 自动定时监控公告、澄清、更正和补充文件；
- 大规模历史项目横向分析和中标/未中标复盘；
- 高级报价模型、敏感性分析和策略模拟；
- 自定义 Dashboard；
- 通用工作流、灵活审批配置、会签和复杂授权；
- 字段级精确影响分析和自动最小化增量复核；
- 多项目任务、团队负载和协同看板；
- 客户版或对外版受控生成与分发；
- 动态水印、下载次数、复杂分发策略；
- 更多外部递交和归档系统集成。

### 三、V3 继续承担的能力

- 投标质量评估产品化；
- Agent、模型、提示词、规则和检索回测；
- 跨项目质量、风险和运营分析；
- 多组织治理和平台化；
- 生产级高可用、安全、监控和灾备；
- 多采购包、联合体和工程类复杂场景；
- 外部平台和 CA 深度集成评估。

### 用户确认

用户确认采用以下版本建设原则：

> V1 核心链路做深，V2 扩展业务广度和协同能力，V3 建设质量运营和平台化能力。

用户确认 V1 继续保留完整闭环和六项工作台能力，同时按照“场景做窄、核心做深”收敛连接器、监控、历史分析、报价策略、审批配置、影响分析和分发能力。

### 文档回写要求

- PRD 必须从功能条目和验收场景层面真实收敛，不能只修改版本规划口号；
- `16-产品版本规划.md` 需要补充“核心做深、场景做窄”原则及明确的 V1/V2 能力边界；
- `15-评估体系与测试计划.md` 质量底线不降低，但评测对象应限定为收敛后的 V1 范围；
- `13-项目决策与工作记录.md` 应记录本次关键决策和文档版本变化；
- 后续技术方案、版本规划、开发计划、数据库设计和生产级增强方案均以更新后的 PRD 为准。

**讨论点完成。**

---

## 六十四、V1 多智能体初步规模估算：Agent、Skill 与工具数量

### 用户提出的问题

用户询问：基于当前已经收敛的 V1 范围，初步应该设置多少个智能体、多少个 Skill 和多少个工具，为下一次具体技术方案讨论提供大致参考。

### 估算前提

本次只做初步架构估算，不作为正式技术方案、开发承诺或最终数量冻结。

划分原则是：

- 需要理解上下文、综合判断、生成方案或进行语义审查的能力，才考虑设计为 Agent；
- 可复用的业务步骤、提示词流程和输出规范设计为 Skill；
- 解析、检索、计算、校验、状态转换、导出和审计等确定性能力设计为工具或平台服务；
- 不按照业务流程“一步一个 Agent”，避免 V1 产生过多 Agent、调用链过长、成本过高和责任边界不清。

### 推荐初步规模

```text
逻辑 Agent：约 6 个
业务 Skill：约 10～14 个，推荐先按 12 个设计
核心工具：约 11～13 个，推荐先按 12 个设计
```

这些 Agent 是逻辑角色，不代表必须部署 6 个独立微服务，也不代表必须使用 6 个不同模型。多个逻辑 Agent 可以复用相同模型、运行框架、状态存储和工具网关。

### 一、建议的 6 个逻辑 Agent

1. **Supervisor Agent**
   - 负责流程编排、任务分派、重试、暂停、恢复、人工接管和结果汇总；
   - 不直接修改项目主状态，不代替人工确认。

2. **招标文件理解 Agent**
   - 理解项目事实、资格条件、否决项、实质性要求、评分项、时间节点、附件和格式要求；
   - 调用文件解析、OCR 和原文定位工具。

3. **响应规划 Agent**
   - 形成响应矩阵、章节计划、任务责任和证据需求；
   - 调用企业证据检索、历史参考、报价校验结果等工具；
   - 不自动确认企业证据、历史用途或最终报价。

4. **标书编制 Agent**
   - 按确认后的响应矩阵和章节计划生成带引用初稿；
   - 处理章节结构、图表说明、材料引用和局部修改建议；
   - 不静默覆盖人工编辑内容。

5. **资格、合规与评分覆盖审查 Agent**
   - 检查资格条件、否决项、实质性要求、评分点覆盖、附件完整性和合规表述；
   - 输出结构化问题和风险等级，不自动审核通过或关闭问题。

6. **一致性与风险复核 Agent**
   - 检查主体、人员、日期、工期、金额、报价版本、章节引用和跨章节冲突；
   - 汇总履约、证据缺口、报价和承诺风险；
   - 无法判断影响范围时建议扩大或全量复核。

### 二、不建议单独设置为 Agent 的能力

- 文件下载、上传、哈希和版本管理；
- PDF、Word、Excel 和 OCR 解析；
- BM25、向量检索、重排和权限过滤；
- Excel 计算和预算、限价、税率、合计校验；
- 状态机、审批权限和人工关口判断；
- DOCX/PDF 导出；
- 哈希、基线、审计和递交记录。

这些能力更适合做确定性工具。报价也不建议单独建立“自动报价 Agent”；大模型只负责解释有效候选和推荐理由，实际计算、规则排除和最终选择分别由 Excel/规则工具和人工完成。

### 三、建议的约 12 个业务 Skill

1. 项目事实和关键时间抽取；
2. 资格、否决项和实质性要求抽取；
3. 评分项抽取与覆盖规划；
4. 响应矩阵生成；
5. 企业证据匹配、引用和无证据拒答；
6. 少量历史项目比较、差异说明和复用边界判断；
7. 报价候选解释、风险说明和人工选择辅助；
8. 章节大纲和任务拆分；
9. 带引用章节初稿生成；
10. 图表、来源和说明材料组织；
11. 资格合规、评分覆盖、一致性和风险审查；
12. 结构化修改建议、差异说明和复核范围建议。

正式技术方案中可以将第 11 项拆分为两个 Skill，或将部分抽取 Skill 合并，因此合理范围约为 10～14 个。

### 四、建议的约 12 个核心工具

1. 主要采购数据连接器或受控模拟连接器；
2. 人工文件上传与文件存储工具；
3. 文件版本、哈希和覆盖关系工具；
4. PDF、Word、Excel、扫描件解析与 OCR 工具；
5. 原文页码、表格和片段定位工具；
6. 企业证据与采购文件混合检索工具；
7. 权限、主体、有效期和适用范围过滤工具；
8. 历史参考项目查询工具；
9. Excel 报价读取和确定性规则校验工具；
10. 工作流状态机、人工中断和审批权限工具；
11. DOCX/PDF 渲染与导出工具；
12. 基线冻结、审计事件和人工递交记录工具。

任务通知、模型网关、对象存储、数据库、日志和监控属于平台基础设施。正式技术方案中需要决定它们是否计入“Agent 可调用工具”数量，避免统计口径混乱。

### 初步结论

当前推荐以以下规模开始明日讨论：

> **6 个逻辑 Agent + 约 12 个业务 Skill + 约 12 个核心工具。**

这套规模能够覆盖 V1 的完整闭环和企业级质量要求，同时避免把文档解析、报价计算、权限、状态机和导出等确定性能力错误包装为 Agent。

正式技术方案讨论时仍需重点确认：Agent 的职责边界、Skill 是否跨 Agent 复用、工具的输入输出契约、人工中断位置、共享状态结构、失败重试方式和评估责任归属。

**本节为初步估算，尚未冻结为正式技术方案。**

---

## 六十五、初步规模澄清：Skill 和工具不应过度拆分

### 用户提出的问题

用户对“约 12 个 Skill + 约 12 个工具”的数量提出疑问，认为 V1 的 Skill 和工具可能过多。

### 澄清结论

该疑问成立。上一节的数量是按照细粒度“业务能力清单”统计，适合检查需求覆盖，但不适合直接作为实际技术模块数量。

例如：

- PDF、Word、Excel、OCR 和页码定位可以统一封装为一个“文档解析工具”；
- BM25、向量检索、重排、权限过滤和有效期过滤可以统一封装为一个“证据检索工具”；
- 文件上传、哈希、版本和覆盖关系可以统一封装为一个“文件与版本工具”；
- DOCX、PDF、图表和材料组装可以统一封装为一个“文档渲染导出工具”；
- 资格、否决项、实质性要求和评分项抽取可以合并为一个“招标文件理解 Skill”。

因此，V1 更合理的初步规模应调整为：

```text
逻辑 Agent：约 6 个
业务 Skill：约 7～9 个，推荐先按 8 个设计
核心工具：约 8～10 个，推荐先按 9 个设计
```

### 推荐的 8 个 Skill 分组

1. 招标文件理解；
2. 响应矩阵与章节规划；
3. 企业证据匹配与拒答；
4. 历史参考比较与用途建议；
5. 报价候选解释与风险说明；
6. 带引用章节生成；
7. 合规、评分覆盖、一致性和风险审查；
8. 结构化修改建议与复核范围建议。

### 推荐的 9 个工具分组

1. 主要采购数据连接器；
2. 文件上传、存储、哈希与版本工具；
3. 文档解析、OCR、表格和原文定位工具；
4. 采购文件与企业证据混合检索工具；
5. 权限、主体、有效期和适用范围校验工具；
6. 历史参考项目查询工具；
7. Excel 读取与报价规则校验工具；
8. 状态机、人工中断与审核权限工具；
9. 文档导出、基线冻结、审计和递交记录工具。

正式技术方案还可以进一步判断第 4、5 项工具是否合并，以及第 9 项是否拆成“导出”和“审计基线”两个工具，因此最终数量可能在 8～10 个之间。

### 当前修正后的推荐

> **6 个逻辑 Agent + 8 个左右的 Skill + 9 个左右的核心工具。**

企业级深度不通过增加 Agent、Skill 和工具数量体现，而通过每个模块的来源、版本、权限、失败恢复、人工关口、审计和评估完整性体现。

**本节仍为初步估算，最终数量等待技术方案讨论后冻结。**

---

## 六十六、2026-08-18 收工与次日恢复检查点

### 用户要求

用户要求保存今天的完整进度，确保 2026-08-19 重新打开 Codex 后能够顺着今天的思路继续，不重复讨论、不丢失顺序。

### 今天完成的核心工作

1. 将 PRD 从 v2.8 更新为 v2.9，正式采用“场景做窄、核心做深”；
2. 从具体功能需求和验收场景层面收敛 V1，而不是只修改版本规划口号；
3. 明确 V1 保留完整闭环和六项核心工作台能力；
4. 明确进入 V2 的连接器、监控、历史分析、报价模拟、通用工作流、精确影响分析和复杂分发能力；
5. 将产品版本规划更新为 v1.1；
6. 将评估体系更新为 v1.1，限定收敛后的 V1 评测对象，但不降低质量门禁；
7. 更新项目决策与工作记录；
8. 初步估算 V1 的 Agent、Skill 和工具规模，并在用户质疑后将 Skill 和工具从细粒度能力清单合并为更合理的技术模块数量。

### 当前最终讨论起点

```text
逻辑 Agent：约 6 个
业务 Skill：约 7～9 个，推荐先按 8 个讨论
核心工具：约 8～10 个，推荐先按 9 个讨论
```

该数量没有冻结。它只是下一次技术方案讨论的起点。

### 2026-08-19 直接讨论的问题

下一次不再重新讨论项目背景、业务环节、六项工作台能力、V1/V2/V3 划分或 V1 范围是否过重，直接讨论：

> **6 个逻辑 Agent 的划分是否合理，以及每个 Agent 的输入、输出、职责边界、调用 Skill、调用工具、共享状态和人工中断点。**

之后依次讨论：

1. Agent 的合并与拆分；
2. Skill 的归属与复用；
3. 工具的合并、接口和平台服务边界；
4. Agent 间结构化数据对象；
5. 失败重试、暂停恢复和人工接管；
6. L1、L2、L3 评估责任映射；
7. 正式技术方案和系统架构设计文档结构。

### 恢复入口

重新打开 Codex 时：

1. 先读取 `specs/13-项目决策与工作记录.md` 第九章；
2. 再读取 PRD v2.9、版本规划 v1.1、评估体系 v1.1 的相关章节；
3. 最后读取本历史的第六十三至第六十六节；
4. 直接进入 Agent 边界讨论。

**2026-08-18 的工作进度已形成次日恢复检查点。**

---

## 六十七、技术方案讨论启动：建设标准、技术栈与解析路线

### 用户确认进入技术方案阶段

用户确认下一阶段直接进入技术方案，并提出 Agent、Skill 和工具数量可以进一步压缩。经过讨论，技术方案初始规模正式确定为：

```text
5 个逻辑 Agent + 6 个业务 Skill + 7 个核心工具
```

压缩原则不是减少业务闭环，而是合并相邻的语义职责、缩短 Agent 调用链，并将解析、检索、计算、状态和审计等确定性能力集中到工具层。

### 一、5 个逻辑 Agent

1. Supervisor Agent；
2. 招标理解与合规 Agent；
3. 响应规划与评分 Agent；
4. 标书编制 Agent；
5. 质量与风险复核 Agent。

其中，原“评分覆盖”并入响应规划，原“一致性审查”和“风险复核”合并。5 个 Agent 是逻辑角色，共享运行框架和基础设施，不拆成独立微服务。

### 二、6 个业务 Skill

1. 文件事实与要求抽取；
2. 资格、否决项和评分项抽取；
3. 响应矩阵与任务规划；
4. 企业证据匹配与无证据拒答；
5. 带引用章节生成；
6. 一致性与风险审查。

### 三、7 个核心工具

1. 文件接入与解析；
2. 项目、文件版本与状态管理；
3. 结构化输出与规则校验；
4. 企业证据与采购文件检索；
5. 报价读取、计算与业务规则校验；
6. 文档渲染与导出；
7. 审计、基线冻结与内部交接记录。

### 四、总体架构确认

用户确认采用：

> **模块化单体 + 异步任务执行 + LangGraph 状态编排。**

具体原则：

- Python 单仓库和模块化后端，不使用微服务；
- Next.js 构建前端工作台；
- FastAPI 提供 API、权限和业务服务；
- LangGraph 编排 Agent、共享状态、暂停恢复和人工中断；
- Celery + Redis 执行文件解析、OCR、Embedding、Agent 工作流和导出等长任务；
- PostgreSQL 保存最终任务状态和业务事实，Redis 只承担队列和临时状态；
- Agent、Skill 和工具共享领域模型、权限、版本、审计和工具网关。

### 五、前端先行开发方式

用户确认优先开发前端页面，再逐步开发后端，但开发方式必须是：

> **前端先行 + 契约驱动 + 纵向切片。**

具体要求：

1. 先确定页面导航、工作台布局和完整业务流程；
2. 同步定义核心领域对象、OpenAPI/JSON Schema 和 TypeScript 类型；
3. 前端使用 Mock API，必须覆盖加载、失败、等待人工确认、版本失效和权限不足等真实状态；
4. FastAPI 按项目与文件、解析与要求、响应矩阵与证据、章节编制、审核风险、交接导出六个纵向切片逐步替换 Mock；
5. 不允许由临时页面字段直接反推并冻结数据库结构。

### 六、建设标准确认

用户在“个人作品演示”和“小团队真实试用”之间选择折中方案：

> **单组织、多角色、支持多个独立项目的可部署旁路试用版。核心流程真实、可恢复、可审计，可以给小团队受控试用，但不建设完整企业 SaaS。**

V1 实现真实登录、后端权限、异步任务、人工暂停恢复、文件与章节版本、证据引用、审核风险、基线冻结和审计记录；暂不实现企业注册、复杂组织后台、多租户计费、实时协同编辑、Kubernetes、高可用集群和自动扩缩容。

### 七、数据底座确认

用户确认使用 PostgreSQL + pgvector：

- PostgreSQL 是唯一核心关系型数据库；
- pgvector 是 PostgreSQL 内的向量扩展，不是独立的第二数据库；
- 项目、版本、权限、来源和向量检索处于同一数据一致性边界；
- 不使用 Chroma，避免独立向量库带来的同步、失效和权限漂移；
- 原始文件、解析产物和导出文件存入 MinIO 等 S3 兼容对象存储。

### 八、异步任务与部署确认

用户确认采用 Celery + Redis + LangGraph：

- Celery 负责任务排队、执行、重试和调度；
- Redis 只负责消息队列和临时状态；
- LangGraph 负责 Agent 内部流程和人工中断；
- PostgreSQL 记录可恢复的最终任务状态；
- FastAPI 与 Worker 可复用同一后端镜像，但使用不同启动命令。

部署采用单台 Linux 服务器上的 Docker Engine + Docker Compose，主要容器为网关、Next.js、FastAPI、Worker、PostgreSQL + pgvector、Redis 和 MinIO。只有网关开放公网 `80/443`，其他服务通过内部网络访问。当前不引入 Kubernetes 或微服务集群。

### 九、文档解析与复杂图表路线

用户最终确认采用分层解析：

```text
原生文本 PDF → PyMuPDF
DOCX/XLSX → 原生结构解析
扫描页或普通图片 → OCR + 版面检测
常规表格 → 表格结构恢复
复杂图表 → 外部多模态 API
全部结果 → 统一 Document IR
```

复杂图表包括流程图、架构图、拓扑图、甘特图、统计图、多层表头、嵌套表格和图文关系决定语义的区域。V1 优先使用外部多模态 API，同时保留本地模型适配接口。

多模态结果只作为视觉解释或候选结构，不直接覆盖 OCR 原文。金额、税率、日期、工期、资格条件、否决项、评分分值、技术参数、人员数量、签字和印章等高风险信息必须人工确认。Agent 只读取统一 `Document IR`，不直接依赖具体解析器或模型供应商。

### 十、用户新增的文档要求

用户要求：

1. 技术方案讨论过程必须持续同步到 `项目开发对话历史.md`；
2. 已冻结的重要技术决策同步写入 `specs/13-项目决策与工作记录.md`；
3. 后续正式技术方案必须记录技术栈安排及选择原因；
4. 每项关键技术同时记录替代方案、未采用原因、V1 实现边界、失败降级方式和后续替换接口；
5. 技术方案不能只列出工具名称，必须解释其与业务范围、领域对象、人工关口、部署方式和评估指标的关系。

### 当前下一讨论点

下一步直接讨论：

> **5 个 Agent 的输入、输出、职责边界、共享状态、调用 Skill、调用工具和人工中断点。**

---

## 六十八、Supervisor Agent 详细设计确认

### 设计定位

用户确认 Supervisor Agent 不是能够自由决定一切的“万能 Agent”，而是：

> **LangGraph 确定性状态机上的智能协调器，负责判断下一步由哪个专业 Agent 处理，但不能绕过业务规则、人工关口或直接修改项目主状态。**

状态机负责流程合法性、权限、前置条件和人工关口；Supervisor 负责语义判断、任务拆解、异常归类和专业 Agent 调度。状态机和后端权限规则优先于大模型判断。

### 负责事项

1. 读取项目当前状态和有效输入版本；
2. 判断当前允许执行的下一项任务；
3. 分派给其他 4 个专业 Agent；
4. 汇总专业 Agent 的结构化结果；
5. 创建后续任务或人工待办；
6. 处理可重试失败、暂停、恢复和人工接管；
7. 记录 Agent 运行使用的输入、输出、模型和版本；
8. 在文件、报价或证据更新后触发旧结果失效。

### 明确不负责

Supervisor 不直接解析文件、执行检索、计算报价、生成正式章节、认定资格通过、关闭高风险问题、接受风险豁免、确认最终报价或冻结交接基线。它也不能直接修改项目主状态，所有状态转换必须调用状态管理工具，由确定性状态机校验权限和前置条件。

### 输入与输出

输入包括项目 ID、当前状态、触发事件、有效文件版本、要求基线、证据范围、报价版本、章节版本、未关闭审核问题、人工待办和历史 Agent 运行记录。

输出必须为结构化调度决策，限定为：

```text
DISPATCH_AGENT
WAITING_HUMAN
RETRY_TASK
EXPAND_REVIEW_SCOPE
INVALIDATE_OUTPUTS
BLOCK_WORKFLOW
COMPLETE_STAGE
```

调度结果同时记录目标 Agent、任务类型、输入基线 ID、原因、所需工具、后续人工关口和幂等键。

### 共享状态边界

LangGraph 状态只保存 `project_id`、`workflow_run_id`、当前阶段、有效基线 ID、当前与已完成任务 ID、待处理人工关口、阻断问题 ID、重试次数、最近错误和下一动作等引用及控制字段。

完整项目、文件、要求、证据、章节和审核内容仍存储在 PostgreSQL 中。LangGraph Checkpoint 不成为第二套业务数据库。

### Skill 与工具调用

Supervisor 原则上不直接调用 6 个业务 Skill，而是调度拥有相应 Skill 的专业 Agent。它主要调用：

1. 项目、文件版本与状态管理工具；
2. 结构化输出与规则校验工具；
3. 审计、基线冻结与内部交接记录工具。

### 人工中断与恢复

有效文件、要求、证据用途或报价尚未确认，可能覆盖人工章节，存在阻断问题或待豁免风险，等待交接冻结，发生版本冲突，或者自动重试达到上限时，Supervisor 必须进入 `WAITING_HUMAN`。

临时网络、模型限流和临时解析错误由 Celery 指数退避重试；结构化输出持续失败、版本冲突或权限不足转人工或阻断；Worker 退出后依据 PostgreSQL 中的任务状态恢复，并使用幂等键防止重复生成；输入版本变化后旧结果标记为 `STALE`，不得继续沿用。

### 确认结论

用户确认 Supervisor 的核心原则为：

> **Supervisor 负责“下一步该由谁处理”，状态机负责“这一步是否允许”，人工负责“关键业务结论是否接受”。**

下一步讨论第二个 Agent：招标理解与合规 Agent。

---

## 六十九、招标理解与合规 Agent 设计确认

### 方案选择

用户确认采用：

> **规则召回 + LLM 结构化理解 + 确定性校验 + 人工确认。**

不采用只依赖 LLM 的方案，因为高风险要求可能分散在表格、附件和补遗中；也不采用只依赖规则的方案，因为规则难以处理否定、例外、交叉引用和复合条件。

### Agent 定位

该 Agent 建立系统对有效招标文件的权威结构化理解，负责回答“采购文件要求是什么”，不负责判断“本企业最终是否满足”。

### 主要职责

1. 读取已确认的有效文件版本和 `Document IR`；
2. 抽取项目名称、采购人、采购包、预算、限价和截止时间；
3. 抽取资格条件、否决项、实质性要求和评分项；
4. 抽取技术参数、服务要求、工期、地点和验收条件；
5. 抽取附件、格式、签章、装订和提交要求；
6. 识别正文、评分表、附件、澄清和补遗之间的引用、覆盖和冲突；
7. 形成待人工确认的要求基线；
8. 文件更新后生成差异和影响候选。

### 处理管线

```text
确定性预处理
→ 规则召回候选
→ LLM 结构化抽取
→ 字段与来源校验
→ 跨文件冲突检查
→ 要求原子化与去重
→ 置信度和风险分级
→ 人工确认
→ Requirement Baseline
```

规则负责召回“必须”“不得”“否则无效”“否决投标”等明确表达，以及金额、日期、比例、资格审查表和评分表。星号、黑体、下划线和颜色只能作为候选信号，不能单独认定为否决项。

LLM 负责理解否定、例外、复合要求、正文与附件关系、评分条件和统一字段归类。确定性工具负责验证来源片段、文件版本、页码、分值、日期、金额、重复、冲突和 Schema。

### 输入与输出

输入包括项目 ID、有效文件版本、确认文件范围、Document IR 片段、文件关系、规则版本、旧要求基线和触发类型。

输出对象包括：

```text
ProjectFactSet
RequirementSet
ScoringCriterionSet
SubmissionChecklist
DocumentConflictSet
```

每条要求必须保存类型、标准化文本、强制等级、后果类型、来源片段、文件版本、页码、置信度、确认状态和判断摘要。

### Skill、工具与边界

该 Agent 使用“文件事实与要求抽取”和“资格、否决项和评分项抽取”两个 Skill，调用文件接入与解析、项目与版本状态管理、结构化输出与规则校验、采购文件范围检索工具。

它不判断企业资格满足、不搜索或确认企业材料、不生成响应矩阵、不生成章节、不计算最终报价、不判断标书最终合规，也不关闭风险或批准投标。

### 人工确认与文件更新

抽取完成后必须进入 `REQUIREMENT_BASELINE_CONFIRMATION`。人工确认项目事实、资格和否决项、实质性要求、评分项、附件格式、文件冲突及补遗覆盖关系。未确认前结果只能为 `DRAFT`，不能驱动正式响应矩阵或章节生成。

补遗或更正文件加入后，系统生成 `Requirement Diff`，旧基线标记为 `STALE`，人工确认变化后创建新基线。新结果不得覆盖历史要求基线。

### 评估重点

评估项目事实准确率、资格和否决项召回率、实质性要求召回率、关键评分项召回率、来源定位正确率、要求原子化和去重质量、文件冲突发现率、补遗变更识别率及人工修改比例。高风险要求漏报属于零容忍门禁。

---

## 七十、创建正式技术方案讨论稿

### 用户要求

用户要求新建独立技术方案文档，把技术讨论持续写入，并说明每个技术点的实现方式和选择原因。

### 文档处理

已创建：

> `specs/17-技术方案与系统架构设计.md` v0.1

文件名称采用“技术方案与系统架构设计”，而不是仅称“数据方案”，因为当前讨论已经覆盖前端、后端、Agent 编排、任务队列、数据库、对象存储、文档解析、检索、模型、部署、安全和评估。

### 写入规则

后续每个技术点均记录：

1. 承担的职责；
2. 具体实现方式；
3. 选择原因；
4. 替代方案和未采用原因；
5. V1 实现边界；
6. 失败降级与人工兜底；
7. 后续替换和扩展接口；
8. 对应领域对象、任务状态和评估指标。

文档明确区分“已确认”“推荐待确认”“待实测”和“后续版本”，避免把早期调研候选误写为最终技术决定。

### 下一讨论点

下一步继续讨论第三个 Agent：响应规划与评分 Agent。

---

## 七十一、响应规划与评分 Agent 设计确认

### 方案选择

用户确认采用“响应规划 + 企业证据候选匹配 + 评分覆盖”的合并方案，不把评分覆盖重新拆成独立 Agent。该设计统一管理要求、评分项、章节、证据、责任人和任务依赖，避免响应矩阵与评分计划形成两套状态。

### Agent 定位

该 Agent 把已确认的招标要求转换成可执行、可追踪、可进行评分覆盖检查的响应计划。它不直接写正式章节，也不决定企业最终满足资格。

### 处理管线

```text
读取已确认 Requirement Baseline
→ 要求分类与响应方式规划
→ 企业证据候选检索
→ 主体、权限、有效期和用途过滤
→ 历史参考候选检索
→ 评分项覆盖规划
→ 章节目录与任务拆分
→ 依赖、缺口和冲突识别
→ 完整度与覆盖率计算
→ 人工确认
→ Response Plan Baseline
```

### 主要输出

```text
ResponseMatrix
ScoringCoveragePlan
ChapterPlan
EvidenceMatchSet
EvidenceGapSet
HistoricalReferenceSet
WorkAssignmentPlan
PlanningConflictSet
```

每个响应矩阵项记录要求、响应方式、目标章节、候选和已确认的证据、评分项、责任人、状态、阻断原因和来源要求基线。

### 响应与证据状态

响应方式包括直接响应、证据响应、表单响应、报价响应、声明响应、人工附件、不适用、证据缺失、冲突和等待确认。系统不能因为缺少证据就自动改为纯文字响应。

证据状态区分候选、已确认、证明不足、过期、主体错误、范围不符、权限拒绝和缺失。语义相似度不能直接推出企业满足要求。

### 评分与历史参考边界

只有评分公式可以确定性计算且证据已经确认时，系统才输出潜在得分区间，并明确该结果不是评标结果或中标预测。主观评分项只记录覆盖状态，不预测分数。

历史项目只用于章节结构、实施方案表达、风险提示、资源安排和差异分析，不得作为当前企业事实、报价、人员或技术参数承诺。所有历史参考必须记录差异、允许用途、禁止用途和人工确认状态。

### Skill、工具与职责边界

该 Agent 使用“响应矩阵与任务规划”和“企业证据匹配与无证据拒答”两个 Skill，调用项目与状态管理、结构化输出与规则校验、企业证据与采购文件检索、报价读取与业务规则校验工具。

它不修改要求基线、不判定企业最终通过资格、不编造或自动确认企业证据、不自动确认历史项目用途、不计算或确认最终报价、不生成正式章节、不审核通过、不承诺人员工期和参数，也不预测中标概率。

### 人工确认、失效和降级

规划完成后必须进入 `RESPONSE_PLAN_CONFIRMATION`。人工确认高风险要求响应方式、证据用途、历史参考用途、评分项与章节映射、缺失材料任务、章节责任人、报价依赖、确定性分值测算和不适用理由。未确认前只能生成预览草稿。

要求基线更新时响应计划整体标记 `STALE`；证据失效时相关矩阵项和章节增量失效；报价版本变化时触发金额引用更新和商务复核；人工章节目录不得被静默覆盖。

向量检索或重排不可用时可以降级，但必须标记降级状态；没有证据时创建缺口；评分规则无法解析或结构化校验持续失败时转人工；报价未确认时阻断金额相关内容。

### 评估重点

评估要求映射准确率、关键评分项覆盖率、证据引用和拒答、错主体或过期证据误用、历史参考用途边界、缺口发现、确定性评分计算、人工修改比例和增量失效正确率。编造证据、越权或过期证据进入正式计划、未确认历史内容变成当前承诺、未确认报价写入正式内容以及高风险要求未进入矩阵均为零容忍错误。

### 下一讨论点

下一步讨论第四个 Agent：标书编制 Agent。

---

## 七十二、标书编制 Agent 设计确认

### 方案选择

用户确认采用结构化内容块生成方案，不采用整章一次性生成，也不把 Agent 限制为只给写作建议。该方案支持块级来源、版本、锁定、局部重写、差异比较和定向失效。

### Agent 定位

标书编制 Agent 根据已确认的响应计划和证据，生成带引用、可局部修改、可版本比较的章节草稿，但不能静默覆盖人工内容。

### 内容模型

章节由 `HeadingBlock`、`ParagraphBlock`、`TableBlock`、`FigureBlock`、`QuoteDataBlock`、`EvidenceBlock`、`NoticeBlock` 和 `PlaceholderBlock` 组成。

内容来源区分 Agent 生成、模板、人工、确定性数据绑定、历史参考、占位和系统内部提示。导出时必须隔离正式内容、内部风险提示和待补充内容，内部信息不得进入外发标书。

### 处理管线与输出

```text
读取 Response Plan Baseline 与当前章节版本
→ 检查要求、证据、报价和模板依赖
→ 识别人工锁定块
→ 检索允许使用的证据和历史参考
→ 生成结构化内容块
→ 确定性数据块填充
→ 引用、事实和禁用承诺校验
→ 生成 VisualAssetSpec
→ 生成版本差异
→ 人工确认
→ Chapter Draft Version
```

输出包括 `ChapterDraftVersion`、`ContentBlockSet`、`CitationManifest`、`VisualAssetSpecSet`、`GenerationDiff`、`MissingInputSet` 和 `GenerationWarningSet`。

### 引用与承诺边界

企业资质、人员、案例、招标要求、报价和技术参数必须关联对应来源版本和原文位置。无有效来源时创建占位和缺失输入，不生成确定性事实。

内容区分可验证事实、项目计划、正式承诺、历史参考和一般说明。可验证事实必须有证据，正式承诺必须来自已确认响应计划或人工输入，历史内容不能伪装成当前事实，一般说明不能伪装成企业已有能力。

### 报价与图表

LLM 不直接生成金额。金额、税率、单位和大小写通过系统锁定的 `QuoteDataBlock` 引用已确认报价版本，由报价工具确定性填充。报价变化只更新金额相关块并触发商务复核。

Agent 只生成 `VisualAssetSpec`，文档渲染工具根据确认数据生成甘特图、流程图、架构图和统计图。没有数据来源时只能保留占位项。

### 人工编辑保护和局部重写

生成任务固定基础章节版本和块版本。若运行期间章节已被人工修改，系统创建冲突候选版本和差异，不覆盖当前内容。

块状态区分 Agent 生成、Agent 修改、人工修改、人工创建、锁定、失效和冲突。人工创建、人工修改和锁定内容默认禁止自动覆盖。

局部重写可以针对段落、表格、图表说明、审核问题、证据变化或报价变化。结果作为差异建议，用户选择接受 Agent 修改、保留当前内容或手工合并。

### Skill、工具和人工关口

该 Agent 使用“带引用章节生成”Skill，调用状态管理、结构化校验、证据检索、报价规则和文档渲染导出工具。

它不修改要求和响应计划、不确认企业证据和报价、不生成无来源企业事实、不擅自承诺人员工期参数、不关闭审核问题、不判定审核通过、不接受风险豁免、不修改锁定内容、不冻结交接基线，也不执行外部递交。

生成完成后必须进入 `CHAPTER_DRAFT_CONFIRMATION`。编写人确认内容、引用、承诺、历史参考、报价字段、图表来源、待补充项和版本差异后，可以将章节置为 `READY_FOR_REVIEW`，该状态不代表审核通过。

### 变更、失败与评估

要求和响应计划变化触发相关内容块失效；证据失效阻断事实引用；报价变化只增量更新金额块；模板变化只能生成候选排版版本。

LLM、结构化输出、引用校验、报价工具、图表渲染和版本冲突均有明确降级路径。输入基线在运行期间变化时，本次生成结果必须废弃。

评估章节和评分覆盖、事实引用、无证据生成、承诺来源、历史参考边界、报价字段、图表来源、人工编辑保护、局部重写范围、版本差异和审核返工修复。编造事实、未确认报价、过期或越权证据、静默覆盖人工修改、内部提示外泄、缺少必备章节和错误导出旧版本均为零容忍错误。

### 下一讨论点

下一步讨论第五个 Agent：质量与风险复核 Agent。

---

## 七十三、质量与风险复核 Agent 设计确认

### 方案选择

用户确认采用“统一复核 Agent + 多阶段检查”，不拆分多个独立审核 Agent。确定性问题由规则工具执行，语义一致性和履约风险由 LLM 辅助，最终统一生成问题清单、风险等级、责任路由和安全复核范围。

### Agent 定位与风险等级

该 Agent 对当前不可变审核基线执行资格合规、评分覆盖、证据引用、报价、跨章节一致性、履约承诺和外发内容检查，但不能自行审核通过、关闭问题或接受风险。

风险等级统一为：

```text
BLOCKING  阻断风险
HIGH      高风险
MEDIUM    中风险
LOW       低风险
```

纯提示使用 `ReviewNotice`，不与正式风险混合。规则认定的阻断风险不能被 Agent 自动降级。

### 审核基线与处理管线

每次审核创建不可变 `ReviewBaseline`，固定要求基线、响应计划、章节、报价、证据、附件、模板、规则、模型和提示词版本。审核结论只对该基线有效，输入变化后原审核结果和相关风险必须失效。

```text
创建 ReviewBaseline
→ 确定性预检
→ 资格与合规检查
→ 评分覆盖检查
→ 证据与引用检查
→ 报价与数据一致性检查
→ 跨章节语义一致性检查
→ 履约与承诺风险检查
→ 附件、格式与外发内容检查
→ 问题合并与去重
→ 风险分级和责任路由
→ 复核范围判断
→ 人工审核
```

### 主要输出与状态规范

输出包括 `ReviewRun`、`ReviewBaseline`、`ReviewIssueSet`、`RiskItemSet`、各专项审核报告、`ReviewScopeDecision` 和 `ReviewSummary`。

讨论中曾使用更通用的处理中状态名称，但核对现有领域模型后，最终确认沿用 `10-核心业务对象与数据字典.md` 的状态，不创建第二套枚举：

- `review_issue.status`：`OPEN`、`PROPOSED`、`WRITER_CONFIRMED`、`PENDING_REVIEW`、`CLOSED`、`REOPENED`、`WAIVED`；
- `risk_item.risk_status`：`OPEN`、`IN_PROGRESS`、`RESOLVED`、`WAIVED`、`STALE`。

Agent 只能创建 `OPEN` 问题或风险、提出 `PROPOSED` 修改建议，并在依赖失效时标记风险结论 `STALE`。`CLOSED`、`RESOLVED` 和 `WAIVED` 必须由有权限人工角色操作。

### 复核范围和返工闭环

复核范围分为 `OBJECT`、`CHAPTER`、`MULTI_CHAPTER` 和 `FULL_PROJECT`。纯语言和不影响事实的格式修改可以局部复核；要求、报价、主体、人员、工期、日期、参数、证据范围、评分映射、正式承诺和共享内容变化必须扩大复核。无法可靠判断时扩大，而不是缩小。

返工流程为：Agent 创建问题和建议，责任人确认并修改，标书编制 Agent 生成候选版本，编写人确认后进入 `PENDING_REVIEW`，有权审核人复核后关闭、重新打开或授权豁免。Agent 不得跳过人工复核。

### 风险豁免与交接门禁

豁免必须记录问题、等级、理由、范围、授权人、角色、有效对象版本、有效期和时间。豁免不等于问题不存在，版本变化后必须重新判断；资格、法定无效和系统安全类问题能否豁免由确定性规则控制。

预检完成后进入 `REVIEW_RESULT_CONFIRMATION`。只有所有必审任务完成、所有 `BLOCKING` 风险为 `RESOLVED` 或合法 `WAIVED`，且审核基线仍有效时，状态管理工具才允许项目进入 `READY_FOR_HANDOVER`。Agent 本身不执行状态转换。

### 失败、降级和评估

语义审查失败时保留确定性结果并重试；持续失败必须转人工，不能以“未发现问题”结束。规则或报价工具失败时阻断对应审核域；引用无法定位至少升级为高风险；影响范围不清时扩大复核；导出预览失败阻断交接前审核；输入变化使审核结果失效。

评估阻断和高风险召回、错误降级、误报、责任路由、修改建议、报价和跨章节冲突、证据引用、内部信息外泄、影响范围、复核安全性、自动关闭和豁免、问题重开、审核耗时与返工轮次。阻断漏报、自动关闭或豁免、越权操作、错误降级、变更后绕过复核和旧审核结论沿用均为零容忍错误。

### 阶段结论

至此，5 个逻辑 Agent 的输入、输出、职责边界、Skill、工具、共享状态和人工中断点已经全部完成确认。

### 下一讨论点

下一步讨论统一 `Document IR` 的字段、来源定位、版本和人工修正模型。

---

## 七十四、Document IR 范围收敛：采用 V1 简化模型

### 用户反馈

用户认为最初提出的 Document、Page、Node、Table Cell、Source Anchor、Segment、Retrieval Chunk 和 Correction 多层模型过于复杂，不适合当前 V1。

### 收敛结论

用户确认采用以下简化模型：

```text
document_version
真实文件版本
    ↓
document_parse
一次解析结果
    ↓
document_page
页面文本和页面图片
    ↓
document_segment
章节、段落、表格或图表区域

document_correction
针对 Segment 的人工修正覆盖层
```

`document_segment` 沿用现有核心业务对象，继续作为要求抽取、检索、引用和原文定位的稳定来源单元。`document_parse`、`document_page` 和 `document_correction` 是轻量技术支撑对象。

### 主要字段和实现方式

`document_parse` 记录文件版本、解析器、解析配置、状态、质量、原始产物和任务。更换解析器或重新解析产生新的解析记录，不产生新的文件版本。

`document_page` 保存物理页码、印刷页码、页面文本、MinIO 页面图片、OCR 标记、质量和状态。

`document_segment` 保存解析记录、页面、类型、章节路径、文本、坐标、扩展 JSON、置信度和状态。Segment 类型只保留标题、段落、列表、表格、图表和其他。

`document_correction` 保存原始内容、修正内容、原因、提出人、确认人和状态。人工修正不能覆盖原始解析值。

### 表格、图表和检索处理

- 普通表格保存区域图片、Markdown 或 HTML、简单行列 JSON、页码和坐标；
- 复杂图表的 OCR、视觉类型、多模态解释和确认状态存入 Segment 的 JSON 扩展字段；
- V1 直接在 `document_segment` 上建立全文和向量索引；
- 更换 Embedding 时重建向量，但不改变 Segment ID 和业务引用。

### 明确后移的复杂能力

V1 不建设通用文档节点树、独立来源锚点表、单元格关系表、独立 Retrieval Chunk、完整跨页逻辑表和通用文档结构编辑器。

上述能力只有在真实样本证明当前简化模型不足时再拆分，避免为了潜在需求提前增加数据模型和开发成本。

### 保留的质量边界

- Segment 必须绑定文件版本、解析记录、物理页码和坐标；
- 要求和审核结论应引用 Segment，不能只保存页码；
- 多模态解释不能覆盖 OCR 原文；
- 高风险区域未确认时不能形成正式要求基线；
- 文件或解析版本变化后，下游结论必须正确失效；
- 关键页面不可用时阻断后续正式生成。

### 下一讨论点

下一步讨论 LangGraph 共享状态、人工关口和五个 Agent 的总体编排流程。

---

## 七十五、LangGraph 总体编排：采用单主图方案

### 方案比较

本轮比较了三种方式：

1. 所有解析、工具、Agent 和人工关口全部进入一张巨型图；
2. 五个 Agent 分别维护独立 LangGraph 工作流；
3. 一张持久化主图，由 Supervisor 路由四个专业 Agent 节点。

巨型图会把技术任务和业务编排混在同一层，节点数量和维护成本过高；五个独立图需要额外处理跨图事件、Checkpoint 协调和版本失效。结合用户要求控制复杂度，推荐第三种。

### 用户确认

用户确认采用：

> **一张持久化 LangGraph 主图 + Supervisor 路由节点 + 四个专业 Agent 受控节点。**

V1 不提前拆嵌套子图。某个专业 Agent 只有在内部出现多个稳定阶段、独立人工中断或独立恢复需求后，才升级为子图。

### 总体流程

```text
开始或恢复
→ 加载 PostgreSQL 业务状态
→ 校验权限、有效版本和输入基线
→ Supervisor 路由
→ 招标理解与合规 Agent
→ REQUIREMENT_BASELINE_CONFIRMATION
→ 响应规划与评分 Agent
→ RESPONSE_PLAN_CONFIRMATION
→ 标书编制 Agent
→ CHAPTER_DRAFT_CONFIRMATION
→ 质量与风险复核 Agent
→ REVIEW_RESULT_CONFIRMATION
→ 确定性交接门禁
→ 允许进入 READY_FOR_HANDOVER
```

### LangGraph、Celery 和 PostgreSQL 的边界

- LangGraph 决定节点顺序、条件分支、人工中断和恢复位置；
- Supervisor 只决定下一步由哪个专业 Agent 处理；
- 确定性状态机和领域服务判断动作是否允许；
- Celery 在 Worker 中执行或恢复主图，负责排队、超时和有限重试；
- PostgreSQL 保存业务状态、基线、Agent 产物、人工确认和审计，是唯一事实源；
- Checkpoint 只保存运行控制字段和业务对象 ID。

FastAPI 创建或恢复运行后统一提交 `run_workflow(workflow_run_id)`。V1 不实现 LangGraph 发布 Celery 子任务、Celery 再回调 LangGraph 的双层调度。解析、OCR、Embedding 和导出等独立技术任务仍可使用 Celery，但其状态通过 PostgreSQL 与主图衔接。

### 返工、版本和恢复

要求问题返回招标理解阶段；评分映射、证据用途和章节规划问题返回响应规划阶段；章节内容和引用问题返回编制阶段；复核问题按影响范围返回规划或编制阶段。

采购文件或关键输入版本变化后，旧 `workflow_run` 标记为 `STALE` 或 `INVALIDATED`，创建新运行并从最早受影响阶段开始。旧 Checkpoint 不倒带、不覆盖，历史产物和人工确认继续保留用于审计。

`project_id` 标识业务项目，`workflow_run_id` 标识一次绑定确定输入版本的编排运行，LangGraph `thread_id` 使用 `workflow_run_id`；`agent_run_id` 和 `task_run_id` 分别标识专业 Agent 调用和 Celery 或工具任务执行。节点通过输入版本、输出版本和幂等键防止重复生成。

### 下一讨论点

下一步细化 LangGraph 共享状态字段、节点输入输出和人工恢复接口。

---

## 七十六、人工关口粒度：采用阶段级中断

### 概念解释

“阶段级中断”是指一个专业 Agent 完成一轮工作后，LangGraph 暂停一次。用户在前端页面逐条处理该阶段产生的要求、证据、章节或风险，全部满足门禁后点击一次“确认并继续”，工作流再恢复一次。

具体业务条目的状态保存在 PostgreSQL，LangGraph 不为每条记录分别创建中断。这样即使要求抽取阶段产生数十或上百条记录，也只产生一个阶段 Checkpoint。

### 用户确认

用户确认四个人工关口均采用阶段级中断：

1. `REQUIREMENT_BASELINE_CONFIRMATION`；
2. `RESPONSE_PLAN_CONFIRMATION`；
3. `CHAPTER_DRAFT_CONFIRMATION`；
4. `REVIEW_RESULT_CONFIRMATION`。

用户点击“确认并继续”后，领域服务必须先校验必确认项、操作权限、有效版本和阶段门禁。只有全部通过，才允许恢复同一个 `workflow_run`。

### 下一讨论点

下一步讨论 LangGraph 最小共享状态字段。

---

## 七十七、LangGraph 共享状态：收敛为七个字段

### 复杂度复核

最初考虑使用独立 `workflow_input_snapshot` 并在 State 保存约十个字段。进一步复核后认为该方案虽然可实施，但对 V1 略有过度设计，因此继续压缩。

### 用户确认

用户确认 LangGraph State 只保存：

```text
workflow_run_id
project_id
current_stage
pending_gate
active_agent_run_id
retry_count
last_error_code
```

`thread_id` 直接等于 `workflow_run_id`，不重复写入 State。Supervisor 的 `next_action` 只是当前路由节点的临时输出；人工恢复请求的幂等标识放在接口幂等记录和审计事件中。

### 输入版本保存方式

V1 不新增独立 `workflow_input_snapshot` 表。文件版本、要求基线、响应计划、证据范围、报价版本、章节版本、审核基线、规则、模型和提示词版本统一保存在 PostgreSQL 的 `workflow_run.input_refs` JSONB 中，并生成 `input_hash`。

工作流恢复前重新读取实际有效版本并计算哈希。如果与原 `input_hash` 不一致，旧运行不能继续，必须标记为 `STALE` 或 `INVALIDATED`，再按影响范围创建新运行。

完整要求、证据、响应矩阵、章节、风险、人工确认明细、阻断问题和任务列表均不进入 LangGraph Checkpoint。

### 选择原因

七字段 State 足以回答“哪次运行、哪个项目、执行到哪里、是否等待人工、当前 Agent 和错误情况”。这种设计不引入额外快照实体，也不会因为新增业务对象频繁修改图状态结构，整体复杂度更适合 V1。

### 下一讨论点

下一步讨论 LangGraph 节点的统一输入输出契约。

---

## 七十八、LangGraph 节点契约：五类节点与五种结果

### 用户确认

用户确认 V1 采用五类顶层节点：

1. 加载校验节点；
2. Supervisor 路由节点；
3. 专业 Agent 节点；
4. 人工关口节点；
5. 确定性门禁节点。

所有节点接收同一个七字段 `WorkflowState`。需要要求、证据、章节或风险等业务数据时，根据 `workflow_run_id` 从 PostgreSQL 查询。节点只返回发生变化的 State 字段，业务产物直接写入 PostgreSQL。

### 专业 Agent 节点步骤

```text
读取 workflow_run 和 input_refs
→ 校验权限、输入版本和前置条件
→ 按幂等键查找或创建 agent_run
→ 执行 Skill、工具和模型调用
→ 校验结构化输出
→ 写入业务产物、来源和审计
→ 返回少量 State 更新
```

### 五种图执行结果

节点层统一使用 `CONTINUE`、`WAITING_HUMAN`、`RETRY`、`BLOCKED` 和 `COMPLETED`。它们分别表示继续执行、等待人工、有限重试、停止自动执行和本次运行正常完成。

`NodeOutcome` 与 Supervisor 的七类业务调度决策不是同一个概念。Supervisor 仍然输出 `DISPATCH_AGENT`、`WAITING_HUMAN`、`RETRY_TASK`、`EXPAND_REVIEW_SCOPE`、`INVALIDATE_OUTPUTS`、`BLOCK_WORKFLOW` 或 `COMPLETE_STAGE`，再由路由节点映射为图执行结果。

临时模型限流、网络超时和可恢复 Worker 异常可以有限重试；权限不足、输入版本变化、业务前置条件缺失、人工确认未完成和结构化输出持续失败必须阻断或转人工，不能无限重试。

### V1 边界

V1 不建设通用工作流 DSL、动态节点注册平台、跨项目图编排或模型自由新增节点能力。主图和条件边由代码显式定义。

### 下一讨论点

下一步讨论人工确认与 LangGraph 恢复 API 契约。

---

## 七十九、人工确认与恢复 API：统一关口动作接口

### 方案选择

本轮比较了四个人工关口分别建设接口、直接暴露 LangGraph 恢复接口，以及统一业务关口动作接口。用户确认采用第三种，避免重复代码，也避免前端绕过业务门禁直接恢复工作流。

### 两个核心接口

```http
GET /api/v1/workflow-runs/{workflow_run_id}/pending-gate
POST /api/v1/workflow-runs/{workflow_run_id}/pending-gate/actions
```

查询接口返回当前关口、阶段、输入哈希、允许动作、必确认项统计、阻断原因和是否允许继续。具体要求、证据、章节和风险明细由各业务接口提供。

动作接口通过 `Idempotency-Key` 防止重复点击，请求包含 `gate_type`、`action`、`expected_input_hash` 和可选原因。动作只保留：

- `CONFIRM_AND_CONTINUE`；
- `RETURN_FOR_REWORK`。

前端不能任意指定返回节点，返工目标由后端影响规则决定。单条业务对象的确认、修改和驳回不通过关口接口处理。

### 确认与恢复事务

后端依次校验用户与项目权限、锁定 `workflow_run`、校验当前关口、幂等键和输入哈希，再检查全部必确认项、阻断问题和阶段门禁。在同一 PostgreSQL 事务中写入阶段确认与审计事件、清空关口并创建 `QUEUED task_run`。事务提交后再投递 `run_workflow(workflow_run_id)`，接口返回 `202 Accepted`。

V1 不新增独立 Outbox 表，使用 `task_run` 作为轻量可靠投递记录。Redis 或 Celery 暂时不可用时，任务保持 `QUEUED`，定时恢复任务重新投递；Worker 使用幂等键去重。重复动作请求返回首次结果，不产生重复确认或重复恢复任务。

### 返工和版本变化

退回时由领域服务确定最早受影响阶段并使相关下游产物 `STALE`。普通业务结果修正继续使用同一个 `workflow_run`；采购文件、报价等关键输入版本变化时停止旧运行并创建新运行。

### 错误语义

- `403`：没有操作权限；
- `404`：工作流不存在或不可见；
- `409`：关口、输入版本、运行状态或幂等键冲突；
- `422`：必确认项、阻断问题或阶段门禁未满足；
- `202`：动作已经可靠记录，等待异步恢复。

### 下一讨论点

下一步讨论 `workflow_run`、`task_run` 和阶段确认记录的数据模型。

---

## 八十、运行数据模型：四类记录各司其职

### 方案选择

本轮比较了全部复用 `agent_run`、四类记录分工和通用 `execution_record` 三种方式。用户确认采用四类记录：

```text
workflow_run
├── agent_run
├── task_run
└── workflow_gate_action
```

`workflow_run` 表示一次端到端 LangGraph 主图运行；`agent_run` 表示 Supervisor 或专业 Agent 的真实调用；`task_run` 表示工作流执行、解析、OCR、Embedding、多模态分析、导出和补投等后台任务；`workflow_gate_action` 表示阶段级确认继续或退回返工。

### 现有模型修正

原 `agent_run` 同时包含 `DOCUMENT_PARSE`、`RAG_RETRIEVAL` 等工具运行类型，职责过宽。本轮确认将这些技术任务迁移到 `task_run.task_type`，`agent_run` 只保留真实 Agent 执行。

单条要求、证据、章节和风险的确认状态继续保存在各自业务表中。`workflow_gate_action` 只记录用户对整个阶段执行了什么动作，不复制业务明细。

### 关键约束

- `workflow_run.id` 直接作为 LangGraph `thread_id`；
- 输入版本通过 `input_refs` 和 `input_hash` 固定；
- Agent、任务和人工动作均使用幂等键；
- `task_run` 区分数据库已排队和消息已投递；
- 阶段动作关联操作人、角色、输入哈希、结果和恢复任务；
- 状态变化写入 `audit_event`，但审计事件不代替当前状态表。

### 复杂度边界

V1 不新增节点运行表、通用执行记录表和独立 Outbox 表。四类记录已经是支持恢复、补投、人工审计和版本失效的最小可靠模型。

用户同时决定继续完成技术方案讨论，全部讨论结束后再绘制整体架构图，并根据整体图识别过重区域、决定哪些能力后移。

### 下一讨论点

下一步讨论 Agent、Skill 与七个工具的具体调用契约和实现形态。

---

## 八十一、Skill 与 MCP：压缩数量但保留真实能力

### 用户要求

用户确认 V1 需要真实使用 Skill 和 MCP，以体现可复用业务能力和标准工具调用，但认为原来的 6 个 Skill、7 个工具仍然偏多，希望继续降低数量，同时不能让功能和项目价值打折。

### 最终数量基线

用户确认采用：

```text
逻辑 Agent：5 个
业务 Skill：4 个
MCP 工具域：4 个
MCP Server：1 个
```

4 个 Skill 为“招标文件理解”“响应规划与证据匹配”“带引用标书编制”“质量与风险复核”。Supervisor 不直接执行专业 Skill，只负责状态编排和专业 Agent 调度。

4 个 MCP 工具域为：

- `document.*`：解析、OCR、复杂图表多模态理解、Document IR 读取、预览和导出；
- `project.*`：项目上下文、版本、状态、人工关口、失效、审计、基线和交接；
- `knowledge.*`：招标文件、企业证据和历史参考检索、来源读取与引用校验；
- `business.*`：业务规则、报价读取与校验、一致性检查和确定性门禁。

4 个工具域由同一个 `bid-tools-mcp` 服务统一暴露，不部署 4 个独立 MCP Server。具体方法仍按操作拆分，保留独立输入输出模型、权限、超时、错误码和审计记录。

### 功能与价值结论

本次压缩减少的是模块命名、部署单元和平台化复杂度，不删除业务能力。文档解析、OCR、复杂图表多模态、要求抽取、证据检索、引用校验、报价校验、章节生成、质量复核、版本失效、四个人工关口和全链路审计均保留。

V1 不建设 Skill 市场、动态安装、多个 MCP Server 编排、服务发现或第三方任意工具接入。Skill 作为版本化业务能力包存在，MCP 作为真实协议调用存在，因此演示价值和后续扩展能力不降低。

### 下一讨论点

下一步讨论 Skill 包与 MCP 方法的最小调用契约，包括版本、请求上下文、工具白名单、权限、错误模型、超时重试和审计。

---

## 八十二、MCP 通信：内部 Streamable HTTP

### 方案比较

本轮比较了三种方式：每个 Worker 通过 stdio 启动 MCP 子进程、FastAPI 和 Worker 共享内部 Streamable HTTP MCP Server，以及直接调用 Python 函数。stdio 会在多个 Worker 中重复启动工具资源；直接函数调用不能体现真实 MCP；共享内部 HTTP 最符合“一个 MCP Server”的既定边界。

### 用户确认

用户确认 V1 使用：

```text
FastAPI / Celery Worker / Agent Runtime
→ MCP Client
→ http://mcp-tools:8000/mcp
→ 4 个 MCP 工具域
```

`mcp-tools` 只加入 Docker 内部网络，不暴露宿主机端口、网关路由或浏览器访问。FastAPI 和 Worker 使用部署时配置、不得写入代码仓库的内部服务凭证访问。

### 请求上下文与权限

每次调用通过 MCP `_meta` 传递请求、追踪、用户、项目、工作流、Agent 运行和 Skill 版本上下文。工具业务参数继续由独立 Pydantic Schema 定义，不在每个工具参数中重复身份和追踪字段。

MCP Server 先校验服务凭证，再校验 Skill 工具白名单、项目权限、对象所属项目和输入版本。高风险状态修改仍调用 FastAPI 领域服务，由 PostgreSQL 事务、权限和状态机执行，不能由 MCP Server 绕过。

### V1 边界

V1 不建设完整 OAuth 授权服务器，不开放第三方 MCP Client，不依赖 MCP 会话保存业务状态。网络和临时服务错误允许有限重试；权限、白名单、版本和业务门禁错误直接失败或转人工。

### 下一讨论点

下一步讨论统一 `McpCallContext`、Skill 包清单和 MCP 工具输入输出的最小字段。

---

## 八十三、Skill 与 MCP：五对象最小调用契约

### 用户确认

用户确认 V1 采用五个逻辑契约对象：

```text
SkillManifest
McpCallContext
McpToolRequest
McpToolResponse
McpError
```

### Skill 清单

`SkillManifest` 保存 Skill 标识、版本、名称、目标、输入输出 Schema、允许调用的工具、模型策略、引用策略、拒答策略、超时和重试上限。Skill 不只是 Prompt，而是 Prompt、结构化契约、业务规则、引用要求、拒答要求和工具白名单的组合。

### 调用上下文与请求

`McpCallContext` 通过 MCP `_meta` 携带请求、追踪、用户、项目、工作流、Agent 运行和 Skill 版本。业务工具参数不重复包含这些身份字段。

`McpToolRequest` 逻辑上包含工具名与版本、业务参数、幂等键和输入引用。工具只接收必要参数及文件版本、基线和报价版本等稳定引用，不接收整份项目状态。

### 返回与错误

`McpToolResponse` 使用 `SUCCEEDED`、`PARTIAL` 和 `REJECTED`。小型结果直接返回，大文本、文件和批量数据写入 PostgreSQL 或 MinIO后返回引用。部分成功必须明确缺失范围和警告。

统一错误包含临时错误、参数校验、权限拒绝、版本冲突、业务阻断、重复请求、资源不存在和内部错误。只有临时错误和一次内部错误允许有限重试；权限、版本和业务门禁错误不得自动重试。

### V1 边界

V1 不建设动态工具发现、万能 `execute` 方法、运行时新增工具、MCP Session 业务状态、复杂事件总线和独立 MCP Outbox。

### 下一讨论点

下一步讨论 4 个 Skill 的具体 MCP 方法白名单，并继续压缩 V1 方法数量。

---

## 八十四、MCP 方法：冻结为十四个

### 方案比较

本轮比较了约 10 个聚合方法、14 个职责清晰的方法和 20 个以上细粒度方法。用户确认采用中间方案，既避免万能工具，也控制权限配置、测试和维护数量。

### 方法清单

```text
document.ingest
document.read
document.render
document.export

project.get_context
project.validate_inputs
project.validate_transition

knowledge.search
knowledge.read_sources
knowledge.validate_citations

business.validate_structure
business.validate_quote
business.check_consistency
business.evaluate_gate
```

`document.ingest` 内部完成解析、OCR、版面识别和复杂图表多模态路由，不再单独暴露图表工具。`document.export` 只允许确定性交接任务调用。状态转换仍由 FastAPI 领域服务执行，`project.validate_transition` 只返回是否合法及阻断原因。

### Skill 白名单

招标文件理解主要使用文档读取、项目输入校验、招标文件检索和结构校验；响应规划与证据匹配主要使用项目输入校验、全部知识检索能力、结构和报价校验；带引用标书编制额外使用文档预览和一致性检查；质量与风险复核可以使用除最终导出外的大部分校验能力。

Supervisor 只使用 `project.*` 和 `business.evaluate_gate`。最终导出由确定性交接任务触发，不由专业 Agent 自主执行。Skill 白名单由 Skill 清单声明并由 MCP Server 二次校验，模型输出不能扩大工具权限。

### 下一讨论点

下一步讨论哪些 MCP 方法同步执行、哪些进入 Celery 异步任务，以及超时、进度和结果查询方式。

---

## 八十五、任务执行：单层 Celery，不嵌套异步

### 用户确认

用户确认采用：

```text
前端或工作流
→ FastAPI 创建 task_run
→ Celery Worker
→ 同步调用 MCP 方法
→ 更新 task_run
→ 前端轮询
```

MCP Server 不再创建第二层 Celery 任务。专业 Agent 已经运行在 Worker 中时直接调用 MCP 方法，避免嵌套任务、重复重试和两套状态记录。

### 同步与异步划分

文件解析、OCR、复杂图表分析、文档预览、最终导出和全项目一致性检查默认通过 Celery。项目上下文、输入与状态校验、有限范围检索、来源读取、引用校验、结构校验、报价校验和门禁判断默认同步调用。

同步调用必须设置规模上限：检索最多返回 50 条，来源读取和引用校验单次最多 100 项。超过上限拆批，或在已有 Celery 任务中调用相同方法，不新增异步 MCP 接口。

### 进度和前端查询

`McpCallContext` 增加可选 `task_run_id`。长任务通过它更新当前步骤、近似进度和心跳。FastAPI 或工作流创建任务，Worker 管理开始、完成、失败和重试，MCP 只报告执行进度。

前端每 2～3 秒调用：

```http
GET /api/v1/task-runs/{task_run_id}
```

V1 不建设 WebSocket、SSE、消息推送或 MCP 内部任务队列。

### 数据模型同步

`task_run` 增加 `current_step`、`progress_percent` 和 `heartbeat_at`，并补充文档预览与一致性检查任务类型。进度只用于用户反馈，任务是否完成仍以任务状态、结果引用和业务校验为准。

### 下一讨论点

下一步讨论文本模型与多模态模型的路由、结构化输出、版本固定和失败降级。

---

## 八十六、模型调用：一个网关、两个配置档

### 用户确认

用户确认采用一个应用内部 `ModelGateway` 和两个模型配置档：

```text
TEXT_PRIMARY
VISION_CHART
```

文本配置档统一服务四个专业 Agent；多模态配置档只用于确定性规则识别出的复杂图表。`ModelGateway` 是 Worker 内的 Python 模块，不新增容器或微服务。

### 路由边界

专业 Skill 固定使用文本配置档，复杂图表由 `document.ingest` 的规则路由到多模态。模型不能自行选择多模态、切换供应商或升级模型。OCR、报价计算、状态转换和业务门禁继续使用确定性程序。

### 输出与失败处理

模型输出必须经过结构校验、引用完整性检查和业务规则校验。JSON 格式错误只允许一次结构修复，之后转人工；缺少必要引用时不得写入正式业务表；信息不足时保留拒答或缺口，不能要求模型编造。

网络和限流最多重试 2 次。复杂图表多模态失败时保留 OCR 结果和原图，并标记为待人工确认。模型不能修改项目状态、计算或确认报价、关闭风险或通过人工关口。

### 版本记录

每次 Agent 运行固定并记录模型配置档、供应商、请求模型名、实际模型标识、模型参数摘要、Skill、Prompt、Schema 版本和 Token 用量。同一次运行不能静默更换模型；配置变化需要创建新运行或使受影响结果失效。

具体供应商和模型名称在开发前使用真实招标文件评测确认，业务代码只依赖统一网关契约。

### 下一讨论点

下一步讨论 PostgreSQL 与 pgvector 下的混合召回、重排和引用定位方案。

---

## 八十七、检索增强：PostgreSQL 词法 + pgvector + RRF

### 术语统一

正式 V1 方案统一使用“PostgreSQL 原生词法检索”，不把它表述成尚未部署的标准 BM25。难点文档同步修正为 PostgreSQL 词法索引和向量索引都是派生检索层。

### 用户确认

用户确认检索流程为：

```text
查询标准化
→ 项目、权限、版本、有效期和保密范围过滤
→ PostgreSQL 原生词法召回
→ pgvector 语义召回
→ 去重
→ RRF 融合
→ 有限候选重排
→ 返回带来源定位的片段
```

词法和向量默认各取 Top 30，RRF 后取 Top 30，重排后返回 Top 10。单次请求最多返回 50 条候选，超过范围拆批或放入已有 Celery 任务。

每条结果必须携带来源片段、文件版本、页码、章节路径、字符或坐标范围、原文、召回路线和得分。Agent 只能引用有效结果，版本变化后重新召回。

### 降级和边界

Embedding 失败时降级到 PostgreSQL 词法召回；Reranker 失败时使用 RRF；两路都失败时创建证据缺口并转人工。V1 不增加 Elasticsearch 或 OpenSearch，具体 Embedding 和 Reranker 模型通过真实样本评测确认。

### 下一讨论点

下一步讨论切片粒度、元数据过滤字段和 `document_segment` 的引用定位方式。

---

## 八十八、切片与引用：以业务语义片段为最小单位

### 用户确认

用户确认 `document_segment` 是 V1 最小引用单位。切片优先按标题和正文、段落、列表项、表格区域或表格行、规则单元、图表 OCR 文本和多模态摘要形成完整语义片段；片段过长时再继续拆分，不首先按固定 Token 数切割。

### 定位字段

片段保存文件版本、解析记录、页面记录、页码、章节路径、字符范围、页面坐标、来源哈希、轻量父片段关系、解析置信度和扩展信息。片段类型统一为标题、段落、列表、表格、表格行、图表和其他。

`parent_segment_id` 只表达章节、列表和表格内部关系，不扩展成通用节点树。表格行列和图表解释继续放在 `metadata_json`，不建设独立单元格表。

### 检索过滤与引用校验

检索前校验项目、知识范围、文件版本、文件状态、有效期、组织范围、保密等级和片段类型。权限和有效性不能由模型事后判断。

引用校验检查片段存在、版本有效、来源哈希、页码与章节、引用文本和允许用途。来源变化后旧引用及依赖结果进入 `STALE`，重新检索后才能使用。

### V1 边界

V1 不新增来源锚点表、表格单元格表和检索块表。检索权限元数据从项目、文件和知识对象读取，不全部复制到每个片段。

### 下一讨论点

下一步讨论 Embedding 和 Reranker 的部署形态。

---

## 八十九、检索模型部署：纯 CPU、复用 mcp-tools

### 硬件约束

用户确认单台 Linux 部署服务器没有 GPU，Embedding 与 Reranker 使用 CPU。

### 用户确认

V1 不调用外部 Embedding API，不新增独立模型服务。`EmbeddingProvider` 和 `RerankerProvider` 运行在已有 `mcp-tools` 容器中，每类模型只加载一个持久实例，并共享有界 CPU 推理资源控制。

文档片段向量化通过 Celery 异步执行，查询向量化允许同步。Reranker 只处理 RRF 后的最多 30 个候选，超时、拥塞或不可用时直接使用 RRF，不阻断检索主流程。

### 资源与优化边界

`mcp-tools` 设置固定 CPU 线程、容器内存限制、有界队列和模型就绪健康检查。不得动态启动多个模型进程。初始版本先限制输入、批量和候选，再根据实测决定是否采用 ONNX 或 INT8，不同时引入量化、独立推理服务和外部 API。

Embedding 保留现有候选，Reranker 需要与更小的 CPU 候选比较。若 CPU 重排性能不满足要求，可以关闭 Reranker 并使用 RRF，不影响功能闭环。

### 下一讨论点

下一步讨论结构化内容块如何生成 DOCX、PDF 预览和最终交接文件。

---

## 九十、文档导出：DOCX 为可编辑交付，PDF 为同源预览

### 用户确认

用户确认使用以下管线：

```text
结构化内容块
→ DocumentModel
→ 受控 DOCX 模板
→ docxtpl + python-docx
→ 可编辑 DOCX
→ LibreOffice Headless
→ PDF
```

`DocumentModel` 只是运行时中立结构，不新增数据库表。持久化模板对象命名为 `document_template_version`，避免和 `handover_package` 混淆。

### 预览与最终导出

`document.render` 生成带草稿或内部使用标识的 DOCX/PDF，允许占位、待确认内容和引用警告，但不能进入正式交接包。

`document.export` 只由确定性交接任务调用。存在占位块、内部内容混入外发正文、引用失效、报价未确认、阻断风险、版本冲突或模板失效时必须阻断。成功后生成 DOCX、同源 PDF、文件清单和 SHA-256。

DOCX 成功但 PDF 失败时只算部分成功，不能形成有效交接包。

### 部署边界

渲染与导出通过 Celery 执行。LibreOffice 初始并发限制为 1，每个任务使用独立临时目录和用户配置目录，完成后清理。渲染依赖直接进入 worker 镜像，不增加在线 Office 或独立渲染服务。

V1 不建设在线 Office 编辑器、不允许任意模板脚本、不让 Agent 执行最终导出，也不自动外部递交。

### 下一讨论点

下一步讨论 Redis 缓存与业务状态边界。

---

## 九十一、Redis：缓存与业务状态分离

### 用户确认

用户确认 Redis 只用于：

```text
Celery Broker
可重建缓存
限流计数
短期分布式锁
```

Redis 不保存项目状态、人工关口、权限、版本、报价、引用有效性、运行最终状态或审计事件。PostgreSQL 仍是唯一业务事实源。

### 缓存键与故障降级

检索缓存 Key 绑定项目、知识范围、文件版本、基线、查询哈希和配置版本；Embedding 缓存 Key 绑定模型版本和标准化查询哈希；草稿预览只缓存临时对象引用。所有缓存有 TTL，版本变化产生新 Key，不依赖复杂主动失效。

Redis 不可用时，任务保持 `QUEUED` 并等待补投；检索跳过缓存直接执行；CPU 模型重新计算；限流退化为应用级保护。Redis 锁不能替代数据库行锁、唯一约束和幂等键。

### 下一讨论点

下一步讨论敏感企业证据是否进入缓存，以及脱敏、隔离和清理策略。

---

## 九十二、敏感缓存：只存引用，不存完整正文

### 用户确认

用户确认 Redis 只缓存检索引用、版本、召回路线、排序分数、查询 Embedding 和必要的脱敏短摘要，不缓存完整企业证据、招标文件、章节正文、身份证件、联系方式、报价明细或内部审核意见。

缓存命中后，系统仍需回 PostgreSQL 校验用户权限、项目归属、文件版本、有效期、保密等级和引用状态，再从 PostgreSQL 或 MinIO 读取原文。权限判断结果本身不能作为缓存事实复用。

查询 Embedding 的 Key 使用模型版本和标准化查询哈希，不保存原始敏感查询文本。检索和重排结果默认缓存 5～15 分钟，脱敏短摘要不超过 15 分钟，查询 Embedding 可保存 30 分钟至 24 小时。

任何权限、版本或有效期校验失败都必须丢弃缓存并重新检索。

### 下一讨论点

下一步讨论外部文本模型与多模态 API 的数据发送、脱敏和审计边界。

---

## 九十三、外部模型：最小数据出域

### 用户确认

用户确认在外部文本模型和多模态 API 前增加内部 `DataEgressGuard`，执行数据分类、项目授权、字段白名单、最小化、脱敏和审计。

数据等级统一为：

```text
PUBLIC
INTERNAL
CONFIDENTIAL
RESTRICTED
```

项目默认只允许公开资料出域。投标经理可以明确授权内部或机密资料的有限使用，但 `RESTRICTED` 永远禁止发送外部模型。

### 发送范围

文本模型只接收当前任务需要的片段、已确认事实、规则和 Schema；多模态只接收复杂图表局部裁剪及必要 OCR。完整知识库、合同、报价底表、成本明细、内部审核意见、风险豁免、账号凭证和完整身份证号码禁止发送。

### 审计与失败

每次调用记录供应商、模型、数据分类、来源对象、发送字段名、脱敏版本、请求响应哈希和 Token 用量。普通日志不保存完整 Prompt、图片或回复正文。

策略拒绝返回 `EXTERNAL_DATA_BLOCKED`，不自动重试。系统继续使用本地检索和确定性规则，或创建人工任务。

### 数据模型同步

`bid_project` 增加外部 AI 策略、批准人和批准时间；文件版本和企业知识资料使用统一分类；`audit_event` 增加外部调用元数据。

### 下一讨论点

下一步讨论 Docker Compose 下的密钥、凭证和轮换方案。

---

## 九十四、密钥管理：Compose Secrets 与仓库外文件

### 用户确认

用户确认 V1 使用 Docker Compose Secrets 配合宿主机仓库外受保护文件，不引入 Vault、云 KMS 或独立密钥管理服务。

普通非敏感配置继续使用环境变量。数据库密码、Session 签名密钥、MinIO 凭证、MCP 内部服务凭证和外部模型 API Key 通过 `/run/secrets/<name>` 按服务授权读取，应用使用 `*_FILE` 配置接口。

### 存放与权限

Secret 文件存放在 `/opt/bid-agent/secrets/`，不进入 Git、镜像、构建参数或普通 `.env`。目录由部署专用账号管理，文件权限设为 `600`。

前端不读取后端 Secret。PostgreSQL 至少拆分管理员、应用运行和迁移账号；MinIO 拆分 Root 与应用账号；MCP 凭证只验证内部服务身份，不能替代用户、项目、对象和版本权限校验；模型 API Key 不保存到 PostgreSQL。

日志、错误、健康检查、任务参数、审计元数据和诊断包不能输出 Secret 值、请求头或完整连接串。开发、测试和生产环境不得复用凭证。

### 轮换与升级

正常轮换按“新旧值短期并存 → 更新授权 → 滚动重建调用方 → 验证新值 → 撤销旧值”执行。紧急泄露时先禁用旧值，再重建受影响服务并检查审计记录。

Docker Compose Secrets 在非 Swarm 模式下主要解决受控文件挂载和服务隔离，不加密宿主机文件，因此必须结合仓库外存放、系统权限、最小挂载、分权账号和日志脱敏。未来如升级 Vault/KMS，沿用 `*_FILE` 接口替换密钥来源即可。

### 下一讨论点

下一步讨论身份认证与会话方案，包括登录方式、Cookie/Session、会话过期、权限加载和敏感操作再确认。

---

## 九十五、身份认证：本地账号与 PostgreSQL Session

### 用户确认

用户确认 V1 使用平台本地账号，不接入企业现有统一登录。账号只允许系统管理员创建、禁用和重置，不开放用户自行注册。未来可以接入 OIDC 或企业统一登录，但不改变平台角色和项目成员权限模型。

### 会话实现

用户确认采用 PostgreSQL 服务端 Session，不采用 Redis Session 或无状态 JWT。

浏览器 Cookie 只保存随机高熵 Session Token，并使用 `HttpOnly`、生产环境 `Secure` 和 `SameSite=Lax`。PostgreSQL 只保存 Token 哈希、用户、最近活动时间、空闲与绝对过期时间、重新认证时间和撤销状态。写操作必须执行 CSRF 防护。

密码使用 Argon2id 哈希，数据库不保存明文。管理员重置密码后，用户首次登录必须修改密码。

### 过期与撤销

会话默认空闲 30 分钟过期，单次登录绝对最长 8 小时。修改密码、禁用账号或修改关键平台角色后，撤销该用户全部有效 Session。

报价确认、风险豁免和最终交接等敏感操作要求用户重新输入密码。登录失败使用 Redis 做账号与来源组合的短期限流；Redis 不可用时退化为应用级保护。

登录、退出、登录失败、密码重置、账号禁用、会话撤销和重新认证都写入审计，但不能记录密码、Session Token 原文或完整 Cookie。

### V1 不做

V1 不建设独立身份服务，不实现自助注册、短信或邮箱验证码、Refresh Token、JWT 黑名单、复杂设备管理和多因素认证。

### 数据模型同步

`user` 增加登录账号、密码哈希、首次登录改密和密码变更时间等字段；新增 `user_session` 保存可撤销的服务端会话。

### 下一讨论点

下一步讨论反向代理，确认 V1 使用 Caddy 还是 Nginx，以及 TLS、上传限制和安全响应头边界。

---

## 九十六、公网入口：Caddy 反向代理

### 部署条件确认

用户确认 V1 不部署在企业内网，使用正式公网域名，DNS 指向部署服务器公网 IP。

### 用户确认

用户确认现有 `gateway` 容器使用 Caddy，不增加新的容器。Caddy 是系统公网最外层入口，负责 HTTPS 证书、请求转发和基础入口保护，不负责业务认证或 Agent 工作流。

```text
公网用户
→ Caddy :80/:443
   ├── 页面请求 → frontend:3000
   └── /api/*   → backend:8000
```

Caddy 自动申请和续期 HTTPS 证书，并将 HTTP 强制跳转到 HTTPS。服务器只开放公网 `80/443`，PostgreSQL、Redis、MinIO、MCP、前端和后端容器端口不直接暴露公网。

### 请求与安全边界

单文件上传初始限制为 200 MB，上传请求最长 10 分钟，普通 API 超时 60 秒。上传完成后立即创建异步任务并返回 `task_run_id`，解析、OCR、Agent 和导出不长期占用 HTTP 连接。

登录接口按账号和来源限流，普通 API 按用户和来源执行基础限流。Caddy 添加 HSTS、内容类型保护、来源策略和基础 CSP；访问日志不记录 Cookie、Authorization、正文、文件内容或敏感 URL 参数。

MinIO 不允许公网匿名访问。文件下载必须先由后端验证用户权限、项目归属和文件版本，再生成短期签名地址。

V1 不使用 Nginx 配合 Certbot，也不增加云负载均衡、CDN 或 WAF。公网规模或安全风险提高后再评估这些组件。

### 下一讨论点

下一步讨论轻量可观测性方案，包括结构化日志、错误收集、指标和告警组件。

---

## 九十七、可观测性：零新增容器的本地方案

### 用户确认

用户确认 V1 不接入外部 Sentry，异常信息和完整堆栈不发送第三方监控平台。系统需要在服务宕机、任务连续失败、磁盘不足和备份异常时主动通知管理员，但只发送脱敏摘要。

### 日志与运行看板

FastAPI、Celery Worker、MCP 和 Caddy 输出结构化 JSON 日志，统一携带请求 ID、项目 ID、工作流运行 ID、Agent 运行 ID、任务 ID、服务名和错误码。

Docker 日志按单文件最大 20 MB、最多保留 5 个文件轮转。日志不能记录文件正文、Prompt、模型完整回复、Cookie、Authorization、密码、Session Token、报价明细或完整企业证据。完整异常堆栈只保存在服务器本地并执行敏感字段过滤。

管理员页面直接使用 `task_run`、`agent_run`、`workflow_run` 和 `audit_event` 展示任务、失败、重试、耗时、模型用量、OCR 触发、人工接管和版本失效，不部署 Grafana。

### 健康检查与告警

宿主机通过 `systemd timer` 每分钟运行轻量 Watchdog，检查容器健康、Caddy/FastAPI/MCP、Worker 心跳、PostgreSQL、Redis、MinIO、磁盘、队列、连续任务失败和备份时效，不增加容器。

磁盘超过 80%、失败增加或队列积压为 `WARNING`；服务连续 3 次不可用、磁盘超过 90%、数据库不可连接、Worker 心跳失效或备份严重过期为 `CRITICAL`。

企业微信机器人或管理员邮箱只接收环境、服务、错误编号、时间、请求或任务 ID、错误分类和后台路径，不接收投标文件、Prompt、报价、企业证据、凭证或完整堆栈。相同告警合并并设置冷却时间，恢复后发送恢复通知。

### V1 不做

V1 不部署 Prometheus、Grafana 和 Loki。后续规模扩大时先增加 Prometheus + Grafana，只有本地日志检索成为明确瓶颈时再增加 Loki。

### 下一讨论点

下一步讨论备份与恢复，包括备份频率、保留周期、异机位置和恢复目标。

---

## 九十八、备份恢复：本机自动备份与接口预留

### 方案调整

用户认为云对象存储的账号、权限、生命周期和恢复管理对 V1 过于复杂，确认暂不接入云对象存储、NAS 或自动异机灾备。

V1 改为：

```text
本机自动备份
+ 每周人工导出加密包
+ 预留 BackupTarget 接口
```

### 本机备份

备份目录为 `/opt/bid-agent/backups/`。PostgreSQL 每 6 小时使用 `pg_dump -Fc` 备份；MinIO 每天增量复制；Compose、Caddy、迁移版本和部署脚本每天备份。所有本机备份保留最近 7 天。

每个批次保存时间、应用版本、Alembic 版本、对象数量、文件大小、SHA-256 和恢复清单。系统自动检查备份命令结果、文件大小、哈希、`pg_restore --list` 和关键对象，失败或过期时通知管理员。

### 人工离机副本

管理员每周执行部署命令生成类似 `bid-agent-backup-2026-08-19.tar.age` 的加密包，下载到管理员电脑、加密移动硬盘或企业批准的网盘。V1 不建设备份管理页面，不自动上传第三方平台，加密私钥不保存在部署服务器。

### 扩展接口

应用内部定义 `BackupTarget`，统一提供：

```text
upload
download
list
verify
delete_expired
health_check
```

V1 只实现 `LocalBackupTarget`，预留 `S3BackupTarget` 和 `NasBackupTarget`。未来接入 OSS、COS、OBS、S3 或 NAS 时新增适配器并修改配置，不改数据库、MinIO 和业务流程。

### 恢复与风险

上线前必须在空环境完成一次真实恢复，之后每季度演练一次，验证登录、项目、文件、引用、任务和导出。

该方案不能防止服务器磁盘和本机备份同时损坏，也不正式承诺 RPO 6 小时或 RTO 4 小时。V1 只承诺本机恢复、人工离机副本和恢复演练能力，自动异机灾备进入后续版本。

### 下一讨论点

下一步可以继续讨论 API 资源与错误结构，或者先进入前端页面信息架构与 Mock 契约。

---

## 九十九、前端基线：复用现有原型并改为政务蓝灰风

### 原型评估

用户提供了组员制作的单文件 HTML 前端。评估确认该原型已经覆盖项目概览、文件解析、响应矩阵、智能编写、合规审核、企业资料、版本导出、原文抽屉和移动端适配，具有较高的视觉和业务原型复用价值。

但原型使用一个约 100 KB 的 HTML 文件承载全部 CSS、JavaScript、全局状态、`onclick`、`innerHTML` 和 `localStorage`，没有 React、TypeScript、真实 API 和 Mock 契约，因此不能直接作为正式代码长期扩展。

### 用户确认

用户确认将该原型作为视觉和交互基础，正式前端迁移为 Next.js 和 TypeScript。优先复用页面布局、卡片、表格、抽屉、模态框和部分演示业务数据，重新实现 API、权限、路由、状态管理、任务轮询和契约校验。

需要新增登录页、我的工作台、全局项目入口和四个人工确认关口；企业资料库移到全局导航。原型中的“一键生成最终投标书”、五个专业 Agent、企业 VPC 内网运行等表述必须按正式技术方案修正。

### 视觉方向确认

用户确认将原有深色紫色 AI 科技风改为白灰政务商务风：

```text
页面背景       #F5F7FA
卡片与导航     #FFFFFF
主要文字       #1D2633
次要文字       #667085
边框           #DDE3EA
主操作蓝       #1F5AA6
深色政务蓝     #153E73
浅蓝选中背景   #EAF2FB
成功           #237B55
提醒           #B7791F
高风险         #C2413B
阻断           #A61B1B
```

页面采用清晰网格、左对齐、细边框、6～8px 圆角、轻阴影或无阴影和高信息密度。取消大面积渐变、发光效果和字符图标。AI、Agent、Skill 与 MCP 不作为视觉中心，用户重点看到当前阶段、待办、证据来源、人工确认、责任人、风险和版本状态。

文案从“AI 一键生成投标书”“AI AUTHORING STUDIO”“MULTI-AGENT REVIEW”调整为“生成章节草稿”“标书编制”“质量与风险复核”等正式业务表达。

### 下一讨论点

下一步继续确认全局导航与项目工作区导航，然后梳理核心页面清单、角色权限和 Mock 场景。

---

## 一百、前端导航：我的工作台与两层信息架构

### 用户确认

用户确认登录后首先进入“我的工作台”，不直接进入最近项目。工作台聚合我的待办、待人工确认、阻断事项、临期项目、运行中任务和最近访问项目。

桌面端采用：

```text
顶部全局导航
我的工作台｜项目｜企业资料库｜模板与规则｜系统管理

项目内左侧导航
项目总览｜文件与解析｜要求与评分｜响应规划
标书编制｜审核与风险｜交接与递交｜项目记录
```

顶部同时保留项目切换器、全局搜索、运行任务入口和用户菜单。企业资料库、模板与规则和系统管理属于组织级能力，不放入具体项目侧边栏。

四个人工关口不单独建设确认中心，而是分别放在要求与评分、响应规划、标书编制和审核与风险页面。我的工作台只显示待确认摘要并跳转到对应业务页面。

菜单按角色和项目权限隐藏或禁用，但最终权限仍由 FastAPI 校验。移动端将两层导航收进抽屉，支持查看、确认和轻量处理，不要求手机完成复杂矩阵和长篇章节编辑。

V1 不建设 Agent、Skill、MCP 独立管理页，不提供自定义菜单、通用流程设计器、复杂数据大屏和独立消息中心。

### 下一讨论点

下一步梳理核心页面清单，确定现有原型页面的保留、拆分、改名、新增和删除范围。

---

## 一百零一、前端页面：14 个一级入口

### 用户确认

用户确认 V1 使用以下核心页面清单：

```text
全局入口
登录
我的工作台
项目
企业资料库
模板与规则
系统管理

项目内入口
项目总览
文件与解析
要求与评分
响应规划
标书编制
审核与风险
交接与递交
项目记录
```

登录页独立于登录后导航。14 个入口不代表 14 套复杂系统，页面内部优先使用标签页、抽屉和对话框，避免继续拆分大量子页面。

### 原型迁移

- 首次上传引导改为新建项目向导；
- 项目概览保留为项目总览；
- 招标文件解析保留并增加版本和人工确认；
- 响应与评分矩阵拆为要求与评分、响应规划；
- AI 智能编写改为标书编制；
- 合规审核按正式 5 Agent 总体架构修正；
- 企业材料库移到全局导航；
- 版本与导出拆为交接与递交、项目记录。

### 删除范围

V1 删除全局“AI 一键生成投标书”、独立通知中心、用户模型策略、企业 VPC 状态卡、Agent/Skill/MCP 技术管理页、不经过门禁的快速导出和虚构 Agent 动态。真实运行动态从工作流、Agent 和任务记录读取。

### 下一讨论点

下一步确认不同角色可以看到哪些页面、读取哪些数据以及执行哪些操作。

---

## 一百零二、前端权限：9 个业务角色映射为 6 种视图

### 用户确认

用户确认不为 9 个业务角色分别建设独立页面，而是归并为：

```text
编制视图
项目管理视图
审核视图
资料管理视图
递交视图
系统管理视图
```

编制视图对应标书编写人、技术方案人员和商务/报价人员；审核视图对应商务审核人员和合规/法务人员。其他四类视图分别对应项目负责人、资料管理员、投标递交责任人和系统管理员。

### 项目职责补齐

权威项目角色补齐为：

```text
BID_MANAGER
BID_WRITER
TECHNICAL_WRITER
PRICING_OWNER
COMMERCIAL_REVIEWER
COMPLIANCE_LEGAL_REVIEWER
SUBMISSION_OWNER
```

六类视图只用于前端页面复用，不取代七个项目职责。资料管理员和系统管理员主要使用组织级平台角色。

### 权限边界

- 编制、技术和报价人员按职责编辑要求、规划和章节，但不能关闭自己的审核问题；
- 商务审核人员处理商务、报价和证明材料问题；
- 合规/法务人员处理资格、实质性要求、承诺、法律风险和授权豁免；
- 项目负责人分配任务、确认响应计划和交接；
- 递交责任人确认接收、记录人工递交并上传回执；
- 资料管理员维护和发布企业资料；
- 系统管理员管理账号、配置和审计，但不能替代业务确认。

前端根据权限隐藏、禁用或只读展示，FastAPI 对所有写操作重新校验 Session、平台角色、项目职责、对象归属、业务状态和版本。

### 下一讨论点

下一步为核心页面定义 Mock 场景，确保前端不仅能展示正常数据，也覆盖空数据、加载、失败、权限和人工关口。

---

## 一百零三、Mock 场景：基础状态与业务专属状态

### 用户确认

用户确认前端 Mock 使用 TypeScript、Zod 和 MSW，并调用与未来 FastAPI 相同的 API 路径。Mock 与真实后端共享字段、状态枚举、状态码和错误结构，页面不能直接导入假数据代替 API 层。

每个核心页面统一支持：

```text
default
empty
loading
error
readonly
forbidden
```

分别表示正常、空数据、加载中、接口失败、只读和无权访问。

### 代表身份

Mock 使用 7 个代表身份：

```text
bid_writer
bid_manager
commercial_reviewer
compliance_reviewer
material_admin
submission_owner
system_admin
```

不生成所有页面、所有状态和所有角色的完全组合。技术方案人员和报价人员通过编制身份叠加不同项目职责测试。

### 页面专属场景

- 文件与解析：未上传、上传中、解析中、部分成功、失败、新澄清版本和复杂图表待确认；
- 要求与评分：低置信度、高风险未确认、基线可确认、门禁未满足和输入失效；
- 响应规划：证据缺失、覆盖不足、报价未确认、计划可确认和退回修改；
- 标书编制：未生成、生成中、无来源、引用失效、人工锁定、版本冲突和草稿可确认；
- 审核与风险：审核中、高风险、阻断、整改、待复核、合法豁免和结果可确认；
- 交接与递交：未达门禁、生成中、部分成功、待接收、递交成功、失败和基线失效。

四个人工关口均准备“可确认”和“门禁未满足”场景。

### 数据边界

Fixture 使用固定 ID、固定时间和 `[SIMULATED]` 标记，保存来源、版本、权限和状态，不随机生成关键业务数据，也不冒充真实企业与政府项目。V1 控制为约 35～45 份固定 Fixture。

后端完成后，以 FastAPI OpenAPI 为实现权威，通过契约测试检查 Mock 和真实响应是否都符合相同 Schema。

### 下一讨论点

本轮整体前端对齐已完成，下一步从“我的工作台”开始逐页确定页面区块、按钮、状态和接口契约。

---

## 一百零四、我的工作台：个人行动中心

### 用户确认

用户确认“我的工作台”是登录后的默认页面，定位为个人行动中心，不做复杂数据大屏。

页面采用：

```text
顶部：标题、当前日期、新建项目
摘要：待办、待人工确认、阻断事项、临期项目
主体左侧：我的待办
主体右侧：最近项目与项目进度
底部：运行中任务、最近失败任务
```

待办展示任务名称、所属项目、类型、风险等级、截止时间、责任动作和跳转目标。工作台只提供前往处理、查看项目和允许时重试任务，具体内容仍进入对应业务页面修改。

### 排序规则

```text
阻断事项
→ 今日到期
→ 待人工确认
→ 高风险事项
→ 普通待办
```

相同优先级按截止时间和创建时间排序。

### 角色差异

六种权限视图共享同一布局，但编制、项目管理、审核、资料管理、递交和系统管理分别突出与自身职责相关的任务与异常。

### Mock 与接口

初版使用：

```http
GET /api/v1/me/workbench
```

一次返回摘要、待办、最近项目、运行任务和失败任务。工作台接口只读，不承担业务状态修改。

Mock 覆盖正常、空状态、多个临期项目、阻断、待人工确认、任务失败和只读身份。

### 下一讨论点

下一步讨论“项目列表”页面，包括列表形式、筛选条件、新建入口和项目摘要字段。

---

## 一百零五、项目列表：桌面表格与移动卡片

### 用户确认

用户确认项目页面在桌面端使用表格，在移动端转换为项目卡片，方便同时比较多个项目的阶段、截止时间和风险。

页面提供项目名称或编号搜索，以及项目状态、当前阶段、负责人、“只看我的项目”和排序筛选。有权限的用户可以进入新建项目向导。

### 展示字段

```text
项目名称与编号
采购方式
当前阶段
项目状态
距截止时间
待确认事项
阻断风险
项目负责人
最近更新
操作
```

模拟项目明确显示 `[SIMULATED]`。项目状态、当前阶段和人工关口分别展示，不能只写“进行中”。

### 默认排序

```text
阻断项目
→ 7 天内截止
→ 待人工确认
→ 正常进行中
→ 草稿项目
→ 已完成或已终止
```

普通成员只能进入项目和查看自己的任务；项目负责人可以管理基本信息和成员。删除项目、确认关口、生成交接包和记录递交不放在列表快捷操作中。

### Mock 与接口

```http
GET  /api/v1/projects
POST /api/v1/projects
```

列表支持分页、关键词、状态、阶段、负责人、只看我的项目和排序。Mock 覆盖正常多项目、没有项目、单项目、多个临期项目、阻断项目、只读用户和项目创建失败。

### 下一讨论点

下一步讨论新建项目向导，包括基础信息、成员、招标文件上传和解析启动方式。

---

## 一百零六、新建项目：三步向导与草稿恢复

### 用户确认

用户确认新建项目采用：

```text
第一步：项目基本信息
第二步：项目成员
第三步：采购文件上传
```

不使用单页长表单。

### 项目基本信息

必填项目名称、采购方式、投标截止时间和项目负责人。项目编号、采购人、代理机构、预算、最高限价、标包名称和说明可以稍后完善。V1 只创建一个主标包。

第一步完成后创建 `DRAFT` 项目并取得 `project_id`，便于后续上传和中途恢复。

### 项目成员

向导可以分配标书编写、技术方案、商务/报价、商务审核、合规/法务和投标递交责任人。项目负责人必填，其他职责可以稍后补充。

系统校验用户状态、平台角色、重复分配和编制与审核职责分离。同一用户可以承担多个编制类职责，但不能对自己编写的同一对象完成最终复核。

### 文件上传

文件分为主采购文件、澄清/更正/补遗、附件和投标格式模板。启动解析前必须上传一个当前主采购文件。页面显示文件大小、类型、哈希、数据分类、上传和校验状态。

外部 AI 策略默认 `PUBLIC_ONLY`，不在普通向导中要求用户配置复杂出域策略。

### 完成方式

```text
保存草稿
创建并启动解析
```

保存草稿不会启动工作流。创建并启动解析会创建文件版本和任务记录，调用 `document.ingest`，然后进入文件与解析页面查看进度。

### 接口

```http
POST  /api/v1/projects
PATCH /api/v1/projects/{project_id}
PUT   /api/v1/projects/{project_id}/members
POST  /api/v1/projects/{project_id}/documents
POST  /api/v1/projects/{project_id}/ingestion-runs
```

项目、成员和文件分开提交，避免文件失败导致全部表单内容丢失。

### Mock 场景

覆盖正常创建、保存草稿、重复提示、截止时间已过去、角色不匹配、职责分离失败、上传失败、不支持格式、超过 200 MB、校验失败和解析排队。

### 下一讨论点

下一步讨论项目工作区的“项目总览”页面。

---

## 一百零七、项目总览：阶段驱动与当前行动

### 用户确认

用户确认项目总览不做复杂大屏，而是作为项目阶段导航和当前行动入口。

### 项目抬头

顶部展示项目名称与编号、采购方式、项目负责人、投标截止时间、项目状态、当前阶段和当前人工关口。项目状态、阶段和人工关口分别显示。

### 阶段进度

横向阶段条固定为：

```text
文件解析
→ 要求与评分
→ 响应规划
→ 标书编制
→ 审核与风险
→ 交接与递交
```

阶段状态为未开始、进行中、等待人工确认、已完成、已失效和阻断。上游输入变化导致旧结果不可继续使用时显示“已失效”。

### 当前行动卡

页面最突出区域显示当前需要谁处理什么，例如：

```text
当前需要：确认要求基线
3 条高风险要求未确认
2 条低置信度要求待处理
1 个复杂图表待人工确认
操作：进入要求与评分
```

项目总览只跳转到对应业务页面，不直接提供人工关口确认按钮，确保确认时可以同时查看证据、版本和风险上下文。

### 摘要与真实变化

摘要展示文件版本、要求与评分覆盖、章节编制进度、阻断风险、待确认事项和运行任务。

其他区域展示阻断与高风险事项、成员待办、最近变化和运行任务。最近变化来自 `audit_event`、`workflow_run` 和业务版本记录，不展示虚构 Agent 动态。

### 接口

```http
GET /api/v1/projects/{project_id}/overview
```

返回 `project`、`stage_progress`、`current_action`、`summary`、`blocking_items`、`member_tasks`、`recent_changes`、`running_tasks` 和 `allowed_actions`。

### Mock 场景

覆盖草稿项目、正在解析、等待要求基线确认、正在标书编制、存在阻断风险、即将截止、等待交接、已完成项目、输入版本失效和只读成员。

### 下一讨论点

下一步讨论“文件与解析”页面，包括文件版本、解析任务进度、原文与结构化结果对照、OCR 与复杂图表人工确认、解析修正、新澄清文件影响提示，以及与第一个人工关口的边界。

---

## 一百零八、文件与解析：三栏校对与版本影响

### 用户确认

用户选择三栏校对工作台：

```text
左侧：文件与版本、解析目录、异常筛选
中间：原文件预览、页码定位、原文高亮和上下文
右侧：结构化结果、置信度、修正、复核与处理状态
```

页面只负责文件上传、版本、解析、校对、修正和影响提示，不承担要求基线确认、风险关闭或标书生成。

### 文件与版本

文件包括主采购文件、澄清/更正/补遗、附件和格式模板。列表显示名称、类型、版本、上传时间、解析状态、是否当前有效及是否已纳入解析。澄清文件与主文件通过版本关系关联，不覆盖旧文件；出现新文件时显示其对要求、评分、章节和审核结论的影响数量。

解析目录提供全文、目录结构、资格条件、实质性要求、评审因素、时间节点、表格、图片与复杂图表和解析异常。选择目录后，原文和结构化结果同步定位。

### 原文与人工修正

中间区始终显示只读原文件，支持页码、缩放、页内搜索、原文高亮与上下文。右侧结果卡展示对象类型、文件页码、来源定位、机器解析值、置信度和处理状态。

机器原始结果永久保留；人工提交修正时保存原值、修正值、原因、处理人和时间，不覆盖机器结果。正文、标题、表格、图片和复杂图表按对象类型处理。复杂图表以 OCR 为主，按数据出域策略调用多模态；调用失败、禁止出域或低置信度时，保留原图与 OCR，转人工确认。

截止时间、资格条件、实质性要求、最高限价、评分分值和格式签章要求属于关键字段，修正后需要投标经理或合规审核人员复核。

### 与第一个人工关口的边界

```text
文件与解析：解析、校对与关键字段处理
→ 要求与评分：抽取和组织资格、实质性、评分和时间要求
→ REQUIREMENT_BASELINE_CONFIRMATION：确认后续编制依据
```

普通低置信度内容可以带标记进入要求与评分；关键字段未处理、解析失败或可能影响要求/评分的复杂图表待确认时，不允许发起第一个人工关口。

### 新文件影响

上传澄清、更正或补遗后创建新版本，不覆盖旧结果。系统解析新版本、对照当前有效结果、标出受影响对象、创建待办和失效标记，并跳转对应页面处理。旧审核结论转待复核，不删除历史。

### 权限、任务与接口

所有项目成员可查看文件和解析结果；编写、技术和报价职责成员处理普通修正；投标经理上传、重新解析、查看影响和分派责任人；商务与合规审核人员复核关键字段。上传、OCR、重新解析和复杂图表识别均异步执行，并以 `task_run_id` 返回进度。

```http
GET  /api/v1/projects/{project_id}/documents
POST /api/v1/projects/{project_id}/documents
GET  /api/v1/projects/{project_id}/documents/{document_id}/versions
GET  /api/v1/document-versions/{document_version_id}/parse-view
POST /api/v1/document-versions/{document_version_id}/reparse
POST /api/v1/document-segments/{segment_id}/corrections
POST /api/v1/document-corrections/{correction_id}/review
GET  /api/v1/projects/{project_id}/document-impact
```

### Mock 场景

覆盖未上传文件、解析中、解析完成无异常、低置信度、关键字段复核、复杂图表、多模态拦截、解析失败、澄清失效和只读成员。

### 下一讨论点

下一步讨论“要求与评分”页面，确定要求和评分项工作台、来源定位、人工基线确认以及澄清后重新确认方式。

---

## 一百零九、要求与评分：采购规则基线工作台

### 用户确认

用户确认将要求与评分放在同一工作台，使用：

```text
要求清单｜评分项｜冲突与待确认
```

三个标签页均采用左侧分类、中间清单、右侧详情与来源的布局。页面确认采购文件中的要求和评分规则，不匹配企业实际证据、不生成响应矩阵、不预测得分，也不生成标书。

### 要求清单

要求项分类为资格、实质性、技术、格式、时间、商务和其他，显示强制等级、后果、来源、置信度和状态。允许确认、退回重新抽取、标记待人工判断、合并、拆分和查看关联评分项。

资格、实质性、截止时间和格式签章等关键要求逐条确认。低风险同类要求可以批量确认，但必须显示数量、共同来源范围和审计摘要。

### 评分项

评分项记录评分维度、分值、条件摘要、评分方式、材料要求、来源、置信度和状态。评分方式区分客观、主观、公式、混合和无法判断；材料要求只表达采购文件要求，不代表企业已经满足。

主观评分仅保存覆盖目标和评分关注点，后续不得预测得分。公式型评分保留原始公式、变量和解析结果；单位、区间、上下限或比较基准不明确时标记为规则歧义。分值、公式、报价评分规则、门槛条件和材料要求修正均逐条确认。

### 冲突与待确认

集中处理跨文件冲突、澄清覆盖、关键字段低置信度、重复或矛盾要求、评分表与正文不一致、规则不完整和人工退回事项。跨文件冲突并排展示来源、版本与覆盖关系；无法确定时保持阻断，系统不得自行解释。

“已豁免”仅记录规则解释或处理方式的业务决定，不能豁免资格、实质性或法定要求。澄清、补遗或文件版本变化后，旧 `Requirement Baseline` 标记 `STALE`，创建新版本重新确认，不覆盖历史。

### 第一个人工关口

页面展示：

```text
草稿
→ 待确认
→ 已确认
→ 已失效
```

只有当前有效采购文件已解析、关键字段完成必要复核、影响要求或评分的复杂图表已处理、无未解决阻断项、且所有要求与评分均可定位来源时，才可发起 `REQUIREMENT_BASELINE_CONFIRMATION`。

投标经理负责确认；合规审核人员协助复核但不能代替确认。确认后生成只读 `Requirement Baseline`，下游响应规划只读取已确认且未失效版本。

### 权限、接口与 Mock

投标经理确认基线、处理跨文件冲突和分派责任人；编写人与技术人员处理普通项；商务与合规审核人员复核关键规则；报价人员只能提出商务/报价规则修正；资料管理员查看来源和材料要求；只读成员不能修改或确认。

```http
GET  /api/v1/projects/{project_id}/requirements
GET  /api/v1/projects/{project_id}/scoring-items
GET  /api/v1/projects/{project_id}/requirement-conflicts
GET  /api/v1/requirements/{requirement_id}
PATCH /api/v1/requirements/{requirement_id}
POST /api/v1/requirements/{requirement_id}/merge
POST /api/v1/requirements/{requirement_id}/split
POST /api/v1/requirement-baselines/validation
POST /api/v1/requirement-baselines/confirmation
GET  /api/v1/projects/{project_id}/requirement-baselines/current
```

Mock 覆盖无异常、低置信度、高风险、公式完整/歧义、跨文件截止时间冲突、澄清导致基线失效、门禁未满足、可确认、已确认只读和只读成员。

### 下一讨论点

下一步讨论“响应规划”页面，确定如何把已确认要求和评分规则转换为响应方式、证据候选、章节任务、责任人和第二个人工关口。

---

## 一百一十、响应规划：从要求基线到执行计划

用户确认响应规划页面使用响应矩阵、评分覆盖、章节与任务、缺口与冲突四个标签页。页面把已确认要求转换为响应方式、证据候选、评分覆盖、章节责任和报价依赖，不生成正式章节、不自动确认企业满足资格。

证据相似匹配只产生候选；不适用理由、关键证据用途、评分映射、缺失材料任务和责任人必须人工处理。全部规划门禁满足后，由投标经理通过 `RESPONSE_PLAN_CONFIRMATION`，生成只读 `Response Plan Baseline`。

初版采用响应计划查询与生成、矩阵修改、证据选择、章节计划、任务分配、基线校验和确认 8 个接口。Mock 覆盖无基线、规划中、证据缺失、报价未确认、冲突、可确认、只读和输入失效。

---

## 一百一十一、标书编制：结构化内容块与版本保护

用户确认标书编制页面采用左侧章节目录、中间结构化章节编辑器、右侧要求/评分/证据/历史参考。章节由标题、段落、列表、表格、图表、报价、引用、附件提示、待补充和内部提示等内容块组成。

企业事实必须有来源；无来源时保留待补充项；人工编辑默认锁定；局部重生成不得覆盖人工内容；报价未确认时只显示占位；图表必须绑定数据来源。编辑使用乐观锁，冲突由用户选择保留、采用最新或人工合并。

章节和引用等门禁通过后，章节负责人确认本人章节，投标经理通过 `CHAPTER_DRAFT_CONFIRMATION`，创建只读草稿基线。初版采用章节列表/详情、生成、内容块编辑与重生成、版本查询、基线校验和确认 8 个接口。

---

## 一百一十二、审核与风险：问题整改与复核闭环

用户确认审核与风险页面使用审核总览、审核问题、风险清单和一致性检查四个标签页。审核问题按提出、整改、待复核、关闭或重新打开流转；编写人不能关闭自己的问题，商务和合规审核只能处理各自审核域。

阻断风险必须解决，高风险接受必须记录依据、责任人和有效范围。文件、章节、证据或报价变化后，相关问题重新打开或失效。只有审核全部完成、无阻断、整改已复核、报价和一致性通过、引用有效且输入未失效时，才能通过 `REVIEW_RESULT_CONFIRMATION`，进入交接准备。

初版采用审核总览/运行/问题、整改与复核、风险与风险接受、结果校验和确认 10 个接口。三页形成 `Requirement Baseline → Response Plan Baseline → Chapter Draft Baseline → Review Result Confirmation` 闭环。

### 下一讨论点

下一批讨论“交接与递交”和“项目记录”。

---

## 一百一十三、交接与递交：冻结基线与人工外部递交

用户确认交接与递交页面使用交接准备、交接包和递交记录三个标签页。交接门禁检查审核确认、输入版本、风险、报价、附件、内外内容隔离、文件生成条件和递交责任人，不满足时不能绕过。

投标经理重新输入密码并确认后创建只读 `HANDOVER_BASELINE`，固定文件、要求、计划、章节、审核、报价、附件、模板和规则版本。输入变化会使基线失效并撤销旧交接包。

交接包包含正式 DOCX、同源 PDF、附件和版本清单、递交检查清单、SHA-256 与内部说明。递交责任人确认接收后，人工在外部平台递交，并在系统登记时间、渠道、结果、流水号、失败原因、备注和回执。平台不自动登录交易平台或操作 CA。

初版使用交接门禁、基线校验与确认、交接包生成/查询/接收、递交查询/登记/更新和回执上传 10 个接口。

---

## 一百一十四、项目记录：只读时间线与审计查询

用户确认项目记录使用时间线、版本与基线、人工确认、任务运行和操作审计五个标签页。页面汇总文件、要求、计划、章节、审核、风险、交接、递交和项目状态变化，记录时间、操作者、前后状态、关联版本和原因。

页面展示完整基线链、四个人工关口、内部交接确认、任务耗时/重试/版本和错误摘要。普通用户查看业务摘要，系统管理员查看必要技术详情，但不读取完整敏感 Prompt。审计记录不可编辑或删除，只允许导出脱敏 CSV 摘要。

项目记录只读取真实的 `audit_event`、`task_run`、`workflow_run`、`agent_run`、业务版本与基线、交接包和递交记录，不展示虚构 Agent 动态，也不能修改业务状态。

### 下一讨论点

下一批讨论企业资料库、模板与规则、系统管理和登录页。

---

## 一百一十五、企业资料库：跨项目材料版本管理

用户确认企业资料库维护资质、人员证书、项目业绩、合同验收、产品技术和说明材料，使用资料列表、有效期与待处理、使用记录三个标签页。材料版本不覆盖，过期或停用会影响项目引用，但是否满足项目要求仍由响应规划人工确认。

资料管理员维护，商务/合规审核人员审核，项目成员只读检索。初版使用材料查询/创建/详情/修改、版本、审核、状态和使用记录 8 个接口。

---

## 一百一十六、模板与规则：受控模板与确定性配置

用户确认页面使用文档模板、规则集、词表与分类三个标签页，不建设 Prompt、Agent、Skill 或 MCP 管理。模板启用前执行结构和样例渲染校验；规则版本不改写历史结果；词表不能运行任意代码。初版使用 10 个接口。

---

## 一百一十七、系统管理：账号、配置与运行状态

用户确认系统管理包括用户与权限、模型与外部服务、运行状态、安全与审计。支持账号管理、管理员重置密码、Session 撤销、模型连接测试、健康/容量/备份状态和全局审计。

API Key 不显示明文，密钥继续使用 Compose Secrets；网页不执行数据库恢复、服务器命令或容器管理；不建设 Agent、Skill、MCP 动态管理。初版使用 11 个接口。

---

## 一百一十八、登录页与 14 个页面方案完成

用户确认登录页采用白灰政务商务风，仅显示平台名称、账号、密码、登录按钮、安全提示和支持信息。不提供注册、社交登录、角色/模型选择或 AI 动画。

登录使用 Argon2id、PostgreSQL 服务端 Session 和安全 Cookie；空闲 30 分钟、绝对最长 8 小时；连续失败限速；用户通过系统管理员重置密码。初版使用登录、退出、会话查询和修改密码 4 个接口。

至此全局 6 个入口与项目内 8 个入口，共 14 个一级页面方案全部完成。

### 下一讨论点

下一步绘制整体架构图，并制定前端实施计划与 Mock 落地顺序。

---

## 一百一十九、V1 开发前正式基线一致性审计

用户确认下一步进入 V1 开发，并计划使用 Claude Code。双方约定先审计正式开发基线，再生成可直接粘贴给 Claude Code 的开发约束（宪法），不单独新建宪法文档。

本次审计范围包括：

- `specs/11-产品需求文档（PRD）.md`
- `specs/09-业务流程与项目状态机.md`
- `specs/10-核心业务对象与数据字典.md`
- `specs/14-API接口设计.md`
- `specs/15-评估体系与测试计划.md`
- `specs/17-技术方案与系统架构设计.md`
- `specs/18-数据库设计与实现方案.md`
- `specs/13-项目决策与工作记录.md`
- `docs/development/2026-08-21-DeepAgents架构迁移设计.md`

审计发现并修正的当前口径问题：

1. 将业务流程中的 `Supervisor Agent` 统一为主 Deep Agent及其原 Supervisor 职责；
2. 将 `workflow_run` 统一解释为长期业务流程运行，不再作为当前 LangGraph 主图语义；
3. 将 `agent_type` 的当前主 Agent 枚举统一为 `MAIN_AGENT`；
4. 明确 `agent_run` 是阶段级短运行，人工关口状态由 `workflow_run` 管理；
5. 将 API 中的通用 LangGraph Resume 表述改为“禁止调用任何通用运行时恢复接口绕过领域门禁”；
6. 将数据库设计中的主图运行表述改为长期业务流程运行；
7. 将 `specs/13` 的旧 LangGraph 决策章节 10.16—10.21 明确标记为历史、已替代；
8. `specs/17` 的旧 LangGraph 5.2.2 章节明确标记为历史、不再实施；
9. 保留历史决策摘要用于审计，但不作为 Claude Code 当前开发依据。

一致性结论：当前正式开发基线可以进入 V1 开发。产品范围以 PRD 为准，最新决策以 `specs/13` 最新章节为准，状态以 `specs/09` 为准，领域对象以 `specs/10` 为准，接口以 `specs/14` 为准，技术实现以 `specs/17` 和 Deep Agents 迁移设计为准，数据库以 `specs/18` 为准，评估与验收以 `specs/15` 为准。

## 一百二十、Deep Agents 迁移设计文档处理结论

用户询问 `docs/development/2026-08-21-DeepAgents架构迁移设计.md` 是否可以删除。结论为：当前不删除。虽然其中大部分内容已同步进正式规格文档，但该文档仍是 Deep Agents 开发实施的集中入口，包含三类 A2A 契约、版本锁定、Skill/MCP 边界、多段短运行、人工关口续接、迁移顺序和验收清单。

历史 LangGraph 章节已经明确标记为历史/已替代，Claude Code 当前实现不得依据这些历史章节。待 Deep Agents 骨架、结构化契约、人工关口续接和回归测试全部落地后，再评估是否将迁移设计压缩为索引或归档。

## 一百二十一、Claude Code 开发入口约定

Claude Code 首次进入仓库时，优先阅读：

```text
项目文件夹导览.md
→ specs/11-产品需求文档（PRD）.md
→ specs/13-项目决策与工作记录.md
→ specs/09-业务流程与项目状态机.md
→ specs/10-核心业务对象与数据字典.md
→ specs/14-API接口设计.md
→ specs/17-技术方案与系统架构设计.md
→ specs/18-数据库设计与实现方案.md
→ specs/15-评估体系与测试计划.md
→ docs/development/2026-08-21-DeepAgents架构迁移设计.md
```

实际任务采用按切片阅读，不要求每次加载全部文档：

```text
PRD 对应 FR
→ 状态机对应状态
→ 数据字典对应对象
→ API 对应接口
→ 数据库设计对应表和约束
→ 技术方案对应实现边界
→ 评估计划对应测试和验收证据
```

前端 `frontend/` 是冻结的界面、功能、交互和视觉基线；后续只允许替换内部工程实现和接入真实 API，不得重新设计页面。

---

## 一百二十二、历史 LangGraph 章节清理与 V1 开发入口确认

用户确认：历史章节如果不再作为当前开发依据，可以删除。经核对，已删除 `specs/13-项目决策与工作记录.md` 中原 `10.16`—`10.21` 的 LangGraph 方案演进细节，并删除 `specs/17-技术方案与系统架构设计.md` 中自维护 LangGraph 单主图、共享 State、interrupt/resume 和节点契约的详细历史章节。

保留的不是历史实现细节，而是必要的迁移索引和决策审计结论。当前 V1 开发只以 Deep Agents Harness 迁移设计、最新决策记录及正式规格文档为准，不得依据已删除的 LangGraph 方案编码。

当前开发入口确认如下：

- 产品范围：`specs/11-产品需求文档（PRD）.md`；
- 最新决策：`specs/13-项目决策与工作记录.md`；
- 流程状态：`specs/09-业务流程与项目状态机.md`；
- 领域对象：`specs/10-核心业务对象与数据字典.md`；
- 接口契约：`specs/14-API接口设计.md`；
- 技术架构：`specs/17-技术方案与系统架构设计.md`；
- 数据库：`specs/18-数据库设计与实现方案.md`；
- 测试验收：`specs/15-评估体系与测试计划.md`；
- Deep Agents 实施细则：`docs/development/2026-08-21-DeepAgents架构迁移设计.md`；
- 前端基线：`frontend/`，页面功能、交互和视觉样式冻结。

结论：正式文档已完成当前架构口径收敛，可以进入 V1 开发。Claude Code 必须先阅读上述入口文档、输出实现计划并等待确认，再开始编码；发现文档冲突时必须暂停并报告。
---

## 一百二十三、Claude Code 语言规范补充

用户补充 Claude Code 开发宪法：开发过程必须使用中文回复；新增或修改的代码注释必须使用中文，禁止使用英文注释，除非是第三方 API、协议、库名、标准术语或代码标识本身不可翻译。