# 投标 Agent AI 项目文件夹导览

> 用途：说明项目目录结构、文件职责和 GitHub 上传前检查事项。  
> 项目根目录：`bid-agent-ai`  
> 当前产品主线：投标文件智能编制与合规审核平台 V1

## 一、项目整体定位

本项目是一个面向投标企业内部团队的投标 Agent 项目，V1 聚焦：

```text
单个政府采购信息化服务项目、单个采购包
→ 项目与采购文件接入
→ 文件解析和要求提取
→ 企业证据匹配
→ 报价方案确认
→ 标书章节生成
→ 审核、风险和返工
→ 内部交接与人工递交留痕
```

当前仓库主要包含产品需求、技术方案、业务流程、评估体系、架构图、前端原型和项目决策记录。后端服务目录尚未建立，下一阶段从 FastAPI 后端第一个纵向切片开始。

## 二、根目录文件

| 文件 | 内容 | 是否建议上传 |
|---|---|---|
| `项目文件夹导览.md` | 当前文件，项目目录说明和上传注意事项 | 是 |
| `项目方向选型论证-招标书-修改版.md` | 项目方向、产品形态和方案选型论证材料 | 是，确认无敏感信息后上传 |
| `项目开发对话历史.md` | 项目需求讨论、范围澄清和决策过程的完整记录 | 谨慎上传，可能包含内部讨论信息 |

## 三、`specs`：产品与技术规格文档

这是项目的主要知识和决策文档目录，保存业务定义、产品需求、技术方案、接口契约、数据库设计和评估标准。

### 3.1 正式业务与产品文档

| 文件 | 内容 |
|---|---|
| `07-客户画像与场景设定.md` | 目标客户、用户角色和典型使用场景 |
| `08-招投标术语与业务知识手册.md` | 招投标领域术语、业务概念和规则说明 |
| `09-业务流程与项目状态机.md` | V1 业务流程、状态转换和人工关口 |
| `10-核心业务对象与数据字典.md` | 领域对象、字段语义、枚举和业务关系 |
| `11-产品需求文档（PRD）.md` | V1 产品范围、功能需求、验收场景和发布门槛；产品主基线 |
| `12-项目难点与解决思路.md` | 项目难点、解决思路和技术/业务边界 |
| `13-项目决策与工作记录.md` | 已确认决策、工作进度、恢复位置和重要约束 |

### 3.2 开发与实现基线

| 文件 | 内容 |
|---|---|
| `14-API接口设计.md` | 前后端共享的 V1 HTTP API 契约 |
| `15-评估体系与测试计划.md` | L1/L2/L3/L4 评估体系、指标、阈值和测试计划 |
| `16-产品版本规划.md` | V1、V2、V3 的产品定位、边界和演进方向 |
| `17-技术方案与系统架构设计.md` | 前端、后端、Agent、Skill、MCP、存储、部署和安全方案 |
| `18-数据库设计与实现方案.md` | PostgreSQL Schema、核心表、约束、版本失效和备份方案 |
| `19-产品经理讲解稿-剥洋葱版.md` | 按层次讲解项目的产品经理讲稿 |
| `20-讲解稿-业务紧贴版.md` | 贴近业务流程的项目讲解稿 |
| `讲解PPT_v4.pptx` | 项目讲解演示文稿 |
| `~$讲解PPT_v4.pptx` | Office 编辑时生成的临时锁定文件 | 不要上传 |

### 3.3 `specs/research`：早期研究资料

该目录保存项目早期调研、方向分析和方案研究结果，主要用于历史参考，不覆盖当前正式 PRD 和技术基线。

| 子目录/文件 | 内容 |
|---|---|
| `01-产品形态.md` | 产品形态研究 |
| `02-数据来源.md` | 数据来源研究 |
| `03-开源项目.md` | 开源项目调研 |
| `04-实现方案.md` | 早期实现方案研究 |
| `05-决策汇总.md` | 早期决策汇总 |
| `06-方向校准.md` | 方向校准记录 |
| `research/_evidence` | 调研佐证材料，如公告附件压缩包和文档样本 |

`_evidence` 可能包含真实或受限制的业务文件，上传 GitHub 前应确认版权、保密等级和个人/企业敏感信息。若只是内部证据，不建议上传公开仓库。

## 四、`frontend`：已冻结的前端界面与功能基线

当前`frontend`是重新开发完成的静态HTML/CSS/JavaScript前端，也是后续V1开发必须保持的界面、页面功能、信息架构、交互方式和视觉样式基线。它暂时不是Next.js工程，但后续迁移只能替换内部工程实现，不得重新设计现有界面和功能。

| 路径 | 内容 |
|---|---|
| `frontend/index.html` | 前端入口页面 |
| `frontend/css/styles.css` | 全局样式、政务蓝灰视觉和页面布局 |
| `frontend/js/app.js` | 应用启动和页面初始化 |
| `frontend/js/api.js` | 前端 API/Mock 调用封装 |
| `frontend/js/data.js` | 原型场景数据和演示数据 |
| `frontend/js/icons.js` | 图标定义和图标辅助代码 |
| `frontend/js/ui.js` | 通用 UI 交互和渲染辅助函数 |
| `frontend/js/views-*.js` | 各业务角色和业务页面视图 |

当前页面覆盖工作台、项目、文件、要求、响应规划、编制、审核、交接、企业资料库、模板规则、系统管理和登录等V1页面。后续接入FastAPI、权限、任务状态、人工关口和版本控制时，应保持当前页面入口、布局、主要交互、文案语义和政务蓝灰视觉风格不变。

## 五、`docs`：架构图与开发设计

### 5.1 `docs/diagrams`

保存可直接在浏览器打开的正式 HTML 图：

- `V1总体架构图.html`：系统总体架构、基础设施和模块边界；
- `V1主业务流程图.html`：从项目创建到内部交接的主流程；
- `投标Agent编排架构图.html`：Deep Agents 主 Agent、专业 Subagent、结构化 A2A、Skill、MCP 和领域门禁关系；
- `记忆与上下文流转图.html`：长期 `workflow_run`、短期 `agent_run`、上下文和 PostgreSQL 事实源关系；
- `评估四层金字塔图.html`：L1、L2、L3、L4 评估关系。

这些HTML图是当前正式交付物，可以直接上传到GitHub并使用浏览器打开。

### 5.2 `docs/development`

保存已经确认、用于指导开发实施的专题设计文档。当前包含：

- `2026-08-21-DeepAgents架构迁移设计.md`：从自维护 LangGraph 主图迁移到 `deepagents==0.7.8` 的正式设计，定义结构化 A2A、七种调度决策、多段短运行、人工关口续接和版本锁定策略。

该目录中的设计文档必须与 `specs/13`、`specs/17`、`specs/18` 等正式基线保持一致；发生冲突时，应先在项目决策记录中明确最新决策，再同步所有受影响文档和架构图。

原`docs/superpowers`历史计划与设计文档已于2026-08-21删除，不再属于当前仓库正式内容。后续实施方案和开发计划应在正式确认目录后重新建立，不恢复已删除的历史副本。

## 六、开发工作流工具目录

### 6.1 `.claude`

保存Claude Code使用的Spec Kit技能说明，包括宪法、规格、澄清、计划、任务、实现和检查流程。该目录属于开发辅助配置，不定义产品业务事实。

### 6.2 `.specify`

保存Spec Kit的宪法模板、规格模板、计划模板、任务模板、工作流注册信息和辅助脚本。后续可以用于“定宪法→写规格→写计划→拆任务→开发”的规格驱动流程。

业务范围和技术事实发生冲突时，仍以`specs`正式文档和项目决策记录为准，不能用工具模板覆盖PRD、状态机、API或数据库设计。

## 七、当前不存在的目录

以下后端目录目前尚未建立：

```text
backend/
server/
api/
```

后续后端计划使用 FastAPI，并按以下顺序逐步接入前端 Mock：

```text
登录会话
→ 项目创建与查询
→ 项目成员
→ 采购文件与文件版本
→ 解析任务
→ 审计记录
```

## 八、GitHub 上传前检查

### 8.1 建议上传

- `specs/` 正式规格文档；
- `docs/diagrams/` 正式架构和流程图；
- `.claude/`和`.specify/`规格驱动开发工具配置；
- `frontend/`已冻结的前端基线源码；
- 根目录项目说明和方向论证文件。

### 8.2 不建议上传或应先确认

- `frontend/node_modules/`：依赖安装目录，体积大，应通过 `package.json` 和锁文件恢复；
- `frontend/.next/`：Next.js 构建缓存或产物，不应作为源码提交；
- `.playwright-mcp/`、`.superpowers/`：已删除的浏览器和头脑风暴临时产物；
- `specs/research/_evidence/`：可能包含真实业务文件、附件或受限材料；
- `specs/~$讲解PPT_v4.pptx`：Office 临时文件；
- 任何 `.env`、密钥、Token、密码、Cookie、私有证书和服务器配置；
- 含真实客户、企业、联系人、项目编号或采购文件的未脱敏材料。

### 8.3 上传前命令参考

在项目根目录执行：

```powershell
git init
git remote add origin https://github.com/yz261402-dotcom/-.git
git add .
git status
git commit -m "Initial project documentation and frontend prototype"
git branch -M main
git push -u origin main
```

如果远程仓库已经有内容，先执行：

```powershell
git remote -v
git fetch origin
git log --oneline --all -5
```

确认远程内容后再决定是合并、覆盖还是使用新的分支，避免误覆盖已有提交。

## 九、上传前最终确认清单

- [ ] 已确认 GitHub 仓库是否为空；
- [ ] 已检查 `git status` 中没有不应提交的文件；
- [ ] 已确认没有 `.env`、密码、Token、Cookie 或私钥；
- [ ] 已确认 `research/_evidence` 是否允许上传；
- [ ] 已确认需求澄清历史是否包含内部敏感内容；
- [ ] 已排除 `node_modules`、`.next` 和 Office 临时文件；
- [ ] 已确认仓库可见性是公开还是私有；
- [ ] 已确认远程地址和默认分支名称。

## 十、文档维护规则

当新增顶层目录、后端服务或正式文档时，应同步更新本文件。临时日志、测试快照、构建缓存和工具状态目录不应作为项目结构说明中的正式模块。
