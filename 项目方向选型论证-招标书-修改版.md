# 项目方向选型论证：多智能体 + RAG 项目

## 一、背景与结论

计划完成两个项目，用于投递通用大模型应用开发、Agent 开发及 RAG 工程相关岗位：

1. 一个多智能体项目；
2. 一个由智能体调用的 RAG 项目，可采用向量 RAG，或向量 RAG + 图 RAG。

候选方向为法律、招投标和保险。综合考虑业务流程与 Agent 的匹配度、公开数据可得性、技术展示空间、项目差异化和个人可完成度，最终选择 **招投标方向**。

法律和保险同样适合做 Agent 或 RAG。招投标更容易在个人作品集中展示复杂文档处理、任务编排、证据追溯和可量化评测，因此更符合当前投递通用大模型应用岗位的目标。

## 二、为什么选择招投标

### 1. 业务流程适合多智能体协作

投标方处理招标文件时，需要完成需求理解、资格核验、证明材料匹配、响应内容规划、评分项覆盖和跨章节一致性检查。这些任务目标不同，依赖的上下文和工具也不同，适合由多个专业 Agent 分工，再由总控 Agent 管理流程。

建议的任务拆分如下：

- **总控 Agent**：维护任务状态，分发子任务，处理失败重试和人工审批；
- **资格与合规审查 Agent**：比对企业资质与招标文件中的实质性要求，标记不满足项和证据缺口；
- **响应规划 Agent**：根据招标要求生成投标文件目录、章节任务和响应策略；
- **评分点覆盖 Agent**：建立“评分项—响应章节—证明材料”映射，计算客观评分项的覆盖情况；
- **一致性审查 Agent**：检查人员、日期、金额、参数、案例等信息是否存在跨章节冲突；
- **风险复核 Agent**：汇总废标风险、材料缺失和需要人工确认的事项。

文档解析、表格抽取和规则匹配等稳定步骤优先实现为工具或工作流节点。只有需要独立决策、不同上下文或并行协作的任务才使用 Agent，避免为了数量硬拆角色。

### 2. 公开规则可以支撑可验证的项目闭环

《招标投标法》第十九条规定，招标文件应包含技术要求、资格审查标准、投标报价要求和评标标准等内容。因此，公开招标文件通常能够提供需求提取、资格检查和评分项覆盖分析所需的主要依据。

项目不做“中标概率预测”或“评委打分预测”。真实得分还受到专家判断、竞争对手报价和其他投标文件的影响，个人项目无法获得完整信息。更可靠的目标是：

- 提取实质性要求和评分因素；
- 检查企业材料能否提供对应证据；
- 计算评分项和需求条款的覆盖率；
- 发现遗漏、冲突和废标风险；
- 输出带来源位置的审查结论，交由人工确认。

这套闭环可以通过人工标注的测试集验证，不需要编造无法解释的内部评分模型。

### 3. GraphRAG 有明确的业务落点

招标文件中的需求、评分标准、响应章节和证明材料之间存在可追踪的业务关系：

`招标要求 → 评分项 → 响应章节 → 证明材料 → 企业资质/项目案例`

向量检索适合处理语义相近但表达不同的内容，图检索适合沿关系链查找证据、检查覆盖情况和解释结论。两者结合后，可以回答：

- 某项招标要求由哪个章节响应？
- 响应内容引用了哪些企业能力和证明材料？
- 某份证书能支撑哪些资格要求和评分项？
- 哪些评分项缺少响应内容或有效证据？
- 同一人员、参数或案例在不同章节中是否出现矛盾？

### 4. 对通用大模型应用岗位有展示价值

该方向能够覆盖以下通用能力：

- PDF、Word、扫描件和表格等复杂文档解析；
- 长文档分块、元数据设计和结构化抽取；
- BM25、向量召回、重排序和图检索；
- 多智能体状态管理、工具调用、失败重试和人工介入；
- 引用溯源、拒答机制和结构化输出；
- 检索、生成和工作流三个层面的评测。

这些能力可以迁移到合同审查、保险理赔、企业知识库、供应链和合规审核等场景，不会把简历限制在招投标行业。

## 三、两个项目的边界

### 项目一：投标合规审查多智能体系统

该项目重点展示 Agent 编排与复杂任务执行能力。

**输入：**

- 一份招标文件；
- 企业基本信息、资质证书、人员资料、产品参数和历史案例；
- 可选的人工审查规则。

**处理流程：**

1. 解析招标文件并生成结构化需求清单；
2. 识别实质性要求、资格条件和评分因素；
3. 调用企业投标知识 RAG 检索对应证明材料；
4. 生成响应矩阵和投标文件章节规划；
5. 检查评分项覆盖、材料缺失和内容冲突；
6. 对高风险结论触发人工复核；
7. 输出合规审查报告和响应任务清单。

**核心展示点：**

- LangGraph 状态机与多 Agent 编排；
- 并行任务、超时处理、失败重试和人工接管；
- Agent 的结构化输入输出；
- 每条结论的证据引用和执行轨迹；
- 确定性规则与大模型判断的边界。

### 项目二：企业投标知识 GraphRAG

该项目重点展示检索架构、知识建模和评测能力，并作为项目一可以调用的独立工具。

**知识库内容：**

- 企业资质与证书；
- 人员经历与专业能力；
- 产品及技术参数；
- 历史项目案例；
- 历史技术方案；
- 公开招标文件、政策法规和标准文件。

企业内部资料无法从公开渠道完整获得时，使用明确标注的模拟企业数据，不把模拟数据描述为真实业务数据。

**检索架构：**

1. 使用 BM25 和向量检索进行候选召回；
2. 使用元数据过滤限制文件类型、行业、地区和有效期；
3. 使用重排序模型提高证据相关性；
4. 使用知识图谱查询需求、评分项、响应章节和证明材料之间的关系；
5. 根据问题类型在普通混合检索和图检索之间路由；
6. 输出原文引用、文件名称、页码、有效期和置信信息；
7. 证据不足时拒绝生成确定性结论。

## 四、为什么暂不选择保险

保险适合做条款检索、责任分析和理赔材料审核，多智能体与 GraphRAG 也有合理落点。主要限制在于个人项目的数据边界：核保规则、费率模型、风险数据和真实理赔案例通常属于保险公司的内部资产。

如果项目只使用公开保险条款，容易收缩成“上传条款并问答”的通用 RAG Demo。若要做出明显差异，需要补充理赔材料审核、责任链分析、异常检测和人工复核，其中一部分只能使用模拟数据。对于当前以通用 Agent 开发岗为目标的作品集，招投标更容易建立可解释、可评测的完整流程。

如果后续明确投递保险科技、金融科技或保险公司的大模型岗位，保险方向仍然值得作为专项项目补充。

## 五、为什么暂不选择法律

法律方向的数据来源和引用要求清晰，法规效力层级、修订关系、法条引用和案例关系也适合 GraphRAG。合同审查、类案检索、事实核验和法律研究同样能够自然拆分为多智能体流程。

暂不选择法律，主要是因为法律问答和合同审查已经是常见的大模型作品集方向。要形成差异，需要在法规时效性、法条冲突、事实与证据映射、案例适用边界和严格评测上投入较多时间。相比之下，招投标的“要求—响应—证据—评分项”链路更容易在有限周期内做成可演示的工程闭环。

## 六、实施范围与风险控制

### 1. 聚焦一个细分类型

第一版不同时覆盖工程、货物和服务三类招标。优先选择 **IT 软件或信息化服务采购**，使文档结构、企业资料和评分逻辑相对一致，减少无关的行业差异。

建议准备 15～30 份同类型公开招标文件用于开发和知识库建设，从中选择 3～5 份进行人工标注，作为独立测试集。项目目标是证明方法有效，不追求大规模爬取。

### 2. 控制文档解析范围

优先支持文本型 PDF、Word 和常见表格。扫描件 OCR 作为可选能力，不让版面分析和表格还原占据主要开发时间。无法可靠解析的内容需要标记并交由人工确认，不能静默丢失。

### 3. 控制图谱规模

第一版图谱只覆盖以下核心实体和关系：

- 招标要求；
- 实质性条款；
- 评分项；
- 响应章节；
- 企业资质；
- 人员与项目案例；
- 证明材料；
- 文件来源与有效期。

先完成单份招标文件与企业知识库之间的证据链，再考虑跨文件分析、行业知识图谱和历史项目复用。

### 4. 不夸大业务能力

系统定位为投标辅助工具，不自动决定是否投标，不承诺预测中标结果，也不替代法务、商务和技术人员的最终审核。所有高风险结论必须显示证据来源，并支持人工修改和确认。

## 七、评测方案

### 文档解析与抽取

- 实质性条款识别准确率、召回率和 F1；
- 资格要求与评分项抽取准确率；
- 表格字段解析成功率；
- 页码和原文位置保留率。

### RAG 检索

- Recall@K、MRR 和 nDCG；
- 证明材料匹配准确率；
- 引用正确率和证据完整率；
- 图检索相对纯向量检索的增益；
- 无证据问题的正确拒答率。

### 多智能体工作流

- 强制性要求覆盖率；
- 评分项覆盖率；
- 证据缺口识别准确率；
- 跨章节冲突检测准确率；
- Agent 任务成功率、重试率和人工接管率；
- 相比人工审查的耗时变化。

## 八、最终项目定位

最终形成两个相互关联但可独立展示的项目：

1. **基于多智能体的投标合规审查与响应规划系统**；
2. **基于混合检索与知识图谱的企业投标知识 RAG 系统**。

多智能体系统负责理解任务、组织流程和检查结果，GraphRAG 系统负责检索企业能力与证明材料。两者通过明确的工具接口连接，分别展示 Agent 工程和 RAG 工程能力。
