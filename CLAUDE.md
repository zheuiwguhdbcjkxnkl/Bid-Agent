  # bid-agent-ai V1 开发宪法

  你现在参与的是 bid-agent-ai 项目的 V1 开发。

  本文件是开发约束，不替代正式产品和技术文档。所有实现必须以仓库中的最新正式文档为准。

---

  ## 一、语言规范

  整个开发过程中必须使用中文回复，包括：

  - 任务分析；
  - 实施计划；
  - 进度汇报；
  - 错误说明；
  - 测试结果；
  - 最终总结。

  新增或修改的代码注释必须使用中文，禁止使用英文注释。

  以下内容可以保留英文：

  - 第三方 API 名称；
  - 协议名称；
  - 开源库和框架名称；
  - 标准技术术语；
  - 类名、函数名、变量名、字段名；
  - 错误码、命令和配置项；
  - 其他必须保留的代码标识。

  除上述必要情况外，不得使用英文解释项目逻辑、业务规则、实现原因或操作说明。

---

  ## 二、文档优先级

  必须按照以下顺序理解项目：

  1. `项目文件夹导览.md`
  2. `specs/11-产品需求文档（PRD）.md`
  3. `specs/13-项目决策与工作记录.md`
  4. `specs/09-业务流程与项目状态机.md`
  5. `specs/10-核心业务对象与数据字典.md`
  6. `specs/14-API接口设计.md`
  7. `specs/17-技术方案与系统架构设计.md`
  8. `specs/18-数据库设计与实现方案.md`
  9. `specs/15-评估体系与测试计划.md`
  10. `docs/development/2026-08-21-DeepAgents架构迁移设计.md`

  文档职责如下：

  - 产品范围以 `specs/11-产品需求文档（PRD）.md` 为准；
  - 最新决策以 `specs/13-项目决策与工作记录.md` 为准；
  - 业务流程和状态以 `specs/09-业务流程与项目状态机.md` 为准；
  - 领域对象和字段以 `specs/10-核心业务对象与数据字典.md` 为准；
  - API 接口以 `specs/14-API接口设计.md` 为准；
  - 技术实现边界以 `specs/17-技术方案与系统架构设计.md` 和 Deep Agents 迁移设计为准；
  - 数据库设计以 `specs/18-数据库设计与实现方案.md` 为准；
  - 测试和验收以 `specs/15-评估体系与测试计划.md` 为准。

  如果文档之间出现冲突，不得自行猜测、折中或选择性实现。

  必须暂停编码，并用中文报告：

  1. 冲突文件；
  2. 冲突章节；
  3. 冲突内容；
  4. 当前实现可能受到的影响；
  5. 建议采用的解决方案。

---

  ## 三、文档读取与上下文管理

  不得在每个任务开始时无差别读取仓库内全部文档。

  不得默认把完整的 `项目开发对话历史.md` 加载到上下文中。

  必须采用“分层阅读、按任务切片”的方式。

  ### 每次任务先读取

  通常先读取：

  ```text
  项目文件夹导览.md
  specs/11-产品需求文档（PRD）.md
  specs/13-项目决策与工作记录.md 的最新决策章节
  docs/development/2026-08-21-DeepAgents架构迁移设计.md
  ```

  ### 然后按任务读取

  后端流程任务：

  specs/09-业务流程与项目状态机.md

  领域模型任务：

  specs/10-核心业务对象与数据字典.md

  API 任务：

  specs/14-API接口设计.md

  数据库任务：

  specs/18-数据库设计与实现方案.md

  Agent、Skill、MCP、Celery 或技术架构任务：

  specs/17-技术方案与系统架构设计.md
  docs/development/2026-08-21-DeepAgents架构迁移设计.md

  测试任务：

  specs/15-评估体系与测试计划.md

  前端任务：

  frontend/

  历史追溯任务：

  项目开发对话历史.md

  只读取与当前任务直接相关的章节和文件。

  优先使用标题、关键词和章节定位读取，不得无目的加载大型文档全文。

  每次开始编码前，必须用中文说明：

  本次任务：
  实际读取的文档：
  实际读取的章节：
  准备修改的文件：
  预计验证方式：

  如果上下文不足，不得猜测。应继续读取相关章节，或报告缺失信息。

  文档读取优先级如下：

  产品范围
  → 最新决策
  → 当前任务对应的流程和领域对象
  → API 与数据库约束
  → 技术实现方案
  → 测试验收要求
  → 历史讨论记录

  ———

  ## 四、当前 V1 架构

  当前正式架构为：

  Deep Agents Harness
  + 主 Deep Agent
  + 四个专业 Subagent
  + 结构化 A2A 消息契约
  + 七种结构化调度决策
  + FastAPI 确定性领域层
  + PostgreSQL 业务事实源
  + Celery 异步任务执行
  + Redis
  + Skill 白名单
  + MCP 工具层
  + 四个人工关口
  + 多段短运行续接机制

  当前主 Agent 承担原 Supervisor 的协调职责。

  项目代码不得直接构建或维护：

  - LangGraph StateGraph；
  - LangGraph 主图；
  - LangGraph 节点和边；
  - LangGraph Checkpoint 业务恢复逻辑；
  - LangGraph interrupt/resume 工作流；
  - 依赖 LangGraph runtime 作为业务事实源的实现。

  Deep Agents 内部可以依赖 LangGraph runtime，但这属于 SDK 内部实现。

  项目业务代码必须通过 Deep Agents Harness 和自研确定性领域层实现业务流程。

  ———

  ## 五、结构化调度契约

  必须保留并实现以下七种结构化调度决策：

  DISPATCH_AGENT
  WAITING_HUMAN
  RETRY_TASK
  EXPAND_REVIEW_SCOPE
  INVALIDATE_OUTPUTS
  BLOCK_WORKFLOW
  COMPLETE_STAGE

  必须定义并校验以下三类结构化契约：

  OrchestrationDecision
  AgentDelegationEnvelope
  SubagentResultEnvelope

  所有结构化契约必须具备：

  - 明确字段；
  - 明确枚举；
  - 输入校验；
  - 输出校验；
  - 版本信息；
  - 追踪标识；
  - 错误信息；
  - 审计记录；
  - 可测试性。

  不得使用以下内容直接驱动业务状态：

  - 模型自由文本；
  - 自由格式 todo；
  - 未校验的模型推理结果；
  - 非结构化 Agent 对话；
  - 任意字符串形式的下一步动作；
  - 未经领域层验证的 Subagent 输出。

  所有调度决策、Agent 委派和 Subagent 返回结果，都必须经过：

  Schema 校验
  → 业务规则校验
  → 权限和版本校验
  → 领域层处理
  → PostgreSQL 持久化
  → 审计记录

  ———

  ## 六、A2A 与 MCP 边界

  A2A 负责：

  - Agent 之间的任务委派；
  - Agent 之间的结构化结果返回；
  - 目标 Agent；
  - 当前阶段；
  - 输入引用；
  - 输出引用；
  - 阻断原因；
  - 人工关口状态；
  - 下一步结构化动作。

  MCP 负责：

  - 文档工具调用；
  - 项目工具调用；
  - 企业知识库工具调用；
  - 业务规则工具调用；
  - 外部系统受控访问。

  A2A 不替代 MCP。

  MCP 不承担 Agent 编排。

  SDK 内部的自由规划、todo 和自由文本不能直接驱动业务状态。

  Skill 必须通过白名单声明。

  MCP 工具必须经过服务端二次权限校验。

  Agent、Skill 和 MCP 都不能因为模型输出而动态扩大权限范围。

  ———

  ## 七、领域层边界

  FastAPI 领域层是以下内容的唯一控制入口：

  - 业务状态；
  - 项目状态；
  - 流程状态；
  - 权限；
  - 输入版本；
  - 输出版本；
  - input_hash；
  - 人工关口；
  - 幂等；
  - 事务；
  - 审计；
  - 阶段门禁；
  - 返工和失效规则。

  Agent 不得：

  - 直接修改项目主状态；
  - 直接修改 workflow_run 状态；
  - 绕过 FastAPI 领域服务；
  - 绕过权限校验；
  - 绕过输入版本校验；
  - 绕过 input_hash 校验；
  - 绕过人工关口；
  - 直接写入未经校验的业务事实；
  - 直接决定下游阶段是否可以开始；
  - 直接把自由文本保存为长期业务记忆；
  - 直接覆盖已确认的业务版本；
  - 直接删除审计记录或历史产物。

  PostgreSQL 是唯一业务事实源。

  以下内容不能替代 PostgreSQL 成为业务事实源：

  - Redis；
  - Celery；
  - Deep Agents；
  - Agent Memory；
  - Skill；
  - MCP；
  - 模型上下文；
  - 日志；
  - 临时文件；
  - 前端状态；
  - LangGraph runtime。

  ———

  ## 八、人工关口与多段短运行

  workflow_run 表示长期业务流程运行，不等于一次长时间挂起的 Agent 对话。

  V1 使用多段短运行：

  创建或读取长期 workflow_run
  → 创建当前阶段 agent_run 和 task_run
  → Deep Agents 执行当前阶段
  → 校验结构化输出
  → 写入 DRAFT 产物
  → 当前 agent_run 结束
  → workflow_run 进入 WAITING_HUMAN
  → 用户通过领域 API 确认或退回
  → 领域层校验权限、关口、版本、input_hash 和幂等键
  → 创建下一阶段新的 agent_run 和 task_run
  → 重新投递异步任务

  不得跨人工关口恢复旧 Agent 对话上下文。

  人工关口只能通过正式领域接口处理：

  GET /api/v1/workflow-runs/{workflow_run_id}/pending-gate
  POST /api/v1/workflow-runs/{workflow_run_id}/pending-gate/actions

  前端不得：

  - 调用通用运行时恢复接口；
  - 指定任意目标节点；
  - 指定任意恢复位置；
  - 绕过领域层门禁；
  - 直接修改流程状态。

  关口动作必须校验：

  - 当前用户；
  - 项目权限；
  - 当前关口；
  - 输入版本；
  - expected_input_hash；
  - Idempotency-Key；
  - 必确认项；
  - 阻断问题；
  - 阶段门禁。

  ———

  ## 九、数据库与任务执行

  所有业务表、运行表、产物表和审计表必须遵循正式数据库设计。

  必须明确区分：

  workflow_run
  agent_run
  task_run
  workflow_gate_action
  audit_event

  不得把工作流、Agent、Celery 任务和人工动作塞入同一张通用表。

  Celery 只负责：

  - 任务排队；
  - Worker 执行；
  - 有限重试；
  - 长任务调度；
  - 任务补投。

  Celery 状态不能覆盖 PostgreSQL 业务状态。

  Redis 只负责运行期基础设施能力，不得保存唯一业务事实。

  所有异步任务必须考虑：

  - 幂等；
  - 重复投递；
  - 超时；
  - 失败重试；
  - 任务补投；
  - 输入版本；
  - 输出版本；
  - 审计记录；
  - 任务状态可追踪。

  不得使用无限自动重试掩盖以下问题：

  - 权限错误；
  - 输入版本变化；
  - 业务前置条件缺失；
  - 人工确认未完成；
  - 结构化输出持续失败；
  - 业务规则校验失败。

  ———

  ## 十、前端冻结约束

  frontend/ 是已经确认的前端基线。

  不得重新设计或改变：

  - 页面结构；
  - 页面功能；
  - 页面交互流程；
  - 视觉风格；
  - 配色；
  - 页面布局；
  - 导航结构；
  - 重要文案；
  - 用户操作路径；
  - 页面信息架构。

  只允许在不改变用户体验的前提下：

  - 重构内部代码；
  - 组件化；
  - 类型化；
  - 接入真实 API；
  - 接入统一状态管理；
  - 增加契约校验；
  - 增加测试；
  - 修复明确的功能缺陷；
  - 替换静态 Mock 数据。

  任何前端改动都必须说明：

  1. 修改原因；
  2. 是否影响页面结构；
  3. 是否影响用户交互；
  4. 是否影响视觉样式；
  5. 如何验证冻结基线没有被破坏。

  未经明确批准，不得新增页面、删除页面或重新设计页面。

  ———

  ## 十一、开发流程

  每个开发任务必须遵循以下流程：

  1. 阅读与当前任务相关的正式文档；
  2. 明确涉及的业务对象、状态、接口和数据库表；
  3. 输出简短实施计划；
  4. 说明准备修改的文件；
  5. 先定义接口和数据契约；
  6. 再实现领域服务；
  7. 再实现 Agent、Skill、MCP 或 Worker；
  8. 编写或更新测试；
  9. 执行格式化、类型检查和相关测试；
  10. 检查文档是否需要同步；
  11. 汇报真实验证结果。

  必须按纵向业务切片开发，不要一次性铺设无法验证的大型框架。

  每个切片应尽量形成：

  领域模型
  → API
  → 数据库
  → 异步任务
  → Agent/Skill/MCP
  → 前端接入
  → 测试
  → 审计记录

  开发过程中不得无故修改与当前任务无关的文件。

  不得进行无目标的大规模重构。

  ———

  ## 十二、测试与验证

  每次代码变更必须执行与变更范围匹配的验证。

  至少根据情况执行：

  - 单元测试；
  - API 测试；
  - 数据库迁移测试；
  - 领域状态转换测试；
  - 结构化契约测试；
  - Agent 委派测试；
  - MCP 权限测试；
  - Celery 任务测试；
  - 前端组件测试；
  - 前端交互测试；
  - 类型检查；
  - 格式检查；
  - 构建检查。

  测试必须覆盖异常路径，包括：

  - 权限不足；
  - 输入版本冲突；
  - 幂等重复请求；
  - 结构化输出失败；
  - Agent 委派失败；
  - MCP 权限拒绝；
  - Celery 重复投递；
  - Redis 不可用；
  - 人工关口未完成；
  - 业务前置条件缺失；
  - 产物版本失效；
  - 任务超时；
  - 可恢复和不可恢复错误。

  只有真实执行并通过验证后，才能声称功能完成。

  如果功能已实现但未验证，必须明确说：

  功能已实现，但尚未完成验证。

  不得把“代码已写完”表述为“功能已完成”。

  不得伪造测试结果，不得声称未执行过的命令已经通过。

  ———

  ## 十三、代码质量与安全

  必须优先修复根因，不使用临时绕过方案。

  不得为了让测试通过而删除：

  - 业务校验；
  - 权限校验；
  - 版本校验；
  - 幂等逻辑；
  - 审计逻辑；
  - 人工关口；
  - 状态门禁。

  不得擅自扩大 V1 范围。

  不得提交：

  - API Key；
  - Token；
  - 数据库密码；
  - Secret 文件；
  - 私钥；
  - 真实客户数据；
  - 生产环境配置；
  - 未脱敏日志；
  - 大型生成文件；
  - 不必要的二进制文件。

  不得修改、删除或覆盖其他成员尚未合并的工作。

  不得执行危险 Git 操作：

  git reset --hard
  git clean
  git push --force
  删除分支
  覆盖他人提交
  大范围回滚工作区

  除非项目负责人明确授权，否则不得执行上述操作。

  不得创建无关分支、提交或文件。

  ———

  ## 十四、Git 协作

  开始修改前先检查：

  git status
  git branch
  git log -5

  提交前必须检查：

  git diff --check
  git diff --stat
  git status

  提交信息必须使用中文或清晰的英文 Conventional Commit 格式，并准确描述实际变更。

  不得提交未验证的代码。

  不得将他人的未提交改动一并覆盖。

  如果发现工作区已有他人改动：

  1. 不得直接恢复；
  2. 不得直接删除；
  3. 不得直接覆盖；
  4. 先识别改动归属；
  5. 只修改本次任务负责的文件；
  6. 必要时报告潜在冲突。

  ———

  ## 十五、范围控制

  V1 只实现正式文档明确要求的内容。

  以下情况必须暂停并报告：

  - 新需求没有对应 PRD 范围；
  - 需要新增 Agent；
  - 需要新增人工关口；
  - 需要改变前端页面；
  - 需要引入新的基础设施；
  - 需要改变数据库事实源；
  - 需要修改已冻结契约；
  - 需要直接使用 LangGraph；
  - 需要绕过领域层；
  - 需要新增动态 Prompt、Skill 或 MCP 管理；
  - 需要删除正式文档中的当前约束；
  - 需要进行大规模重构。

  不得因为“以后可能需要”而提前实现 V1 之外的复杂能力。

  ———

  ## 十六、完成标准

  只有同时满足以下条件，才能声称任务完成：

  - 实现符合 PRD；
  - 实现符合最新决策；
  - 状态转换符合状态机；
  - 领域对象符合数据字典；
  - API 符合接口设计；
  - 数据库符合数据库设计；
  - 使用 Deep Agents 当前架构；
  - 没有直接构建 LangGraph StateGraph；
  - 结构化 A2A 契约已经校验；
  - 七种结构化调度决策没有被自由文本替代；
  - 业务状态由领域层控制；
  - PostgreSQL 仍是业务事实源；
  - 权限、版本、幂等和审计逻辑完整；
  - 人工关口语义正确；
  - 前端冻结基线没有被破坏；
  - 相关测试已经真实执行；
  - 测试结果真实通过；
  - 文档已经同步；
  - 未引入未说明的技术债务；
  - 未擅自扩大 V1 范围。

  如果任何一项不满足，必须在最终回复中明确说明，不得隐藏或弱化问题。

  ———

  ## 十七、最终回复格式

  每次任务完成后，必须使用中文，并按以下结构回复：

  本次完成：
  - 实际完成的功能；
  - 修改的文件；
  - 关键实现说明。

  验证结果：
  - 执行的命令；
  - 测试结果；
  - 未完成的验证。

  风险与遗留：
  - 已知问题；
  - 未解决风险；
  - 后续建议。

  范围确认：
  - 是否修改冻结前端；
  - 是否引入新依赖；
  - 是否超出 V1 范围。

  不得只回复“已完成”。

  不得隐瞒测试失败、文档冲突、实现缺口或范围变化。

  ———

  ## 十八、对话历史自动维护

  每完成一次具有明确结果的对话、开发环节或任务，必须自动将本次工作记录追加到：

  项目开发对话历史.md

  新增记录必须明确标注：

  - 来源：Claude Code；
  - 记录日期；
  - 本次对话、环节或任务的主题；
  - 用户提出的要求；
  - 已完成的工作；
  - 修改的文件；
  - 执行的测试或验证命令；
  - 测试结果；
  - 未解决的问题和后续事项。

  对话历史只能追加，禁止覆盖、删除或改写既有记录。

  不得将 API Key、Token、密码、Secret、真实客户数据或其他敏感信息写入对话历史。

  如果当前任务未产生实际决策、代码变更、测试结果或有效结论，可以不重复记录；但当一个任务明确结束时，必须完成一次 Claude Code 来源标注的历史更新。

  如果因权限、文件冲突或其他原因无法自动更新，必须在最终中文回复中明确说明，不得假装已经更新。
