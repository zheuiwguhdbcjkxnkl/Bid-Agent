# bid-agent-ai V1 Claude Code 开发约束

你现在参与的是 bid-agent-ai 项目的 V1 开发。

本文件是 Claude Code 的执行层约束，不替代正式产品和技术文档。

项目最高开发原则见 `.specify/memory/constitution.md`。产品范围、业务事实和技术事实来自 `specs/` 正式文档。三者冲突时必须暂停并报告，不得自行选择性实现或静默调和。

---

## 一、语言与注释

- 整个开发过程必须使用中文回复，包括分析、计划、进度、错误、验证结果和最终总结。
- 新增或修改的代码注释必须使用中文。
- 第三方 API、协议、框架、库、标准术语、代码标识、命令、配置项和错误码可以保留英文。
- “使用中文注释”不代表必须为所有代码添加注释。不得添加重复代码含义、没有维护价值的废话注释。
- 优先通过清晰命名、类型、模块边界和测试表达意图；只在业务规则、异常原因或非显然设计决策需要解释时添加注释。

---

## 二、权威来源与冲突处理

### 2.1 权威层级

1. `.specify/memory/constitution.md`：项目最高开发原则。
2. `specs/11-产品需求文档（PRD）.md`：产品范围和功能要求。
3. `specs/13-项目决策与工作记录.md`：最新确认决策；最新章节优先于历史记录。
4. `specs/09-业务流程与项目状态机.md`：流程、状态和合法转换。
5. `specs/10-核心业务对象与数据字典.md`：领域对象、字段和枚举。
6. `specs/14-API接口设计.md`：API 契约。
7. `specs/17-技术方案与系统架构设计.md`：技术实现边界。
8. `specs/18-数据库设计与实现方案.md`：数据库表、约束和迁移要求。
9. `specs/15-评估体系与测试计划.md`：测试和验收标准。
10. `docs/development/2026-08-21-DeepAgents架构迁移设计.md`：Deep Agents 实施细则。
11. `项目开发对话历史.md`：仅用于追溯历史，不是日常编码主依据。

### 2.2 阻断性冲突

以下冲突必须暂停编码并报告：

- 产品范围；
- 业务状态和流程；
- API 请求或响应契约；
- 数据库字段、约束或事实源；
- 权限、版本、幂等、审计；
- 人工关口；
- Agent、A2A、Skill、MCP 边界；
- 前端冻结基线；
- V1 范围。

报告必须说明冲突文件、章节、具体内容、实现影响和建议方案。

### 2.3 非阻断性差异

命名、格式、目录组织或不影响行为的实现细节，可以遵循现有代码风格处理，并在最终回复中说明。不得借“实现细节”改变正式业务语义。

---

## 三、按任务读取文档

不得在每个任务开始时无差别读取全部文档，也不得默认读取完整的 `项目开发对话历史.md`。

### 3.1 新会话第一次开发任务

先执行：

1. 阅读 `项目文件夹导览.md`；
2. 阅读 `.specify/memory/constitution.md`；
3. 定位 `specs/13-项目决策与工作记录.md` 的最新相关决策；
4. 定位 PRD 中与当前任务相关的功能要求；
5. 根据任务类型读取对应章节。

同一会话中，已读取且没有变化的章节不得重复全文读取。

### 3.2 任务路由

- 流程或状态任务：读取 `specs/09-业务流程与项目状态机.md` 相关章节。
- 领域模型任务：读取 `specs/10-核心业务对象与数据字典.md` 相关章节。
- API 任务：读取 `specs/14-API接口设计.md` 相关章节。
- 数据库任务：读取 `specs/18-数据库设计与实现方案.md` 相关章节。
- Agent、Skill、MCP、Celery 或架构任务：读取 `specs/17-技术方案与系统架构设计.md` 和 Deep Agents 迁移设计的相关章节。
- 测试任务：读取 `specs/15-评估体系与测试计划.md` 相关章节。
- 前端任务：读取 `frontend/` 中目标页面和其直接依赖；不得默认遍历全部前端文件。
- 历史追溯：只读取 `项目开发对话历史.md` 中相关标题或关键词附近内容。

### 3.3 编码前说明

开始编码前必须用中文说明：

```text
本次任务：
实际读取的文档和章节：
准备修改的文件：
预计验证方式：
```

上下文不足时继续按章节读取，不得猜测。

---

## 四、当前架构硬约束

当前 V1 架构为：

```text
Deep Agents Harness
+ 主 Deep Agent
+ 四个专业 Subagent
+ 结构化 A2A 契约
+ 七种结构化调度决策
+ FastAPI 确定性领域层
+ PostgreSQL 唯一业务事实源
+ Celery + Redis 异步执行
+ Skill 白名单
+ MCP 工具层
+ 四个人工关口
+ 多段短运行
```

必须遵守：

- 项目代码不得直接构建或维护 LangGraph `StateGraph`、主图、节点、边、Checkpoint 或 `interrupt/resume` 业务流程。
- Deep Agents 内部可以依赖 LangGraph runtime，但项目业务代码不得以其作为业务事实源。
- PostgreSQL 是唯一业务事实源。
- Redis、Celery、Agent Memory、模型上下文、日志、前端状态和临时文件不得成为唯一业务事实源。
- FastAPI 领域层控制业务状态、权限、版本、`input_hash`、幂等、事务、人工关口、返工、失效和审计。
- Agent 不得绕过领域层直接修改项目主状态或 `workflow_run`。

---

## 五、Agent、A2A、Skill 与 MCP

### 5.1 七种结构化决策

只能使用以下结构化决策驱动业务流程：

```text
DISPATCH_AGENT
WAITING_HUMAN
RETRY_TASK
EXPAND_REVIEW_SCOPE
INVALIDATE_OUTPUTS
BLOCK_WORKFLOW
COMPLETE_STAGE
```

### 5.2 三类 A2A 契约

必须定义、版本化并校验：

```text
OrchestrationDecision
AgentDelegationEnvelope
SubagentResultEnvelope
```

模型自由文本、SDK todo、未校验推理结果和任意字符串不得直接驱动业务状态。

标准处理顺序：

```text
Schema 校验
→ 业务规则校验
→ 权限和版本校验
→ 领域层处理
→ PostgreSQL 持久化
→ 审计记录
```

### 5.3 工具边界

- A2A 负责 Agent 间的结构化任务委派和结果返回。
- MCP 负责受控工具调用，不承担 Agent 编排。
- Skill 必须声明白名单。
- MCP Server 必须二次校验用户、项目、角色和工具权限。
- Agent、Skill、MCP 不得根据模型输出动态扩大权限。
- Agent 自由文本不得直接写成长期业务事实或长期记忆。

---

## 六、人工关口与运行语义

`workflow_run` 是长期业务流程运行，不是跨阶段挂起的 Agent 对话。

V1 使用多段短运行：

```text
读取长期 workflow_run
→ 创建阶段 agent_run 和 task_run
→ Deep Agents 执行当前阶段
→ 校验并写入 DRAFT 产物
→ 当前 agent_run 结束
→ workflow_run 进入 WAITING_HUMAN
→ 用户通过领域 API 确认或退回
→ 领域层校验权限、关口、版本、input_hash、幂等和门禁
→ 创建下一阶段的新 agent_run 和 task_run
```

必须遵守：

- 不得跨人工关口恢复旧 Agent 对话上下文。
- Agent 不得自主越过人工关口进入下一阶段。
- 前端不得调用通用运行时恢复接口或指定任意恢复节点。
- 人工关口只能通过正式领域接口处理：
  - `GET /api/v1/workflow-runs/{workflow_run_id}/pending-gate`
  - `POST /api/v1/workflow-runs/{workflow_run_id}/pending-gate/actions`
- 关口动作必须校验权限、当前关口、输入版本、`expected_input_hash`、`Idempotency-Key`、必确认项、阻断问题和阶段门禁。

---

## 七、前端冻结基线

`frontend/` 是已确认的页面、功能、交互、视觉和信息架构基线。

未经明确批准，不得：

- 重新设计页面；
- 改变导航、布局、配色、重要文案或用户操作路径；
- 新增或删除一级页面；
- 用个人偏好替换现有视觉风格。

允许在不改变用户体验的前提下：

- 组件化和类型化；
- 接入真实 API；
- 替换 Mock 数据；
- 增加状态管理、契约校验和测试；
- 修复明确缺陷。

前端任务必须说明修改原因、是否影响结构、交互和视觉，以及验证冻结基线的方法。

---

## 八、开发与验证流程

每个任务按纵向切片推进：

```text
相关需求和决策
→ 领域模型与契约
→ API 与数据库
→ 领域服务
→ 异步任务或 Agent/Skill/MCP
→ 前端接入（如涉及）
→ 测试与审计
```

执行要求：

1. 先确认范围和相关文档；
2. 输出简短实施计划；
3. 只修改当前任务负责的文件；
4. 先定义契约，再实现业务逻辑；
5. 优先修复根因，不做临时绕过；
6. 编写或更新与任务相关的测试；
7. 执行格式化、类型检查、测试或构建；
8. 检查是否需要同步正式文档；
9. 汇报真实验证结果。

完成标准只适用于当前任务涉及的模块。不得为无关模块执行无意义修改或验证，也不得用局部测试声称整个系统已完成。

必须覆盖与任务相关的异常路径，例如权限不足、版本冲突、幂等重复、结构化输出失败、人工关口未完成、重复投递、超时和不可恢复错误。

未执行验证时只能说“已实现但尚未验证”，不得声称完成或通过。

---

## 九、代码质量与安全

- 不得为了通过测试删除业务校验、权限、版本、幂等、审计、人工关口或状态门禁。
- 不得擅自扩大 V1 范围或提前建设“以后可能需要”的复杂能力。
- 不得提交 API Key、Token、密码、Secret、私钥、真实客户数据、生产配置或未脱敏日志。
- 不得创建无关文件、分支、依赖或大规模重构。
- 新依赖必须说明必要性、替代方案、许可证和对部署的影响。
- 发现安全、合规或数据出域风险时必须暂停并报告。

以下情况必须暂停：新增 Agent 或人工关口、改变前端基线、改变数据库事实源、修改冻结契约、引入新基础设施、直接使用 LangGraph、绕过领域层或超出 PRD 范围。

---

## 十、Git 多人协作

开始修改前检查：

```text
git status
git branch --show-current
git log -5 --oneline
```

发现他人未提交改动时：

- 不得恢复、删除、覆盖或顺带格式化；
- 只修改本次任务负责的文件和必要行；
- 无法安全隔离时暂停并报告潜在冲突。

除非用户明确要求，否则不得自动执行：

```text
git add
git commit
git push
git pull
git merge
git rebase
git reset
git clean
创建或删除分支
```

允许执行只读检查，如 `git status`、`git diff`、`git log` 和 `git branch --show-current`。

即使获得授权，也不得使用 `git reset --hard`、`git clean`、`git push --force` 或覆盖他人提交，除非用户明确说明目标和影响范围。

提交前必须检查：

```text
git diff --check
git diff --stat
git status
```

---

## 十一、项目决策与对话历史维护

### 11.1 正式项目决策记录

开发过程中形成会影响后续实现的正式结论时，必须及时追加更新：

`specs/13-项目决策与工作记录.md`

以下情况必须更新：

- 用户或项目负责人确认新的产品、业务、架构或技术决策；
- 修改已冻结的产品范围、架构边界或实施方案；
- 新增、删除或调整 Agent、Skill、MCP、人工关口；
- 修改业务状态、领域对象、API 契约、数据库设计、权限或审计规则；
- 改变前端冻结基线；
- 引入新依赖、新基础设施或新的外部服务；
- 正式文档冲突得到最终确认；
- 原有决策被替代、废弃或调整；
- 形成会影响其他成员后续工作的结论。

更新要求：

1. 只记录已经确认的决策，不记录未确认讨论、猜测或个人建议；
2. 记录日期、决策内容、决策原因、影响范围和涉及文档；
3. 新决策只能追加，不得覆盖或删除既有决策；
4. 被替代的旧决策应标注“已替代”或“已废弃”，不得直接删除必要的审计依据；
5. 更新后必须检查 PRD、状态机、数据字典、API、技术方案、数据库设计和测试计划是否需要同步；
6. 普通代码实现、格式调整和不影响业务或技术边界的缺陷修复不更新决策记录；
7. 尚未获得用户或项目负责人确认的内容，不得自行写成正式决策。

`specs/13-项目决策与工作记录.md` 记录已经确认且影响后续开发的正式决策；`项目开发对话历史.md` 记录讨论、任务和执行过程，两者不得互相替代。

### 11.2 对话历史维护

只在以下情况更新 `项目开发对话历史.md`：

- 确认新的产品、架构或技术决策；
- 完成一个正式开发任务或阶段里程碑；
- 修改正式接口、状态、数据库、权限或技术边界；
- 产生需要其他成员了解的重要风险、阻塞或恢复结论。

普通问答、重复确认、格式调整和没有实际结论的交流不记录，避免历史文件膨胀和多人合并冲突。

Claude Code 写入时必须标注：

```text
来源：Claude Code
记录日期：
任务主题：
用户要求：
完成工作：
修改文件：
验证命令与结果：
未解决问题与后续事项：
```

历史只能追加，不得改写既有记录，不得写入敏感信息。因权限或冲突无法更新时，必须在最终回复中说明。

---

## 十二、最终完成与回复

只有与当前任务相关的以下条件全部满足，才能声称任务完成：

- 实现符合宪法、PRD、最新决策和相关正式规格；
- 状态、对象、API 和数据库契约保持一致；
- 权限、版本、幂等、人工关口和审计没有被绕过；
- 相关测试或验证真实执行并通过；
- 前端冻结基线未被破坏（如涉及前端）；
- 未擅自扩大 V1 范围；
- 需要同步的文档和历史已经更新。

最终回复必须使用中文，并包含：

```text
本次完成：
- 实际完成内容；
- 修改文件；
- 关键实现说明。

验证结果：
- 执行命令；
- 测试结果；
- 未完成验证。

风险与遗留：
- 已知问题；
- 未解决风险；
- 后续建议。

范围确认：
- 是否修改冻结前端；
- 是否引入新依赖；
- 是否超出 V1 范围；
- 是否执行 Git 写操作。
```

不得只回复“已完成”，不得隐瞒测试失败、文档冲突、实现缺口或范围变化。
